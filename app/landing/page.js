import HomeClient from "../HomeClient";

/**
 * AUXILIARY LANDING (React Home)
 * - 메인 서비스 화면은 /static/index.html
 * - 이 경로는 보조 랜딩/실험용 React 홈으로만 사용
 */
export default function LandingPage() {
  return <HomeClient />;
}
