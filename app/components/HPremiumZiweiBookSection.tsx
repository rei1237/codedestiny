// HPremiumZiweiBookSection.tsx
// 이 파일은 HPremiumZiweiSection.tsx를 기반으로 복사된 프리미엄 자미두수 북 섹션 컴포넌트입니다.
// 필요에 따라 별도 UI/로직 분리 가능

"use client";

import HPremiumZiweiSection from "./HPremiumZiweiSection";

// 프리미엄 자미두수 북 섹션: 현재는 HPremiumZiweiSection을 그대로 래핑
export default function HPremiumZiweiBookSection(props: any) {
  return <HPremiumZiweiSection {...props} />;
}
