# 저장소 정리 — 삭제 가능/금지 목록 (2026-08-15 실측 · 2026-08-30 집행 · 2026-09-06 집행)

> 🔴 **2026-08-30 에 아래 1~5 를 실제로 집행했다.** 각 절의 `집행` 줄이 그날 확인한 값과 결과다.
> 그때 **원 문서의 판정 두 개가 틀렸다는 것이 드러났다** — 4번의 `.gitignore` 서술과 판단 보류의 `status.json`.
> 둘 다 아래에서 정정했다. 새로 지울 것을 찾을 때는 옛 판정을 인용하지 말고 이 문서의 `집행` 줄부터 본다.

> 이 파일은 필요할 때만 읽는 참조 문서입니다. 항상 로드되는 규약 요약은 루트 [CLAUDE.md](../../CLAUDE.md)에 있습니다.

**검색 범위 (이 문서의 모든 판정이 선 곳)**: `scripts/**`, `.github/workflows/**`, `package.json`,
`AGENTS.md`, `CLAUDE.md`, `docs/CONTEXT_AUDIT.md`, `docs/CURRENT_DEV_BASELINE.md`, `.gitignore`.
🔴 **`app/`·`worker/`·`lib/`·`js/`·`index.html`·`__tests__/` 는 보지 못했다.** 아래 "미검증" 항목은 그 범위를 뜻한다.

---

## 🔴 절대 삭제 금지 — 살아있는 가드가 파일로 읽는다

원칙 9의 "임포터 0은 죽었다는 증거가 아니다"가 그대로 적용되는 사례들이다. import 가 없어도 검증기가 **파일로 읽어 단언**한다.

| 파일 | 읽는 곳 | 지우면 |
|---|---|---|
| 루트 `MOBILE_*.md` **13개** | `scripts/verify-mobile-final-audit.mjs` 의 `reportFiles`·`finalAuditRequiredText`·`scanCoreMojibake` (2026-08-26 기준) | `npm run verify:mobile-final-audit` 파괴 |
| └ 그중 `MOBILE_FEATURE_REGISTRY.md` | `scripts/verify-mobile-entry-actions.mjs:7` 이 **엔트리 액션 정본**으로 파싱(감사 리포트가 아니다) | 모바일 진입 경로 104행 검증 전부 파괴 |
| `PAYMENT_POLICY.md` | `scripts/verify-payment-policy-md.mjs:55` — 운명 찻집 **가격 정본**으로 파싱 | 가격 정합성 가드 파괴 |
| `PROJECT_STRUCTURE.md` · `PAYMENT_CONCURRENCY_AUDIT.md` · `CLOUDFLARE_PAGES_SETUP.md` · `DEPLOY_CHECKLIST.md` | `scripts/verify-doc-freshness.mjs:50-54` `ROOT_REPO_PATHS` | `verify:doc-freshness` 파괴 |
| `ads.txt` (root·public) | `scripts/ensure-ads-txt.mjs` · `verify-adsense-readiness.mjs` | 광고 수익 경로 파괴 |

`docs/CONTEXT_AUDIT.md` 는 위 4개 루트 문서를 "Historical-Only References" 로 분류하지만 **그건 "코딩 근거로 삼지 말라"는 뜻이지 "지워도 된다"는 뜻이 아니다.**

---

## 안전하게 지울 수 있는 것 (근거 확보)

### 1. 레포 전용 정리 스크립트가 이미 담당하는 것 — 새 명령을 만들지 말 것

`scripts/safe-clean-repo.mjs` 가 `.next`·`out`·`dist`·`build`·`coverage`·`playwright-report`·`test-results`·`.wrangler`·`.cloudflare`
디렉터리와 `*.tsbuildinfo`·`*.log`·`_tmp_*`·OS 잔재를 지운다. 보호 목록(`index.html`·`package.json`·`worker/`·`app/`·`public/` 등)도 내장돼 있다.

```bash
npm run clean:repo:dry      # 계획만 (reports/cleanup-plan.json 에 기록)
npm run clean:repo:apply    # 실제 삭제
```

이걸로 `tsconfig.tsbuildinfo`(464KB) · `llm.log` 가 함께 정리된다. 🔴 **같은 일을 하는 rm 명령을 따로 짜지 말 것**(원칙 6 중첩 금지).

### 2. 감사·측정 산출물 `reports/` — 약 22MB

`psi-*.json`(0.5~1.1MB × 12) · `lighthouse-*.json` · `i18n-hardcoded-audit.json`(4.8MB) · `unused-files-report.json`(0.9MB) ·
`duplicate-files-report.json`(0.6MB) 등 전부 스크립트 산출물이다.

- `scripts/verify-doc-freshness.mjs:20` 이 `reports/` 를 **활성 문서에서 참조 금지 경로**로 규정한다 = 영속 자산이 아니라는 선언.
- `scripts/generate-adsense-route-audit.mjs:14` 와 `safe-clean-repo.mjs` 가 실행 시 다시 만든다.

```bash
git ls-files reports | head          # 추적 중인지 먼저 확인
npm run clean:repo:apply             # 위 1번 스크립트가 그대로 담당한다
```

**집행(2026-08-30)**: `git ls-files reports` **0건** — 추적된 적이 없고 `.gitignore:128` 이 이미 무시한다.
따라서 `git rm --cached` 는 필요 없다(원 문서의 그 줄은 지웠다). 디스크 정리는 1번 스크립트 하나로 끝난다.

### 3. 중복 스킬 `.agents/skills/`

`.claude/skills/` 의 caveman 계열 5종과 **SKILL.md 가 바이트 단위로 동일**(`diff` 확인).
Claude Code 가 읽는 곳은 `.claude/skills/` 다.

- 위 검색 범위에서 `.agents` 참조 **0건**. 🔴 **미검증** — 소스 전체는 보지 못했다. 지우기 전에:

```bash
grep -rn "\.agents" --exclude-dir=node_modules --exclude-dir=.git .
```

결과가 없으면 `rm -rf .agents`.

**집행(2026-08-30)**: `.agents/skills/` 추적 파일 **14개 전부가 `.claude/skills/` 의 같은 경로와 바이트 동일**
(SKILL.md 뿐 아니라 하위 파일까지 전수 비교). 남아 있던 `.agents` 문자열은 전부 `.claude/skills/**` 안의
**조건부 폴백 조회**(있으면 쓰고 없으면 넘어감)라 의존이 아니다. → `git rm -r .agents` 완료.
🔴 함께 지운 것: 루트 `.ignore` 의 `/.agents/` 줄과 그 중복본만 설명하던 주석 문장.

### 4. 도구 스크래치 디렉터리

`.codex-tmp/` · `.playwright-mcp/` — 위 범위에서 참조 0건(🔴 미검증, 위와 같은 grep 으로 확인).

🔴 **정정(2026-08-30)** — 원 문서의 "`.gitignore` 에 추가했다" 는 **정리됐다는 뜻이 아니었다.**
`.gitignore:138-139` 에 두 경로가 실제로 있지만, **`.playwright-mcp/` 의 파일 2개는 그대로 추적 중이었다** —
gitignore 는 *이미 추적 중인* 파일을 인덱스에서 빼지 않기 때문이다. 즉 그 줄을 추가한 시점 이후로
**아무 일도 일어나지 않은 채 2주 반을 남아 있었다.** `git rm` 을 해야 실제로 빠진다.

**집행(2026-08-30)**: `.codex-tmp/` 는 **존재하지 않는다**(디렉터리 없음). `.playwright-mcp/` 는 추적 2개
(`page-2026-04-20T*.yml`)를 `git rm` 으로 제거했다.

🔴 일반화: **이 문서에서 "`.gitignore` 에 넣었다" 는 삭제 근거가 아니다.** 추적 여부는 언제나
`git ls-files <경로>` 로 따로 확인한다.

### 5. 이름이 이모지 1자인 0바이트 파일

`🎊` — 크기 0, 내용 없음. 셸 오타로 생성된 것으로 보인다.

**집행(2026-08-30)**: `git rm` 으로 제거 완료.

### 6. 정리되지 않은 워크트리 (2026-08-30 실측 20개)

각각이 레포 전체 사본이라 디스크와 grep 양쪽을 오염시킨다.

2026-08-30 실측: `.claude/worktrees/` 아래 **디렉터리 20개**인데 `git worktree list` 등록은 **19개**(+메인)다.
차이 1개는 `git worktree remove` 가 윈도우에서 남긴 **등록 끊긴 빈 껍데기**이고, 그건 `git worktree prune` 이
아니라 디렉터리 삭제로만 없어진다.

```bash
git worktree list                    # 어떤 브랜치가 물려 있는지 확인
git -C .claude/worktrees/<이름> status --short   # 미커밋 작업이 있는지 개별 확인
git worktree remove .claude/worktrees/<이름>
git worktree prune
```

🔴 **일괄 삭제하지 말 것** — 미커밋 작업이 남아 있을 수 있다. `status --short` 가 빈 것부터 지운다.

**집행(2026-09-06)**: 워크트리 **28개 제거**(등록 24 + 등록 끊긴 빈 껍데기 4). 남긴 7개는 전부 다른 세션이
쓰는 중이었다. 강제 제거한 4개의 미커밋 변경은 전부 **생성물 드리프트**였다(`rss.xml` 타임스탬프 · `.ignore`
CRLF · `scratch-*.mjs`). 머지된 PR 브랜치도 함께 지워 로컬 브랜치가 **79 → 22** 가 됐다.

그날 확인한 것 셋:

- 🔴 **`node_modules` 는 정션(junction)이라 워크트리를 지우기 전에 링크부터 끊는다** —
  `cmd /c rmdir "<워크트리 경로>\node_modules"`. 안 끊고 지우면 공유 설치본이 함께 날아간다.
  ([delivery-and-ci.md](delivery-and-ci.md) §격리 워크트리에서 명령 돌리기)
- 🔴 **스쿼시 머지는 조상 관계를 끊는다** — `git merge-base --is-ancestor <브랜치> origin/main` 이 false 를
  돌려준다. 머지 여부의 신뢰 신호는 `gh pr list --json state,mergedAt` 쪽이다.
- 🔴 **`.git` 파일이 없는 껍데기 디렉터리에 `git -C` 를 쓰면 조용히 부모 레포로 해석된다**(브랜치=main,
  변경 0으로 보인다). 껍데기는 `git worktree prune` 이 아니라 디렉터리 삭제로만 없어진다.

삭제한 브랜치의 복구 원장은 `reports/deleted-branches-2026-09-06.json`(추적 안 됨) 에 있다 —
`git branch <브랜치> <sha>` 로 되살린다. 🔴 **59개 중 56개는 원격 사본이 없으므로 `git gc --prune=now` 를
돌리면 이 복구 경로가 사라진다.**

### 7. 참조 0건 레거시 자산 (2026-09-06 집행 · PR #1705)

원칙 9 의 3면 grep 으로 판정해 **파일 8개 + `sync:public` 미러 7개**를 지웠다:
`js/fortune-engine.js` · `js/splash.js` · `js/tarot-data.js` · `js/web-vitals-console.js` ·
`js/inline/mobile-safe-render-init.js` · `js/inline/pwa-theme-init.js` · `css/sibyl-premium.css` ·
`lib/market-policy/context.js`.

🔴 **같은 감사에서 "임포터 0이지만 살아 있다"로 판정돼 남긴 것들** — 다음에 다시 후보로 올리지 말 것:
`js/astral-soul.js`(`js/core/uiBindings.js:63` 의 `__loadScriptOnce` 가 동적 로드) ·
`js/cd-google-translate-lazy.js` · `lib/indexnow.ts` · `lib/seo/dynamicOgImage.ts` ·
`lib/yeon/profileSeed.ts` · `lib/market-policy/market-policy-registry.js` ·
`lib/market-policy/legal-packs/legal-market-packs.js` · `js/dream-meaning-library.js`.

사용자가 **유지하기로 결정한** 목록은 따로 있다 — [../handoff/unused-candidates-sweep-2026-09-02.md](../handoff/unused-candidates-sweep-2026-09-02.md) 의
보류 항목. 🔴 다시 물어보지 말 것.

---

## 판단 보류 — 사용자 확인 필요

🔴 원래 이 표에 있던 `prepare*.json` · `status.json` 두 행은 **2026-08-30 에 판정이 끝나** 표에서 빼고
아래 "판정 결과" 로 옮겼다. 결과가 정반대로 갈렸다 — 하나는 삭제, 하나는 **삭제 금지**다.

| 파일 | 내용 | 왜 보류 |
|---|---|---|
| `AUDIT.md` · `SEO_SUBMISSION_GUIDE.md` · `QUICK_START.md` · `DEPLOYMENT_MODE.md` · `MOBILE_*` 외 루트 문서 | 위 "금지" 표에 없는 루트 문서 | 검증기 참조는 없었으나 소스 전체 미확인 |

확인 명령(🔴 `git grep` 을 쓴다 — 루트 `.ignore` 가 `sync:public` 미러를 rg/grep 에서 빼기 때문에
위 형태의 `grep -rn` 은 미러의 참조를 못 본다):

```bash
git grep -n "prepare\.json\|pprepare\.json\|prepare_astrology\.json\|status\.json"
```

### 판정 결과 (2026-08-30)

**`prepare.json` · `pprepare.json` · `prepare_astrology.json` → 삭제 완료.** 3면 `git grep` 참조 **0건**
(소스 · `__tests__/` · `scripts/verify-*`), `public/**` 미러도 없다. PortOne prepare 실패 응답 덤프가 맞았다.

🔴 **`status.json` → 살아 있다. 지우지 말 것.** 원 문서의 "헬스체크가 서빙할 가능성" 이라는 보류 사유가
정확했다. 실제 참조 4곳:

| 읽는 곳 | 무엇 |
|---|---|
| `js/kill-switch.js:3` | `var STATUS_URL = "/status.json?v=1.0.1";` — **킬 스위치의 상태 원본** |
| `service-worker.js:123-126` | `/status.json` 을 **절대 캐시하지 않는다**는 예외 규칙 |
| `_headers:243` | `/status.json` 전용 캐시 헤더 |
| `.ignore:176` | `/public/status.json` 검색 제외 |

지우면 킬 스위치가 상태를 못 읽는다. `public/status.json`(47B)은 배포되는 실파일이다.

---

## 정리 후 반드시 돌릴 것

```bash
npm run verify:doc-freshness      # 활성 문서의 참조 무결성 (CLAUDE.md 포함)
npm run verify:mobile-final-audit # 루트 MOBILE_*.md 를 읽는 가드
npm run verify:guard-wiring       # 가드 배선 (fail-closed 3방향)
npm run typecheck && npm run lint
```
