# 구매 여정 최적화 — 기존 이미지형 팝업과 공유

사용자 피드백에 따라 별도 소개 화면을 추가하지 않고 기존 이미지형 팝업을 개선했다. 현재 구현 범위와 검증 결과는 [IMPLEMENTATION.md](IMPLEMENTATION.md)에 정리한다. 기존 `review.html`은 초기 구상 보관용이며 현재 제품 적용안이 아니다. 최초 요청의 전체 서비스 실제 구매 여정 감사는 아직 미완료다.

## 실행

워크트리: `D:\Development\code-destiny-purchase-journey`

브랜치: `codex/purchase-journey` · 시작 기준: `f90fe7fc5a41f77b2ce7438b27375361a1352948`

초안 PR: https://github.com/rei1237/codedestiny/pull/1858

조사 후 최신 main의 모바일 복귀 변경을 충돌 없이 통합했다. `MOBILE_JOURNEY_MATRIX.md`와 `scripts/verify-mobile-journeys.mjs`가 추가되어 있으므로 후속 실제 화면 감사는 이 기준선을 재사용한다. 이 문서 역시 개별 화면을 아직 미검증으로 표시한다.

```powershell
Set-Location D:\Development\code-destiny-purchase-journey
node scripts/build-purchase-journey-review.mjs
python -m http.server 4319 --bind 127.0.0.1 --directory docs/purchase-journey
```

브라우저에서 `http://127.0.0.1:4319/review.html`을 열면 초기 구상을 볼 수 있다. 현재 적용한 실제 팝업은 `node scripts/verify-feature-popup-journey.mjs`로 검증하며 캡처는 OS 임시 폴더 `code-destiny-popup-journey`에 저장한다.

## 현재 근거와 한계

| 조사 대상 | 현재 소스에서 수집 | 해석 |
| --- | ---: | --- |
| 홈 서비스 레지스트리 | 57 | 전체 서비스 목록과 같지 않음 |
| 시각 상세 목적지 | 64 | 기존 검수 표시는 62개; 이번 화면 검증 증거가 아님 |
| 홈 타일 DOM | 62 | 레지스트리에 없는 진입 후보도 별도로 유지 |
| 마케팅 카피 키 | 155 | 상속·별칭 포함 |
| App Router 페이지 | 243 | 관리·정책·동적 라우트도 포함; 실제 서비스로 분류 필요 |
| 병합 후 조사 후보 | 218 | 상품 별칭만 서버 정규화; 동일 서비스의 다른 진입은 아직 중복 가능 |

`inventory.json`은 기존 추출기·시각 상세 생성기·가격 해석 함수를 읽기 전용으로 사용한다. 프로필 액션과 상품 키의 호출부 후보도 수집한다. 문자열 참조와 라우트 파일 존재는 실제 동작 증거가 아니므로, 모든 구매 6단계 점수는 `null`이다. 파일 밖의 동적 액션·라우팅 별칭·카테고리별 가격은 수동 추적이 남아 있다.

현재 단순 키 연결 기준으로 진입점 미연결 후보 98개, 개별 카피 미연결 후보 108개가 있다. 이는 **실제 화면이 없다는 판정이 아니다**. 하위 상품·공용 상세·동적 진입의 연결 확인 목록이다. 숫자를 기능 누락률이나 서비스 수로 홍보하면 안 된다.

기존 카피에서 분량·기간·추가 제공·경력 관련 문장이 있는 16개 후보를 근거 확인 대상으로 표시했다. 이 역시 거짓 주장이라는 판정이 아니다. `existingCopy`는 현재 소스의 기록이며 승인된 새 카피가 아니다.

## 목업의 결정

- 홈은 고민 선택을 첫 행동으로 삼는다. 기존 전체 검색과 프로필 결과 복귀는 제품 적용 시 유지한다.
- 대표 상세는 숙요·자미두수·초융합이다. 질문·가치·도식은 서로 다르게 구성한다.
- 개인 결과를 흉내 내는 문장 대신 기존 결과 항목의 구성 도식을 사용한다. 공개 범위를 늘리거나 개인 결과를 생성하지 않는다.
- 결제 안내는 세 이용 방식의 설명 위치만 정한다. 실제 가격·잔액·이용권 판정을 목업에 복제하지 않는다.
- 결과 하단은 원래 결과를 보존하면서 관련 체계로 이동할 이유를 제시한다. 자동 생성·구매·공유는 없다.
- 검토 도구 설명과 데이터 연결 자리표시는 제품 문구가 아니다. 검토 헤더는 제품 화면에 추가하지 않는다.

숙요와 자미두수 구성의 출발 근거는 `index.html`의 `FEATURE_VISUAL_DETAILS`, `FEATURE_MARKETING_COPY`와 기존 시각 상세다. 최종 카피 확정에는 해당 계산·결과 렌더러 검증이 추가로 필요하다. 초융합도 각 체계의 실제 전달 데이터와 결과 렌더러를 대조한 후 게시한다.

## 아직 해야 할 일

1. 후보를 최종 사용자 서비스·하위 상품·별칭·비서비스 라우트로 확정하고 전체 화면을 mock으로 순회한다.
2. 각 서비스의 소개·입력·결과·잠금·결제·복귀·저장·추천을 연결하고 근거 있는 6단계 점수표를 작성한다.
3. 전 상품의 Hook 3~8개·가치·제공 결과·FAQ·추천 근거를 작성한다. 현재 목업의 세 상품만으로 완료 처리하지 않는다.
4. 기존 이미지형 팝업 방향으로 확정되어 62개 공개 상세에 적용했다. 나머지 미연결 기능은 전체 감사에서 추적한다.
5. 이용권 상점·실제 결제 주변 설명·결과 추천·기존 공유·analytics 이벤트 연결을 구현한다.
6. 다른 언어와 SEO 경계, A~E 사용자 여정 및 서비스별 결제·잠금·resume를 mock에서 검증한다.
7. 생성 미러, 최신 main 정합과 필수 CI까지 통과시킨다. 머지·운영 승격은 별도 승인이다.

## 예상 영향 TOP 10 / 이후 실험 가설

아래는 효과 실측이 아닌 우선 검증할 가설이다.

| 변경 가설 | 1차 지표 | 함께 볼 부작용 |
| --- | --- | --- |
| 홈 첫 행동을 고민 선택으로 | 상세 진입률 | 전체 검색 이용 저하 |
| 기능명 앞에 질문형 가치 | 카드→상세 클릭률 | 상세 즉시 이탈 |
| 상품별 결과 구성 공개 | 상세→CTA 클릭률 | 잘못된 결과 기대 |
| 실제 제공 항목 구체화 | 상세→결제 시작률 | 스크롤 부담 |
| 계산·해석 구조 설명 | 상세→결제 시작률 | 과도한 전문용어 |
| CTA 옆 유료 조건 표시 | 결제 시작→완료율 | 가격 오해·취소 |
| 이용권 선택 기준 설명 | 상점→이용권 구매율 | 불필요한 상향 구매 유도 |
| 긴 상세의 반복 CTA | 모바일 CTA 클릭률 | 본문 가림·오터치 |
| 결과와 관련된 추천 | 관련 서비스 상세 진입률 | 기존 결과 재열람 방해 |
| 기존 공유 위치와 설명 개선 | 자발적 공유 시작률 | 개인정보 노출·강요 |

계측은 기존 `cdTrack` 단일 진입점을 사용한다. `js/core/analytics.js`에 `cross_sell_click`, `share_receive`, `retention_visit`가 이미 있고 `app/hooks/useAnalytics.ts`는 공유 발신을 다룬다. 요청한 관련 서비스·공유 이벤트와 이 이벤트의 관계를 먼저 정해 이중 발화를 막는다. 완료 이벤트의 소유권과 중복 여부도 확인한다. 실험은 동시 배포해 효과를 섞지 않고 충분한 표본과 원래 퍼널 기준선 확보 후 한 가설씩 진행한다. 할인·정책 변경 실험은 포함하지 않는다.

## 검증

```powershell
node scripts/build-purchase-journey-review.mjs --check
node scripts/verify-purchase-journey-review.mjs
npm run check:fast -- --plan
npm run check:fast
```

목업 전용 검증은 360·390·430·1280px의 5개 화면, 가로 넘침, 44px 버튼, 고민→상세 이동, 검색, 비활성 결제 버튼, 외부 요청 부재를 검사한다. 캡처는 OS 임시 폴더 `code-destiny-purchase-journey`에 저장한다. **프로토타입 검증은 실제 서비스·결제·기기 검증을 대신하지 않는다.**

실행 결과: 인벤토리 재생성 정합 및 목업 검사 통과. 최초 main 기준 `check:fast` 전체 통과(Jest 221 suites, 2,442 tests; lint 기존 경고 있음). 최신 main 통합 후에는 인벤토리 정합을 다시 확인했으며 최신 PR CI 결과는 별도로 확인한다.

Impeccable 검사에서 목업의 개별 글자 크기에 디자인 타입 스케일 advisory가 나왔다. 제품 적용 시 기존 토큰에 연결한다. 이번 단계에서 전역 디자인 규칙을 바꾸지 않는다.

## 이어서 작업

```text
cwd=D:\Development\code-destiny-purchase-journey
document=docs/purchase-journey/README.md
branch=codex/purchase-journey
next=IMPLEMENTATION.md의 최신 검증 및 PR 상태 확인. 전체 후보의 실제 구매 여정 감사는 별도 잔여 항목이며 목업 승인을 다시 요청하지 않는다.
```
