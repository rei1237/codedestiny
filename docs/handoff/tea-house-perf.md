---
updated: 2026-08-30
status: 계획 승인됨 — 0단계 미착수
---

# 찻집 렉 최적화 — 작업 상태

## 지금 어디까지

**계획 승인 완료, 코드 변경 0건.** 조사(3갈래 병렬 Explore)와 계획 작성까지만 끝냈고 컨텍스트 200k 에서 세션을 끊었다.

🔴 **계획 정본은 여기다 — 먼저 읽는다:**
`C:/Users/user/.claude/plans/worktree-app-login-completion-humming-neumann.md`
(단계·수치 근거·회귀 위험·재사용 대상이 전부 그 안에 있다. 여기서 반복하지 않는다.)

## 환경 상태 (그대로 이어받으면 된다)

| | |
|---|---|
| 워크트리 | `.claude/worktrees/app-oauth-return-path` |
| HEAD | **detached `2a81233c8`** (= `origin/main`). 작업 시작 전 브랜치를 딴다 |
| 워킹트리 | clean |
| `dist/` | 2026-08-30 16:51 빌드, `2a81233c8` 기준 — **재빌드 없이 0단계 측정 가능** |
| `node_modules` | 워크트리에 링크 없음. 상위 탐색으로 도는 명령이 대부분이나 빌드는 정션 필요 |

## 다음 행동

계획 **0단계**부터. `scripts/measure-app-route-perf.mjs` 를 새로 짜되 서버·에뮬레이션·스로틀·Tracing 은 전부 기존 스크립트에서 조립한다(계획의 「이미 있는 것」표). 베이스라인 5회 중앙값 + min–max 밴드를 이 문서에 표로 추가한다.

🔴 **1단계 절제 전에는 어떤 수정도 커밋하지 않는다.** [mobile-home-perf.md](mobile-home-perf.md) §3 에서 그럴듯한 레버 4개가 실측으로 죽었다.

## 사용자 쪽에 열려 있는 것

1. **`code-destiny-1.0.39-vc39.aab` 플레이 콘솔 업로드 미완료** (바탕화면에 있음). vc40 이 나오면 vc39 는 건너뛰어도 된다.
2. 🔴 **업로드 키스토어 비밀번호가 2026-08-30 세션 트랜스크립트에 평문으로 남았다.** 외부 전송·커밋은 없었고 파일은 gitignored 다. 로테이션(Play 앱 서명으로 업로드 키 교체) 여부는 사용자 판단이며 **미결**이다.
3. **네오 결과 화면 하단 safe-area 수정(PR #1318)은 미검증** — `?neoPreview=` 가 `NODE_ENV !== "production"` 전용이라 프로덕션 `dist/` 로 렌더 불가, `next dev` 는 이 레포에서 깨져 있다. 기기에서 눈으로 확인해야 한다.
