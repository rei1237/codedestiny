---
status: active
updated: 2026-08-30
next: "GSC 실적(검색결과 → 페이지·쿼리) 내보내기를 받아 '색인은 됐는데 노출이 없는' 페이지를 판정한다 — 커버리지 쪽은 종결됐다"
---

# GSC 커버리지 드릴다운 2차 — 판정: 커버리지에 고칠 것이 없다 (2026-08-30)

> 원본: `~/Downloads/code-destiny.com-Coverage-Drilldown-2026-08-30{,(1),(2)}/테이블.csv`
> 1차 문서는 [gsc-coverage-drilldown-2026-08-17.md](gsc-coverage-drilldown-2026-08-17.md) — **먼저 읽을 것.** 이 문서는 그 문서의 결론을 2주 뒤 데이터로 검증한 것이다.

## 1. 결론부터

**404 763건 · 크롤됨-미색인 382건 · 중복(표준 미선택) 4건 어디에도 새로 고칠 것이 없다.**

근거는 하나다 — 🔴 **404 763건의 최신 크롤 날짜가 2026-08-16 이다.** 1차 세션의 수정이 배포된 뒤로 신규 404 유입이 **0건**이고, 763건은 1차가 전수 분류한 754건과 같은 모집단이다.

## 2. 404 763건 분해 + 라이브 재측정 (2026-08-30)

| 패턴 | 건수 | 최신 크롤 | 라이브 실측 | 판정 |
|---|---|---|---|---|
| 로케일 `de-de`·`es-es`·`fr-fr`·`hi-in`·`ms-my`·`nl-nl` | **362** | 08-10 | 404 | 1차 §4 의 **의도적 방치** 유지 |
| 로케일 `en-us`·`ja-jp`·`zh-cn`·`ko` | 168 | 06-24 | **301 → 200** | 고쳐짐. 재크롤 대기 |
| `/fortune/**.html` | 104 | 07-28 | **301 → 200** | 고쳐짐 |
| `/insights/<슬러그>` (슬래시 유무 양쪽) | 58 | 08-16 | **301 → 200** | 고쳐짐 |
| `/fortune/**` 무확장 | 47 | 07-22 | **301 → 200** | 고쳐짐 |
| 기타 | 17 | 06-23 | — | 1차 §2-3 전수 조사 완료 |
| `/high-value/category/{faq-page,informational-article}` | 4 | 07-26 | 404 | 참조 0. 아래 §5 |
| `?v=build-` 자산 · 다른 호스트 | 3 | 07-03 | 404 | 삭제된 레거시 자산 |

재현(스크래치에서 즉석으로 쓴 3줄짜리다 — 레포에 커밋하지 않았다):
- **버킷 집계** — `테이블.csv` 를 읽어 URL 을 정규식 패턴표로 분류하고 버킷별 건수와 `최종 크롤링` 열의 min/max 를 찍는다. 🔴 **최신 크롤일을 함께 보지 않으면 이미 고쳐진 건을 다시 고치게 된다** — 이번 763건 중 401건이 그랬다.
- **라이브 재측정** — 경로 목록을 `fetch(url, { redirect: "manual" })` 로 돌려 `status` 와 `Location` 을 찍는다. 🔴 Git Bash 에서 `/insights/...` 같은 인자를 넘기면 `C:/Program Files/Git/...` 로 치환되므로 **경로는 파일로 넘길 것**.

## 3. 크롤됨-미색인 382건 — 실제 콘텐츠 페이지는 8개뿐

| 패턴 | 건수 | 판정 |
|---|---|---|
| 로케일 프리픽스(규칙 없음/있음) | 177 | §2 와 같은 모집단 |
| `/js/*.js?v=build-` · `manifest-*.json?v=build-` | 153 | 삭제된 레거시 자산. `/js/life-book.js` 는 페이지 참조 0 |
| 홈·`/insights` 쿼리 변종(`?from=footer-*`·`?topic=`·`?lang=`) | 29 | 🔴 **`?from=footer-*` 는 산출물에서 이미 0건**(`grep -roh 'from=footer' dist` → 0). 과거 잔재 |
| 실제 콘텐츠 페이지 | ~8 | 대부분 사이트맵 URL 의 **무슬래시 변종**(308 → 200). 정상 |

🔴 `/insights?q={search_term_string}` 는 WebSite JSON-LD 의 `SearchAction.target` 템플릿을 구글이 문자 그대로 크롤한 것이다. 스키마 표준 용법이라 **고치지 않는다.**

## 4. 중복(표준 미선택) 4건 — 무시 가능

`/destiny-poker?lang=en` · `/geomancy-oracle-v4?lang=en` · `/{es-es,de-de}/insights/asia-divination-traditions-deep-guide`.
앞 둘은 canonical 이 **200 을 반환하는 무슬래시 정본**을 정확히 가리킨다(실측: `/destiny-poker` 200, `/destiny-poker/` 308 → `/destiny-poker`). 뒤 둘은 §2 의 죽은 로케일이다.

## 5. 이번에 **새로 나온** 것 — 가드 사각지대 (PR #1306)

🔴 **`verify:indexable-prose-depth` 는 사이트맵 URL 만 검사한다.** "인덱서블인데 사이트맵에 없는" 페이지는 어떤 가드도 안 본다. `dist/` 744개 HTML 전수 스윕 결과 **7개**가 나왔다.

| 페이지 | 본문 | 조치 |
|---|---|---|
| `/500/` | **0자** | ✅ PR #1306 — `pages/404.tsx` 에는 있던 robots 선언이 `pages/500.tsx` 에 없었다 |
| `/ifa-oracle-about.html` | 745자 | ✅ PR #1306 — `noindex,follow` |
| `/sukuyo/calendar/` | 1668자 | ⬜ **판단 남음.** 실제 도구 페이지라 임의로 손대지 않았다 |
| `/destiny-poker.html` | 2822자 | ✅ canonical 이 200 을 가리킴. 정상 |
| `/contact-us/`·`/privacy-policy/`·`/terms-of-service/` | 3.2k~12.3k | ✅ 의도됨. AdSense 신뢰 지표라 인덱서블이 맞다 |

재현: `dist/**.html` 를 전수 순회하며 (a) `robots`/`noindex` 없는 것, (b) `dist/sitemap.xml` 의 `<loc>` 집합에 없는 것만 남기고 태그를 걷어낸 본문 길이로 정렬한다. 스크래치에서 즉석으로 썼고 커밋하지 않았다.

🔴 **`ifa-oracle-about.html` 은 루트/`public/` 두 벌이 바이트 동일해야 한다.** 이 파일은 `scripts/sync-legacy-static-to-public.mjs` 의 **손으로 쓴 `staticTargets` 배열**에 없어 자동 미러링되지 않는데, `sync:public` 이 `.ignore` 미러 목록을 만들 때 "루트와 바이트 동일한 `public/` 사본"만 넣는다. 그래서 **한쪽에만 주석을 달아도** `.ignore` 에서 항목이 빠지고 `verify:public-mirror-fresh` 가 CI 에서 실패한다 — 실제로 이번에 한 번 밟았다(수정 커밋 별도). 🔴 **로컬 윈도우의 `.ignore` 실패는 개행 위양성이라 CI 를 봐야 구분된다.** CLAUDE.md 원칙 10 의 "손으로 쓴 대상 목록은 가드가 아니다" 실례이기도 하다.

## 6. 남은 일

1. **커버리지는 종결.** 재크롤로 소멸할 것을 기다리는 것 외에 코드 작업 없다. 🔴 **§2 의 362건 방치 결정을 다시 검토하지 말 것** — 1차 §4 의 근거 4개 중 무엇이 바뀌었는지 먼저 적을 것.
2. **`/sukuyo/calendar/` 판단** (§5) — 사이트맵 등재(본문 보강 필요, 현재 1668자로 임계 미달) vs `noindex`.
3. 🔴 **아직 못 받은 자료: GSC 실적(검색결과 → 페이지·쿼리, 최근 3개월).** 커버리지는 "구글이 URL 을 어떻게 처리했나"만 말하고 **"콘텐츠가 값어치를 하나"는 말하지 않는다.** AdSense 판정과 연결되는 것은 실적 쪽이다.
4. **가드 확장 후보** — `verify:indexable-prose-depth` 에 "인덱서블 미등재" 축을 추가하면 §5 부류가 재발하지 않는다. 이번엔 정본 가드 변경 범위가 커서 하지 않았다.

## 7. 🔴 AdSense 와의 관계 — 오해 방지

**404 는 AdSense 심사 항목이 아니다.** 이번 데이터로 "가치가 별로 없는 콘텐츠" 거절이 설명되지 않는다.
그리고 AdSense 콘솔이 **재검토 요청 횟수 소진 → 2026-09-05 부터 재요청 가능**을 표시하고 있다. 그 전에 들어가야 하는 것은 PR #1301·#1305·#1306 의 **머지와 프로덕션 승격**이지 커버리지 정리가 아니다.
