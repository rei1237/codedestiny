---
status: handoff
updated: 2026-09-15
next: "최신 체크포인트를 읽고 main/CI 확인 후 꿀편지 저장·차감 유실 mock부터 재개한다."
---

# 유료 LLM 결과 전달 후속 인수인계



## 최신 체크포인트 — 사용자 요청으로 세션 인수인계 (2026-09-15)

**이 절과 바로 아래 최신 기능별 기록을 우선한다. 뒤쪽 최초 조사·옛 남은 작업·옛 CI 실패 기록은 당시 이력이며 현재 미완료 목록이 아니다.**

- 마지막 구현: `982b2a813d26ddf9546dc581a6cb168dd9cef9c5` (`fix(tarot): deliver paid love reading with saved progress`), main push 완료. 공식 PR CI `34901003368`은 타입/빌드/정적 성공, Critical의 옛 locale 문자열 검사 1건 실패(3487건 성공)였다. `27b9abbaa33bd64e183f597c2588feb0069df41a`에서 변수명 문자열 단언을 순수 locale 문맥 검사와 실제 라우트의 외국어 본문 저장·렌더·재열람 검사로 교체했다. 해당 선택 Jest 54건 통과. 후속 PR CI `34901581659`는 Build, Critical, Static, Typecheck/lint, CI required 모두 성공 확인했다. 이전 `28e78bfe4fdbee7e89c0c9f364803a33f1fa1a86` / CI `34899595899` 성공 확인.
- 사용자 우선순위: 모든 실제 LLM 상품에서 결제 이후 생성이 끊기지 않게 짧은 요청으로 자동 이어가기 → 정상 부분 확정 저장 → 모바일에서 마지막 본문 전달. 기존 더 큰 분량은 줄이지 않는다. 상세 유료 본문 최소 20,000자, 짧은 후속 대화·정해진 시간의 요가·카드 연결문은 각 기능 계약 유지. 기존 완료 구매 결과에 새 분량 기준을 소급하지 않는다.
- 구현/선택 검사/main CI로 전달한 범위는 아래 기능별 기록에 있다: 사주 질문·연애 비책·인생의 책/총운·신년, 심화 자미두수 PDF·초융합·숙요 궁합·점성술·베다·일반 자미두수·카르마·네오·나크샤트라·마스터 코덱스·휴먼 디자인·운명의 지도·운명의 섬·작명·셀레스티얼·관계 경계·반려동물·동물·꿈·지오맨시·요가·찻집·오라클·마인드스캔. 연애 6카드는 이번 구현. **모든 SKU와 모든 짧은 대화 경로 완료를 뜻하지 않는다.**
- 이번 연애 단위 이후 코드 수정은 시작하지 않았다. 꿀편지와 fortune 잔여 경로는 아래 읽기 조사만 수행했다. 인수인계 문서 외 미커밋 작업이 없는 상태에서 전달한다. 본 세션의 loopback mock 프리뷰 프로세스는 종료했으며 3106/3107/3108 listen 없음도 확인했다. TEMP 합성 fixture/스크린샷은 보조 자료이며 재현에 필요한 저장소 테스트는 커밋했다.

### 다음 세션 시작점과 남은 위험

1. **꿀편지** — `worker/routes/fortune-tea-house.js`의 `generateHoneyLetter`, `handleHoneyLetter`, `refundHoneyLetterSpend`; 실제 회귀 하네스 `__tests__/worker/fortune-tea-house-honey-drops.test.js`. 짧은 800~1200자 후속 편지이며 상세 2만자 기준 대상이 아니다. 현재 2회×22초 호출, TTL 없는 honeyLetterLock, LLM 후 wallet 차감→spend ledger→최종 result 저장 순서. 최종 저장 예외가 generation_failed로 섞이며 차감 응답 유실/중단 후 중복 소비·영구 lock 위험이 남는다. 먼저 실제 라우트 mock으로 재현하고, 생성문 선보존·잠금 만료·원래 증빙·차감 멱등성·저장 불확실성 분리를 설계한다. 기존 ‘최종 저장 실패가 확정되면 꿀방울 환급’ 테스트/정책을 유지한다. 불확실한 저장/차감 응답에 추정 환불을 추가하지 않는다. 새 영수증 저장 구조는 아직 구현/확정하지 않았다.
2. **fortune 질문형 API** — `worker/routes/fortune.js`의 `runFeatureAiConsultation`, `handleAstrologyAIPrompt`, `handleVedicAIPrompt`, `handleZiweiAIPrompt`, `handleSukuyoAIPrompt`. 현재 단일 긴 결과+이어쓰기 뒤 200/400자 문턱만으로 성공할 수 있고 별도 결과 저장 없이 반환하는 경로가 남아 있다. 실제 활성 호출: `js/saju-engine.js`의 `/api/fortune/astrology/ai-prompt`, `/api/fortune/ziwei/ai-prompt`; `js/saju-engine-tarot-sukuyo-quantum.js`의 `/api/fortune/sukuyo/ai-prompt`. 베다 실제 호출부는 더 추적해야 한다. 이미 수정한 각 체계의 ‘전체 상세 리포트’ API와 혼동하지 말고 상품 registry/가격/유료 재개 kind를 대조한다. 질문형이라고 임의로 2만자 예외로 정하지 말고 판매 계약/사용자 요구와 맞춘다.
3. **전체 실제 LLM 목록 대조** — guardian/fortune-chat/prashna/generate, 손금 vision 등 간접 호출과 카르마 등 짧은 후속 대화의 긴 시간 예산을 조사한다. 무료·결정론 타로 draw/reading/연운/숫자/이직/crystal-soul에 LLM을 새로 넣지 않는다. 기존 조사 표는 시작점일 뿐 SKU 전수 완료표가 아니다.
4. **남은 검증 한계** — 실 LLM 의미 품질, 자연어의 모든 계산 모순 검출, 실 PG/실기기 결제 앱 복귀/운영 DB/배포는 미검증. background Worker를 신설한 것이 아니므로 장기 오프라인에는 저장 후 사용자 복귀가 필요하다. 최신 pending/계정별 로컬 ID 복구이며 모든 과거 결과의 새 목록을 만든 것은 아니다. 기능별 과거 미완료 스냅샷/결제 복귀 입력 부재의 제한은 각 절을 확인한다.

### 이어받을 실행 규칙

- main/동시 편집 상태와 원격을 먼저 확인한다. 다른 변경은 보존하고 명시한 파일만 stage한다. 혼자면 main 직접, 동시 세션일 때만 저장소의 safe-worktree 규칙. 브랜치/PR은 만들지 않는다.
- 실제 라우트 mock + 외부 fetch 차단. 실 제공자·실결제·운영 DB fallback 금지. 재개는 같은 사용자/결과/원래 requestId·증빙·언어·계산 입력, 취소/환불 확인, 추가 소비 없음. 202 부분/503 저장 실패를 완료로 처리하지 않는다.
- `npm run check:fast -- --plan` 후 관련 targeted 검사. 전체 로컬 lint/typecheck/test/build 반복 금지. 검증 단위 commit→push origin main→PR CI의 CI required 성공 확인. staging/배포 검증은 하지 않는다.
- 정적 JS cache hash는 import 단계마다 전파된다. 이번에는 순차 sync 후 고정점까지 확인해야 했다. `node scripts/sync-legacy-static-to-public.mjs`, 필요한 sitemap 생성, 커밋 후 깨끗한 트리에서 `node scripts/verify-public-mirror-fresh.mjs` 통과를 확인한다. 진단 실패를 숨기거나 generator 계약을 약화하지 않는다.
- 컨텍스트가 길어지면 진행 기능을 검증 가능한 단위에서 마감하고 새 기능에 착수하지 않는다. 실제 변경/테스트 수/CI/남은 위험/작업 트리/정확한 다음 행동을 두 문서의 최상단에 기록한다. 최종 전달에는 두 파일명 코드 블록, 절대 링크, 실제 SHA와 경로가 든 재개 명령을 제공한다. 문서가 commit/push/CI를 대체하지 않는다.

### 복사할 최신 재개 지시

```text
D:\Development\code-destiny에서 D:\Development\code-destiny\docs\handoff\paid-llm-delivery-20260914.md와 D:\Development\code-destiny\docs\handoff\paid-llm-delivery-findings-20260914.md의 최상단 최신 체크포인트를 먼저 읽어라. 마지막 코드·검사 커밋 27b9abbaa33bd64e183f597c2588feb0069df41a(구현 982b2a813d26ddf9546dc581a6cb168dd9cef9c5)가 main에 포함됐는지와 최신 main CI, 동시 편집·미커밋 상태를 확인하고 기존 변경을 보존하라. 꿀편지의 저장 예외/차감 응답 유실 mock 재현부터 시작하고 fortune ai-prompt 잔여 실제 호출부를 이어 점검하라. 가격·환불 정책과 이미 충분히 긴 본문은 유지하며 결제 후 생성·저장·모바일 전달을 우선하라. 실 LLM·실결제·운영 DB·배포 없이 실제 라우트 mock으로 검증하고 검증 단위만 commit/push/main CI 확인하라. 컨텍스트가 길어지면 새 기능을 시작하지 말고 검증된 단위에서 두 문서와 다음 재개 명령을 갱신하라.
```

## 연애 6카드: 결제 후 같은 요청으로 끊김 없이 연속 생성 (2026-09-15)

- 실제 활성 화면은 메인 정적 연애 모달이다. 결제 전 LLM prefetch를 제거하고 결제 게이트/복귀 descriptor/생성 API를 원래 requestId로 연결했다. 보존된 React LoveRelationshipTarot 컴포넌트도 같은 계약으로 수정했으나 현재 app 호출부는 없으며 실제 페이지 검증 대상으로 주장하지 않는다.
- 기존 6카드/방향/위치 계산과 가격·이용권·월정석·단건 결제를 유지한다. 기존 실행 컬렉션에서 21부분(하한 합계 26,400자)을 요청당 4부분 병렬·45초·부분당 누적 3회로 생성한다. 필수 부분/본문 20,000자/근거 해시/반복 검사/최종 저장 재조회 후만 완료한다. 정상 부분은 재사용하고 제공자 fallback·잘린 응답·짧은 기본 해석 완료를 허용하지 않는다.
- POST love-reading 및 GET love-result에서 소유권·원래 증빙·취소/환불을 재확인한다. 202는 다음 요청으로 자동 연결하고 저장 장애는 503 RESULT_STORAGE_UNAVAILABLE이다. 정적/React 클라이언트는 계정별 원래 입력·결과 ID를 보존하며 저장 장애에 결제창을 다시 열지 않는다. 정적 모달은 서버 복구, 화면/네트워크 복귀, 목차·읽던 스크롤·긴 본문 줄바꿈을 제공한다.
- 저장/전송 장애는 환불하지 않는다. 기존 정적 화면의 생성 실패 환불은 서버가 누적 생성 한도 도달을 확정하고 원래 차감 transactionId가 있을 때만 동일 환불 요청 ID로 실행한다. 모바일 복귀에서 원래 차감 ID가 없는 경우 자동 환불을 새로 추정하지 않는다.
- 실제 라우트 30건 및 timeout 보호 27건, 총 57건 통과(임시 합성 preview fixture 제거 후 최종 재확인). 3결제, 저장 throw/null/확인 유실, 부분 재사용, 누락/짧음/잘림/중단/mock/해시 불일치, 동시 요청·계정·증빙 거부·3회 한도를 검증했다. 실제 정적 JS/jsdom 검사는 즉시 카드, 결제 전 호출 0, 동일 ID, 202/503 재개, 재열람, 확정 실패 환불, 계정 전환을 검사한다. 외부 fetch/제공자/DB/결제는 mock이다.
- 정본 index.html 모달 DOM·CSS·실제 JS를 격리 브라우저에 올려 390/1280px에서 서버 pending → 원래 결과 ID POST 1회 → 마지막 본문 → 새로고침 재열람 통과, 페이지 오류/가로 넘침 0. TEMP/love-partial-* 및 love-ending-*는 합성 본문이다. 전체 메인 셸/실기기/실 PG/실 LLM/운영 DB/배포 검증은 아니다.
- check:fast -- --plan critical, 선택 ai-consultation-flows 20.3초, resilience 918, worker-security 및 paid-resume React 41/정적 47/kind 44 통과, 변경 lint 오류 0. 전체 로컬 검사는 반복하지 않는다. 정적 미러/사이트맵을 갱신하고 main CI는 push 후 확인한다.
- 마인드스캔 4c55의 정적 검사는 공유 접근 함수의 선택적 다섯 번째 인수를 오탐했다. 28e78bfe4fdbee7e89c0c9f364803a33f1fa1a86에서 AST로 필수 인수와 선택 인수 계약을 검사하도록 수정했으며 공식 main CI 34899595899 성공이다.
- 남은 범위: 꿀편지의 차감/최종 저장 불확실성, fortune ai-prompt 실제 호출 경로 및 짧은 후속 상담의 시간 예산. 자연어 의미 모순 전체 검출이나 오프라인에서도 계속 도는 백그라운드 작업자를 구현한 것은 아니다.

## 마인드스캔: 7개 위치 해설을 저장하고 끝까지 전달 (2026-09-15)

- 기존 5쌍 카드에서 7개 위치/메인·보조 카드/정역방향을 확장하는 정본 계산을 그대로 사용한다. 새 buildMindscanDeliveryFacts는 실제 카드 5쌍을 검증하고 기존 질문별 상담 근거·카드 상징·주제 고정 규칙을 만든다. 계산한 위치와 방향은 LLM이 바꾸지 않으며, 상대 마음을 확정하거나 감시/압박을 권하지 않는 기존 상담 원칙을 유지한다.
- 유료 /api/tarot/mindscan은 기존 실행 컬렉션/CAS에 원래 질문·카드·언어·requestId·증빙을 보존하고, 위치별 근거/감정/행동 21부분과 종합 4부분을 생성한다. 부분 하한 합계 26,000자, 전체 본문 20,000자 및 필수 부분/해시/반복/종합 열 문단/최종 저장 확인을 완료 기준으로 쓴다. 요청당 4부분 병렬, 제공자 45초/9500토큰, 부분당 누적 3회. 정상 부분은 저장 후 재사용하며 실패한 부분만 이어 쓴다.
- 202 진행/503 RESULT_STORAGE_UNAVAILABLE을 분리하고 저장 실패를 기본 해석 완료나 자동 환불로 처리하지 않는다. 기존 카드/키워드/조합/척도는 보존하되 핵심 유료 해설 필드는 생성된 본문으로 채운다. 과거 유틸리티의 로컬 fallback 계약은 보존했으나 활성 유료 라우트에서는 호출하지 않는다. GET /mindscan-result는 계정의 지정 결과/최신 미완료 결과를 제공하고 재개 시 접근·취소·환불을 확인한다.
- 실제 React 결제 진입/복귀가 같은 요청 ID와 입력을 보존하며 생성은 공유 authFetch의 95초 예산으로 이어진다. 계정별 서버/로컬 복구, 202 자동 다음 요청, 전송 3회 재시도, 화면/네트워크 복귀, 본문 위 재개 버튼, 저장 실패 중 정상 본문 유지, 계정 전환의 늦은 응답 차단을 연결했다. 완료 전에는 기본 요약 대신 저장된 부분을 먼저 제공한다. 완료 본문의 목차·읽던 위치·긴 해설 줄바꿈/본문 크기·가로 넘침을 확인하고 준비된 결과를 시간차로 숨기는 표시 대기는 제거했다.
- 실제 라우트 30건과 기존 오라클/타로 게이트/timeout 보호 포함 Jest 100건 통과. 3결제 경로의 저장 throw/null/확인 유실, 정상 부분 재사용, 짧음/누락/잘림/중단/mock/해시 불일치, 3회 한도, 원래 언어/입력/카드/방향, 접근·취소·계정 격리·동시 요청을 검증했다. 공유 실제 UI 연속 요청 함수 8건 및 immersive/timeout 포함 Node 28건 통과. 외부 fetch·제공자·DB·결제는 mock으로 차단했다.
- 실제 Next mock의 390/1280px에서 서버 pending 복원 → 같은 저장 ID POST 1회 → 마지막 본문 → 새로고침 재열람 → 다른 계정에서 이전 결과 비노출 통과, 페이지 오류/가로 넘침 0. TEMP/mindscan-partial-390/1280.png와 mindscan-ending-390/1280.png는 합성 본문이며 실 LLM 품질 증거가 아니다.
- check:fast -- --plan critical, 선택 ai-consultation-flows 14.5초, resilience 918, Mongo query 939/위반 0, cron/security/paid-resume/auth-listener, 변경 lint 오류 0. 기존 Impeccable 장식 제목 권고 1건은 유지한다. 정적 동기화/사이트맵 추적본을 생성하고 공식 main CI는 push 후 확인한다. 전체 로컬 lint/typecheck/test/build는 반복하지 않는다.
- 타로 오라클 986d852b9b706bddd6e778763084b7939d841bd2의 main CI 34897211705는 Typecheck/lint, Build, Critical, Static, CI required 모두 성공했다. 마인드스캔의 실 제공자 품질·실PG·실기기 결제 복귀·운영 DB·배포는 미검증이다. 백그라운드 작업자를 새로 만들지 않았으며 장기 오프라인/한도 도달은 저장 상태를 유지하고 재개/확인을 안내한다. 다음은 연애 6카드, 꿀편지, fortune 잔여 실제 LLM 경로다.

## 타로 오라클: 결제 후 부분 저장과 자동 연속 생성 (2026-09-15)

- /api/tarot/oracle-consultation을 기존 실행 컬렉션의 paidNarrative 저장 흐름으로 연결했다. 카드 1~14장에 따른 기존 4개 가격 티어/이용권 판정을 유지하며 제출 카드 수에서 서버가 티어를 결정한다. 원래 요청/증빙/카드/방향/질문/언어를 고정하고 재개마다 소유권·접근·취소/환불을 확인한다. GET /oracle-result는 소유자의 지정 결과 또는 최신 미완료 결과를 반환한다.
- 공통 해석 12부분과 카드별 해석/조합을 분리해 요청당 최대 4부분 병렬, 제공자 45초, 부분당 누적 3회로 제한한다. 정상 본문과 시도 횟수를 저장·재조회하고 미완료 부분만 이어 쓴다. 카드/질문 영역 정본 프롬프트를 유지하고 전체 JSON의 짧은 필드 지시는 새 부분 계약으로 대체한다. 본문 최소 20,000자 및 모든 부분 하한/해시/반복 검사·최종 저장 확인을 통과해야 완료다. 기존 더 큰 설정 목표도 유지한다.
- 기존 긴 단일 호출의 llm_short 성공·프롬프트 복사로 유료 실패를 대신하던 활성 라우트 경로를 제거했다. 옛 라이브러리/어댑터는 참조 중인 유틸리티 검사 계약 때문에 보존했으나 이 유료 라우트에서는 사용하지 않는다. 미완료 202, 저장 throw/null/확인 유실 503 RESULT_STORAGE_UNAVAILABLE이며 저장된 결과는 timeout 환불 대상에서 보호한다. 추가 차감·보상 정책은 추가하지 않았다.
- 실제 React 화면에서 결제 전 원래 입력/ID를 보존하고 계정별 로컬/서버 결과를 복원한다. 202는 완료로 보지 않고 저장한 본문/진행 수를 표시하면서 다음 부분을 자동 요청한다. 전송 실패는 같은 ID로 최대 3회 재시도하며, 네트워크/화면 복귀 또는 본문 위의 이어서 생성하기로 계속한다. 95초 생성 요청 예산, 늦은 계정 응답 차단, 저장 실패 중 기존 본문 유지, 목차/읽던 위치/줄바꿈/가로 넘침을 반영했다. 완료 결과는 저장 ID로 재열람하며 새 결제창·LLM 호출이 없다. 레노먼드의 기존 무료 프롬프트 전용 동작은 유지한다.
- 실제 라우트 35건 통과: 3결제 경로, 1/4/7/10/14카드 티어, 낮은 티어 증빙 거부, auth 401/403/503, 정상 부분 재사용, 저장 throw/null/완료 확인 유실, 누락/짧음/잘림/중단/mock/해시 불일치, 동시 요청, 원래 언어/입력/소유자/취소·접근 거부, 3회 한도. 다른 타로/timeout 보호 포함 Jest 98건, 실제 UI 연속 생성 함수 7건 및 기존 요청 timeout 포함 Node 24건 통과. 외부 fetch·제공자·DB 접근은 mock으로 차단했다.
- 실제 Next 페이지의 격리 Playwright 390/1280px에서 로컬 기록 없는 서버 pending 복원 → 같은 ID POST 1회 → 마지막 문단 → 새로고침 재열람 통과, 페이지 오류/가로 넘침 0. 합성 본문 screenshot은 TEMP/oracle-partial-390/1280.png 및 oracle-ending-390/1280.png. 실 LLM 문장 품질·실기기·PG 복귀·운영 DB·배포는 미검증이다.
- check:fast -- --plan critical, 선택 ai-consultation-flows 14.5초, resilience 918, Mongo query 939/위반 0, cron/security/paid-resume/auth-listener, 변경 lint 오류 0. Impeccable의 기존 장식 제목 권고 1건은 범위 밖으로 유지한다. 전체 로컬 lint/typecheck/test/build는 반복하지 않았다. 정적 동기화와 sitemap 추적본도 확인한다. 이번 작업의 공식 CI는 push 후 확인한다.
- 이전 찻집 50ca903fc41a2f8f15cd15f902553f1ff7bf0d16의 CI 34894898387은 타입/빌드/critical 성공, sitemap 서명 누락으로 static 실패였다. 생성기로 서명만 갱신한 af9604e3d33c0cb44726eb8c72725889b9452c42의 main CI 34896000839는 CI required 포함 성공했다. 가격·내용·사이트맵 URL 변화 없이 추적 signature만 갱신했다.
- 한계: 제공자 장애 자체의 제거/백그라운드 서버 작업자는 아니다. 각 요청의 정상 부분을 확정 저장하고 브라우저가 연속 요청한다. 장기 오프라인/호출 한도 도달 후에는 저장 내용과 재개 정보를 유지한다. 계정별 최신 결과 ID/최신 미완료 복구이며 과거 결과 전체 목록을 신설하지 않았다. 카드 정체성은 고정 프롬프트/출력 위치로 묶으나 모든 자연어 의미 오류를 완전 검출한다고 주장하지 않는다. 다음은 tarot love-reading/mindscan 및 fortune/꿀편지 잔여 실제 LLM 경로다.

## 찻집: 요청을 짧게 나누고 정상 본문을 저장하며 이어 제공 (2026-09-15)

- 기존 저장 장애 복구 위에 생성 중 checkpoint를 추가했다. 요청당 최대 4부분을 병렬로 생성하고 제공자 45초/하드 제한 49초, 부분당 누적 3회로 제한한다. 정상 부분과 시도 횟수를 기존 결과 컬렉션에 저장·재조회하고 다음 요청은 미완료 부분만 작성한다. 제공자 실패/잘림/mock/짧은 결과를 기본 문구 완료로 제공하지 않는다. 원래 입력·명식/카드/숙요 계산·언어·요청/결제 증빙은 보존한다.
- 본문 20,000자 완료 기준(제목·마크다운 기호·공백 제외), 모드별 필수 섹션, 전체 반복 문장 검사를 적용했다. 타로는 각 카드 5개 해설 필드를 검증하고 사주 단독 상담은 기존 십성 확정표 검사도 적용한다. 사주/궁합의 기존 카테고리별 제목은 유지한다. 기존 완료·구매 결과에는 새 하한을 소급하지 않는다. 이전 짧은 필드 권장 분량과 새 목표의 충돌을 제거하고 프롬프트 캐시를 v3으로 분리했다.
- 202에는 저장한 부분과 전체 개수/같은 resultId/재시도 가능 여부를 제공한다. 생성 저장 예외/null/재조회 실패도 503 RESULT_STORAGE_UNAVAILABLE이며 cancel/환불/완료/리워드로 넘어가지 않는다. 최종 본문 보존 → 기존 deferred apply → completed 저장 확인 → 리워드 순서와 이전 응답 유실·취소·환불 검증은 유지한다.
- 로그인 후 로컬 재개 기록이 없으면 서버의 최신 미완료 상담을 조회한다. 검증된 사용자 캐시가 없던 재로그인에서도 공유 인증 스토어를 먼저 초기화한다. 계정별 원래 요청을 이어 사용하며 저장 본문은 로딩 중과 재시도 화면에서 먼저 읽을 수 있다. 최종 완료 전 재개 기록을 지우지 않는다. 완료된 타로 상담문도 즉시 열고 카드 공개 연출은 기존 결과 화면의 다시 보기에서 제공한다.
- 실제 라우트/기존 결제 apply/모드 검사 105건 통과: 이용권·월정석·단건별 생성 부분 저장, 정상 부분 재사용, 잘림, 저장 throw/null/확인 유실, apply 응답 유실, 추가 차감 없음, 완료 전 리워드 없음, 동시 요청, 원래 증빙 취소/환불/계정 격리 등을 확인했다. 사주 정상 완료와 십성 모순 부분만 재작성, 숙요 계산값 보존 완료, 3/5카드 완료도 포함한다. 외부 fetch/실 제공자/DB는 mock으로 차단했다.
- 실제 재개 함수 13건 및 관련 UI/static/CAS 검사 포함 33건 통과. 격리 Next mock 페이지 390/1280px 모두 로컬 기록 없는 서버 pending → 같은 원문 POST 1회 → 완료/마지막 문단 → 새로고침/기존 상담 기록 재열람 통과, 오류·가로 넘침 0. 새 결제창과 재열람 LLM 호출 없음. screenshot은 TEMP의 tea-partial/complete/ending-390/1280.png이며 합성 본문이다.
- check:fast -- --plan critical, 선택 ai-consultation-flows 14.0초, resilience 918, Mongo query 938/위반 0, cron/security/paid-resume/auth-listener/hero-contrast/mobile-detail 검사와 변경 lint 오류 0. 전체 로컬 lint/typecheck/test/build는 반복하지 않고 공식 CI를 사용한다.
- 요가 ae0576e8cee6e7a4e49904a0ec4c014c0415b40d의 main CI 34891225239는 CI required 포함 성공했다. 이번 찻집 CI는 push 후 확인한다. 남은 범위는 꿀편지 짧은 후속 요청, tarot.js 및 fortune.js의 잔여 실제 LLM 경로다.
- 한계: 네트워크/제공자 장애 자체를 없애지는 못한다. 자동 실행 한도 뒤에는 저장 내용을 보존하고 추가 확인을 안내한다. 서버 복구는 최신 미완료 1건이며 전체 미완료 보관함을 신설하지 않았다. 전체 결과의 모호한 품질 실패는 제한 내에서 여러 부분을 다시 검토할 수 있다. 십성 단독 명식 검사 외 모든 자연어 의미 모순이나 사주 궁합의 두 사람 혼동을 완전히 검출하는 것은 아니다. 실 LLM 품질·실결제·실기기 결제 앱 복귀·운영 DB·배포는 미검증이다.

## 요가 구루: 코스 생성 시간 정렬과 저장 후 제공 (2026-09-15)

- 이전 브라우저 29~43초 중단/다중 endpoint 재시도와 서버 55초 생성 예산의 불일치를 제거했다. 제공자 1회 최대 45초, 클라이언트 90초, 원래 코스당 누적 3회다. 기존 실행 컬렉션에 원래 상태/30·60분/프롬프트/언어/결제 요청을 저장하고 같은 결과 ID로 재개한다. 완전한 코스를 먼저 보존한 뒤 최종 저장/재조회 확인 후에만 200 완료를 반환한다.
- 누락된 필드·단계·안내를 기본 코스로 채워 성공 표시하던 신규 생성 경로를 제거했다. 단계 4~16개, 첫 Warmup/마지막 Relaxation, 필수 안내와 시간 합계(1,800/3,600초)를 검증한다. 기존 800자 수련 안내 하한은 유지하고 생성 목표 1,600~2,400자를 명시했다. 이 상품은 시간에 맞춘 동작/호흡 코스로, 상세 운세 리포트 20,000자 기준으로 동작 지시를 늘리지 않았다.
- 저장 예외/null/확인 유실은 503 RESULT_STORAGE_UNAVAILABLE, 미완료는 202와 같은 재개 ID다. 서버의 기존 결제 접근/취소·환불을 재확인하고 CAS 잠금으로 동시 생성을 막는다. 브라우저가 모든 오류를 /billing/refund로 넘기던 경로를 제거해 저장 장애·응답 유실을 생성 실패 환불로 처리하지 않는다. 기존 가격 30분/60분 및 이용권/월정석/단건, 서버 환불 정책은 유지한다.
- 결제 전에 요청 ID와 선택 시간을 고정하고 기존 재개 descriptor에 포함한다. 계정별 로컬 재개 정보와 서버 pending 조회, 온라인/화면 복귀, 같은 요청 재시도, 계정 전환의 늦은 응답 차단을 연결했다. 수련 단계 전체는 저장 후 바로 읽을 수 있으며 기존 시간별 자동 안내는 유지한다. 재열람 시 열린 단계와 음악 조작부를 복원하고 음악 패널은 접어 본문을 가리지 않게 했다.
- 실제 라우트 28건과 로케일/timeout 보호 포함 62건, 실제 HTML UI 3건 통과. 이용권/월정석/단건 및 두 코스 완료, 저장 throw/null/확인 유실, 잘림/누락/시간 불일치/중단/mock 거부, 동시 요청, 원래 언어, 접근/취소/계정 변경, 3회 한도와 재열람 추가 LLM 없음 등을 검증했다. UI에서는 저장 실패 재시도에 새 결제/환불 요청이 없음을 확인했다.
- 실제 정적 페이지의 격리 브라우저 mock에서 30/60분 × 390/1280px 네 조합 모두 pending → 같은 ID POST 1회 → 완료 → 새로고침/마지막 단계 복원 통과, 오류·가로 넘침 0. 외부 네트워크/제공자/DB/결제는 차단했다. 실기기·실PG 복귀·실 LLM 코스 품질/안전성·운영 DB·배포는 미검증이다.
- check:fast -- --plan critical, 선택 ai-consultation-flows gate 13.9초, resilience 918, security/paid-resume/auth-listener 및 lint 오류 0 확인. sync:public과 sitemap 1,264 URL 일치도 확인했다. 전체 로컬 검사는 반복하지 않았다.
- 해몽의 줄바꿈 정규화 d9cfe7426c277813ed58c5be40e251731e27c690 main CI 34889561001은 성공했다. 지오맨시 63eb2be0b 및 사이트맵 서명 916e0d87250225c7adf347a6ff392652a8a046cd의 main CI 34890122583은 CI required 포함 성공했다. 복수 과거 코스 보관함과 자동 안내의 초 단위 재생 위치 복원은 별도이며, 마지막 저장 코스/열린 단계를 복원한다. 다음은 찻집/타로/fortune 잔여다.

## 지오맨시 오라클: 결제한 점괘 고정과 연출 중 생성 (2026-09-15)

- 실제 oracle.js의 유료 경로는 /geomancy다. 이집트·IFA·로열 티 등을 이 파일의 완료 범위로 추정하지 않았다. 기존 7개 응답 항목을 각각 3,000자 이상으로 생성해 합계 21,000자 하한/전체 20,000자 완료 기준을 적용한다. 제공자 최대 45초, 4부분 병렬, 부분당 누적 3회다. 실패 시 기본 문구를 성공으로 제공하지 않고 정상 부분을 저장한다.
- 기존 실행 컬렉션/CAS 잠금/부분 저장을 재사용한다. 원래 질문·세 형상·테마·언어·requestId·결제 증빙을 고정하고 재개마다 소유자/기존 결제 접근/취소·환불을 확인한다. 저장 예외/null/재조회 실패는 503 RESULT_STORAGE_UNAVAILABLE, 미완료는 202, 최종 저장 확인 후 completed다. 새 차감/리워드/환불 정책은 추가하지 않았고 timeout 정리에서 보존된 오라클을 보호한다.
- 결제 전에 16행의 점괘와 요청 ID를 정하고 기존 결제 재개 descriptor에 보존한다. 생성은 점괘 연출과 동시에 시작하며 같은 요청을 이어 읽는다. 90초 브라우저 예산, 계정별 저장 ID/입력, 서버 pending 복구, 온라인/화면 복귀, 늦은 계정 응답 차단, 항목 목차/읽던 위치를 연결했다. 긴 본문을 글자마다 기다리던 동작을 제거했다.
- 라우트 24건과 로케일/timeout 보호 포함 57건, 실제 HTML 스크립트 UI 2건 통과. 이용권/월정석/단건 각각 저장 throw/null/완료 확인 유실, 정상 부분 재사용, 짧음/누락/해시 불일치/잘림/중단/mock 거부, 동시 요청, 원래 입력/언어, 접근 거부/취소, 3회 한도를 검증했다. 결제 도구·제공자·DB·외부 fetch는 mock으로 차단했다.
- 실제 정적 페이지의 격리 브라우저 mock에서 390/1280px 모두 서버 부분 표시 → 같은 ID POST 1회 → 완료 → 새로고침/읽던 항목 확인, 오류·가로 넘침 0. 복구 시 시작 화면이 결과 클릭을 막던 문제를 수정했다. 본문 표시와 목차 대비를 확인했으며 기존 장식/작은 글자에 대한 Impeccable 권고는 유지 범위다.
- check:fast -- --plan critical, 선택 ai-consultation-flows gate 14.3초, resilience 918, Mongo shape 938/위반 0, cron/security/paid-resume/auth-listener, lint 오류 0 통과. sync:public 및 sitemap 1,264 URL 일치 확인. 전체 로컬 검사는 반복하지 않았다.
- 해몽 393c4572991e25f3af5ed4117491d8d484dff503의 CI 34888592747은 빌드/타입/critical 성공, 미러 6개 실패였다. Windows CRLF와 Linux LF에서 정리 후 빈 줄이 달라졌고, d9cfe7426c277813ed58c5be40e251731e27c690에서 입력 줄바꿈을 정규화해 동일 출력 검사를 통과했다. 해당 CI 34889561001 확인 중이다.
- 한계: 실제 LLM 의미 품질·실결제·실기기 PG 복귀·운영 DB·배포는 미검증이다. 기존 명칭/계산값은 해설 근거로 고정하지만 전체 의미 모순을 자동 판정하지 않는다. 이전 입력 없는 복귀 descriptor는 원래 점괘를 소급 복원할 수 없다. 여러 과거 결과를 고르는 보관함은 별도이며 저장 ID 재열람과 마지막 결과 복구를 제공한다. 다음은 요가·찻집/타로/fortune 잔여다.

## 해몽: 생성 중단 방지와 저장된 5장 재개 (2026-09-15)

- 유료 psycho-analysis를 기존 5장 제목을 유지한 10부분으로 나눴다. 부분당 본문 2,000자, 전체 20,000자 하한과 24,000~28,000자 생성 목표를 적용한다. 요청당 최대 4부분 병렬/제공자 45초/부분당 누적 3회이며 정상 부분을 저장·재조회한다. 짧거나 잘린 결과, 근거 해시 불일치, 반복, 명랑한 꿈을 불안 중심으로 바꾸는 결과를 완료로 인정하지 않는다. 실패 시 전체 템플릿을 성공으로 반환하던 경로를 제거했다.
- 기존 실행 컬렉션에 원래 꿈/문진/언어/요청/결제 식별자를 보존하고 소유자·취소/환불을 재확인한다. 저장 장애는 503 RESULT_STORAGE_UNAVAILABLE, 진행은 202/같은 resultId, 최종 저장·재조회 뒤에만 완료다. 만료된 임시 토큰은 원래 요청의 기존 이용 증빙으로 재확인한다. 새로운 차감·리워드·환불 정책은 추가하지 않았다. 무료 결정론 꿈 경로는 변경하지 않았다.
- 결제 전에 꿈과 requestId를 기존 결제 재개 descriptor에 넣고 실제 분석 시작에서 한 번 게이트를 연다. 계정별 저장 ID, 서버 pending 조회, 온라인/화면 복귀, 같은 결과 재시도와 목차/읽던 장을 연결했다. 브라우저 POST 예산은 90초이며 한 글자씩 수분 동안 출력하던 동작을 없애 저장된 본문을 바로 표시한다. 공용 reader는 기존 잠금이 끝날 때까지 5초 간격의 제한된 대기를 허용한다.
- 실제 Dream 라우트/접근/timeout 보호 54건, 펫·토템 공용 회귀 66건, Dream/reader UI 6건과 토템 UI 2건 통과. 제공자·DB·외부 fetch는 mock으로 차단했다. 저장 예외/null/완료 재조회 실패, 부분 재사용, 권한 거부/취소, 원래 입력/언어, 동시 요청, 3회 한도, 만료 토큰의 원래 증빙, 계정 전환과 늦은 응답을 검사했다.
- 실제 모달 마크업/스타일/스크립트의 격리 브라우저 mock에서 390/1280px 모두 부분 표시 → 같은 ID POST 1회 → 완료 → 새로고침/읽던 장/마지막 본문 통과, 오류·가로 넘침 0. 진행 문구 대비도 보완했다. 전체 결제 셸·실기기·PG 앱 복귀·실 LLM 의미 품질·운영 DB·배포는 미검증이다.
- check:fast 계획 critical, 선택 ai-consultation-flows gate, resilience 918, Mongo shape 938/위반 0, cron/security/auth-listener, lint 오류 0 확인. 전체 로컬 검사는 반복하지 않았다. 동물 토템의 앞선 CI 실패는 public 캐시 키 전파와 오래된 렌더 fixture였고 4d91f275/5aa18e51에서 각각 보완했다. 5aa18e51dd44bc5cf2bed15c5ff2129453e9c4da의 main CI 34887542738은 CI required 포함 성공했다. sync:public 및 sitemap 1,264 URL 일치도 확인했다.
- 남은 한계: 미완료 꿈이 있으면 새 분석 대신 기존 요청을 우선 재개한다. 복수의 과거 결과를 목록으로 고르는 보관함과 복구 불가능한 과거 입력의 자동 복원은 구현하지 않았다. 완료 결과는 저장 ID로 재열람한다. 모든 LLM 상품 완료가 아니며 다음은 오라클·요가·찻집/타로/fortune 잔여다.

## 동물 토템: 짧은 연결 해설 저장·재개 (2026-09-15)

- 1/3/5장 토템의 기존 카드별 정적 본문과 450/800자 연결 해설 사양은 유지했다. 이미 제공하는 카드 해설을 다시 늘리거나 짧은 연결 서사에 상세 리포트 20,000자 기준을 적용하지 않았다. 제공자 시도는 요청당 1회, 기본·최대 45초(더 짧은 기존 환경 설정은 존중)이며 누적 3회로 제한한다. 짧은 13초 시계를 반복하던 대신 한 번의 생성에 여유를 주고, 클라이언트는 DB 왕복까지 90초를 확보한다. 카드 공개와 LLM 호출을 동시에 진행한다.
- 기존 ServiceExecutionTransaction의 paidNarrative를 사용해 원래 카드/질문/출생 배경/언어/프롬프트/요청을 고정한다. 실제 LLM 해설의 필수 필드·슬롯·동물·잘림·반복 검사 및 최종 저장/재조회 후에만 completed다. 전부/일부 템플릿을 HTTP 200 성공으로 반환하던 신규 생성 경로를 제거했다. 저장 예외/null/확인 실패는 503 RESULT_STORAGE_UNAVAILABLE/resultId, 미완료는 202와 같은 서버 재개 ID다.
- 소유자/상품/기존 증빙/취소·환불을 다시 확인하며 CAS 잠금으로 중복 호출을 막는다. 새 차감·리워드·환불은 추가하지 않는다. 공통 timeout 정리도 보존된 토템 해설을 생성 실패 환불로 바꾸지 않는다. 신규 요청의 기존 quota는 유지하고 소유자가 저장 ID로 재개하는 경우만 batch로 분류한다.
- 결제 전에 카드·요청 ID·출생 배경을 결정하고 기존 결제 재개 descriptor에 포함한다. 복귀 핸들러는 최종 저장 완료일 때만 성공을 반환한다. 계정별 요청/서버 ID 보존, 서버 pending 복구, 온라인/화면 복귀, 늦은 응답의 화면 반영, 읽던 카드 복원, 같은 결과 재시도를 연결했다. 새 보관함에는 본문 대신 서버 결과 ID를 보관한다.
- 실제 라우트 31건, 펫 회귀/공통 security 포함 120건, timeout guard 추가 18건, 실제 모달/공용 reader 실행 UI 2건 통과. 이용권/월정석/단건, 저장 실패/재조회 유실, 권한 거부, 원래 언어, 누락/짧음/잘못된 카드/중단, 3회 한도, 동시 요청, 계정 전환·늦은 응답, 결제 재개 중복, 재열람 추가 POST 없음 등을 확인했다. 외부 fetch·제공자·DB는 mock으로 차단했다.
- 실제 모달 마크업·스타일·스크립트의 격리된 브라우저 mock에서 390/1280px 모두 서버 pending 즉시 표시 → 같은 ID POST 1회 → 완료 → 새로고침/읽던 카드/마지막 본문 확인. 오류/가로 넘침 0이다. 초기 미리보기의 charset/CSS 누락을 바로잡아 정본 스타일로 확인했고, 복구 중 첫 화면에 머물던 문제도 수정했다. 실기기·PG 앱 복귀·실LLM 품질·운영 DB·배포는 미검증이다.
- check:fast -- --plan critical, 선택 ai-consultation-flows gate 13.9초 통과, resilience 918, Mongo shape 938/위반 0, cron/security/paid-resume/auth-listener 통과. lint 오류 0/레거시 var 등을 포함한 경고 249, Impeccable anti-pattern 0/기존 15px 권고 1. sync:public·sitemap 1,264 URL 일치. 전체 로컬 검사는 반복하지 않았다.
- 한계: 이전 비귀속 로컬 보관함 데이터는 삭제하지 않았으나 새 계정별 보관함으로 자동 귀속시키지 않았다. 소유자 증거가 없는 과거 로컬 본문의 계정 연결은 추가 검토가 필요하다. 새 결과와 서버에 보존된 구매 결과는 소유자 확인 후 재열람하며 재생성하지 않는다.
- 펫 4fe97827fa51c1aae331b124e0f9ccb8de7befb9의 CI 34883128345는 삭제된 generateNarration 이름을 찾는 정적 검사 때문에 실패했다. cb88162efaf2e46464bbba95d1cb3f3246653d42에서 해당 검사를 현재 연결 구조로 고치고 실제 결제 거부 동작 검사를 추가했다. 결정론 180/라우트 35 통과, main CI 34884232435는 CI required 포함 성공이다. 동물 토템 단위는 push 후 CI 확인한다.
- 다음은 꿈·오라클·요가·찻집/타로/fortune 잔여다. 모든 LLM 상품 완료가 아니다.

## 펫 사주 심층·궁합: 실제 생성 본문 보존과 모바일 제공 (2026-09-15)

- 서버에 결과를 보관하지 않고 정적 문구를 정상 리포트로 반환하던 두 유료 경로를 부분 생성 저장으로 변경했다. 심층은 성격·장소별 해석·놀이·코칭·생활 관리·맺음, 궁합은 개요·5축·공유 장소·주의점·루틴을 각각 생성한다. 기본 청사진에서 13/14부분이며 합계 하한은 각각 20,200/20,160자다. 장소 수가 달라도 전체 본문 최소 20,000자를 충족하도록 배분한다.
- 요청당 최대 4개 병렬, 제공자 1회/최대 45초(기존 timeout 설정을 상한 안에서 사용), 부분당 누적 3회 한도다. 호출 예약과 정상 본문을 즉시 저장·재조회하고 누락·잘림·근거 해시 불일치·반복을 완료로 인정하지 않는다. 원래 날짜/아이/청사진/명식/궁합 점수/언어를 보존하며 LLM은 해설만 한다. 무료 청사진 계산과 가격·이용권/월정석/단건 정책은 유지한다.
- 기존 ServiceExecutionTransaction에 paidNarrative를 보관한다. 새 컬렉션·스키마·인덱스/운영 마이그레이션은 없다. 완료 저장 확인 전에 성공으로 응답하지 않으며 저장 예외/null/재조회 실패는 503 RESULT_STORAGE_UNAVAILABLE/resultId, 진행 중은 202/동일 서버 재개 ID/부분 본문이다. 저장 장애·응답 유실을 환불로 바꾸지 않고, 새 차감/리워드/환불 호출을 추가하지 않았다.
- 소유자·상품별 조회와 잠금 토큰으로 재사용/동시 요청을 보호하고 취소·환불을 다시 확인한다. GET /result?kind=report|compat에서 계정의 pending 또는 지정 결과를 복구한다. 저장본의 재개만 security batch 버킷을 사용하며 report/compat를 서로 바꿔 재개할 수 없다. 공통 timeout 정리도 해당 체크포인트를 환불하지 않는다.
- 정적 화면은 계정별 원래 요청/서버 ID 저장, 서버 pending 복구, 원래 코어 재시도, 부분 본문/진행/이어서 생성 버튼, 새로고침·온라인/화면 복귀 재개를 제공한다. 재시도 버튼이 새 결제 게이트로 돌아가지 않는다. 긴 놀이 해설은 작은 칩 대신 문단으로 표시하고 줄바꿈·목차·읽던 장 링크를 제공했다. 로컬 본문을 구매 증빙으로 사용하지 않는다.
- 실제 라우트 34 + 관련 로케일/timeout/security 포함 115 mock, UI 루프 3 통과. 이용권/월정석/단건 × 두 상품 저장 throw/null/확인 유실, 정상 부분 재사용, 중단/짧음/근거 불일치/누락, 원래 언어/입력/소유자/상품/취소, 동시 재개를 검사했다. 실제 DB/제공자/외부 fetch는 차단했다.
- 실제 정적 화면 mock은 심층·궁합 각각 390/1280px에서 부분 → 동일 ID POST 1회 → 완료 → 새로고침/읽던 장/마지막 본문까지 통과했다. 4조합 모두 오류/가로 넘침 0, 재열람 추가 POST 0. 목차 대비를 보강했으며 기존 하단 메뉴의 10.88px 글자 등 이전 디자인 권고는 별도 범위로 남겼다. 실기기·실결제 앱 복귀·실LLM 품질·운영 DB·배포는 미검증이다.
- check:fast -- --plan critical, 선택 ai-consultation-flows paid gate, resilience 918, Mongo shape 938/위반 0, cron/security/paid-resume/auth-listener 검사 통과. 변경 lint 오류 0/기존 공통 security 경고 7, inline JS 문법, sync:public, sitemap:generate/verify:sitemap-drift 1,264 URL 확인. 생성 미러와 홈페이지 자산 해시 6개가 함께 동기화됐다. 전체 로컬 검사는 반복하지 않았다.
- 직전 관계 경계 26db92a7bb1d2255f50cfa93a3265bcb7462f066 main CI 34880833138의 CI required 포함 전체 성공을 확인했다. 이번 펫 단위는 push 후 main CI 확인한다. 다음은 동물 토템·꿈·오라클·요가·찻집/타로/fortune 잔여이며 모든 LLM 상품 완료가 아니다.

## 사주 관계 경계 테스트: 5장 부분 저장·재개 (2026-09-15)

- 기존 5장 제목/점수 계산을 유지하고 장당 두 부분(각 본문 공백·제목 제외 최소 2,000자, 목표 2,200~2,500자)과 짧은 프레임으로 나눴다. 전체 최소 20,000자이며 기존 완료 구매본은 새 분량 기준을 적용하지 않는다. 요청당 최대 4개 병렬·45초·각 부분 누적 3회 한도, 정상 부분만 즉시 저장한다.
- 실제 계산 명식과 원래 입력/언어/결제 키를 서버에 보존하고 해설 근거 해시·분량·반복을 검사한다. LLM은 점수나 명식을 수정하지 않는다. 자연어 전체의 의미 모순을 증명하는 검사나 실제 LLM 품질 검증은 아니다.
- generating/partial/delivery_pending/completed를 구분했다. 저장 예외/null/재조회 실패는 503 RESULT_STORAGE_UNAVAILABLE/resultId, 진행 중은 202/부분 본문/동일 resumeSessionId를 반환한다. 최종 저장과 재조회 이후만 완료이며 저장 오류·제공자 응답 유실을 생성 실패 환불로 바꾸지 않는다. 확정된 무효 출력 한도 초과는 실패 상태 저장 확인 후 기존 환불 함수를 사용한다.
- 소유자+결과 ID+잠금 토큰으로 중복/동시 재개를 막고 원래 결제 증빙과 취소/환불 상태를 확인한다. 완료 전 실행 완료/리워드 호출은 없다. 같은 저장본의 재개만 security batch 버킷을 쓰며 신규 요청 예산은 유지한다. 공통 10분 만료 정리도 이 저장소를 확인해 부분 결과를 환불하지 않게 했다.
- 기존 RelationshipBoundaryTest 스키마에 선택 llmMeta와 부분 상태 enum을 추가했다. 컬렉션/인덱스/가격/이용권·월정석·단건 구조 변경이나 운영 마이그레이션은 없다. Mongoose 내부 subdocument _id를 제외하고 실제 저장 본문을 비교한다.
- 화면은 계정별 재개 기록 및 서버 pending 조회, 부분 본문/진행/이어 생성 버튼, 숨김·오프라인 시 새 호출 중지, 복귀 재개, 계정 변경 늦은 응답 차단을 제공한다. 같은 요청 재시도는 결제창을 열지 않으며 usePaidResume는 최종 완료만 승인한다. 5장 목차와 계정/결과별 읽던 장, 명시적인 새 대상 분석도 제공한다.
- 실제 라우트 26 + 기존 계산/가격 5 + timeout guard 14 + security 50 = 95 mock 검사 통과. 저장 throw/null/확인 유실, 원래 언어/입력/소유자, 중복/동시, 취소 거부, 정상 부분 재사용, 무효/응답 유실 분리, 19,999/20,000자 경계, 내부 subdocument id, 조작된 ID를 검사했다. UI 동작 3개 통과. 외부 fetch·DB·제공자/결제/환불 함수는 mock으로 차단했다.
- 실제 Next 화면 390/1280px에서 서버 부분 결과 → 동일 session POST 1회 → 완료 → 새로고침 재열람/읽던 장 → 마지막 메시지까지 확인했다. 오류/가로 넘침 0, 재열람 추가 POST 0. 실제 PG 앱 복귀·실기기·실LLM·운영 DB·배포는 미검증이다.
- check:fast -- --plan critical, targeted paid gate(ai-consultation-flows), 타입 검사/변경 lint 오류 0(공통 security 기존 경고 7), resilience 918, Mongo shape 932/위반 0, cron coverage, security guard, staging mock 검사 통과. sitemap:generate/verify:sitemap-drift 1,264 URL 일치. Next/worker 수정으로 public 미러 생성 대상 없음. 전체 로컬 검사는 반복하지 않았다.
- 직전 타로 0a9c2e101679fec174cb3470f642f9e6e1580a25 main CI 34879401975에서 CI required 포함 전체 성공 확인. 이번 관계 경계 단위는 push 후 main CI 확인한다. 다음은 동물 토템·꿈·펫·오라클·요가·찻집/타로/fortune 잔여이며 전 상품 완료가 아니다.

## 천상의 하모니 타로: 카드별 생성과 모바일 전달 (2026-09-15)

- 전달 보완: 872c4f1bedc9f1aa6faadf0971034ff3870ddf46 CI 34878795120의 Critical checks에서 분리된 호출 파일과 환경설정 consumer 원장의 불일치 6건을 발견했다. 기존 3개 설정을 새 helper에 연결하되 timeout 45초/출력 11,000토큰 상한을 유지하고 원장 경로를 갱신했다. env-parity 통과(기존 경고 8), 설정 상한 회귀 포함 실제 라우트 26개 및 resilience 904 통과. 후속 커밋 CI로 공식 검증한다.

- 11장 전체를 한 번에 생성하던 호출을 카드 11개와 종합 1개로 나눴다. 요청당 최대 4개 병렬, 호출별 45초, 부분별 누적 3회 한도로 실행하고 정상 부분을 즉시 저장·재조회한다. 기존 카드당 여섯 본문 각각 500자 이상을 유지한다(카드 본문 합계 최소 33,000자). 20,000자 전체 하한도 검사하며 이미 완료된 과거 결과에는 새 기준을 적용하지 않는다.
- 실제 LLM 실패를 정적 템플릿 완료로 반환하던 서버·화면 경로를 제거했다. 미완료는 202와 부분 본문/동일 서버 실행 ID, 저장 예외/null/재조회 실패는 503 RESULT_STORAGE_UNAVAILABLE로 반환한다. 최종 저장 확인 후에만 completed를 반환한다. 소유자·원래 카드·결제 증빙·언어를 보존하고 취소/환불을 재확인한다. 재개 잠금은 원래 lock token까지 비교해 동시 재개가 중복 호출하지 않게 했다.
- 기존 ServiceExecutionTransaction과 90일 보존 기간을 사용한다. 새 차감·리워드 호출은 없다. 저장된 체크포인트를 10분 타임아웃 정리가 생성 실패 환불로 처리하지 않게 보호했다. 무응답/한도 소진 결과는 성공이나 확정 실패로 꾸미지 않고 보존한다.
- 모바일은 서버 부분 본문을 GET 직후 표시하고 같은 실행 ID로 이어 작성한다. 계정별 재개 저장, 계정 변경의 늦은 응답 차단, 숨김/오프라인 시 다음 호출 보류, 온라인/화면 복귀 재개를 적용했다. 결제 앱 복귀 descriptor에 원래 카드 배열도 보존한다. 완료 재열람은 서버 확인하며 새 결제창을 열지 않는다. 기존 카드별 펼침 화면을 유지했고 읽던 스크롤 위치의 별도 영속 저장은 아직 적용하지 않았다.
- 실제 라우트 mock 25개 통과: 이용권/월정석/단건 유형, 저장 예외/null/재조회 유실, 중단 카드만 재생성, 소유자/취소 거부, 원래 언어, 카드 근거·짧은 본문·반복 거부, 과거 짧은 완료 결과 재열람, 중복 및 동시 재개. 관련 기존 품질/로케일/만료 보호까지 합계 54개, UI 재개 행동 3개 통과. 외부 fetch와 실제 DB/제공자는 차단했다.
- 실제 정적 화면 mock 390/1280px에서 서버 부분 결과 → 동일 실행 ID POST 1회 → 완료 → 새로고침 재열람을 확인했다. 두 크기 모두 브라우저 오류/가로 넘침 0, 재열람 추가 POST 0. 실기기·실PG·실LLM·운영 DB·배포 검증은 아니다. 자연어 전체의 의미 모순이나 실제 모델 출력 품질을 증명한 검사는 아니다.
- check:fast -- --plan critical, 변경 lint 오류/경고 0, 선택 ai-consultation-flows paid gate, resilience 904, Mongo shape 930/위반 0, cron coverage, paid resume/auth listener 검사 통과. sync:public 및 sitemap:generate/verify:sitemap-drift(1,264 URL) 확인. public 홈페이지의 6개 자산 해시는 동기화 생성 변경이다. 전체 로컬 검사는 반복하지 않는다.
- 직전 공통 만료 보호 14535a77e85a31bfe43019f7afe4755b7ee70c93의 main CI 34875457837 및 e3125692dcfe9b608af9e21393f481ac5fbb2f81 CI 34874820871 성공을 확인했다. 이번 타로 단위는 commit/push 후 main CI로 공식 확인한다.
- 다음: 관계 경계 테스트, 동물 토템, 꿈, 펫 사주, 오라클, 요가, 찻집과 타로/fortune 잔여 라우트. 전 상품 개선 완료가 아니다.




## 공통 만료 정리와 저장된 유료 결과의 충돌 차단 (2026-09-15)

- 후속 조사에서 ServiceExecutionTransaction의 10분 timeout 정리가 정상 부분 결과 또는 완료 저장 후 실행 닫기 응답 유실도 생성 실패처럼 환불할 수 있음을 확인했다. 타임아웃 정리에만 서버 결과 소유자/결과 ID/원래 요청 ID 검사를 추가했다.
- 휴먼 디자인·수호신 나침반 상세·나크샤트라 결정판·점성술·네오·심화 자미두수 PDF 6개 저장소를 대조한다. partial/delivery_pending/재개 스냅샷이 있는 generating은 정리 확인을 10분 뒤로 미루고 결제 증빙과 본문을 보존한다. DB 장애나 요청 연결 불명확도 실패로 단정하지 않는다. 실제 completed 저장본은 실행 원장만 success/delivered로 닫는다. 새 LLM/결제/환불 호출은 없다.
- 이 변경은 확정된 생성 실패의 명시적 환불, 저장본이 없는 기존 만료 실행, 다른 상품의 정리 정책에 적용하지 않는다. 가격·환불 금액·이용권/월정석/단건 방식은 그대로다. 사용자 취소/환불된 원장은 pending 조건을 통과하지 않는다. 완료되지 않은 부분 결과의 장기 보존/지원 처리와 기존 retention TTL 정책은 별도 운영 확인 대상이다.
- 실제 sweep/판정 mock 12 + 기존 이용권/월정석/단건 환불 mock 8 = 20 통과. 이미 시작된 환불은 완료 원장으로 덮어쓰지 않는다. 외부 fetch·실PG·실DB 차단. 새 조회는 소유자와 결과 ID로 제한한다. Mongo shape 924/위반 0, cron op coverage 및 no-nested-retry 통과, 변경 lint 오류 0/기존 경고 2, check:fast -- --plan critical 확인. 공식 검증은 push 후 main CI다.
- 작명첩 17e59d3f4 CI의 Critical checks/Typecheck and lint/Build Pages and Worker는 성공했으며 Static guards의 사이트맵 원장 누락만 e3125692dcfe9b608af9e21393f481ac5fbb2f81로 보완했다. 해당 후속 CI 34874820871 확인 후 다음 유료 라우트 작업을 계속한다.
- 천체의 선율은 11장 단일 생성·저장 실패 성공 응답·브라우저 로컬 본문 완료 폴백이 남아 있다. 기존 정적 카드 본문은 이미 길어 줄이지 않으며, 실제 LLM 부분 생성 저장과 서버 재개로 바꾸는 다음 구현 단위다. 동물 토템/꿈/펫/오라클/요가/관계 경계/찻집·타로 잔여도 아직 전수 완료가 아니다.

## 작명첩 8장 분할 생성·모바일 자동 재개 (2026-09-15)

- 후보 카드 1회 후 8장을 최대 4개 병렬로 생성한다. 호출별 45초·제공자 폴백 없음·각 부분 누적 3회 한도이며, 호출 예약과 정상 장을 즉시 저장·재조회한다. 장별 본문 최소 2,500자/전체 20,000자, 목표 24,000~27,200자를 적용한다. 역사적 completed 결과와 이전 delivery_pending 본문은 새 분량으로 차단하지 않는다.
- 원래 실행 ID/소유자/입력/결제 증빙/명식/출력 언어/후보를 재사용한다. 후보 획수·수리 숫자를 계산값으로 채택하지 않는다. 장별 근거 해시·분량·중복 문장을 검사한다. 자연어 전체의 명리 모순을 증명하는 의미 검증이나 공인 한자 등록/획수 DB 검증은 아니다.
- 결과 페이지는 서버 resumeExecutionId만 사용해 자동으로 다음 묶음을 요청한다. 계정 변경의 늦은 응답을 무시하고 숨김/오프라인 시 새 호출을 멈춘다. 서버 pending 복구, 계정별 재개/읽던 장 보존, 부분 본문 표시, 완료 전 PDF/결제 재개 삭제 차단을 적용했다. 재시도에서 결제창을 열지 않는다.
- 작명첩·운명의 섬은 소유자와 상품이 일치하는 서버 저장본만 재개용 요청 한도를 사용한다. 신규 생성 한도와 가격·이용권·월정석·단건 결제·환불 정책은 유지한다.
- 실제 라우트 mock 17 + 작명 로케일 12 + security bucket 49 = 78 통과. 이용권/월정석/단건 증빙, 저장 throw/null/재조회 유실, 정상 장 재사용, 중복, 취소/소유자 거부, 원래 언어/입력 보존, 근거 해시 불일치, 19,999/20,000자 경계를 검사했다. 외부 fetch는 차단하고 DB/제공자를 mock으로 대체했다. UI 재개 행동 3개 통과.
- 실제 Next 화면 mock 390/1280px: 로그인 힌트만 두고 서버 부분 결과를 조회하여 원래 실행 ID POST 1회로 완료(오류/가로 넘침 0). 시각 2회 확인, 상단 공통 버튼 겹침 수정. 읽기 위치는 계정/결과별 장 ID를 저장한다. 실기기·실PG·실LLM·운영 DB·배포 검증은 아니다.
- check:fast -- --plan critical, 선택 naming/ai-consultation-flows paid gate, resilience 904, Mongo shape 923/위반 0, 타입 검사 및 변경 lint 오류 0 통과(공통 security 기존 경고 7). public 사본 없는 Next/worker 변경이다. 전체 로컬 검사는 반복하지 않았다.
- 직전 작명 저장 수정 0cb627d8badc1c3fac68d0e8194429d78bfc0e2e main CI 34871810372 및 운명의 섬 42c4e4d0f7614439ab40dc4e58f050d4a9f72026 CI 34870945152 성공 확인. 이번 단위는 push 후 main CI 확인한다.
- 다음: 동물 토템·천궁/오라클·꿈·펫 사주·관계 경계·요가·찻집·타로 및 fortune 잔여 라우트를 실제 상품 목록과 대조한다. 짧은 대화/카드 연결문을 상세 리포트와 구분하고, 우선 결제 후 생성 실패를 성공처럼 반환하거나 결과를 보존하지 않는 경로를 수정한다. 모든 LLM 기능 완료 선언은 아직 하지 않는다.


- 전달 보완: 17e59d3f47af67cc2b702ebe60148f1cc7d361e5 CI 34874435433 Static guards에서 naming-ai 소스 변경에 따른 sitemap-lastmod signature 누락 발견. npm run sitemap:generate 후 config/sitemap-lastmod.json의 /naming-ai/ signature 1건만 갱신했고 verify:sitemap-drift 1,264 URL 통과. 본문 날짜·가격·정책 변경 없음. 후속 커밋 CI로 공식 확인한다.

## 작명첩 저장 실패·잘못된 완료 응답 차단 (2026-09-15)

- 생성 중 레코드의 generatedPrompt를 generatedResult처럼 반환하던 완료 우회를 막았다. 생성용 프롬프트만 남은 과거 completed 구매본의 재열람 계약은 유지한다.
- 생성 시작 저장을 재조회하고 소유자+실행 ID+작성 잠금으로 보호한다. 생성 본문은 delivery_pending으로 먼저 보존한 뒤 최종 completed 저장·재조회를 확인한다. 최종 저장 예외/null/확인 유실은 RESULT_STORAGE_UNAVAILABLE 503/resultId로 반환하고 같은 본문을 재사용한다. 저장 오류로 실패 덮어쓰기나 월정석 환불을 하지 않는다. 확정된 생성 실패도 실패 상태 저장이 확인되어야 기존 복구 함수를 실행한다.
- 재시도는 원래 입력/명반/프롬프트를 보존하고 취소·환불 증빙을 재확인한다. 가격·회당 결제·월정석 환불 정책을 변경하지 않았다. 모든 호출은 기존 저장소와 정책 함수이며 새 운영 마이그레이션은 없다.
- LLM에게 명식·용신을 직접 재계산/수정하게 하던 프롬프트를 계산값 해설과 적용 조건 설명으로 수정했다. 승인된 한국어 골든을 갱신하고 OS 줄바꿈만 정규화해 검사한다.
- 실제 라우트 mock 9개 + 로케일 12개(총 21) 통과. 이용권/월정석/단건 접근 유형은 차감 증빙 mock으로 실행했으며 실 PG·실차감 검증이 아니다. 생성 중 동시 요청, 최종 저장 throw/null/재조회 유실, 결과 재사용, 취소 재열람 거부, 소유자 격리를 검사했다. 외부 fetch와 DB/LLM 함수는 mock으로 차단했다.
- 선택된 naming paid gate, verify-naming-prompt-flow, Mongo shape 922/위반 0, 변경 lint 오류 0/기존 ctx 경고 1, check:fast -- --plan(critical) 확인. 이 단위는 서버 수정으로 UI/전체 로컬 검사 반복 없음. push 후 main CI 확인.
- **작명첩 전체 개선 완료가 아니다.** 다음 단위는 기존 8장·이름 카드 포맷을 유지한 짧은 호출/챕터 체크포인트/20,000자 완료 기준과 계정별 모바일 결과 페이지 자동 재개다. 현재 단일 생성 호출/기존 5,000자 하한은 다음 작업으로 남아 있다. 실 LLM 품질·실결제·실기기·운영 DB·배포는 미검증이다.


## 운명의 섬 12궁 상담 생성·저장·모바일 복구 (2026-09-15)

- 12궁의 기존 3/4개 섹션 키와 제목을 유지하고 내부만 6/8개 부분으로 나눈다. 최대 4개 병렬, 호출별 45초·1회·제공자 폴백 없음, 부분별 누적 3회 한도다. 정상일 때 두 요청으로 이어지고 완료된 부분은 즉시 서버에 저장한다. 각 부분 최소 분량의 합은 본문 20,000자 이상이며 생성 목표에는 여유를 둔다. 기존 완료 구매 결과는 새 분량 검사에서 제외한다.
- 원래 입력/명반/접근 증빙/멱등키를 보존한다. 소유자 CAS 잠금과 저장 후 재조회 확인, 부분 생성/저장 대기/완료 구분, 완료 전 취소·환불 재확인을 적용했다. 생성 본문을 보존한 뒤 기존 apply를 실행하고 저장 완료를 확인한다. 저장 예외·null·재조회 유실·차감 응답 유실은 RESULT_STORAGE_UNAVAILABLE 503으로 동일 결과를 재개하며 환불/실패 덮어쓰기로 넘기지 않는다. 확정된 빈 품질 실패만 기존 복구 함수를 호출한다. 제공자 응답 유실로 호출 한도가 소진된 미완성은 보존하고 별도 확인이 필요하다.
- 월정석 잔액만 확인한 토큰은 사용 증빙으로 취급하지 않는다. 기존 결제 게이트와 원장 증빙을 사용하며 가격/이용권/월정석/단건 결제/환불 정책은 유지한다. 리워드를 새로 추가하지 않았다. 별도 5,000원 고정 명반 해금 상품은 수정하지 않았다.
- 모바일은 계정별 원래 요청을 보존하고 서버 pending 조회로 복구한다. 숨김/오프라인 중 새 회차를 시작하지 않으며 복귀 시 같은 요청을 재개한다. 부분 본문·목차·읽던 섹션 보존을 추가했다. 완료 확인 전 결제 재개를 승인/삭제하지 않고 재시도에서 결제창을 열지 않는다.
- 실제 라우트 mock 25개, UI/분량/기존 PDF 동작·정적 15개 통과. 외부 fetch/실 PG/제공자/DB 차단. 동시 요청, 오래된 잠금 작성자, 정상 부분 즉시 저장, 초기 insert 실패 후 같은 결과 ID 복구, 부분/최종 저장 예외·null·확인 유실, 차감 응답 유실, 4개 취소 원장, 과거 구매 재열람을 검사했다. 계산 근거 검사는 대상 궁/주성/대운 증빙과 본문 주성 포함 및 반복 검사이며 모든 자연어 의미 모순을 증명한 것은 아니다.
- 실제 Next mock 390/1280px에서 인증 힌트만 두고 로컬 출생 입력·결제 재개 캐시 없이 서버 pending 조회 → original-paid-request 한 번 POST → 4개 완료 섹션 표시, JS 오류 0/가로 넘침 0을 확인했다. 임시 실행 파일 $env:TEMP/island-delivery-preview.cjs, 화면 island-partial/complete-390/1280.png. 읽던 위치는 구현했으며 실기기 재열람은 미검증. 외부 이미지 요청 차단에 따른 캐릭터 이미지 누락은 mock 화면의 한계다.
- typecheck/변경 lint 오류 0(기존 UI·프롬프트 경고 14), Mongo shape 919/위반 0, cron coverage, resilience 902, selected paid gate ai-consultation-flows, check:fast -- --plan(critical), impeccable detector 통과. 원본/public 동기화 대상 변경 없음. 본 세션 preview PID 14816 종료.
- 선행 초융합 400f264fc5964d1b6dc1157c0f4e1ed8fce4ccdb CI 34868218209 및 8d35da19265883e55a72c9b7acb9bcdfac379ac8 CI 34868855164 성공. 이 운명의 섬 단위는 push 뒤 최신 main CI를 확인한다.
- 다음: 남은 실제 LLM 상품(동물 토템·타로/오라클·작명·반려동물·꿈·요가·관계 경계 및 찻집/짧은 후속 대화)과 상품 레지스트리 대조. 전체 LLM 완료로 보고하지 않는다. 실 LLM 품질·실결제·실기기 결제 앱 복귀·운영 DB·배포 검증은 수행하지 않았다.


## 초융합 본문 완료 기준과 읽던 위치 (2026-09-15)

- 30,000~60,000자 기존 목표는 유지한다. 새 생성 완료 직전에 제목·목차·마크다운·공백을 제외한 본문 20,000자를 확인한다. 미달은 같은 2단계 partial로 보존하며, 원래 여섯 체계 해석을 유지하고 통합/행동/최종 판정 묶음만 기존 범위와 누적 호출 한도 안에서 보완한다. 과거 완료본 POST/GET 재열람은 새 기준을 적용하지 않는다.
- 읽던 챕터를 계정+원래 요청 키로 보존하고 재열람 시 복원한다. 아직 생성되지 않은 뒤 챕터가 저장되어 있으면 앞 챕터로 덮어쓰지 않고 기다린다.
- 생성/실제 라우트 mock 79개(19,999/20,000 경계, 2단계만 재생성/1단계 보존 포함), UI 읽기/로케일 26개, typecheck, UI lint 오류·경고 0/서버 기존 경고 3, resilience 902, 선택된 초융합 paid gates 4 및 check:fast -- --plan 통과. 읽기 위치 영속화는 훅 동작 검사이며 실기기 재열람은 미검증이다.
- 선행 모바일 복구 400f264fc5964d1b6dc1157c0f4e1ed8fce4ccdb PR CI 34868218209는 확인 시 진행 중이었다. 이 작업 단위를 push 후 최신 main CI를 확인한다.
- 다음은 운명의 섬 12궁 상담과 나머지 실제 LLM 상품이다. 운명의 섬은 단일 호출 기본 120초(공용 clamp 뒤 85초)×2, apply 뒤 null 저장을 성공 취급하고 저장 예외를 생성 실패 환불로 보내는 경로를 확인했다. 아직 수정 전이다. 전체 LLM 완료로 보고하지 않는다.

## 초융합 모바일·계정별 복구 (2026-09-15)

- 계정별 로컬 키에 원래 요청/입력을 보존하고 다른 계정이나 소유자 미상의 옛 캐시를 추정 복원하지 않는다. 계정에 묶인 미완료 기록은 하루가 지났다는 이유로 지우지 않는다. 늦은 생성/조회/목록 응답은 계정·로케일 범위 밖에 적용하지 않는다.
- 서버에서 소유 미완료 기록을 찾고 최근 목록에도 미완료 상담을 포함한다. 로컬 입력 없이 재로그인/재진입해도 같은 요청을 이어가며, 목록에서 연 부분 결과도 동일하게 재개한다. 숨김/오프라인 중 새 회차를 시작하지 않고 복귀 시 같은 요청을 잇는다. 일시 장애 재개는 클라이언트 네 번/서버 기존 묶음 한도 내에서 제한한다.
- 실제 Next mock에서 인증 저장소 초기화가 없으면 로그인해도 pending 서버 조회를 시작하지 않는 문제를 발견했다. 공용 refreshAuth 초기화와 인증 상태 구독을 연결한 뒤 로컬 입력·결제 캐시 없이 소유 pending 조회 → 같은 original-paid 한 번 POST → completed/cid 재열람까지 390/1280px 모두 확인했다. 결과 13개 항목, JS 오류 0, 가로 넘침 0. 본문을 숨기던 기본 opacity-0을 제거하고 본문·목차를 시각 확인했다.
- UI 동작 24개/소유 저장소 2개/기존 초융합 정적 20개, 실제 라우트·스트림 mock 37개 통과. typecheck, 변경 UI lint 오류·경고 0, Mongo shape 916/위반 0, 선택된 초융합 paid gates 4와 check:fast -- --plan 확인. impeccable detector 수행. 본 세션 preview PID 13556만 종료했다.
- 직전 fbcb80f87a4bac2e422becfce1527d4a1c3ef9b9 PR CI 34866439380은 Static guards의 오래된 retryable:true 문자열 단언 한 건으로 실패했고 Build/Critical lane은 성공했다. 저장 장애/취소 응답은 실제 라우트 검사로 대체하고 계정별 저장·표시 검사를 갱신했다. 이 커밋 push 뒤 CI를 재확인한다.
- 남은 범위: 초융합 순수 본문 20,000자 floor/읽던 위치 영속화, 나머지 실제 LLM 상품과 짧은 후속 대화의 생성·저장 경계. 실 LLM 품질·실기기 결제 앱 복귀·실결제·운영 DB·배포는 미검증이다. UI mock 임시 실행 파일은 $env:TEMP/fusion-delivery-preview.cjs, 화면은 fusion-partial/complete-390/1280.png이다.

## 초융합 저장 잠금·취소 재확인·완료 재사용 (2026-09-15)

- 세대별 예약 토큰과 결과 문서의 150초 lease를 함께 사용한다. 두 단계가 공유하는 결과 저장·제공자 호출 예약에 token/만료 조건을 붙이고, 오래된 요청의 release/commit/save가 후속 요청을 건드리지 못하게 했다. 저장 장애가 있더라도 정상 형제 체크포인트는 보존하고 보완 호출의 저장 예외도 503으로 전달한다.
- 최종 본문은 delivery_pending으로 저장·재조회한 뒤 기존 네 결제/환불 원장 검사와 completed 저장·재조회를 거친다. 새 호출 전과 소유 결과 조회에서도 취소·환불을 거부한다. 완료 응답 유실 뒤 POST 재시도는 저장된 완료본을 직접 반환하며 LLM/결제 증빙 조회를 다시 실행하지 않는다. 기존 완료본의 새 분량 재검사는 없다.
- 실제 라우트→생성 진입점→저장 helper mock 24개(결제 종류별 partial/same-stage/완료 재사용, 저장 throw/null/readback, 최종 저장 null/응답 유실, 취소 직전/후)와 기존/lease mock을 합쳐 90개 통과. 외부 fetch는 차단했다. 결제 검증기는 read-only mock이므로 실 세 결제 흐름 증명은 아니다.
- typecheck, UI 로케일 동작 22, 선택된 초융합 paid gates 4, Mongo shape 915/위반 0, cron coverage 통과. check:fast -- --plan critical 확인. 옛 상태 문자열 두 항목은 실제 public status 함수와 Mongoose schema 기본값/enum 검사로 바꿨다. 선행 76a370b940ece61806709a9a879806a2b82e6f0a의 PR CI 34865327337 success.
- 다음: 초융합 순수 본문 floor/계정별 저장/백그라운드 복귀/모바일 동작, 나머지 상품 목록 대조와 짧은 LLM 기능을 이어간다. 모든 상품 완료는 아니며 실 LLM·실결제·운영 DB·실기기·배포는 미검증이다.

## 초융합 최초 근거 보존과 같은 단계 재개 (2026-09-15)

- 최초 입력과 여섯 체계 계산 결과를 사용자+원래 requestId에 묶인 비공개 generationSnapshot으로 생성 전에 저장·재조회한다. 재시도 입력이 달라도 보존된 입력/계산 시점을 재사용한다. 목록에는 snapshot을 노출하지 않으며 소유자 부분 결과 조회에서만 재개 입력을 반환한다.
- 묶음 호출은 실행 전에 서버에서 누적 예약하고 그룹별 세 번으로 제한한다. 예약 재조회 실패가 NaN 비교로 통과하던 경로를 mock으로 발견해 차단했다. 저장·예약 장애는 RESULT_STORAGE_UNAVAILABLE 503이며 불확실한 호출 수를 초기화하지 않는다.
- 미완료 묶음은 202/nextStage로 같은 단계를 이어가고 정상 묶음을 재사용한다. 화면은 원래 요청으로 최대 12회차까지 이어가며 완료 전 증빙을 지우지 않는다. 로컬 입력이 없을 때 소유 서버 snapshot으로 복구하고 재개 버튼이 결제 폼을 다시 제출하던 경로를 제거했다. 기존 30,000~60,000자 목표는 유지한다.
- 검증: 실제 생성 진입점/저장 helper/스트림 라우트와 결제 증빙 mock 86개, UI 로케일·동일 단계 재개 22개, 초융합 정적 UI 25개 통과. typecheck, Mongo query 912/위반 0, resilience 902, check:fast -- --plan 및 선택된 paid gate 초융합 네 항목 통과. lint 오류 0/기존 경고 4. 외부 fetch·실 LLM·실결제·운영 DB 호출 없음.
- 나침반 보정 a04b1d11717f32eab31c91e46b36a4c8e499c614의 main PR CI 34863146780 success 확인. 이 초융합 작업 단위는 커밋/push 후 CI를 확인한다.
- 남은 한계: 초융합 만료 잠금의 CAS 토큰, 완료 직전 취소·환불 재확인, 20,000자 순수 본문 floor, 계정 전환과 숨김/오프라인 자동 재개, 실제 모바일 화면 검증은 이어서 수행한다. 전체 LLM 완료가 아니다. 실 LLM 품질·실기기 결제 복귀·운영 DB·배포는 미검증이다.

## 나침반 기존 로케일 검사 갱신

- bff45cc94d53369a54d2ad2add596659d9a3a26b CI 34862600825의 Static guards에서 기존 함수 추출 mock 두 건이 실패했다. 새 계정 보호 훅/인증 fetch를 mock에 반영했고, 로케일 변경 뒤 원래 구매 본문을 보존하되 오래된 화면 갱신·추가 회차를 멈추는 동작을 검사했다.
- 계정 구분이 없는 옛 캐시는 삭제하지 않지만 새 계정 화면에 추정 복원하지 않는다. 소유자+로케일이 일치하는 키와 소유 서버 결과를 사용한다. locale-request-boundaries.behavior.test.js 20개 통과. 이 보정 커밋의 main CI를 확인한다.

## 공통 본문 계수 경계 (2026-09-15)

- 짧은 독립 굵은 제목과 Setext 제목/밑줄을 본문 글자 수에서 제외한다. 문장 안 강조와 링크 본문은 유지한다. 기존 완료 문서의 재열람 검사는 바꾸지 않는다.
- 19,999/20,000자, 제목·목차·마크다운·공백, Unicode 정규화/코드 포인트, 중복 제목과 반복 본문 구분 3개 동작 검사 통과. 휴먼 디자인/나침반 실제 라우트 46개와 llm-generation-resilience 902개도 통과했다. check:fast -- --plan으로 critical 범위를 확인했다.

## 운명의 나침반 심층 리포트: 원래 결제 회차·짧은 생성·독립 재열람 (2026-09-15)

- 결제 시와 생성 시 서로 다른 요청 ID를 만들던 경로를 원래 결제 requestId로 통일했다. 정규화된 최초 근거/언어/결제 증빙을 생성 전에 보존하고, seed 원문은 해시로만 저장한다. 재요청은 새 이용권/월정석/단건 판정 전에 소유 저장 회차를 찾고 취소·환불 원장을 다시 검사한다.
- 기존 10개 섹션 각각 본문 2,000~3,600자, 출력 상한 8,500토큰으로 보강했다. 요청당 네 개 병렬/제공자 42초/한 번, 섹션당 누적 세 번이며 긴 같은 요청 내부 교정을 없앴다. 정상 섹션은 즉시 저장·재조회하고 완료에는 모든 섹션/본문 20,000자/중복·기존 근거 검사/최종 저장·재조회가 필요하다. 과거 completed는 새 분량으로 차단하지 않는다.
- partial 202와 저장 장애 503을 구분한다. 섹션 저장 응답 유실 뒤 형제 저장에서도 정상 본문을 보존하며, 불확실한 시도 한도 소진을 생성 실패 환불로 보내지 않는다. 확정 빈 결과 실패는 기존 환불 경로를 유지하고, 완료 bookkeeping 장애는 저장 결과 전달을 막지 않는다. 생성/저장 lease는 소유자와 토큰으로 제한한다.
- 같은 reportId로 재개하므로 새 결과에는 10분짜리 토큰 만료가 장애가 되지 않는다. 예전 토큰 요청도 처리한다. 계정별 localStorage, 늦은 응답 차단, 숨김/오프라인 정지와 복귀, 원래 시작 본문 재시도를 적용했다. 결제 복귀 완료 확인은 본문 완료 뒤에만 반환한다. /journey/?reportId=…는 출생 입력 없이 소유 서버 결과를 읽는 리더로 열리며, 긴 목차는 접고 본문을 바로 표시한다.
- 실제 라우트 mock 24개(세 결제 방식, 저장 throw/null/readback, 형제 저장 유실, 취소 네 원장, 다른 계정, 동시 요청, 최초 null, bookkeeping 장애, 누적 시도 불확실성, 과거 완료), UI 재개 6개와 기존 부분 결과 5개 통과. typecheck, llm-generation-resilience 902, Mongo shape 907/위반 0, cron coverage, check:fast -- --plan 수행. 원래 문자열 기반 compass 저장 검사를 실제 새 저장 helper 실행으로 갱신했다.
- Next mock 390/1280px: 로컬 입력 없이 4→10개 섹션/같은 reportId POST/JS 오류·가로 넘침 없음. 첫 화면에서 상단 공통 탐색과 겹치던 자체 링크를 정리하고 목차를 접었다. 두 번째 시각 검증에서 본문이 첫 화면에 노출됨을 확인했다. 실서비스 네트워크는 모두 차단했다.
- 한계: 나침반 계산 엔진은 기존 클라이언트 TS 정규화 근거 계약을 유지한다. 서버가 새로운 천체 계산으로 대체 검증한 것은 아니다. 분량/근거 판정은 기존 결정론 규칙과 mock이며 실 LLM 품질·실기기/결제/DB/배포는 미검증이다. 새 리더는 기존 완료 서버 ID URL로 열고, 최초 결제 복귀 이전의 탭 전체 입력 유실은 공용 세션 복원 의존성이 남는다. 모든 LLM 기능 완료로 보고하지 않는다.
- 휴먼 디자인 4fa3251ff0c268abbd820fc1631805434efd0fe5의 PR CI 34860827543는 success. 다음은 초융합 추가 복구와 남은 LLM 실제 상품/짧은 후속 대화 경계다.

## 휴먼 디자인: 짧은 생성·저장 확인·차트 없이 재입장 복구 (2026-09-15)

- 기존 18개 섹션과 25,000자 목표를 유지한다. 제공자 45초/1회, 요청당 네 섹션 병렬, 섹션당 세 번으로 제한하고 시도 예약과 각 정상 섹션을 즉시 저장·재조회한다. 형제 저장 확인 유실 뒤에도 DB의 최신 섹션을 병합해 정상 본문을 덮어쓰지 않는다.
- 정상 18개/본문 20,000자/기존 계산 근거 검사를 충족한 뒤 delivery_pending → 취소·환불 증빙 확인 → completed 저장·재조회를 거친다. 저장 예외/null/확인 실패는 RESULT_STORAGE_UNAVAILABLE 503이며 경과 시간만으로 생성 실패 환불하지 않는다. 확정 빈 결과 실패는 기존 환불 경로, 불명확한 호출 한도 소진은 보존/503이다. 환불 확인 전에는 화면의 결제 재개 정보를 지우지 않는다.
- 계정별 복구 키, 늦은 응답 차단, 숨김/오프라인 중 새 회차 정지 및 복귀 재개를 적용했다. 새 결과에 당시 계산 차트도 보존하고, 서버의 소유 결과/기존 계산 아카이브로 복구하므로 브라우저 출생 입력이 없어도 본문을 연다. 과거 완료 결과를 새 분량으로 차단하지 않는다.
- 검증: 실제 라우트 22개 + 기존 start/revive 3개, UI 동작/정적 37개, typecheck, human-design-report 전체 검증, llm-generation-resilience 902개, Mongo query shape 908개/위반 0, cron coverage 통과. check:fast -- --plan은 critical로 분류하며 전체 로컬 게이트는 반복하지 않았다.
- Next 실제 화면 mock 390/1280px에서 로컬 출생 정보 없이 부분 4장 → 완료 18장, 동일 reportId POST, JS 오류/가로 넘침 없음. 최초 브라우저 검사의 data-chapter 선택자가 내부 도표도 세어 24개가 된 것은 리포트 section 선택자로 바로잡았다. 실 LLM/실결제/실기기/운영 DB/배포는 미검증. 새 revocation helper의 namespace 모델 접근은 Mongo 정적 검사 집계 밖이므로 실제 인덱스 실행 계획을 검증했다고 주장하지 않는다.
- 앞선 인연의 서 e4b9b2a7396635be189cb8c677fdf61376cde1d4 CI 34858277959는 구형 함수 추출 mock 두 건에서 실패했다. 해당 검사를 새 저장 helper와 형제 보존 계약에 맞춰 갱신했고 solo/compat 모두 로컬 통과했다. 이번 커밋의 main CI를 별도로 확인한다.
- 남은 범위: 초융합 전체 스냅샷/같은 단계 재개와 누적 예산, 운명의 나침반, 찻집 잔여 확인, 꿈/작명/동물 토템/펫 사주/오라클/천상 조화/요가/관계 경계/자미두수 섬의 실제 유료 라우트 대조, 짧은 후속 대화의 시간·저장 장애, 공통 분량 계수기 경계. 모든 LLM 기능 완료로 보고하지 않는다.

## 마스터 인연의 서 개인판·궁합판 저장 전달

- 기존 20장·5만 자 분량 목표와 계산 근거 검사를 유지한다. 한 요청 네 장 병렬, 제공자 45초/1회, 장별 누적 세 번으로 제한한다. 불완전한 장 뒤의 정상 장도 deliveryMeta에 즉시 저장·재조회하고, 공개 본문은 원래 연속 장 순서를 유지한다. 저장된 사주/자미두수/궁합 계산 자료를 재사용한다.
- 최종 본문 20,000자·중복 여부 및 전체 장 존재를 검사한 뒤 delivery_pending, 권한 재확인, completed 저장·재조회를 거친다. 저장 예외/null/확인 유실은 RESULT_STORAGE_UNAVAILABLE 503이며 생성 실패 환불로 보내지 않는다. 한도 소진 중 실행 확인이 불명확한 경우 추가 LLM/자동 환불을 멈춘다. 현재 필드와 상태를 CAS lease로 한정해 다른 완료를 덮지 않는다.
- 회수 크론도 같은 생성 함수를 사용하며 저장 대기 상태를 찾는다(크론 일정 변경 없음). 취소 카드, 결제 실행/월정석 환불 원장, 세션의 이용권·금액 환불 마커를 다시 검사한다. 이미 구매한 이용권 회차는 이용권 만료/소진 이후에도 기존 재열람 정책을 유지한다.
- 기존 chapter schema가 버리던 구조화 content를 보존한다. 모바일 결과는 네트워크 실패 뒤 같은 세션을 조회/재개하고 같은 장 수라도 완료 상태 변경을 반영한다. 계정 변경 뒤 늦은 응답을 버리며, 숨김/온라인 복귀에서 이어간다. 완료 전 PDF와 결제 재개 확인을 끝내지 않는다.
- 검증: 실제 라우트 mock 19개, 기존 서버 90개(품질 단일 호출 추가 포함), UI 재개/구매 복구 10개, typecheck, master-love-codex-flow/batch-budget, llm-generation-resilience 902, Mongo index shape 907/위반 0, cron coverage, check:fast -- --plan. 기존 '무조건 제공자 폴백 유지'와 단순 요청 키 삭제 문자열 검사를 현재 제한 호출·계정 변경 경계로 갱신했다.
- Next mock 390/1280px 부분→완료/동일 세션 요청/JS 오류·가로 넘침 없음. 기존 reader 회귀도 로컬 origin을 지정해 360/390/430/1280px 목차·포커스·저장소 차단·취소 구매 비노출 통과(최초 기본 포트 3107 연결 실패 후 실제 서버 3105 지정). 외부 이미지·서비스 요청은 차단했고 실기기/실 LLM/실결제/운영 DB/배포는 미검증. Impeccable 오류 0, 기존 글꼴 단계 advisory 5개.
- 나크샤트라 947f4bbeebc632f369c60ad0485f76c2f7d6d0c7 CI 34856633090 성공. 다음은 나머지 실제 LLM 상품과 초융합/공통 실패 분류다.

## 나크샤트라 상세 상담의 짧은 호출·즉시 저장·모바일 복구

- 기존 아홉 장을 유지하고 부족했던 목표를 장별 합계 21,600자(제목·공백 제외)로 보강했다. 숙요/베다의 실제 계산 요약과 원래 요청·결제 증빙을 서버에 보존하며, 계산을 다시 만들지 않고 누락 장만 생성한다. 제공자 한 번 최대 45초, 요청당 네 장 병렬, 장당 누적 세 번으로 제한했다.
- 정상 장은 형제 호출을 기다리지 않고 저장·재조회한다. delivery_pending 보존 뒤 현재 소유권/이용권/월정석/단건 증빙과 취소·환불 네 원장을 확인하고 완료를 저장·재조회한다. 저장 예외/null/확인 실패는 RESULT_STORAGE_UNAVAILABLE 503이며 완료 문서를 실패로 덮지 않는다. 실행 확인이 유실된 채 호출 한도를 소진한 경우도 생성 실패 환불로 바꾸지 않고 추가 호출을 멈춘다(이 경우 자동 회복 한계가 남는다).
- 모바일은 서버의 미완료 결과를 찾고 같은 ID로 GET→generate를 이어간다. 저장된 본문과 진행률을 함께 표시하고 완료 전 PDF/결제 재개 확인을 끝내지 않는다. 초기 저장이 없으면 같은 메모리 요청/증빙으로 start를 재시도한다. 숨김/오프라인에서는 새 호출을 멈추며 계정 변경 후 응답을 버린다. 개발 Strict Mode 재실행 때 복구 busy가 남던 문제도 실제 화면 검사로 수정했다.
- 검증: 실제 라우트 mock 22개, 실제 화면 재개 함수 5개, typecheck, sitemap 재생성/드리프트 검사, verify:nakshatra-ai-flow, llm-generation-resilience 902, Mongo index shape 910쿼리/위반 0, check:fast -- --plan. 외부 fetch를 차단했다. Next 실제 화면 390/1280px에서 부분→완료, 동일 ID POST, JS 오류/가로 넘침 없음. 진행 문구와 상단 탐색 겹침을 수정했다. Impeccable의 기존 버튼 색상 지적 2건은 이번 복구 변경 밖이다.
- 네오 2차/재개 rate bucket CI는 별도 소개 화면의 public 이미지 누락으로 Static guards 실패였다(34853304858, 34853817693). 다른 세션의 후속 main 변경을 보존했으며 1390e28030715769d2095b5755d750eb4e820ecc CI 34855292994 성공을 확인했다. 나크샤트라는 별도 커밋/CI로 전달한다. 실 LLM의 글 품질·실결제·실기기·운영 DB·배포는 미검증이다.
- 다음: 마스터 연애 코드 및 나머지 실제 LLM 기능, 초융합의 추가 복구, 짧은 후속 대화와 공통 실패 분류를 계속 점검한다.

## 저장본 재개 요청의 일일 시작 예산 분리

- 점성술·베다·일반 자미두수·연애 비책·인생의 책·신년·숙요 궁합·네오의 서버 ID 재개를 공통 보안 계층에서 구별한다. 인증된 사용자와 저장 문서의 소유권, 원래 서버 요청 snapshot을 확인한 경우만 기존 batch 90회/분 버킷을 사용한다. 각 실제 라우트의 소유권·취소/환불·결제 증빙 검사는 그대로 실행한다.
- 새 리포트 시작의 기존 60건/일 한도와 미분류/위조/다른 계정/다른 상품/DB 조회 실패의 제한은 유지한다. 큰 입력은 재개 분류에서 제외하고 복제 요청 스트림은 4 KiB까지만 읽는다. 원본 요청 body를 소비하지 않는다. 구 completed의 소유자 재열람은 생성 없이 재사용한다.
- 검증: 실제 enforceAiRouteSecurity mock 47개(기존 35+재개 12) 통과. 시작 일일 예산 소진 상태에서도 소유자의 저장 ID는 재개되며, 새 입력·위조/다른 계정·원본 없는 미완료·DB 장애·다른 자미두수 상품·큰 입력은 기존 예산을 우회하지 않는다. 기존 레이트리밋 mock의 누락된 lean 체인도 수정해 실제 초과 판정까지 검사했다.
- worker-security-guards, worker-no-undef, 대상 ESLint 오류/경고 0, check:fast -- --plan, Mongo 기존 추적 908쿼리/위반 0 확인. 신규 모델 선택은 여덟 기존 컬렉션의 사용자+결과 ID 조회이며 모델명 동적 선택은 현재 Mongo 정적 추출기의 집계 밖이다. 운영 DB/실 LLM/실결제/배포 미검증.
- 네오 2차 12df7ad22의 CI는 진행 중이다. 다음은 남은 실제 LLM 상품과 초융합 추가 복구, 짧은 후속 대화의 시간·저장 경계 확인이다. 사주 기본 라우트는 /fortune 아래 별도 경로이며 이번 여덟 서비스 분류에 포함하지 않았다.

## 네오 2차 작전 수정의 저장·이어받기

- 기존 후속 작전 8영역/8,000자 목표를 유지한다. 상세 1차의 20,000자 기준을 짧은 후속 답변에 강제하지 않는다. 실제 refine는 네 영역씩, 제공자 최대 45초/1회, 영역별 누적 세 시도로 생성한다. 필수 키·목록·분량·반복 검사 후 정상 영역을 서버에 즉시 보존한다.
- 원래 현실 점검 답변/해시와 계산 요약·1차 결과를 재사용하고 DB lease·소유권·취소/환불 증빙을 재확인한다. 저장 예외/null/재조회 실패는 RESULT_STORAGE_UNAVAILABLE이며 1차 completed를 실패로 덮거나 환불하지 않는다. 전체 수정 결과 저장·재조회 뒤 한 번만 수정 이력/메시지를 확정한다.
- 결과 화면은 1차가 완료 상태여도 미완료 refinement를 발견해 같은 ID로 자동 이어받는다. 부분 수정 본문을 제공하고 계정 변경 뒤 늦은 응답을 무시한다. 재화/리워드를 추가 소비하지 않는다.
- 검증: 네오 실제 라우트 mock 28개(1차 22+2차 6), 실제 이어받기 함수 5개 통과. typecheck, neo/neo-output-safety/worker-no-undef, resilience 902, Mongo 908쿼리/위반 0, check:fast -- --plan, sitemap 갱신. 대상 ESLint 오류 0(기존/미사용 legacy helper 등 경고 7, 폐기된 UI 폴링 상수 네 개 제거). Impeccable detector 종료 0.
- 외부 요청 전부 차단한 실제 Next 390px/1280px에서 1차 보존·부분 수정→동일 ID refine→완료 수정 본문 제공을 확인했다. 브라우저 오류/가로 넘침 없음. 실 LLM 의미 품질·실결제·운영 DB·실기기·배포는 미검증.
- 네오 1차 7d2e20eb1d89108fb493a9a2a56c6c39d0617b5b main CI 34851703385 성공. 별도 상세 소개 화면 작업 c0af17eb7d5abeffc3b0d38e3b6d35bb35225eca가 main에 합류했으며 보존했다. 이 2차 수정은 별도 커밋/CI로 확인한다. 다음은 재개 rate bucket, 나머지 실제 LLM 기능 및 초융합 추가 복구다.

## 네오 1차 브리핑 생성·저장·모바일 이어받기

- 기존 14영역·일반 20,050자/궁합 20,550자 목표를 유지하고 실제 렌더 본문에서 제목·별칭 중복·공백을 제외한 최소 20,000자를 완료 조건으로 추가했다. 필수 키/목록 수·영역별 최소 분량·반복·기존 계산 근거 인용을 검사한다. 생성 목표에는 20% 여유를 준다.
- 실제 start는 네 영역 병렬, 제공자당 최대 45초/1회, 영역별 누적 세 시도다. 정상 형제 영역은 즉시 저장·재조회하며 한 영역 저장 실패가 나머지 정상 영역 보존을 막지 않는다. 전체 계산 요약과 원래 입력/요청/결제 증빙을 서버에 보관한다.
- 소유권·DB lease 120초·취소/환불 증빙 네 저장소를 확인한다. 토큰/클라이언트 PASS만으로 생성 권한을 확정하지 않고 현재 서버 정책을 재확인한다. 월정석은 기존 정본 원장 조회를 유지한다. 과거 completed는 신규 분량 기준 없이 재열람한다.
- delivery_pending 보존 → 기존 사용 처리 → completed 쓰기/재조회 순서다. 저장 예외/null/확인 실패 및 apply 응답 유실은 503 RESULT_STORAGE_UNAVAILABLE이며 환불로 넘기지 않는다. 완료 사용 기록 오류는 저장된 결과 제공을 막지 않는다.
- 실제 화면은 부분 본문을 먼저 표시하며 같은 서버 ID로 start를 이어간다. 계정 분리·숨김/오프라인 중 신규 생성 중지·화면/온라인 복귀·서버 미완료 검색을 연결했다. 최종 완료 전 재개 정보를 보존하고 PDF/혜택 조작을 비활성화한다. 부분 결과의 완료 문구와 모바일 제목/고정 탐색 겹침을 수정했다.
- 검증: 실제 라우트 mock 22개(세 결제 방식, 저장 6종, apply 응답 유실, 잘린 출력/형제 보존, 취소/환불, 동시 요청/lease, 소유권/과거 구매본), 실제 클라이언트 함수 4개, 기존 네오 Jest 48개 통과. typecheck 통과, 대상 ESLint 오류 0/경고 6(기존/미사용 legacy helper 포함), neo/neo-output-safety/worker-no-undef, resilience 902개, Mongo 909쿼리/위반 0, check:fast -- --plan, sitemap 동기화 확인. Impeccable detector 종료 0.
- 외부 요청 전부 차단한 실제 Next 390px/1280px에서 부분→동일 ID POST→완료 본문 제공, 브라우저 오류/가로 넘침 없음. 원격 이미지 일부는 차단으로 미표시. 실 LLM 품질·실결제·운영 DB·실기기 결제 복귀·배포 미검증. 의미 수준의 모든 계산 모순 검출을 보장하지 않는다. 원래 resumeBody가 없는 오래된 미완료 문서는 원래 요청이 필요하다.
- 카르마 2b6fa1259e893fd494fb621ee2802315956d8df6 main CI 34849327175 성공. 이번 네오 단위는 push 후 CI로 공식 판정한다. **네오 2차 refine, 나머지 실제 LLM 상품, 초융합 추가 복구, 재개 rate bucket 및 짧은 후속 대화 경계는 아직 진행할 작업이다.** 모든 기능 완료로 간주하지 않는다.

## 카르마 상세 리포트 생성·저장·모바일 복구

- 기존 15장·30,000자 목표 및 다섯 렌즈 분업을 유지한다. 실제 generate-batch는 최대 네 장 병렬, 제공자당 45초/1회이며 같은 요청에서 JSON 재수선·전체 재생성을 겹치지 않는다. 챕터별 누적 세 시도를 호출 전에 저장하고 정상 챕터는 형제 챕터의 실패와 무관하게 즉시 저장·재조회한다.
- 원래 입력/결제 증빙과 계산 결과를 서버에 보존한다. 부족한 필수 챕터·반복/일반론·전체 분량 부족 영역만 보완한다. 기존 품질 기준과 본문만 20,000자 기준을 함께 만족해야 delivery_pending이며, 기존 apply 후 최종 저장/재조회가 확인되어야 completed다.
- 소유권·DB 원자적 lease·취소/환불 증빙 재검사를 적용한다. 저장 예외/null/재조회 실패, apply 응답 유실은 503 RESULT_STORAGE_UNAVAILABLE이며 생성 취소/환불로 넘기지 않는다. 계산 저장 실패도 서버 원래 요청으로 재개할 수 있다. 이미 완료된 과거 구매본은 신규 품질 기준으로 차단하지 않는다.
- 화면은 서버의 미완료 기록을 찾아 같은 ID의 generate-batch로 이어간다. 부분 본문을 먼저 표시하고 숨김 상태에서는 새 호출을 시작하지 않는다. 온라인/화면 복귀/계정 변경을 처리하며 저장 장애 자동 재시도는 제한한다. 최종 완료 전 유료 재개 정보를 지우지 않고 PDF를 비활성화한다. 스크롤 observer 신호를 기다리며 본문이 빈 영역처럼 숨던 CSS도 처음부터 읽히도록 수정했다.
- 검증: 실제 라우트 mock 22개(이용권/월정석/단건/deferred, 저장 6종, 형제 챕터 실패·checkpoint null, 계산 저장 복구, lease, 취소/환불/계정/과거 재열람, apply 응답 유실), 실제 화면 재개 함수 4개 통과. typecheck, karma-destiny-ai-flow, worker-no-undef, resilience 902개, Mongo 902쿼리/위반 0, ESLint 오류 0(이전 생성 helper 포함 경고 8), check:fast -- --plan, sitemap 일치 확인.
- 외부 요청 차단한 Next 실제 프리뷰 390px/1280px에서 부분 본문→동일 ID POST→완료 PDF 활성화 및 기본 본문 가시성, 브라우저 오류/가로 넘침 없음 확인. 실 LLM 의미 품질·실결제·운영 DB·실기기·배포 미검증. 렌즈 근거는 서버 계산값을 사용하며 의미 수준의 모든 모순 판정까지 보장하지 않는다. 기존 비배치 순수 helper와 짧은 후속 대화 경로는 상세 리포트 완료 기준과 별도다.
- 일반 자미두수 22eaeba7d210314f3c80ef6b9c4ff2fad0924cdf main CI 34847375235 성공 확인. 이 카르마 단위는 push 후 main CI를 확인한다. 다음은 네오 및 나머지 실제 LLM 상품, 초융합의 추가 복구, 재개 요청 rate bucket, 짧은 후속 대화의 시간/저장 경계 점검이다.

## 일반 자미두수 생성·저장·모바일 전달

- 기존 6묶음·필수 섹션·20,700~30,000자 목표를 유지한다. 요청당 본문 한 묶음(첫 요청에는 작은 메타 병렬), 제공자 45초/1회, 묶음별 누적 세 시도를 호출 전에 기록한다. 제목/공백 제외 본문·누락·반복·기존 명반 근거 검사를 통과한 묶음만 보존한다. 부족한 근거의 소유 묶음만 보완한다.
- 전체 계산값·원래 요청/증빙과 정상 묶음을 서버에 저장한다. 소유권/상품/입력 해시·DB lease를 검사하고 202 부분 응답에서 같은 결과 ID로 재개한다. 취소·환불 4개 증빙 저장소를 재확인한다. historical completed는 신규 분량 검사 없이 다시 읽는다.
- delivery_pending을 저장·재조회한 뒤 기존 apply를 원래 요청 키로 실행하고 completed 쓰기·재조회를 확인한다. 저장 예외/null/확인 실패와 apply 응답 유실은 503 RESULT_STORAGE_UNAVAILABLE, 환불·전체 재생성 없이 같은 저장본으로 재시도한다. 결제 fulfilled 기록은 완료 후 별도 처리한다.
- 화면은 정상 묶음을 읽으며 다음 생성으로 이어지고 숨김/오프라인 뒤 서버 기록으로 재개한다. 계정 변경 뒤 응답은 무시하고 최종 완료 전 재개 정보/PDF 완료 처리를 보류한다. 부분 상태의 완료 오표시와 큰 화면에서 결과가 한 열에만 갇히던 배치도 수정했다.
- 검증: 실제 route Jest 20개(이용권/월정석/단건, 저장 6종, apply 응답 유실, 근거 보완, 완료 직전 취소, lease, 과거 결과), 실제 화면 함수 4개 통과. typecheck, 대상 ESLint 오류 0(기존 경고 3), worker-no-undef, ziwei-ai-consultation-flow, resilience 902개, Mongo 899쿼리/위반 0, check:fast -- --plan, sitemap 일치 확인.
- 외부 요청을 차단한 실제 Next 390px/1280px 프리뷰: 부분 본문→동일 ID POST→완료 PDF 활성화, 브라우저 오류/가로 넘침 없음. 실 LLM 품질·실결제·운영 DB·실기기 결제 복귀·배포 미검증. 의미 수준의 모든 명반 모순 검출을 보장하지 않으며 기존 근거 검사를 유지/완료 조건에 적용했다. 오래된 미완료 문서에 원래 resumeBody가 없으면 원래 요청이 필요하다.
- 베다 문구 보완 d15c18c85efec2760c9748c633fe40533d3f3d39 main CI 34846309068 성공. 이 자미두수 작업의 공식 판정은 push 후 main CI로 확인한다. 카르마·네오·나머지 실제 LLM 상품, 초융합 추가 복구와 재개 요청 rate bucket 점검은 계속 진행한다.

## 베다 재개 안내 로케일 보완

- b1afc4d0e1494b74b99207667b7bccb36ca95c98 CI 34845493154: 빌드·핵심 검사 통과, 새 재개 배너 한국어 직접 작성 1건으로 Static guards 실패. 기존 결과 카피 사전에 ko/en/ja/zh-CN/zh-TW를 추가하고 결과 화면에서 참조하도록 보완했다.
- 점성술 ca0a72ff5e5f0d51becc877471f42a5c8de29370 CI 34844608835 성공 확인.

## 2026-09-14 베다 상세 생성·저장·모바일 재개

- 네 주제 섹션과 다섯 근거 흐름의 기존 구조를 유지한다. 새 본문은 읽기 묶음당 4,400~6,000자, 근거 묶음 2,600~4,200자이며 합산 본문 20,000자 이상·필수 키·계산 근거 대조를 통과해야 완료한다. 제목/JSON 키/공백은 본문 합산에서 제외한다.
- 요청당 한 묶음, 제공자 45초/1회, 누적 묶음별 세 시도로 제한한다. 정상 묶음과 전체 VedicChartResult를 Mixed 메타데이터에 저장하여 스키마에서 계산 근거 필드가 누락되지 않게 했다. 저장된 언어·입력·결제 요청을 재사용한다.
- DB lease와 동일 isolate 응답 복제, owner+결제 키+입력 검증, 취소·환불 재확인을 적용했다. delivery_pending 저장 후 완료 쓰기·재조회가 통과해야 completed다. 저장 예외/null/확인 실패는 503 RESULT_STORAGE_UNAVAILABLE이며 생성 실패 환불로 넘기지 않는다. 기존 차감·환불 정책과 과거 완료 구매본은 유지한다.
- 화면은 부분 결과를 표시하고 같은 서버 ID로 다음 묶음을 요청한다. 새로고침·로그인·온라인·화면 복귀는 서버 진행 중 목록에서 복구한다. 완료 전 재개 정보를 지우지 않고 PDF 저장 버튼도 완료 후 활성화한다.
- 실제 라우트 Jest 20개(세 결제 방식·저장 6종·누적 한도·라그나 모순·취소/환불·동시 요청·DB lease·언어/계산 재사용) 및 실제 화면 재개 함수 4개 통과. typecheck, 대상 ESLint 오류/경고 0, worker-no-undef, resilience 884개, vedic-ai-flow, Mongo 892쿼리/위반 0, check:fast -- --plan, sitemap 일치 확인.
- 외부 요청을 차단한 390px/1280px 로컬 실제 프리뷰에서 부분→동일 ID POST→완료 PDF 활성화, 가로 넘침·브라우저 오류 없음. 실 LLM·실결제·운영 DB·실기기 결제 복귀·배포는 미검증.
- 이 변경의 공식 결과는 push 이후 main CI로 판정한다. 점성술 경로 분류 보완 ca0a72ff5e5f0d51becc877471f42a5c8de29370의 CI 34844608835는 이 기록 시점에 진행 중. 일반 자미두수·카르마·네오 및 나머지 상품, 초융합 추가 복구 보완은 계속 필요하다.

## 2026-09-14 점성술 상세 생성·저장·모바일 재개

- c166902f2667accaa876aea5de6d89d1d8098ca7의 CI 34843552227은 신규 /pending 경로 분류 누락 한 항목으로 실패했다(238 suites 통과). 계정/화면/온라인 복귀마다 1회 조회하는 경로이며 기존 read 버킷(100회/분)에 등록했다. 경로 분류 실제 보안 검사 35개 통과. 이 보완 커밋을 다시 main CI로 검증한다.

- 신규 상세 상담은 기존 여섯 섹션을 유지하며 섹션당 3,400~5,000자, 전체 본문 최소 20,000자를 검사한다. 400자 degrade 허용은 신규 start 완료 경로에서 제거했다. 과거 완료본은 그대로 재열람한다.
- 요청당 두 섹션 병렬, 제공자당 45초/1회, 섹션별 누적 세 시도를 호출 전에 저장한다. 부족·누락·반복·필수 전문 항목 누락 섹션만 재시도한다. 계산한 천궁도와 원래 입력·결제 키를 서버에 보존한다.
- 소유권과 DB lease로 동시 실행을 제한하고 부분 문서·delivery_pending·완료를 구분한다. 저장 null/예외/재조회 실패는 503 RESULT_STORAGE_UNAVAILABLE이며 환불하지 않는다. 완료 보관 이후 사용 기록 실패는 결과 제공을 막지 않는다. 기존 생성 실패 환불 정책은 유지한다.
- 토큰과 클라이언트 PASS 표시만 믿던 지점을 서버 증빙·현재 권한 확인으로 변경했다. 취소·환불된 결제/실행/월정석 원장/차감 기록을 먼저 거부한다.
- 결과 페이지는 저장된 부분 본문을 표시하면서 동일 서버 ID로 이어 생성한다. 새로고침·계정 복귀·온라인 복귀 시 서버의 진행 중 문서를 찾는다. 계정이 바뀐 뒤 도착한 응답은 표시하지 않는다. 완료 전 결제 재개를 성공으로 정리하지 않는다.
- 실제 라우트 mock Jest 18개, 실제 재개 함수·언어 경계 node:test 24개 통과. 생성 resilience 884개, typecheck, worker-no-undef, mongo-query-index-shapes 881쿼리/위반 0, 대상 ESLint 오류 0, check:fast -- --plan 및 sitemap 일치 확인. 옛 sectioned helper 검사도 통과했지만 실제 신규 start의 부분 생성 계약은 새 18개 검사가 담당한다.
- 로컬 실제 390px/1280px 화면에서 부분 본문→같은 ID POST→완료 PDF 버튼 표시, 가로 넘침·브라우저 오류 없음. 상단 고정 탐색 버튼과 결과 링크의 겹침 수정. 외부 요청은 차단하고 API는 fixture만 반환했다.
- 초융합 32696228d98cecaf6caa80fdce23dc90c90788ae PR CI 34841990025 success. 점성술은 별도 push 뒤 CI 판정. 베다·자미두수·카르마·네오 및 나머지 실제 LLM 상품 확대, 초융합 추가 복구 보완은 남아 있다. 실 LLM 해설 품질·모든 사실 모순 자동 판정·실결제·운영 DB·실기기 결제 복귀·배포는 미검증.

## 2026-09-14 초융합 실행 예산·저장 순서 수정

- 요청 실행 예산을 120초에서 82초, 제공자 상한을 45초로 줄였다. 저장 여유 시간이 부족하면 새 호출을 시작하지 않으며 보완 호출도 남은 예산 안에서만 실행한다. 기존 긴 리포트 목표는 유지한다.
- 정상 묶음 저장이 하나 실패해도 다른 묶음의 저장은 진행하며 저장 오류는 503 원인을 유지한다. 보완 결과를 수락한 후에만 저장하여 더 짧은 결과가 기존 본문을 덮지 않도록 했다.
- 일반 generate API도 한 요청에 한 단계만 실행하고 1단계는 HTTP 202와 동일 요청 ID·nextStage를 반환한다. 최종 저장은 생성 예약 완료 처리보다 먼저 실행한다. 실제 화면은 기존 단계별 SSE를 사용한다.
- Jest 실제 생성기/라우트 54개, 단계 흐름·재열람·동일 결제 키 재시도 검사 통과. check:fast -- --plan 확인. 숙요 변경 355b4faccf6278d0fcadc864ff33b85f26cf8372의 PR CI 34841131502 success 확인.
- 남은 범위: 초융합 서버 기록만으로 복구, 누적 호출 한도·부분 단계 자동 재개, 기타 LLM 상세 상품 확대. 실 LLM·실결제·운영 DB·배포 검증 없음.

## 2026-09-14 숙요 궁합 분할 생성·전달 개선

- 실제 코드는 다섯 묶음과 요약을 한 요청에서 생성하고 600자만 있어도 완료했다. 이제 요청당 한 묶음, 제공자 호출당 45초/1회로 제한한다. 첫 묶음의 요약은 병렬 실행하며 본문 정상 묶음은 서버에 즉시 저장·재조회한다. 묶음별 최대 세 시도를 호출 전에 예약한다.
- 15개 기존 섹션 키와 계산 체계는 유지한다. 새 본문은 모든 섹션의 최소 분량과 전체 공백 제외 2만 자를 통과해야 완료 후보가 된다. 짧은 섹션·잘림·반복은 완료에서 제외하며 정상 섹션을 보존한다. 처음 계산한 근거와 원래 입력·결제 증빙을 서버에 저장한다.
- 결과 ID만으로 소유권을 확인하고 원래 요청을 재개한다. 유효 토큰만으로 취소·환불을 우회하지 않고 원장/결제/실행 기록을 다시 확인한다. storage null/예외는 503 RESULT_STORAGE_UNAVAILABLE이며 생성 실패 환불로 넘어가지 않는다. 실패 상태도 생성 잠금을 소유한 요청만 저장한 뒤 기존 환불 정책을 적용한다.
- 모바일 화면은 부분 본문을 표시하며 같은 결과 ID로 이어 생성한다. 완료 전에는 결제 재개 정보를 지우지 않는다. 온라인/화면/로그인 복귀 시 서버의 진행 중 결과를 찾고, 계정 전환 이후 응답은 표시하지 않는다. 과거 완료 구매본은 기존대로 읽는다.
- 로컬 실제 화면에서 소개 본문이 결과 모달을 가리는 부모 stacking context 문제를 재현했다. 모달을 body portal로 이동하고 고정 탐색 버튼보다 위에 배치했다. 390px/1280px 프리뷰에서 결과 제목·본문·닫기 버튼 표시, 가로 넘침 없음, 브라우저 오류 없음 확인. 외부와 API 네트워크는 모두 차단했다.
- 실제 라우트 Jest 16개(이용권·월정석·단건, 저장 예외/null, 동시 요청, 서버 ID 재개, 취소·환불), 실제 클라이언트 함수 4개 통과. generation-resilience 884개, typecheck, worker-no-undef, mongo-query-index-shapes(877쿼리/위반 0), 대상 ESLint 통과. check:fast -- --plan 확인.
- 직전 심화 자미두수 1fc2e22522b90faeb6bb50b949fc2ea04fa090d6 CI 34839131337와 공통 요청 제한 9e7e5aa5c6c47374a6a593d0b1634acf58761816 CI 34839479274는 success. 이 숙요 변경은 별도 push 후 main CI로 판정한다.
- 미검증: 실 LLM 품질·실결제·운영 DB·물리 모바일 결제 복귀. 개인 숙요 짧은 상담과 후속 대화의 분량은 이 궁합 상세 리포트 기준에 포함하지 않았다. 모든 점술 의미의 모순을 자동 판별하는 것은 미완료다. 초융합 및 기타 LLM 상세 상품의 서버 생성·복구 확대는 계속 필요하다.

## 2026-09-14 공통 LLM 요청의 조기 연결 종료 수정

- authFetch의 일반 API용 22초 제한이 생성 요청에도 적용되고 있었다. 확인된 LLM 생성 POST는 서버의 생성·저장 응답을 기다리도록 95초 상한으로 분리했다. 일반 조회·인증·결제 확인의 22초 제한과 호출자의 취소 signal은 유지한다.
- 실제 fetchAuthRequest를 가상 시간과 mock fetch로 실행한 17개 검사 통과. 60초 생성 응답 수신, 95초 상한, 일반 조회 22초 상한, 타이머 정리를 검사했다. 외부 호출 없음. 대상 ESLint 오류 0(기존 경고 16), check:fast -- --plan 확인.
- 서버 자체의 장시간 실행과 모바일 OS 종료까지 해결된 것은 아니다. 각 기능의 서버 체크포인트·완료 저장·복구 확대는 진행 중이다.

## 2026-09-14 심화 자미두수 PDF 생성 중단 예방

- 첫 LLM 호출 전에 원래 입력·멱등키·명반을 저장한다. 요청당 최대 네 챕터를 병렬 생성하고 각 챕터 완료 즉시 개별 체크포인트를 저장·재조회한다. 서버에 저장된 미완료 챕터만 이어 생성하며 클라이언트 startIndex나 토큰의 누적 분량을 완료 근거로 믿지 않는다.
- 기존 15챕터·충분한 분량 목표는 유지한다. 모든 필수 챕터의 최소 본문·잘림·반복 검사를 통과해야 완료 후보가 된다. 챕터별 최대 세 시도를 호출 전에 예약하고 보완 호출에서는 실패 캐시를 재사용하지 않는다. 정상 챕터는 유지한다.
- 원래 결제 증빙을 재확인하고 월정석은 정본 차감 원장으로 확인한다. 취소·환불된 증빙을 이용권으로 우회하지 않도록 재조회한다. 계정·원래 입력 변경을 거부하며 완료된 과거 구매본은 새 분량 기준으로 차단하지 않는다.
- delivery_pending 본문 저장·재조회 후 completed 저장·재조회를 확인한다. 저장 실패는 503 RESULT_STORAGE_UNAVAILABLE로 반환하고 생성 실패 환불과 분리한다. 생성 잠금으로 동시 요청의 중복 생성을 방지한다.
- 화면은 부분 본문을 표시하며 같은 결과 ID로 이어받는다. 새로고침·온라인 복귀·계정 재로그인 시 서버의 진행 중 결과를 찾는다. 계정 전환 뒤 도착한 응답을 화면에 섞지 않는다. 숙요 궁합의 긴 결과를 화면 진입 비율 때문에 숨기던 애니메이션 조건도 제거했다.
- 실제 라우트·생성기·클라이언트 함수 mock 및 숙요 결과 SSR 검사 18개 통과. verify:ziwei-deep-report-flow, worker-no-undef, mongo-query-index-shapes(868쿼리, 위반 0) 통과. check:fast -- --plan 확인. 기존 저장 함수 회귀도 흐름 검사에서 실행한다.
- 직전 신년운세 24285603d13e9fff47cd653a012a2cd48cd04fa9 main CI 34837667967 success. 이 단위는 별도 push 후 CI로 판정한다.
- 한계: 실 LLM 품질·운영 DB·실결제·물리 모바일 복귀는 미검증. 심화 명반의 모든 의미적 모순을 자동 판별하는 것은 미완료이며 최초 계산값 보존과 기존 프롬프트 근거를 유지했다. 전체 LLM 기능 개선은 진행 중이다.

## 2026-09-14 신년운세 생성·전달 개선

- 신년운세는 한 요청에서 다섯 분야와 보완 호출을 몰아서 실행하지 않고, 요청당 한 분야만 생성한다. 분야별 52초 한도, 총 요청 예산 82초, 분야별 누적 3회 한도로 미완료/분량 미달/모순/잘림/반복 분야만 보완한다.
- 각 분야를 서버에 즉시 저장하고, 최초 계산 근거와 원래 입력·멱등키·결제 증빙을 보존한다. 생성 잠금은 DB에서 원자적으로 획득한다. 중단된 요청은 저장된 분야를 재사용한다.
- 다섯 분야와 공백 제외 본문 2만 자·기존 계산 일치 검사를 통과한 결과를 delivery_pending으로 먼저 저장·재조회한다. 기존 deferred apply 확인 후 완료 저장과 재조회를 통과해야 completed/saved:true를 반환한다. 저장 null/예외와 apply 응답 유실은 503·동일 결과 ID로 복구하고 생성 실패 환불과 분리한다.
- 모바일 화면은 부분 본문을 완료로 취급하지 않는다. 저장된 분야를 보여주며 같은 상담 ID로 이어받고, 새로고침·온라인 복귀·로그인 후 서버의 진행 중 상담에서도 재개한다. 기존 완료 결과 재열람은 유지한다.
- 현재 증빙 판정은 접근 토큰의 유효성만 믿지 않고 취소/환불 내역과 원래 증빙을 다시 확인한다. 같은 결과의 다른 계정 접근과 같은 멱등키의 입력 변경을 거부한다.
- 실제 라우트/생성기/클라이언트 mock 16개 통과(이용권·월정석·단건, 최종 저장 null, apply 응답 유실, 중복, 부분 저장, 짧은 본문, 19,999/20,000자 경계, 취소 증빙). verify:new-year-ai-flow, generation-resilience 884개, typecheck, worker-no-undef, mongo-query-index-shapes 통과. 대상 ESLint 오류 0. check:fast -- --plan 확인.
- 직전 인생의 책 커밋 31a63015e main CI 34835912367 success. 이 신년운세 변경은 별도 push 후 main CI로 판정한다.
- 실 LLM/실결제/운영 DB/물리 모바일 결제 복귀는 미검증. 다음은 심화 자미두수 PDF·초융합·숙요 궁합의 남은 생성 중단 예방과 기타 LLM 상품 대조다. 전체 상품 개선 완료로 보고하지 않는다.

- 로컬 Next 개발 프리뷰에서 390px/1280px 인생의 책·신년운세 결과를 확인했고 가로 넘침이 없었다. 신년운세의 motion/LazyMotion 충돌과 긴 카드의 화면 진입 비율 조건 때문에 본문이 숨는 문제를 재현해, 결과를 처음부터 노출하도록 수정했다. 프리뷰 상태도 실제 completed/saved 계약에 맞췄다. 외부 및 API 네트워크는 브라우저에서 전부 차단했다. 이 검사는 결제 E2E나 실 LLM 결과의 증거가 아니다.

## 2026-09-14 인생의 책·인생 총운 전달 개선

- 실제 장애 원인: 첫 네 섹션 저장 뒤 상태가 generating인 요청을 전부 작업 중으로 간주하여 다음 웨이브가 실행되지 않았다. 실제 DB 잠금이 살아 있는 경우만 대기하고, partial에서는 다음 미완료 섹션을 진행한다.
- 한 요청은 최대 네 섹션, 섹션 호출은 최대 45초. 시도 횟수를 호출 전에 예약하고 각 섹션 종료 직후 저장·재조회한다. 서버에 최초 계산 근거와 원래 요청/증빙을 보존한다.
- 최종 본문을 delivery_pending으로 저장·재조회한 뒤 기존 deferred apply를 실행한다. 완료 저장 및 재조회 뒤에만 completed/saved:true를 응답한다. 저장 예외/null과 apply 응답 유실은 환불 경로에서 분리한다. 완료 문서와 정상 섹션은 재사용한다.
- 재개 ID는 계정 소유권을 검사한다. 원래 증빙의 취소/환불 상태를 다시 읽고 토큰만으로 접근을 허용하지 않는다. 실패 확정도 생성 잠금을 소유한 요청만 수행한다.
- 인생 총운의 기존 3만~6만 자 생성 목표는 유지한다. 인생의 책은 전체 본문 2만 자 하한에 맞춰 섹션 목표를 보강했다. 충분히 긴 내용은 자동으로 잘라내지 않으며, 과거 completed 결과는 새 품질 기준으로 차단하지 않는다.
- 모바일 결과 화면은 저장된 본문을 표시하면서 다음 웨이브를 요청하고, 화면 복귀/온라인 복귀 때 같은 세션을 이어받는다. 계정 전환 이후 도착한 결과는 폐기한다. 생성 화면은 재개 ID와 같은 멱등키를 쓰고 완료 전 복구 정보를 보존한다.
- 실제 라우트/클라이언트 함수 mock 15개, 섹션 검사 13개, UI 계약 검사 9개 통과. verify:life-book-ai-flow, typecheck, worker-no-undef, mongo-query-index-shapes 통과. check:fast -- --plan 확인. 별도 실브라우저 시각/실기기/실 LLM/실결제/운영 DB 검증은 미실행.
- 연애 비책 커밋 b908e655c main CI는 빌드·critical 검사를 통과했으나 sitemap 기록 누락으로 실패했다. fd1c56725의 사이트맵 생성에는 진행 중 인생의 책 소스가 포함되어 원장 서명만 다시 불일치했다. 인생의 책 소스와 최종 사이트맵 원장을 함께 전달한 뒤 main CI를 확인한다.
- 다음 작업: 신년운세의 긴 생성/차감 후 저장 실패 경로, 이어 나머지 LLM 상품의 중단 예방과 복구 계약. 전체 상품 개선은 아직 완료되지 않았다.


## 2026-09-14 연애 비책 생성·전달 개선

- 사용자 우선순위 재확인: 모든 LLM 기능에서 긴 호출이 실행 제한으로 끊기지 않게 하는 것이 우선이다. 결제 후 정상 생성·저장·전달을 먼저 고치며 충분히 긴 기존 본문은 불필요하게 늘리지 않는다.
- 연애 비책은 기존 6그룹·28절·3만~3만6천 자 생성 목표를 유지한다. 요청당 한 그룹(최대 기본 생성+보완 2회), 그룹별 누적 4회 한도. 정상 그룹과 최초 계산 근거를 서버에 보존하고 다음 요청은 미완료/품질 미달 그룹만 생성한다.
- 최종 저장 대기와 부분 생성을 분리했다. 완료는 필수 절/공백 제외 2만 자/기존 근거 품질 검사를 거쳐 저장 및 재조회 뒤에만 반환한다. 저장 장애는 환불 경로와 분리하고 최종 본문을 재사용한다.
- 같은 결과 ID의 서버 재개, 원래 멱등키/증빙 유지, 만료된 접근 토큰 대신 원래 증빙 재확인, 취소·환불 거부, 생성 claim CAS를 추가했다. 새로고침·계정 재로그인 시 서버의 최근 상담으로 접근할 수 있다.
- 모바일 화면은 부분 본문으로 완료 처리하지 않고 자동 이어받는다. 결과 화면은 저장된 절을 보여주며 이어받고 화면 복귀 시 다시 확인한다. 저장 장애/재시도는 결제창을 다시 열지 않는다. 계정 전환 뒤 응답은 폐기한다. 90초 완료 안내는 단계별 저장 안내로 12개 언어에서 수정했다.
- mock: 실제 라우트·클라이언트 함수·생성기 호출 범위·취소 증빙 결정 18개 통과. love-secret flow(28절/본문 23,835자 fixture), typecheck, worker-no-undef, Mongo 쿼리 모양, generation-resilience 884개 통과. 대상 ESLint 오류 0, 기존 경고 15개. 실제 LLM 출력·운영 DB/PG·물리 모바일 결제 복귀는 미검증이다.
- 앞선 a7d9f6b8095f7653a66eb16729a7ab69e6b6e09f main CI 34832408776은 success. 이 연애 비책 작업의 CI는 push 후 별도로 확인한다.
- 다음 우선 작업은 인생의 책/인생 총운과 신년운세의 생성 중단 예방·체크포인트·최종 저장 계약이다. 다른 LLM 서비스 전체 목록 대조 및 남은 품질/결제 조합 검증도 계속 필요하다.
## 2026-09-14 저장 장애 확대 중간 검증

- 추가 지시: 사주 기반 연애 비책·인생의 책/인생 총운·신년운세 등 모든 개별 LLM 서비스를 포함한다. 이미 충분히 긴 리포트의 생성 목표를 불필요하게 늘리지 않는다.
- 심화 자미두수 PDF 배치 저장 throw/null/재조회 실패를 503 RESULT_STORAGE_UNAVAILABLE로 분리했다. 저장 장애에 생성 실패 환불을 실행하지 않으며 완료 문서를 덮지 않는다.
- 초융합은 저장 ID가 없거나 저장 예외가 나면 성공 SSE/HTTP 결과를 보내지 않는다. 기존 3만 자 이상 생성 목표는 유지한다.
- 숙요 궁합은 delivery_pending 본문을 재사용해 최종 저장을 확인한다. claim 토큰과 입력 해시로 중복 생성/오래된 실패 쓰기를 제한한다. 아직 전체 장별 재개와 모든 결제 증빙 취소 조합의 검증은 완료하지 않았다.
- mock 검증: 숙요 실제 라우트 12개, 초융합 실제 스트림 라우트 7개, 자미두수 실제 저장 함수 8개 통과. 외부 네트워크는 차단했고 천문 좌표 입력은 mock이다. 자미두수 flow, 초융합 stage/reopen, Mongo 쿼리 모양 통과. 관련 ESLint 오류 0/미사용 경고 10.
- 사주 CI의 구형 mock 의존성은 2627b0da781e460bf02ff70c4867057fb02b7981에서 수정(39개 통과). 이어 public cache key 미러 불일치를 d995ea5b1에서 수정했다. 최신 CI 통과는 별도 확인 중이다.
- 남은 작업: 연애 비책·인생의 책·신년운세를 우선하여 다른 상세 리포트의 2만 자/근거/필수 장 완료, 체크포인트, 모바일 재개, 결제 취소·환불 재확인을 적용한다. 현재 저장 장애 수정만으로 전체 계획 완료를 의미하지 않는다.
- 실 LLM 품질·실결제·운영 DB·실기기 결제 복귀·배포는 미검증이다.
## 2026-09-14 사주 우선 확대 (진행 중)

- 사용자는 사주 전체부터 다른 유료 LLM 상세 리포트까지 개선을 승인했다. 최우선은 결제 이후 실제 LLM 생성·저장·모바일 제공이다. 리포트당 본문 최소 20,000자(제목·목차·마크다운 기호·공백 제외), 기존 12챕터 유지, 기존 구매 결과 재열람 유지.
- 사주 v7: 요청당 미완료 묶음 하나, 묶음별 본문 4,000자 및 챕터별 균등 하한, 총 20,000자. 호출 전 시도 수 예약, 묶음당 최대 4회, 정상 묶음 보존. 명식 십성표 모순·누락·반복·잘림을 완료로 넘기지 않는다.
- 상태는 generating → partial 또는 delivery_pending → 저장 및 재조회 확인 후 completed. 저장 예외/null/재조회 실패는 RESULT_STORAGE_UNAVAILABLE 503이며 생성 실패 환불 경로를 타지 않는다. claim 토큰과 상태 조건으로 완료본 덮어쓰기를 막는다. 중단 회수는 환불 스트라이크에서 제외한다.
- 클라이언트는 부분 결과를 완료로 취급하지 않고 원래 요청 본문·결제 증빙을 보존한다. 서버 jobId 재개, 계정별 로컬 키, 계정 변경 응답 폐기, 서버 기록 탐색, 목차·긴 본문 줄바꿈을 추가했다.
- 검증: 사주 라우트 저장/재개 mock 22개, 기존 사주 근거·stale 포함 61개 통과. 화면·캐시·기본 사주 회귀 16개 통과. section-plan 160개, generation-resilience 884개, auth listener 분류, worker-no-undef, Mongo 쿼리 모양, sitemap drift, typecheck 통과. ESLint 오류 0, 기존 미사용 선언 등 경고 23개.
- check:fast는 paid-gate 88개 중 구형 10챕터/버전 fixture, 새 auth listener 등록, 출력 여유 검사에서 중단했다. 해당 항목은 갱신 후 targeted 재검사 통과. 전체 로컬 suite는 반복하지 않는다. 공식 완료는 해당 push의 main CI로 확인한다.
- 실제 렌더 함수를 이용한 네트워크 차단 Chromium fixture: 360/390/430/1280px에서 12챕터·목차 12링크·가로 넘침 없음. 390px 캡처 확인. 전체 로그인 페이지/실기기 PG 복귀 검증은 아니다. Impeccable 기존 대형 엔진 파일 검사는 기존 advisory를 포함하며 관련 없는 디자인은 수정하지 않았다.
- 실 LLM·실결제·운영 DB·수동 배포 호출 없음. 다른 상세 리포트의 2만 자 계약과 저장 경로는 아직 완료로 보고하지 않는다.

## 찻집 단계 기준 (이후 승인 범위는 위 진행 기록 참조)

- 작업 디렉터리: `D:\Development\code-destiny`, `main`.
- 마지막 구현 커밋: `dd01ead6fc290d2a320bd8a82681aeb9ea87f4a2` (`fix(tea-house): recover saved paid deliveries`). 기준 커밋은 `ffc080579c5c22f8d36d2b308b0f15b27c08285c`였다.
- 사용자의 중지 요청 뒤 “다시 재개해줘” 지시로 미커밋 초안을 이어받아 구현했다. 중지 체크포인트는 이 문서로 대체한다.
- 이번 승인 범위는 **찻집 저장·복구까지**. 심화 자미두수 PDF·초융합·숙요 궁합 등은 미수정이며 해결 완료로 보고하지 않는다.
- 조사 상세: [paid-llm-delivery-findings-20260914.md](paid-llm-delivery-findings-20260914.md).

## 찻집 구현과 계약

- 생성 결과를 기존 결과 컬렉션의 `delivery_pending`으로 저장/재조회한 뒤 원래 증빙으로 deferred apply, 최종 `completed` 저장/재조회, 리워드 순으로 진행한다.
- 저장 throw/null/완료 확인 실패는 HTTP 503 `ok:false`, `retryable:true`, `reason:RESULT_STORAGE_UNAVAILABLE`, `resultId`. apply 응답 유실은 `RESULT_DELIVERY_PENDING`으로 안내하고 cancel/새 요청으로 바꾸지 않는다.
- pending 재개는 생성 본문을 그대로 사용한다. 사용자·결과 ID·requestId·입력 해시·결제 식별자를 묶고 UUID 잠금 토큰의 조건부 쓰기로 늦은 응답이 다른 소유자의 결과를 덮지 못하게 한다. 원문/증빙 변경은 409다.
- 명시적 증빙은 현재 이용권보다 먼저 확인한다. 취소/환불된 실행 레코드와 원결제 상태를 거부하며, 생성 후 apply 직전에도 권한을 재확인한다. 조회 실패는 다른 증빙이나 이용권으로 우회하지 않는다.
- 리워드 실패는 저장된 상담 성공과 분리한다. 저장 성공 응답 유실 뒤 재조회되는 기존 완료 결과도 새로 생성/차감하지 않는다.
- 화면은 계정별로 원문·찻잔·attemptId·증빙을 보존한다. 기존 유료 재개 코어를 사용하고 이용권 직접 통과도 같은 요청으로 재시도한다. localStorage 우선/sessionStorage 폴백, 24시간 유효 기간이며 저장소가 막히면 인페이지 메모리 복구만 가능하다. 로그인/계정 전환 시 해당 계정의 기록만 복원하고 완료 시 해당 시도만 지운다.
- 상담 API 호출 이후의 저장/전달 실패는 로컬 초안 성공으로 바꾸지 않는다.

## 수정 파일과 유지 범위

- 서버: `worker/routes/fortune-tea-house.js`.
- 화면/복구: `src/features/fortune-tea-house/FortuneTeaHousePage.tsx`, `src/features/fortune-tea-house/lib/consultRecovery.ts`.
- 테스트: 찻집 honey-drops, delivery-billing, saju-timing, tarot-cardwise Jest 및 recovery.behavior Node 검사. 기존 콘텐츠 검사 두 곳의 빈 DB 스텁을 `__tests__/fixtures/fortune-tea-result-store.cjs`로 보완했다.
- 화면 소스 변경으로 찻집 URL lastmod/signature 및 사이트맵 미러를 재생성했다. URL 집합 변경 없음.
- 가격·이용권·월정석·단건 결제·환불 정책, 인증 공통 코드, billing 공통 구현, DB 스키마/인덱스, 타 기능의 생성 코드는 변경하지 않았다.

## 검증 근거와 한계

- `npm run test:jest -- --runInBand fortune-tea-house`: **6 suites, 85/85 통과**.
- `npm run test:jest -- --runInBand __tests__/worker/fortune-tea-house-honey-drops.test.js __tests__/worker/fortune-tea-house-delivery-billing.test.js __tests__/worker/deferred-billing-proof.test.js`: **73/73 통과**(앞 검사와 중복 포함).
- `node --test __tests__/ui/fortune-tea-house-recovery.behavior.test.js __tests__/ui/fortune-tea-house-paid-resume.static.test.js`: **14/14 통과**. 실제 제출 함수와 복구 effect를 추출하여 저장 실패/409/402/응답 유실, 원문 유지, 저장소 차단, 재로그인/계정 전환을 mock으로 실행했다. 실제 브라우저/실기기 로그인 E2E 증명은 아니다.
- 첫 push CI `34819300548`의 Static guards에서 기존 찻집 정적 검사 2개와 concurrency VM 3개가 실패했다. 예전 변수명/저장 블록 문자열 단언과 randomUUID 의존성 누락을 새 계약에 맞췄으며, 실제 freshness 함수를 사용하는 VM에 pending 동시 재개를 추가했다. `node --test __tests__/ui/fortune-tea-attempt-reuse.static.test.js __tests__/ui/fortune-tea-generation-concurrency.test.js __tests__/ui/fortune-tea-house-recovery.behavior.test.js` **23/23 통과**. 첫 push의 Critical checks 및 타입/린트는 통과했다. 최종 CI는 후속 push의 run을 확인한다.
- billing 검사 3개는 실제 apply/완료 함수 본문을 실행하고 DB/최하위 소비 함수를 mock으로 대체했다. 완료 응답 유실 후 동일 키 재요청이 소비 함수에 재진입하지 않음을 확인했다. 실 PG/실차감·운영 DB 증명은 아니다.
- `npm run check:fast -- --plan`: critical 선택. `npm run check:fast`는 문서/변경 lint/전체 lint를 통과하고 sitemap drift에서 중단했다. `npm run sitemap:generate` 후 `npm run verify:sitemap-drift` 통과. check:fast 전체 성공으로 보고하지 않는다.
- 별도 `npm run typecheck`, `verify:billing-pass-policy`, `verify:portone-single-payment`, `verify:paid-gate-ui`, `verify:payment-choice-parity`, `verify:checkout-pass-card`, `verify:paid-feature-billing-policy`, `verify:ai-prompt-billing-policy`, `verify:paid-resume-wiring`, `verify:worker-no-undef`, `verify:mongo-query-index-shapes`, `git diff --check` 통과.
- 추가 `verify:ai-consultation-flows`는 사주 캐시 쓰기 조건을 찾는 `verify:saju-ai-consultation-recovery`에서 실패했다. 해당 검사 및 입력 4파일(`fortune.js`, 사주 엔진/미러, `lib/llm-cache.ts`)은 기준 HEAD와 동일함을 확인했다. 이번 찻집 범위 밖 기존 실패이며 미수정이다.
- Impeccable 변경 UI detector: `[]`. 레이아웃 변경 없음, 실브라우저 시각 검사 미실행.
- 실 LLM·실결제·운영 DB·수동 배포 호출 없음. 로컬 전체 검사를 반복하지 않고 공식 판정은 push 후 main CI를 확인한다.

## 전달 확인

이 문서는 구현 커밋 후 작성했다. 최종 push의 CI는 다음 명령으로 확인하고, 같은 commit의 `CI required` 성공을 전달 기준으로 삼는다. 이전 기준 커밋의 CI 성공을 이번 변경의 근거로 쓰지 않는다.

```powershell
git status -sb
git log -2 --oneline
gh run list --branch main --workflow pr-ci.yml --limit 3
```

## 남은 작업

1. 이번 찻집 범위 외 P1: 심화 자미두수 PDF의 첫/후속 저장, 초융합 최종 저장, 숙요 궁합 null 저장 경로. 별도 요청 시 진행한다.
2. 점성술·베다·자미두수·신년운세·연애 비책 품질 미달 partial 보존은 이전 조사 그대로 미수정이다.
3. 기존 인연의 서의 의미적 모순 검사/경계 fixture 및 나머지 registry 전수 검사는 별개다. 기존 구매 재열람을 새로운 품질 계약으로 차단하지 않는다.
4. 실제 브라우저/실기기 PG 복귀·실 LLM 품질·운영 주문 복구 및 환불 경합은 이번 mock 검증과 분리한다. 실서비스 검증에는 별도 승인이 필요하다.

## 복사할 재개 지시

최상단 최신 체크포인트의 명령을 사용한다. 아래 최초 찻집 작업의 다음 행동은 역사적 기록이며 현재 목록은 최상단을 따른다.
