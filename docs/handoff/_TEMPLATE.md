---
status: active
updated: YYYY-MM-DD
next: <한 줄 — 이어받는 세션이 처음 할 일>
---

# <주제>

> 🔴 이 파일은 **템플릿**이다. 복사해서 `docs/handoff/<주제 이름>.md` 로 만들고, 이 인용 블록은 지운다.
> 목표 분량 **2,000자**. 넘으면 잘라낼 곳은 언제나 "이미 끝난 것"이다.

## 왜

사용자 요구 원문. 3줄 이내.

## 지금 상태

- 브랜치 / PR 번호 / 머지 여부 — **3줄 이내**. 다음 세션이 같은 일을 다시 하지 않게 하는 것이 목적이지, 과정을 남기는 게 아니다.
- 🔴 상세는 쓰지 않는다. `gh pr list --state all --limit 20` · `git log --oneline -20` 이 정본이다.

## 남은 작업

- [ ] 대상과 **개수**를 명시한다 (예: "12개 로케일 중 en·ja 2개 남음")
- [ ] "됐다"의 판정 기준을 적는다 — 무엇을 보고 끝났다고 하는가

## 정본 예시

`파일:줄` 하나. 🔴 코드를 복사해 붙이지 말고 경로만 적는다.

## 함정

이 작업 고유의 것만. CLAUDE.md·`docs/context/*.md`·메모리에 이미 있으면 링크 한 줄로 끝낸다.

## 검증

```
npm run ...
```

## 모르는 것

근거를 못 찾은 지점. 🔴 **추측해서 채우지 말고 사용자에게 물어라.**

---

## 🔴 여기에 쓰지 않는 것

실측 2026-08-29 — 기존 인수인계 문서 표본에서 아래가 평균 **55%**(최악 90%)를 차지했고,
정작 "현재 상태 + 다음 작업"은 **8%** 뿐이었다. 54개 중 **29개는 302개 세션을 통틀어
한 번도 다시 읽히지 않았다** — 즉 그만큼이 순수한 작성 비용이었다.

- 머지된 PR 의 상세 과정, 브랜치명·base SHA 나열 → `git log` · `gh pr list`
- 기각된 시도의 디버깅 서사, 되돌려진 계획 원문 → 남길 가치가 있으면 메모리 한 줄로
- 코드·타입·설정 전문 복사 → 파일 경로와 줄번호
- 변경 파일 목록 표 → `git diff --name-only <base>...<head>`
- 레포 공통 규칙(워크트리·`sync:public`·머지는 사용자) → CLAUDE.md 가 매 세션 자동 로드된다
