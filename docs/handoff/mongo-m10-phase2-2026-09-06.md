---
status: active
updated: 2026-09-06
next: PR #1608 머지 확인 → 사용자가 `npm run verify:entitlement-orderid-status-index`(explain 읽기 전용) 결과를 보고 인덱스 생성(`migrate:entitlement-orderid-status-index`)을 허가·실행 → 1-a·#8 결과 수령 → Phase 3 계측
---

# MongoDB Atlas M10 × Workers 최적화 — Phase 2 잔여

## 왜

M10(3노드, 노드당 신규 커넥션 15/s) 한계 안에서 결제(PortOne/Inicis·Google Play)·로그인/세션/권한 경로를 최적화한다. 게이트형 4단계: 진단 → 보고·승인 → 구현(1변경 1커밋, 결제 실행 파일은 별도 브랜치·별도 승인) → 전후 비교·회귀 체크리스트. 결제 정합성(멱등·majority)은 성능을 이유로 완화하지 않고, M20 상향으로 결론내지 않는다.

## 지금 상태

- 승인된 Phase 1 보고서: `C:\Users\user\.claude\plans\mongodb-warm-conway.md` (§3 변경표 #1~#10, §5 제외 목록).
- 일반 게이트 4건(#6 슬로우 op 로그 · #1 majority 쓰기 보장 · #5 연결 지터 · #9 문서)은 PR #1603 머지됨. #7 은 `verify:worker-config-parity` 가 스테이징 전용 키를 금지해 미수행.
- 결제 게이트 #2(existing 인자, 왕복 6→5) · #3(환불 되감기 프로젝션) · #4(인덱스 선언 + 스크립트)는 PR #1608 (`perf/mongo-m10-payment`). 🔴 **인덱스는 아직 Atlas 에 만들지 않았다** — `explain` 도 미실행(로컬에 URI 없음).
- #3 에서 뺀 것(사용자 승인): `orders.js findOrder`(payments 컬렉션, 호출부 6곳이 문서를 폭넓게 읽음) · `auth.js findExistingSocialUser`(반환 문서가 세션 생성에 통째로 쓰임). 다시 열려면 소비 필드 전수 감사부터.
- 1-a·#8 은 사용자 실행, 결과 미수령.

## 남은 작업

- [ ] **#4 실행** — 사용자가 `MONGO_URI=… npm run verify:entitlement-orderid-status-index` 로 플랜을 보고(IXSCAN 이면 생성 불필요할 수도 있다 — orderId 단일 인덱스가 실재하면 그렇다), 허가 시 `migrate:entitlement-orderid-status-index` 1회. 롤백은 스크립트 `--rollback`.
- [ ] **#10** `compressors` — 스테이징에서 zstd/snappy 연결 성공·왕복 비교를 통과한 경우에만. Workers 런타임 드라이버 지원 여부 미검증.
- [ ] **Phase 3** — `[db-slow-op]` 로그로 로그인·결제 확정 p50/p95 채취(스테이징 `wrangler tail`), 회귀 체크리스트(결제→권한 파이프라인 · 웹훅 즉시 2xx · 멱등 · 503 시 로그아웃 없음 · 앱/웹 분기).

## 정본 예시

`worker/lib/db.js` 의 `mongoWriteConcern()` — 커넥션 2곳·트랜잭션 옵션이 같은 헬퍼를 쓴다. 가드는 `__tests__/worker/db.transaction-budget.test.js`.

## 함정

- `payment-service.js:141` `DEFAULT_TRANSACTION_OPTIONS` 는 `{timeoutMS: 8000}` 그대로 — `db.transaction-budget` 의 정규식이 그 모양을 요구한다. 유일한 호출부 `billing.js:1948` 가 `mongoTransactionOptions()` 를 넘기므로 실효 경로는 majority 다.
- `[vars]` 노브를 올리면 `__tests__/worker/db.vars-code-default-parity.test.js` 의 `PAIRED_KNOBS` 에 넣고 `wrangler.staging.toml` 에도 같은 값을 둔다(스테이징 전용 키 금지).
- 로컬 `check:quick` 은 마지막 `build:worker` 가 `workers-og` 미설치로 실패한다(코드 회귀 아님, CI 로 확인).

## 검증

```
npx --no-install cross-env NODE_OPTIONS=--experimental-vm-modules jest --runInBand --testEnvironment node __tests__/worker/db.
npm run verify:worker-config-parity && npm run verify:payment-freeze
```
결제 파일을 건드리면 결제 최소 5종(`verify:billing-pass-policy` · `verify:portone-single-payment` · `verify:paid-gate-ui` · `verify:payment-choice-parity` · `verify:checkout-pass-card`)까지.

## 모르는 것

- 프로덕션 `MONGO_URI` 에 `w=` 가 있는지(사용자만 확인 가능, 값 비출력).
- `content_entitlements` 문서 수와 환불 필터의 실제 플랜(explain 미실행).
- Atlas 피크 커넥션 수·복제 지연(대시보드 미확인).
