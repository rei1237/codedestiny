---
status: active
updated: 2026-09-06
next: 사용자가 백업 뒤 `npm run migrate:reconcile-index-drift -- --apply --expect 16` 과 `npm run migrate:purge-test-reports -- --email <본인 계정> --apply --expect 61` 을 승인·실행 → `docs/db-audit-2026-08/05-execution-log.md` 에 2행 → PR-1(웜 핑 생략)·PR-2(인덱스·삭제) 머지 → 프로덕션 승격 → D(8초 hang) 스테이징 재현
---

# MongoDB M10 × Workers 최적화 — Phase 3-b (코드·설정 정리 · 쓰레기 삭제 · 인덱스)

## 지금 상태

- Before 실측·Phase 2 배경은 PR #1615 본문이 정본. 사용자 결정(2026-09-06): 트래픽이 없어 전후 비교 대신 **코드·설정만으로** 최적화한다.
- 조사 결과 `[vars]` 68키·크론 2개에 죽은 것 0 → 서버 설정 제거 대상 없음. 세션 하트비트 5분, 버전 폴러 2중(정적 셸 `share.js` / App Router `AppVersionGuard` 는 페이지가 겹치지 않음)은 유지.
- **PR-1** (`m10/phase3b-warm-ping`): `connectDb` 웜 분기가 이웃 op 이 살아 있으면 ping 을 보내지 않는다(`timings.pingSkipped`). 존재하지 않는 `fortuneviewlogs` 익명화 블록(auth withdraw) 삭제.
- **PR-2** (`m10/phase3b-index-purge`): 마이그레이션 2개 + 선언만 있고 실물이 없던 인덱스 선언 84건 삭제 + 죽은 모델 `GuardianFortuneDailyUsage` 삭제(읽는 코드 0·문서 0). 🔴 **실 DB 는 아직 손대지 않았다.**

## DB 적용 대기 — 사용자 승인 필요 (아래 값은 `--check` 실측, 2026-09-06)

| 스크립트 | 읽기 전용 확인 | 적용 | 내용 |
|---|---|---|---|
| reconcile-index-drift | `npm run verify:reconcile-index-drift` | `npm run migrate:reconcile-index-drift -- --apply --expect 16` | create 15(unique 정합성 9 · 동적 조회 1 · 리터럴 COLLSCAN 5) + drop 1(`paid_execution_records.paymentId_1`, hint explain 으로 부분 유니크가 대신 타는 것 증명) |
| purge-test-reports | `npm run verify:purge-test-reports -- --email <계정>` | `npm run migrate:purge-test-reports -- --email <계정> --apply --expect 61` | 본인 계정 상담·리포트 결과 48건(18 컬렉션) + 코드 참조 0 고아 컬렉션 13개(문서 41건) |

- 🔴 `--apply` 는 훅 `guard-costly-commands` 가 잡는다(1회 허락). 실행 전 `npm run backup:mongo -- --out <레포 밖 경로>`. 스크립트가 남기는 before-image(`backups/`)에는 개인정보가 있다.
- 테스트 계정(`CD_PREVIEW_TEST_EMAIL`)에는 결과가 **0건**이었다. 사용자가 "내가 테스트하며 남긴 결과"라 한 것은 본인 계정이므로 `--email` 로 지정한다. 다른 계정의 결과(약 30건)는 대상이 아니다.
- 실행 뒤: `docs/db-audit-2026-08/05-execution-log.md` 에 2행 추가, `npm run verify:mongo-launch-indexes` 로 미생성 0 확인, 두 `verify:*` 가 `RESULT OK`.

## 남은 것

- [ ] **D — 8초 hang 스테이징 재현**: 계획 `C:\Users\user\.claude\plans\zazzy-wibbling-feigenbaum.md` §D. PR-1 이 스테이징에 오른 뒤 `GET /api/insights`·`/api/reviews/summary`·`/api/reviews` 동시 3건 × 5회(5분 간격), `npx wrangler tail code-destiny-web-staging --config worker/wrangler.staging.toml --format json` 캡처(IP 있음 — 커밋 금지) → `npm run report:worker-tail-latency -- <파일> --all`. 수정은 `connectDb`·`withMongoRetry` 안에서만, 새 타임아웃·재시도 계층 금지.
- [ ] `security_events` 3,302건 — 읽는 코드 0(2026-08-24 확인), TTL 없음. 보존 기간은 사용자 결정 → 별도 마이그레이션.
- [ ] After 실측 · #10 compressors · 1-a/#8 은 PR #1615 인수인계 그대로 보류.

## 함정

- `db.js` 는 `autoIndex:false` — 스키마 선언은 아무것도 만들지 않는다. 정적 가드 `verify:mongo-query-index-shapes` 는 **선언 + 마이그레이션 리터럴**을 원장으로 쓰므로 선언이 거짓이면 가드가 거짓 통과한다. 새 인덱스는 선언과 마이그레이션 둘 다 넣는다.
- `verify:reconcile-index-drift` 의 `unused` 는 "리터럴 쿼리가 없다"이지 "쿼리가 없다"가 아니다 — 변수로 조립하는 필터는 `DYNAMIC_READERS` 에 파일:줄 근거로 올린다(예: `fortuneChatSessions.anonymousSessionId`, 비회원 bootstrap).
- 워크트리엔 `.env.local` 이 없다. `MONGO_URI`·`CD_PREVIEW_TEST_EMAIL` 은 루트 `.env.local` 에서 PowerShell 로 환경변수에 주입해 돌린다(값 비출력, `.env*` 수정 금지).
- 로컬 `check:quick` 의 `build:worker` 는 `workers-og` 미설치로 실패한다(회귀 아님, PR CI 로 확인).

## 검증

```
npx --no-install cross-env NODE_OPTIONS=--experimental-vm-modules jest --runInBand --testEnvironment node __tests__/worker/db.
node scripts/migrations/20260906-reconcile-index-drift.mjs --self-test
node scripts/migrations/20260906-purge-test-reports-and-orphan-collections.mjs --self-test
npm run verify:mongo-query-index-shapes && npm run verify:guard-wiring && npm run verify:payment-freeze
```
