---
status: active
updated: 2026-09-06
next: 사용자가 프로덕션 승격 뒤 한국 시간 저녁 피크에 tail 을 30분 이상 캡처해 `npm run report:worker-tail-latency -- <파일>` 로 After 표를 뽑고 아래 Before 표와 나란히 적는다 → #4 인덱스(explain → 허가 → migrate) → 1-a·#8 결과 수령
---

# MongoDB Atlas M10 × Workers 최적화 — Phase 2 잔여 · Phase 3 계측

## 왜

M10(3노드, 노드당 신규 커넥션 15/s) 한계 안에서 결제(PortOne/Inicis·Google Play)·로그인/세션/권한 경로를 최적화한다. 게이트형 4단계: 진단 → 보고·승인 → 구현 → 전후 비교·회귀 체크리스트. 결제 정합성(멱등·majority)은 성능을 이유로 완화하지 않고, M20 상향으로 결론내지 않는다.

## 지금 상태

- 승인된 Phase 1 보고서: `C:\Users\user\.claude\plans\mongodb-warm-conway.md` (§3 변경표 #1~#10).
- 일반 게이트(#1·#5·#6·#9)는 PR #1603, 결제 게이트(#2·#3·#4)는 PR #1608 — 둘 다 `main` 머지·스테이징 반영. 🔴 **프로덕션은 아직 #1600(0d518ef7d) 이라 두 PR 이 안 올라가 있다** — 그래서 지금 프로덕션 tail 이 곧 Before 다. #7 은 스테이징 전용 키 금지로 미수행. 🔴 #4 인덱스는 Atlas 에 아직 없다(explain 도 미실행).
- Phase 3 도구는 이 PR: `scripts/report-worker-tail-latency.mjs`(`npm run report:worker-tail-latency`). `wrangler tail --format json` 이벤트의 `wallTime`(워커가 잰 요청 처리 시간)을 라우트별 p50/p95 로 집계하고 `[auth-timing]`·`[db-slow-op]` 을 라우트에 붙인다. 임계값 로그만으로는 p50 이 안 나오는 문제를 이것으로 우회했다.
- #3 에서 뺀 것(사용자 승인): `orders.js findOrder` · `auth.js findExistingSocialUser`. 다시 열려면 소비 필드 전수 감사부터.

## Phase 3 — Before 실측 (2026-09-06, 프로덕션 0d518ef7d, 일요일 05:26~06:32 KST)

캡처: `npx wrangler tail code-destiny-web --config worker/wrangler.toml --format json > tail.jsonl`(읽기 전용). 집계: `npm run report:worker-tail-latency -- tail.jsonl`.

이벤트 49건(요청 42 + 크론 7), 66분. 결제 확정(`/api/billing/confirm`)·로그인(`POST /api/auth/login`)은 **0건** — 표본 없음.

| route | n | wallTime p50 | p95 | max | `[db-op-timeout]` |
|---|---|---|---|---|---|
| GET /api/auth/me | 5 | 12,354 | 20,384 | 20,384 | 4 |
| GET /api/subscription/status | 3 | 11,500 | 16,006 | 16,006 | 3 |
| GET /api/me/access-state | 2 | 4,489 | 4,489 | 4,489 | 0 |
| POST /api/auth/refresh | 2 | 10 | 10 | 10 | 0 |
| GET /api/profile (참고) | 7 | 4,881 | 13,962 | 13,962 | 3 |

`[auth-timing]` `/api/auth/me`: authenticated 3건 total 9,689/16,136(resolveAuth 8,000 고정 = op 타임아웃, loadUser 1,689/8,136) · token_fallback 2건 20,300.

**관찰(원인 미확정, 재현 3회: 05:59·06:01·06:25 KST)** — 저트래픽이라 아이솔레이트가 매번 콜드다. 사용자 1명이 페이지를 열면 `/api/auth/me`·`/api/profile`·`/api/subscription/status` 가 동시에 뜨고, 콜드 `[db-connect]` 1.3~1.8초 뒤 첫 op 가 8,000ms 에서 타임아웃(`[db-op-timeout]` 13건, 페이로드의 `maxCommandMs` 는 173 — 명령이 느린 게 아니라 `maxCheckoutWaitMs` 2,812·`checkOutFailed`·`commandFailed` 로 풀 체크아웃/스테일 소켓에서 샌다) → `[worker-auth-error]` `verify-access-token-user` "MongoDB operation timed out in Worker" 10건 → 재시도로 200. 같은 창에 `MongoWaitQueueTimeoutError` 미처리 1건(`/api/insights`), 드라이버 소켓 close 가 이전 요청 컨텍스트에서 풀리는 경고 35건, "11 timeout listeners" EventEmitter 누수 경고 134건. 🔴 이 수치는 **Phase 2 미배포** 상태다 — 승격 뒤 같은 시간대·같은 패턴(콜드 + 동시 3요청)으로 After 를 잰다. 개선이 없으면 후보는 콜드 첫 op 의 체크아웃 대기(풀 5 · 동시 admission 8)이며, 새 타임아웃/재시도를 **덧씌우지 말고**(원칙 6) `withMongoRetry`·`connectDb` 안에서 본다.

🔴 이 시간대는 트래픽이 거의 없다. **After 는 같은 도구로 저녁 피크(20~23시 KST)에 30분 이상** 캡처해야 로그인·결제 확정 표본이 찬다. 두 표는 같은 라우트·같은 열로 나란히 둔다.

## 남은 작업

- [ ] **After 캡처** — 프로덕션 승격(사용자) 뒤 위 절차. 판정 기준: `POST /api/auth/login`·`GET /api/auth/me`·`POST /api/billing/confirm` 의 wallTime p95 가 Before 이하이고 `[db-op-timeout]` 0건.
- [ ] **#4 실행** — 사용자가 `MONGO_URI=… npm run verify:entitlement-orderid-status-index` 로 플랜을 보고, 허가 시 `migrate:entitlement-orderid-status-index` 1회. 롤백은 `--rollback`. 🔴 로컬 `.env.local` 의 `MONGO_URI` 는 2026-09-06 현재 **인증 실패**(Authentication failed) — 유효한 URI 를 사용자가 넣어야 한다.
- [ ] **#10** `compressors` — 스테이징에서 zstd/snappy 연결 성공·왕복 비교를 통과한 경우에만. Workers 런타임 드라이버 지원 미검증.
- [ ] 1-a·#8 — 사용자 실행, 결과 미수령.

## 회귀 체크리스트 (After 캡처와 함께 한 번에)

| 축 | 무엇을 보나 | 근거 명령/파일 |
|---|---|---|
| 결제→권한 파이프라인 | 미결제 confirm 이 지급 없이 거절, 결제 후 스냅샷 무효화 | `node scripts/verify-payments-v2-live-smoke.mjs --live`(PENDING 주문 2건 생김·1회 허락) · `__tests__/ui/access-store.static.test.js` · `paid-flow-gates.yml` "direct confirm pending" |
| 웹훅 즉시 2xx | 무거운 일은 `waitUntil` 로 넘기고 즉시 ack | `__tests__/worker/payments.webhook-ledger.test.js` "immediate ack" |
| 멱등 | 같은 키 재전송이 같은 주문·1회 지급 | `verify:billing-pass-policy`(idempotencyKey 파생 규칙) · `__tests__/worker/app-store.google-billing.test.js` "idempotent" |
| 503 시 로그아웃 없음 | DB 장애는 401 이 아니라 503(세션 유지) | `__tests__/worker/numerology-tarot.route.test.js` · `auth.login-verify-once.test.js`(isAuthInfraFailure) · `verify:static-paid-gate-failsafe` |
| 앱/웹 분기 | Play 결제·앱 헤더 경로가 PortOne 경로와 섞이지 않음 | `__tests__/worker/app-store.google-billing.test.js` · `verify:capacitor-plugin-registration` · `verify:app-store-pricing` |

## 함정

- `payment-service.js:141` `DEFAULT_TRANSACTION_OPTIONS` 는 `{timeoutMS: 8000}` 그대로 — `db.transaction-budget` 의 정규식이 그 모양을 요구한다. 실효 경로(`billing.js` 유일 호출부)는 `mongoTransactionOptions()` 로 majority 다.
- `[vars]` 노브를 올리면 `__tests__/worker/db.vars-code-default-parity.test.js` 의 `PAIRED_KNOBS` 에 넣고 `wrangler.staging.toml` 에도 같은 값(스테이징 전용 키 금지).
- 스테이징 DB(`code_destiny_staging`)에는 테스트 계정이 없어 `POST /api/auth/login` 이 401 이다. `seed:preview-test-account` 는 `MONGO_DB_NAME=code_destiny_staging` 로 스테이징을 겨냥할 수 있지만(해석기로 확인) 위 URI 문제로 미실행.
- tail 캡처 파일에는 IP·헤더가 있다 — 커밋 금지, 보고에는 집계만.
- 로컬 `check:quick` 은 마지막 `build:worker` 가 `workers-og` 미설치로 실패한다(코드 회귀 아님).

## 검증

```
npx --no-install cross-env NODE_OPTIONS=--experimental-vm-modules jest --runInBand --testEnvironment node __tests__/worker/db.
npm run verify:worker-config-parity && npm run verify:payment-freeze
```

## 모르는 것

- 프로덕션 `MONGO_URI` 에 `w=` 가 있는지(사용자만 확인 가능, 값 비출력).
- `content_entitlements` 문서 수와 환불 필터의 실제 플랜(explain 미실행).
- Atlas 피크 커넥션 수·복제 지연(대시보드 미확인).
- 부수 관찰(범위 밖): Before 캡처에서 `GET /api/reviews`·`/api/reviews/summary` 가 `canceled` 로 1.5~2.4초, `GET /api/insights` 가 4.5~5.7초였다. 원인 미조사.
