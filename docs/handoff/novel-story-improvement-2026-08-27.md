# 라이트 노벨 개선 인수인계 (2026-08-27 · 세션 4까지 완료)

> **이 문서만 읽고 시작할 수 있게 쓴다.** 정본은 44화 8,836비트 · 한글 41.9만 자라 한 세션에 다 못 한다.

## 🚩 여기서 시작 — 다음 세션이 읽을 순서

**끝난 것** (전부 `main` 머지 완료)

| PR | 내용 | 머지 커밋 |
|---|---|---|
| #1166 | 파이프라인 복구 — 인라인 대본 → 청크 로더 | `eb6fe7003` |
| #1169 | **세션 1** — 후반 아크 BGM/BG 큐 매핑(§5-4·§5-5) | `58267ff8b` |
| #1172 | **세션 2** — 개명 「박지은」→「서한비」 · skill 정합 · `im` 재조준(§3·§5-7·§5-8D) | `890769b60` |
| #1175 | **세션 3** — EP.23 재구성 + EP.19~22 복선 분산 + 제목 변경(§4) | `c59c68f24` |
| #1177 | **세션 4** — EP.40 재구성 + 정지된 세계 컷어웨이 3개 + 말미 서한비 스팅어(§5-8A) | `83993be3a` |
| #1179 | **세션 5** — EP.39 설득 감축(침묵으로 실패시키기) + EP.35 적 등장 89%→20%(§5-8B·C) | 🟡 **머지 대기** |

**남은 것** — §9 의 세션 6 하나. **다음은 세션 6(`金克木` 회수 + 이누야샤 보강 + 문서, §5-6·§6·§7)** 이다. 🔴 **PR #1179 가 머지된 뒤에 시작할 것** — 정본을 고치면 산출물 44청크가 전부 바뀌어 병렬 분기가 44개 파일 충돌이 된다.

**읽는 순서**
1. **§0** 사용자 요구 원문 — 무엇을 하기로 했는지
2. **§2** 구조·제약 + **§2-A 현재 상태 실측**(아래) — 지금 정본이 어떤 상태인지
3. 할 세션의 설계 절 하나 — 세션 6 → **§5-6** + **§6** + **§7**
4. **§8** 검증 · **§10** 다시 하지 말 것
5. §1(완료 기록)은 **되돌리면 안 되는 판단**만 모여 있다. 그 화를 건드릴 때만 읽으면 된다.

🔴 **시작 전에 `git fetch origin main` 하고 `origin/main` 에서 분기할 것.** 정본을 고치면 산출물 44청크가 전부 바뀌므로(§2-A 참조) **같은 생성 파일을 건드리는 PR 을 병렬로 열지 말 것.**

## 0. 사용자 요구 원문

> 라이트 노벨의 스토리를 좀 더 재밌게 개선해주고 최종 보스의 이름이 박지은인데 문제가 생길 가능성도 있으니 다른 이름으로 변경해줘 그리고 인성의 도서관에서 갑자기 사서의 아들 이야기가 나오는건 몰입을 깨니까 그 부분도 수정해주고 전반적으로 이누야샤 이야기 플롯으로 가도록해주고 그에 맞게 대사, 효과, bgm도 매핑해줘

### 사용자가 확정한 결정 (물어서 받은 답)

| 항목 | 결정 |
|---|---|
| 보스 개명 | 「박지은」 → **「서한비」**(徐寒霏, '찬 비'). 수식(水息)·검은 비 이미지와 직결 |
| 네오 진명 「박병하」 | **유지** (선역이라 실명 리스크가 낮음) |
| 작업 범위 | **지목 문제 + 후반 연출** (도서관 아크 + EP.31~41 연출) |
| 파이프라인 | **청크 로더 전환으로 복구** → PR #1166 에서 완료 |
| PR #350 회귀 | **전체 복원** → PR #1166 에서 완료 |

## 1. 끝난 것 — PR #1166 (머지 완료) · 세션 1 (브랜치 `feature/novel-scene-direction`)

### 세션 5 — EP.39 설득 감축 + EP.35 적 등장 앞당기기 완료 (2026-08-27, 브랜치 `feature/novel-ep39-ep35-pacing`, PR #1179)

§5-8 B·C 를 실행했다. 🔴 **세션 3·4 와 달리 비트를 하나도 지우거나 삽입하지 않았다 — 전부 같은 자리 교체다.** 그래서 비트 수 200 은 산술이 아니라 구조로 보장되고, 세션 1·2 가 심은 `bg`/`bgm`/`im` 큐가 **하나도 안 죽었다**(§2-A 의 큐 인덱스가 전후 완전 동일). 재구성 세션이 이 방식을 쓸 수 있으면 쓰는 게 낫다.

실측 전/후 — `content/novel/episodes.source.json`:

| 항목 | EP.39 전 | EP.39 후 | EP.35 전 | EP.35 후 |
|---|---|---|---|---|
| 비트 | 200 | 200 | 200 | 200 |
| 한글 | 14,145 | 13,479 | 13,765 | 13,846 |
| 연이 발화 | 79 | **66** | 78 | 77 |
| 지문(`n`) | 46 | **59** | 31 | 32 |
| 서한비 첫 등장 | #1 | #1 | **#179 (89%)** | **#39 (20%)** |
| 바뀐 비트 | — | 25 | — | 19 |
| bgm·bg·im 큐 | 6·3·14 | 6·3·14 | 7·2·12 | 7·2·12 |
| hold% | 98% | 98% | 98% | 98% |

**EP.39 — 설득을 침묵으로 바꿨다.** 핵심은 `#164~183`(무릎 꿇은 뒤 20비트)로, **연이 발화 8회 → 2회**(`#165` 혼잣말 · `#181` 「…미워, 가 아니었죠. 그립다, 였죠.」 한 줄). 나머지 자리는 지문의 신체감각(언 손가락이 안 펴짐 · 무릎에 힘을 옮김)과 **서한비가 침묵을 상대로 혼자 무너지는 대사**로 채웠다. 🔴 **이게 이 화의 진짜 설계다** — 서한비는 반박에는 받아칠 수 있지만 침묵에는 못 받아친다(`#173` 「네가 입을 다무니까 내가 자꾸 혼자 떠들어… 네 그 침묵이, 네 말보다 훨씬 아프네」). `#177` 이 실패를 명시적으로 착지시킨다(「말로는 이 사람을 붙잡지 못한다는 것을, 오늘 밤 내내 배웠기 때문이다」). 앞쪽 `#35~51`·`#140~163` 의 설득 핑퐁도 같은 방향으로 덜어 냈다.

**EP.35 — 적을 20% 지점으로 끌어왔다.** 티저 2곳(`#39` 서한비의 첫 목소리 · `#111` 무성이 고친 부녀 실에 대한 반응)과, `#125~135` 를 **실시간 방해**로 재작성했다(연이가 예쁘다고 말한 첫사랑 실이 뒤에서부터 검게 물들고, 무성이 되돌리지만 한 뼘뿐이다). 조달처는 전부 **축자 중복**이었다 — `#39`=`#27~28`, `#111`=`#31`, `#125~126`=`#33~34`, `#131~135`=`#78~90` 의 무성 잇기 수업 재탕.

#### 🔴 세션 5 가 의도적으로 벗어난 것 — 다음 세션이 되돌리지 말 것

- **§5-8B 의 「`#93` 검은 탑 발견을 `#55` 로 이동」은 하지 않았다.** ①`#93` 은 `bgm=crisis`+`fx=stars`+`im` 을 달고 있고 `#93~95` 가 한 덩어리라 3비트를 큐째 옮겨야 한다. ②`#53~63` 은 검은 실 발견 → 서한비의 붉은 실(열쇠) 발견으로 이어지는 완결된 블록이라, 그 한가운데를 탑으로 쪼개면 열쇠 발견이 흐려진다. ③애초에 이 지시의 목적인 「적이 92비트 늦게 온다」는 티저 2곳으로 이미 해소됐다(89%→20%). 위험 대비 이득이 없다고 판단했다.
- **§5-8C 의 「설득 6회→2회」는 `#164~183` 블록에 대해서만 문자 그대로 적용했다.** 화 전체의 연이→서한비 설득 대사는 실측 40회였고, 그걸 2회로 줄이면 EP.36~38 이 쌓아 온 5단 계단(진단→원인→증명)의 회수 지점이 사라진다. 화 전체로는 26회 수준으로 줄이고, **무력해야 할 후반부만 2회로** 눌렀다.

#### 함께 고친 기존 결함 2건 (내 변경 범위 안에서 발견)

- **EP.35 `#40` 이 아직 발견하지도 않은 소품을 불렀다.** 「저 여자 마음속 그 발버둥 치는 붉은 실 하나」라고 말하는데, 그 실은 **20비트 뒤 `#60` 에서 처음 발견**되고 거기서 연이가 「저렇게 저항하는 실은 처음 봐」라고 한다. `#40` 을 티저 반응으로 교체하면서 같이 없앴다.
- **EP.35 `#112`(구 `#111` 자리) 가 `#98` 과 어긋났다.** 무성은 `#98` 에서 「봉인 속에서 나는 저 여자의 밤을 다 들었어」라고 하는데, 새로 쓴 대사에 「백 년 만에 듣는데」를 넣었다가 **초고 검토에서 잡아 「봉인 밖에서 듣는 건 처음인데」로 고쳤다.** 새 대사를 쓸 때 그 인물이 이미 말한 것과 대조할 것.

### 세션 4 — EP.40 재구성 완료 (2026-08-27, 브랜치 `feature/novel-ep40-rebuild`, PR #1177)

§5-8A 를 실행했다. **삭제 23비트 · 삽입 23비트 · 비트 수 200 유지**(패치 스크립트가 삭제 수 ≠ 삽입 수면 중단하도록 자기 검산했다).

실측 전/후 — `content/novel/episodes.source.json` + `scene-matrix.generated.json`:

| 항목 | 전 | 후 |
|---|---|---|
| 비트 | 200 | 200 |
| 한글 | 16,636 | 16,349 |
| bgm 큐 | 8 | 9 |
| bg 큐 | 8 | **14** |
| im | 14 | 15 |
| hold% | 90% | **85%** |
| 화자 | n·yeon·god·ln·sys (5) | + **pje**(서한비) = 6 |

**설계에서 벗어난 판단 7건 — 되돌리지 말 것**

1. 🔴 **§5-8A 의 산술이 안 맞는다.** "~25비트 확보 → §5-5 배경 6개 + 컷어웨이 3비트 + 말미 2비트"인데, **배경 6개는 PR #1169 에서 비트 증가 없이 이미 들어갔다.** 남은 삽입은 3+2=5비트뿐이라 그대로 하면 195비트가 된다. 그래서 컷어웨이를 1비트가 아니라 **6비트짜리 장면 3개 + 말미 스팅어 5비트 = 23비트**로 키웠다. 세션 5·6 도 §5 의 "확보 → 투입" 산술을 **더하기로 검산한 뒤** 착수할 것.
2. 🔴 **이 화의 진짜 최대 결함은 "논지 5회 반복"이 아니라 계약이 두 번 접수된다는 것이었다.** `#129` 에서 신이 "접수했단다… 두 번 다시 못 돌아가"라고 못 박은 뒤, `#131` 이 다시 묻고 `#143` 이 또 묻고 `#175` 가 **다시 접수한다.** `#131`·`#143`·`#175` 를 빼서 접수를 `#129` 한 곳으로 모았다.
3. **배웅도 세 번이었다**(`#175`·`#179`·`#186`). `#175`·`#186` 제거로 두 번으로 줄였다.
4. **제거 후보 `#144~149` 를 통째로 빼지 않았다.** `#145`·`#147~149`(불운 논의 — "그 불운들이 사실 서한비가 심은 것이었고, 그 나쁜 운이 병하를 데려왔다")는 이 화에서 **유일하게 새로운 논지**라 살렸다. 대신 진짜 중복인 `#69`(#68 재탕) · `#139`(#9·#21·#123 재탕) · `#168`(#125 와 거의 축자 중복) · `#172`(#174 와 같은 말) · `#185`·`#195`(#193 과 축자 중복)를 추가로 뺐다.
5. **유지 비트 3건의 문면을 손봤다** — 앞 비트를 지워 지시어가 붕 떴기 때문이다. 새 인덱스 기준 `#142`("정하기 전에" → "정하고 나서", `#131` 재질문이 사라져 이미 정한 뒤가 됐다) · `#156`("그 자신감이" → "제 불운까지 제 이야기라 안는 눈이구나", `#159` 가 사라졌다) · `#172`("그렇단다." → "그리고 아이야.", `#172` 가 사라져 긍정할 대상이 없어졌다).
6. **컷어웨이 말미를 연이 대사로 닫으면 안 된다.** 첫 초안은 컷어웨이 1을 연이 대사로 닫았는데, 바로 뒤 신의 대사(`#41` "그리 단순하진 않단다")가 **연이의 두 비트 전 질문에 답하는 구조**라 대화가 붕 떴다. 세 컷어웨이 모두 **지문으로 닫고** 원래의 문답 흐름을 그대로 이어 붙였다.
7. **말미 서한비 스팅어의 첫 대사는 EP.39 `#183` 에서 끊긴 대화를 그대로 잇는다.** 서한비에겐 한순간도 흐르지 않았으므로 "어디까지 했더라" 같은 재개 대사는 틀린다. 초안이 그랬고 고쳤다.

**새로 심은 것 4블록** (새 인덱스)

- `#35~40` **컷어웨이 1 · 네오** (`bg:neoBound3` → `fateTarot` 복귀) — 연이가 "병하도 제가 붙잡아 둘 수 있는 거잖아요"라고 말한 직후에 꽂아, 신의 "그리 단순하진 않단다"에 무게를 준다. 네오의 손이 실을 끊는 쪽이 아니라 **연이가 무릎 꿇었던 자리 쪽으로** 뻗은 채 멈춰 있다. 🔴 이 「반 걸음」 이미지는 **§5-6 의 `金克木` 회수 라인(세션 6)의 재료로 일부러 남긴 것**이다. 지우지 말 것.
- `#100~105` **컷어웨이 2 · 루나** (`bg:river` → `fateTarot` 복귀) — 연이의 "그럼 네오는요. 친구들은요." 바로 뒤. 루나가 물길 끝에서 **세계의 끝까지 한 뼘 남기고** 멈춰 있다. 🔴 이것이 `#184`("물의 아이가 거의 다 올라왔더구나")를 **데우스 엑스 마키나에서 복선 회수로 격상**시키는 장치다. 세션 5·6 이 EP.39·EP.35 를 만질 때 루나의 물길 진행을 바꾸면 여기와 어긋난다.
- `#162~167` **컷어웨이 3 · 서한비** (`bg:sukuyo`, 복귀는 `#168` 의 `fateTarot` 가 겸한다) — 정지한 세계 안에서 **가슴의 붉은 점만 멈추지 않는다.** 신이 "저 한 점만은 나도 못 만진단다. 남의 심지는 남이 켜야 하는 법"이라고 못 박아, 연이가 직접 가야 하는 이유를 논리로 세운다. 🔴 `#168` 이 복귀를 겸하므로 **컷어웨이 3 안에 `bg:fateTarot` 를 넣으면 `#168` 이 무효 큐가 된다.**
- `#194~198` **말미 서한비 스팅어** (`bg:handsRush`, `bgm:zeroPoint`, `im`) — 시간이 다시 흐르자 서한비가 EP.39 의 끊긴 협박을 그대로 잇는다. 다만 **검은 실 한 올이 이유 없이 끊겨 있고**, 그녀는 아홉 번째 글자가 늘었다는 걸 모른 채 다시 손을 치켜든다. 🔴 `bgm` 을 `crisis` 로 두지 말 것 — EP.41 `#0` 이 `crisis` 라 무효 큐가 된다. `zeroPoint` 는 `#49`(원래 쓰여 있던 결말)와 수미상관이기도 하다.

### 세션 3 — EP.23 재구성 완료 (2026-08-27, 브랜치 `feature/novel-library-arc`, PR #1175 → `c59c68f24`)

§4 설계를 그대로 실행했다. **비트 수 200 유지**, 블록 크기도 설계표와 정확히 일치한다(18/60/20/14/16/24/8/20/20).

**설계에서 벗어난 판단 5건 — 되돌리지 말 것**

1. **§4 의 EP.19~22 표 인덱스는 1-based 였다.** §2-A 의 큐 인덱스(0-based)와 섞여 있다. 본문을 열어 대조해 0-based 로 −1 해서 적용했다(16곳 전부 내용이 맞아떨어졌다). 세션 4~6 에서 §5 의 `#숫자` 를 쓸 때도 **먼저 본문을 열어 확인할 것.**
2. **대출증과 인질의 순서를 합쳤다.** 원문은 #9~12(대출증이 사서인을 가져감)와 #14~18(소망을 인질로 잡아 사서인을 요구)이 서로 어긋나 있었다. 몽타주 2-4 에서 **백문이 함정을 읽고 거절 → 청토끼가 뜯긴 종이 물고기 꼬리를 내밈 → 알고도 찍음**으로 한 줄기로 폈다. 두 원문의 사실은 모두 남아 있다.
3. **네오는 회상에 안 들어간다.** 「저 종이엔 제 이름이 없어요」로 밖에 남긴다. 그래야 블록 3 의 「연이가 떨며 나오고 네오가 붙든다」가 성립한다.
4. **`im` 을 14 로 맞추느라 뺀 3개**: 청토끼 회중시계 첫 등장 · 상승 바람 · 「나는 최선을 다했다」. 전부 큐가 아니라 강조 표시라 서사 손실은 없다.
5. **hold 85% 는 배경 큐 4개를 후반부에 더 걸어서 맞췄다**(몽타주만으로는 88%). 건 자리는 `#128`(「앉게」 — 열람대로 이동) · `#138`(밤, 네오와 둘) · `#166`(오염 서가) · `#169`(식탁). 전부 장면이 실제로 옮겨 앉는 지점이다.

**작업 중 발견해 함께 고친 결함 3건**

- EP.23 에 **무효 큐 2건**이 있었다 — `#130 bgm=stillLake` 뒤 `#136` 이 또 `stillLake`, `#136 bg=library` 뒤 `#164` 가 또 `library`. §2-A 가 말한 「EP.00~30 의 33건」 중 2건이다. 재구성으로 사라졌다. **남은 31건은 여전히 미조치.**
- **하루가 두 번 지났다** — `#136` 「이튿날 아침」(작별 아침) 뒤 `#164` 가 또 「이튿날 새벽」. 백문의 예고를 「내일 아침」으로, 배웅을 「아침을 다 먹고, 볕이 가장 좋을 때」로 맞췄다.
- 새로 심은 「백문이 백 년째 안 펼치는 얇은 책」(EP.19 #92·EP.23 몽타주 2-7)이 EP.23 말미에 **연이가 되살리는 얇은 책과 겹쳐 읽혔다.** `#166` 에서 「그 책은 그대로 두고 그 옆의 다른 한 권에」로 명시적으로 갈랐다. 🔴 **백문의 그 한 권은 아직 회수처가 없는 열린 이미지다** — 세션 6 이나 그 뒤에 쓸 자리가 있으면 쓰고, 없으면 그대로 두는 게 낫다(백 년째 못 펼치는 것 자체가 뜻이다).

**측정값 (2026-08-27, 커밋 `1046d7681`)**

| | 전 | 후 |
|---|---|---|
| bg 큐 / 배경 종수 | 3 / 3 | 15 / 4 |
| bgm 큐 | 4 | 9 |
| fx | 8 | 27 |
| `im` | 23 | 14 |
| hold% | 98% | **85%** |
| 지문(n) 비율 | 18% | 36% |
| 한글 | 12,226 | 10,515 |
| 「소망」 언급 | 47 | 26 |

신규 이미지·음원 **0건**. `firstLight` 데드 엔트리를 몽타주 2-1 에 배치했다. `bigeopIn5` 는 **여전히 미사용**이다.

### 세션 2 — 개명 · skill 정합 · `im` 재조준 완료 (2026-08-27)

브랜치 `feature/novel-antagonist-rename`. 커밋 `feat(novel): rename the antagonist to 서한비 and re-target the impact beats`.

**(1) 개명 「박지은」 → 「서한비」(徐寒霏)** — 정본 373건 + 코드·문서 46건 = **419건**. §3 의 10단 규칙은 **한 줄이 모자랐다**:

> 🔴 **인수인계 표에 없던 11번째 규칙** — `'박지은'이라는` 처럼 **이름과 조사 사이에 인용부호가 낀 자리**(정본 1건). 8번 일반 규칙이 먼저 먹으면 `'서한비'이라는` 이라는 비문이 된다. `박지은'이라` → `서한비'라` 를 **0번 규칙으로 맨 앞에** 뒀다. 전수 확인 정규식: `/박지은(['’”」』»)\]]{1,2})([가-힣])/g` → 2건(위 1건 + `「박지은」에서`, 후자는 조사 무관).

- 규칙별 실측 건수는 §3 표와 **전부 일치**했다(2·1·1·52·26·11·1·267·11·1). 남은 「지은」 **5건은 전부 동사**(「애써 지은」·「쪽이 지은」·「셈법으로 지은」·「값으로 지은」·「죄지은」) — §3 은 4건이라 적었으나 실제는 5건이고, 전부 그대로 둬야 한다.
- 🔴 **안 바꾼 것 3종** — ①R2 자산 파일명 9종(바꾸면 흑막 전 컷 404) ②화자 키 `pje` ③연애 시뮬의 별개 「박지은」. ①은 `public/codedestiny-novel.html` `var PJE` 위와 `scripts/fetch-novel-assets.mjs` 에 **주석으로 사유를 박아 뒀고**, 개명 이력(묘아 → 박지은 → 서한비)과 실명 위험 사유를 `webnovel_story_guideline.md` · `webnovel_ending_arc_outline.md` 의 이름 정본 문장에 적었다.
- **§3 표에 없던 추가 대상 3곳**: 셸의 화자 배지 `ICON.pje:"박"` → `"서"`(성이 바뀌었는데 배지만 「박」으로 남는다) · 셸 주석 2곳(`박지은 결전` · `박지은(PJE)`).
- 검증: `git grep "박지은"` 잔여는 **연애 시뮬 + 자산 파일명 + 이 인수인계 문서**뿐. `서한비(이|은|을|과)` 조사 오류 **0건**. 연애 시뮬·i18n **파일 무변경**(`git status` 로 확인).

**(2) skill 카드 정합 7건 + 미사용 1건 회수** — §5-7 의 7건이 실측과 **정확히 일치**했다(EP.32 #21 · EP.38 #18/#98/#131/#141/#178 · EP.41 #26).

| 위치 | 조치 | 대사도 함께 고침 |
|---|---|---|
| EP.32 #21 | 이의 멸 해일 → **수몰**(水沒) | `해일.` → `수몰.` · #22 지문에 「해일처럼」 삽입해 이미지 보존 |
| EP.38 #18 | 카드 제거(`fx:water` 유지) | — (「수벽」은 기술명이 아니라 대사로 남는다) |
| EP.38 #98 | 이의 박 연화 → **연소**(燃燒) | `연화!` → `연소!` |
| EP.38 #131 | 삼의 박 화룡 → **화염 스텝**(炎步) | `화룡!` → `화염 스텝!` · 🔴 **#132 지문의 「불룡」도 「불길」로** — 안 고치면 기술명이 사라진 단어를 지문이 계속 부른다 |
| EP.38 #141 | 삼의 형 금강 → **이의 형 금선** | `금강!` → `금선!`. 제거가 아니라 **강등**을 골랐다 — 대사가 「조금 보여 줄게」라 최상위 단위가 아닌 게 맞고, #15 에서 삼의 형 숙살이 이미 나왔다 |
| EP.38 #178 | 삼의 결 → **사의 결 세계수** 신설 | — |
| EP.41 #26 | 삼의 멸 → **삼의 류 청류** 신설(루나 전용) | — |
| EP.41 #2 | **일의 결 움**(萌) 배치 | — (후반 아크 미사용 기술 회수) |

- `webnovel_story_guideline.md` 배정표에 **루나 행(류 流: 일의 류 물길 · 이의 류 씻김 · 삼의 류 청류)** 과 **연이의 사의 결 세계수**를 추가하고, 두 줄짜리 🔴 원칙(멸=흑막 전용 / 사의 결=최종전 한정)을 달았다.
- 🔴 **skill 을 검사하는 가드는 없다.** `build-novel-runtime.mjs:148` 은 `skill` 을 그대로 통과시킬 뿐 el/unit/name 을 대조하지 않는다. **정합의 유일한 근거가 저 문서**이므로 표를 안 고치면 다음 세션이 다시 어긋난다.
- EP.41 #2 는 **지문(`s:"n"`) 비트에 붙은 유일한 skill 카드**다. 렌더러(`runSkill`, 셸 1748행)가 화자와 무관하게 오버레이만 그리므로 동작하고, 지문이 「여린 새싹이 다시 돋았다」라 萌 이 캡션으로 읽힌다. **의도된 변주다.**

**(3) `im:true` 재조준** — 후반 288 → **164**(화당 12~15). 🔴 **감축이 아니라 재조준이다.**

> §5-8D 가 지목한 「진짜 정점」 13곳 중 **6곳에는 `im` 이 아예 없었다** — EP.32 #141(거목)·#161(후드를 벗은 소년)·#196, EP.34 #58(「나는 무성이야!」), EP.39 #184(「세계가, 숨을 멈췄다」), EP.41 #88(포옹). 대신 반복되는 액션 지문에 붙어 있었다. 그래서 **제거 136 · 추가 6**이다.

- EP.39 는 `#184`(짧고 단단한 선언)에 붙이고 **`#185`(같은 사건의 묘사)에서 뗐다.** 둘 다 두면 정점이 두 번 온다.
- EP.43 은 46비트 종장이라 이미 6개다 — **손대지 않았다.**
- 화별 최종 집합은 커밋의 작업 스크립트에 배열로 남아 있다. 되돌리려면 그 배열만 고치면 된다.

**(4) 검증** — `novel:build` · `verify:story-sync` · `verify:novel-runtime`(셸 129,828 bytes) · `verify:novel-player-start` · `verify:vn-override-safety` · `verify:doc-freshness` · `verify:sitemap-drift` · `typecheck` · `lint`(경고만). 250자·1,800자 위반 0건. **HEAD 와 필드 단위 대조**로 비트 수·화자·fx·tone·표정·슬롯·`bg`/`bgm` 이 전부 그대로이고(즉 PR #1169 의 큐 작업이 온전하다), 개명으로 설명 안 되는 본문 변경은 **위 표의 6건뿐**임을 확인했다.

> 🔴 **`scripts/fetch-novel-assets.mjs` 는 CRLF 파일이다.** 주석 한 줄을 `\n` 으로 끼워 넣었다가 짝 없는 LF 를 만들었고(커밋 전에 CRLF 로 정규화해 해결), `public/codedestiny-novel.html` 은 반대로 LF 전용이다. **정본 JSON·셸은 LF, `scripts/**`·`lib/**`·`app/**` 는 CRLF** 다 — 문자열로 파일을 조립할 때 개행을 확인할 것.

### 세션 1 — 후반 아크 연출 매핑 완료 (2026-08-27)

**§5-4·§5-5 를 브랜치 `feature/novel-scene-direction` 에서 적용했다.** 커밋 `feat(novel): map BGM and background cues across the EP.31-43 climax`.

- **텍스트 무수정 확인 완료** — HEAD 와 필드 단위로 대조해 `t`·`s`·`fx`·`im`·`tone`·`x`·`skill`·`l/c/r` 슬롯·비트 수가 전부 동일하고 `bg`/`bgm` 만 바뀌었다(대조 스크립트로 실측). 정본 diff `+104/−11`.
- **BGM 전환 33 → 91**(큐 1개당 80비트 → 27비트). **BG 전환 25 → 54**, 후반 배경 종류 EP.40 은 1 → 7.
- `warTheme` **정확히 3진입**(EP.38 #167 · EP.39 #81 · EP.41 #24 — EP.39 #0 은 연속 유지라 진입 아님). `firstLight` **정확히 2진입**(EP.38 #43 · EP.41 #116).
- `RIVER_FALLBACK.firstLight` `daily` → `stillLake` (§5-3 부수 권고) 반영.
- 검증 통과: `novel:build` · `verify:story-sync` · `verify:novel-runtime` · `verify:novel-player-start` · `verify:vn-override-safety` · `verify:guard-wiring`. `verify:public-mirror-fresh` 는 `.ignore` CRLF 만 다른 알려진 윈도우 헛실패(`git diff --numstat` 0줄로 확인).

#### 🔴 §5-4·§5-5 표에서 **의도적으로 벗어난 6곳** — 다음 세션이 되돌리지 말 것

| 위치 | 문서 | 실제 적용 | 이유 |
|---|---|---|---|
| EP.31 #112 | `crisis` | **`zeroPoint`** | 기존 #132 `crisis` 가 무효 큐(직전 값과 동일)가 되어 **왕좌가 처음 말하는 최대 비트의 음악 표시가 사라진다.** #112 는 「이름 없는 별이 소리도 없이 삼켜졌다」라 §5-1 의 `zeroPoint`(존재의 영점·소멸) 정의와 문자 그대로 일치 |
| EP.32 #57 | `ziwei1/3/4` 중 택1 | **`ziwei2`** | 6부 4화가 인접 중복 없이 네 얼굴을 다 도는 배치. 최종 사슬 31:ziwei1 → 32:ziwei3·ziwei2 → 33:ziwei4·hands·memoryVault·ziwei1 → 34:ziwei4·ziwei2·ziwei3 |
| EP.33 bg | #107 삽입 | **#114 를 `ziwei4`→`ziwei1` 로 재지정** | #114 가 「별빛이 꺼지고 소년만 남았다」= 원저자가 쓴 명반 복귀 비트다. #107 에 넣으면 7비트짜리 조각이 생기고 #114 가 그걸 즉시 덮는다 |
| EP.35 #178 | `crisis2` | **`crisis`** | crisis2 로 두면 **EP.36 #0 의 기존 `crisis` 로 내려가는 역전**이 화 경계에서 생긴다. #179 는 교전이 아니라 흑막의 첫 조롱이라 §5-1 의 `crisis`(대치·협박) 쪽이 맞고, #139 `neo2` 가 사이에 있어 무효 큐도 아니다 |
| EP.43 #24 | `teaMoonlight` 삽입 | **#28 의 `teaMoonlight` 를 #24 로 이동** | 삽입하면 #28 이 무효 큐가 된다. 문서 표의 「후 6」과도 이 해석이 맞는다 |
| — | (미기재) | **컷어웨이 복귀 7개 추가** | 아래 |

#### 추가한 컷어웨이 복귀 7개 (문서에 없던 것)

정본 규칙상 `bg` 는 **지정한 비트에서만 바뀌고 이후 계속 유지**된다. §5-5 의 회상용 배경 삽입은 복귀 지점이 없어 **1비트짜리 회상 이미지가 60~150비트 동안 화면에 남는** 결함을 만든다. 그래서 함께 넣었다.

| 위치 | 복귀 | 근거가 되는 원문 |
|---|---|---|
| EP.36 #71 | `sukuyo` | 「자, 곳간 구경은 됐고」 — `memoryVault`(#19)가 150비트를 잡던 **기존 결함**도 같이 닫힌다 |
| EP.37 #53 | `sukuyo` | #52 `hands` 는 「손이 점점 투명해졌어」 한 비트짜리 컷어웨이. #53부터 현재 시제 대화 |
| EP.37 #66 | `sukuyo` | #62 `river` 는 벚꽃 강가 회상. #63~65 약속 교환까지 유지하고 #66(감옥의 무성)에서 복귀 |
| EP.38 #89 | `handsRush` | 모카 착지 = 전투 복귀. #43 `clearMoonWater` 가 140비트를 잡는 걸 막는다 |
| EP.40 #167 | `fateTarot` | 「떠 있던 카드들이 일제히 뒤집혔다」 — `cherryMoonPortal`(#88) 복귀 |
| EP.41 #62 | `sukuyo` | #56 `library` 는 서한비의 첫 기억. #62 에서 흑막이 현재 시제로 답한다 |
| EP.42 #24 | `sukuyo` | #13 `ziwei2` 는 무성이 돌아가는 컷어웨이. #24 루나가 매달리는 현재로 복귀 |

#### 다음 세션이 쓸 수 있는 도구

무효 큐(직전 값과 같아 화면·음악이 안 바뀌는 큐)는 정본 **EP.00~30 에 33건 남아 있다**(EP.31~43 은 0건). 판정 코드는 위 커밋의 작업 스크립트와 동일하게 화별로 `bg`/`bgm` 의 직전 유효값을 들고 비교하면 된다. 이 33건은 이번 범위 밖이라 **손대지 않았다**.

### PR #1166 — 파이프라인 복구 (머지 완료, `eb6fe7003`)

브랜치 `worktree-novel-pipeline-restore`. **CI 5개 전부 통과.** 변경 파일은 `public/codedestiny-novel.html` 하나(+155/−5763).

### 무엇을 고쳤나

**PR #345 가 만든 데이터 구동 플레이어를 PR #350(`4ba09df3b` "remaster chapter backgrounds and intro pacing")이 통째로 되감고 있었다.** 커밋 제목과 정반대로 44화 대본 931KB 가 셸에 다시 인라인됐다. `d02cd6b6b` 에서 복원하되 #350 이후 변경은 전부 보존했다.

- 인라인 대본 → manifest/청크 로더. 셸 **1.9MB → 129,659 bytes**(예산 180,000)
- `setSceneDirection` + `ambientLayer` + camera/ambient CSS — 빌드가 만드는 `scene` 메타데이터가 다시 화면에 도달
- `showNovelDataError`, `window.matchMedia` 방어
- 로컬 리마스터 자산: 연이 12종 · 모카 시트(3×2 크롭) · 까마귀 2종 · `river-of-names-v1.webp`
- id 북마크(`episodeId`/`beatId`), 구 `{ep,bi}` 형식도 읽어 이월

### 복원 중 발견해 함께 고친 것 (두 리비전 어디에도 없던 결함)

1. **청크 프리페치 무한 연쇄** — 로더가 fetch 핸들러 안에서 `warmNextEpisode` 를 불러 0→1→2→… 로 44화를 전부 받았다. 이제 화 진입 시 한 번만.
2. `startNovel`/`nextEpisode`/`hydrateTo`/`stepPrev` 가 beats 를 동기로 읽어 청크 방식에서 `null` 을 만났을 코드 → 청크 대기로 변경. 목차는 manifest `beatCount` 사용 + 펼칠 때 로드.
3. `#play` 진입 시 `bootDirectPlay()` **이중 실행** + 청크 도착 전 배경 프리로드.

### 🔴 왜 오래 아무도 몰랐나 — 다음에도 같은 함정

- `verify:novel-runtime` 이 `scripts/verify-guard-wiring.mjs:95` 의 `UNWIRED_BY_DESIGN` 에 **"실브라우저 필요"** 라는 **사실과 다른 사유**로 등록돼 있다. 실제로는 순수 정적 파일 검사다. → 미배선 가드의 사유를 믿지 말 것.
- `verify:novel-player-start` 는 별개 이유로 실패 중이었다. 플레이어가 최상위에서 `window.matchMedia` 를 방어 없이 읽는데 jsdom 이 미구현이라 **스크립트 전체가 그 줄에서 죽고**, 가드는 아무것도 검사 못 한 채 타임아웃했다. 빨간불인데 검증량이 0이었던 사례.

### 후속으로 판단 필요 (PR #1166 본문에도 적어 둠)

1. **`river` 배경이 R2 원본 → 리마스터 자산으로 돌아간다**(12곳 영향). 가드가 그 파일의 참조·용량(320KB 상한)을 검사하므로 리마스터가 정본 의도로 보이나, #350 이 의도적으로 R2 로 되돌린 것이면 되돌려야 한다.
2. **`verify:novel-runtime` 배선** — 거짓 사유를 지우고 PR CI 에 배선할지. 사용자에게 물었을 때 "전체 복원"만 선택했고 배선은 고르지 않았다.
3. **죽은 자산 3개** — `abyss-moon-awakening-v1.webp` · `mirror-trial-v1.webp` · `tarot-door-awakening-v1.webp` 가 레포에 있으나 정본이 그 배경 키를 안 써 참조 0. 후반 연출 작업에서 쓸 수 있다.

## 2. 반드시 알고 시작할 것 — 구조와 제약

```
content/novel/episodes.source.json   ← 유일한 손편집 대상 (44화 8,836비트, 2.27MB)
  │  npm run novel:build
  ├→ public/data/novel/manifest.json + episodes/*.json (44청크)
  ├→ lib/stories/vn/episodes.generated.json   ← 텍스트 리더 /stories
  └→ content/novel/scene-matrix.generated.json ← 연출 검수 매트릭스

public/codedestiny-novel.html  ← VN 엔진. PR #1166 이후 청크를 fetch 한다
```

**제약 (빌드·가드가 강제)**
- 비트 텍스트 **250자**(편집 규칙) / 빌드 상한은 260자 — 빌드는 250을 안 잡는다
- 화당 한글 **1,800자** 하한 — 현재 최저가 EP.0 의 2,041자라 실질 여유는 크다
- 화당 **200비트**가 사실상 고정. 실측 편차: EP.14=402, EP.31/32=201, EP.38=202, EP.41=204, EP.43=46 → **200±4 로 운용 중**
- `bg`/`bgm` 은 **지정한 비트에서만 바뀌고 이후는 직전 값 유지**
- 자산은 **BG 46종 · TRK 31종 · fx 23종**뿐. 목록 밖 키는 빌드가 throw
- **효과음(SFX) 오디오는 없다.** `fx` 는 전부 인라인 SVG/CSS 시각 연출
- 선택지·분기 없음(키네틱 노벨)

**빌드가 안 잡는 것을 손으로 재는 명령** (빌드는 260자만 본다):
```bash
node -e "
const d=require('./content/novel/episodes.source.json');
for(const e of d.episodes){
  const ko=e.beats.reduce((s,b)=>s+((b.t||'').match(/[가-힣]/g)||[]).length,0);
  const over=e.beats.filter(b=>(b.t||'').length>250).length;
  const beatOK=[402,46].includes(e.beats.length)||Math.abs(e.beats.length-200)<=4;
  const flag=[ko<1800&&'KO<1800',over&&('OVER250:'+over),!beatOK&&'BEATS'].filter(Boolean);
  if(flag.length)console.log(e.no,e.beats.length,ko,flag.join(' '));
}"
```

## 2-A. 현재 상태 실측 (2026-08-27, 세션 5 기준 · 세션 4까지는 `83993be3a`)

재현: `node scripts/…` 없이 `content/novel/episodes.source.json` 을 직접 읽어 세면 된다. hold% 는 `content/novel/scene-matrix.generated.json` 의 `visualCues[].transition` 기준이다.

| 화 | 제목 | 비트 | 한글 | bgm | bg | im | skill | hold% |
|---|---|---|---|---|---|---|---|---|
| EP.19 | 책들이 숨 쉬는 곳 | 200 | 10,364 | 2 | 3 | **24** | 0 | 95% |
| EP.20 | 본뜻의 서가 | 200 | 11,188 | 4 | 6 | **24** | 0 | 91% |
| EP.21 | 기억의 열람실 | 200 | 11,675 | 5 | **12** | **24** | 0 | **84%** |
| EP.22 | 네 번째 기둥 | 200 | 11,399 | 5 | 2 | 17 | 3 | 98% |
| EP.23 | 안 도망가는 물고기 | 200 | 10,515 | 9 | 15 | 14 | 1 | 85% |
| EP.31 | 별들의 궁 | 201 | 13,784 | 7 | 1 | 13 | 0 | 99% |
| EP.32 | 검은 깃털의 주인 | 201 | 12,959 | 7 | 2 | 13 | 5 | 98% |
| EP.33 | 그릇 | 200 | 15,422 | 8 | 4 | 13 | 0 | 97% |
| EP.34 | 이름을 돌려주다 | 200 | 14,980 | 7 | 3 | 13 | 2 | 97% |
| EP.35 | 붉은 실의 세계 | 200 | 13,846 | 7 | 2 | 12 | 0 | 98% |
| EP.36 | 서한비 | 200 | 14,131 | 8 | 4 | 14 | 0 | 96% |
| EP.37 | 박병하 | 200 | 14,245 | 9 | 6 | 14 | 0 | 95% |
| EP.38 | 끊을 수 없는 실 | 202 | 12,555 | 5 | 4 | 13 | 8 | 97% |
| EP.39 | 붙잡힌 파수꾼 | 200 | 13,479 | 6 | 3 | 14 | 0 | 98% |
| EP.40 | 운명의 신 | 200 | 16,349 | 9 | 14 | 15 | 0 | 85% |
| EP.41 | 사랑의 힘 | 204 | 15,517 | 8 | 5 | 12 | 3 | 96% |
| EP.42 | 읽는 이름, 쓰는 운명 | 200 | 11,747 | 11 | 11 | 13 | 0 | 52% |
| EP.43 | 벚꽃 강가의 약속 | 46 | 3,552 | 6 | 3 | 6 | 0 | 82% |

🔴 **EP.35·EP.39·EP.40 은 재구성이 끝났다(세션 3~5). 세션 6 은 이 세 화를 도너로 쓰지 말 것.** 한글 하한 1,800자는 어느 화도 근처에 없다(최저 EP.43 의 3,552자).

### 🔴 재구성 세션이 반드시 지킬 것 — 비트를 지우면 그 비트의 큐도 같이 죽는다

`bg`·`bgm`·`im`·`skill` 은 **비트 객체의 필드**다. 재배치는 큐를 함께 데려가지만 **삭제는 큐를 없앤다.** 세션 1·2 가 심어 둔 큐가 그렇게 조용히 사라지면 회귀를 아무도 못 본다. **가장 안전한 길은 세션 5 가 쓴 「같은 자리 교체」다** — 비트를 지우지도 넣지도 않으면 비트 수와 큐가 둘 다 구조적으로 보장된다. 재구성이 끝난 세 화의 현재 큐 위치는 다음과 같다 — **압축 도너를 고를 때 이 인덱스를 피하거나, 큐를 옆 비트로 옮겨 살릴 것.**

```
EP.35  bgm 0:templeGate 12:memWater 24:riverCross 53:gloom2 93:crisis 139:neo2 178:crisis
       bg  0:sukuyo 178:handsRush
       im  1 2 12 54 83 85 93 140 142 188 197 199

EP.39  bgm 0:warTheme 57:neo2 81:warTheme 100:gloom2 119:zeroPoint 184:templeGate
       bg  0:handsRush 119:neoBound2 143:neoBound3
       im  10 26 53 56 57 58 81 82 100 119 164 184 188 199

EP.40  bgm 0:stillLake 6:templeGate 15:gloom2 49:zeroPoint 80:stillLake 124:starsName 168:templeGate 187:riverCross 194:zeroPoint
       bg  0:fateTarot 35:neoBound3 39:fateTarot 50:room 64:neoBound3 70:memoryVault 81:ziwei2 88:handsRush
           93:cherryMoonPortal 100:river 104:fateTarot 162:sukuyo 168:fateTarot 194:handsRush
       im  0 6 7 15 49 50 64 70 71 93 124 168 187 194 199

(EP.35·EP.39 는 세션 5 이후 값이고 세션 4 이전과 **완전히 동일**하다 — 같은 자리 교체만 했기 때문이다.
 EP.40 은 세션 4 이후 값. 세 화 모두 재구성이 끝났으니 다른 세션이 여기를 도너로 쓰지 말 것)
```

추가로 지켜야 할 두 가지:

- 🔴 **`warTheme` 3진입 · `firstLight` 2진입 상한**(§5-2·§5-3). EP.39 의 `#0`(EP.38 `#167` 에서 이어지는 연속 유지)과 `#81`(`#57` 의 `neo2` 뒤 재진입)은 **둘 다 의미가 있다.** `#57`~`#81` 사이를 압축하다 `neo2` 를 없애면 `#81` 이 무효 큐가 되고 3진입이 2진입으로 준다.
- 🔴 **무효 큐를 만들지 말 것** — 큐가 직전 유효값과 같으면 화면·음악이 안 바뀐다. 재구성 뒤 화별로 `bg`/`bgm` 의 직전 유효값을 들고 훑어 0건인지 확인한다. **EP.31~43 은 현재 0건이고**(세션 5 가 패치 스크립트 안에서 재확인했다) **EP.00~30 에는 31건이 남아 있다**(세션 3 이 EP.23 재구성으로 2건을 없앴다. 나머지는 범위 밖이라 손대지 않았다).

### 알려진 미해결 — `im` 배분이 중간 아크에서 역전돼 있다

세션 2 는 §5-8D 의 지정 범위대로 **EP.31~42 만**, 세션 3 은 EP.23 만 12~15개로 재조준했다. 그 결과 **EP.19~21 이 아직 화당 24개**라, 지금도 **중간 아크가 클라이맥스보다 임팩트가 많다.** 🔴 **EP.17~22 는 아무 세션에도 안 잡혀 있다.** 후속으로 처리하려면 세션 2 와 같은 방식(현행 im 목록을 훑어 진짜 정점만 남기기)으로 하면 되고, 텍스트를 안 건드리므로 위험은 낮다.

## 3. 작업 1 — 개명 「박지은」 → 「서한비」  ✅ **완료 (PR #1172)**

> 아래 규칙표와 검증 명령은 **기록용**이다. 다시 돌릴 일은 없고, 이름을 또 손댈 일이 생기면 §1 세션 2 항의 「안 바꾼 것 3종」을 먼저 읽을 것.

### 🔴 치환 규칙 10단계 (긴 것부터 순서대로) — 실측 검증 완료

「박지은」은 **ㄴ 받침**, 「서한비」는 **받침 없음**이라 조사가 전부 바뀐다.

| # | 찾기 | 바꾸기 | 건수 |
|---|---|---|---|
| 1 | `박지은이라` | `서한비라` | 2 |
| 2 | `박지은이랑` | `서한비랑` | 1 |
| 3 | `박지은이야` | `서한비야` | 1 |
| 4 | `박지은이` | `서한비가` | 52 |
| 5 | `박지은은` | `서한비는` | 26 |
| 6 | `박지은을` | `서한비를` | 11 |
| 7 | `박지은과` | `서한비와` | 1 |
| 8 | `박지은` | `서한비` | 267 |
| 9 | `지은아` | `한비야` | **11** |
| 10 | `지은이를` | `한비를` | **1** |

**🔴 앞 세션이 실제로 빠뜨렸던 함정 — 반복하지 말 것**

- 첫 드라이런에서 `.{25}지은.{20}` 정규식으로 "박 없는 지은"을 세어 **5건**이라고 판정했다. **틀렸다.** 정규식이 겹치는 매치를 건너뛴다(lastIndex 진행). 실제는 `지은아` **11건** + `지은이를` 1건. 개수는 반드시 `(src.match(/패턴/g)||[]).length` 로 셀 것.
- `지은아` 11건은 전부 **호칭**이다 — EP.37~41 클라이맥스에서 병하·연이가 이름을 부르며 설득하는 감정 정점 대사들("지은아. 이제 무대에서 내려와도 돼"). 놓치면 개명이 반쪽이 된다.
- `박지은이` 56건 중 4건은 **주격이 아니다**(`이라`2·`이랑`1·`이야`1). 규칙 4를 먼저 돌리면 "서한비가라는" 같은 비문이 생긴다.
- **`박` 없는 "지은" 나머지 4건은 동사다** — "애써 지은", "값으로 지은", "죄지은". 건드리지 말 것.

### 바꾸는 파일

| 대상 | 위치 | 건수 |
|---|---|---|
| 정본 본문 + EP.36 `title`·`tag` | `content/novel/episodes.source.json` | 361 + 2 |
| 화자 표시명 · 7부 아크 요약 · EP.36 로그라인 | `lib/stories/vn/index.ts:48` · `:118` · `:162` | 3 |
| 화자 표시명(CMS) | `app/admin/cms/_components/BeatsEditor.tsx:32` | 1 |
| 인물 소개 카드 | `app/stories/page.tsx:36` | 1 |
| 플레이어 `NAME` 맵 | `public/codedestiny-novel.html` (`pje:"박지은"`) | 1 |
| 기획 문서 3종 | `docs/webnovel_review/` SAJU_SERIES_BIBLE(10)·ending_arc_outline(5)·story_guideline(2) | 17 |
| 산문 초고 | `lib/stories/chapters/chapter-27~31.ts` | 17 |

파생물(`public/data/novel/**`, `lib/stories/vn/episodes.generated.json`, `content/novel/scene-matrix.generated.json`)은 **직접 고치지 말고 `npm run novel:build`** 로 재생성.

### 🔴 절대 안 바꾸는 것

1. **화자 키 `pje`** — 데이터 키. 바꾸면 전 화 화자 매핑이 깨진다.
2. **R2 이미지 자산 파일명 9종** — `var PJE={base:NOVEL+enc("박지은 기본.webp"),…}` 와 `scripts/fetch-novel-assets.mjs:62`. **R2 실물 파일명**이라 바꾸면 전 컷 404. 주석으로 "표시명 서한비 / 자산 파일명은 구명 유지"를 남길 것.
3. **연애 시뮬의 「박지은」** — `app/saju/love-simulation/**`, `src/features/master-love-codex/**`, `i18n/**`, `public/i18n/**`. **완전히 무관한 별개 캐릭터**(계수 일간, 1998-12-12생)이고 12개 로케일 번역까지 걸려 있다. **전역 치환 절대 금지.**

### 검증

```bash
git grep -n "박지은" -- content/ lib/ app/ docs/          # 0 이어야 함
git grep -nE "서한비(이|은|을|과)" -- content/ lib/ app/   # 조사 오류 0
git grep -n "지은아\|지은이를" -- content/                 # 0
git grep -n "박지은" -- public/codedestiny-novel.html      # var PJE 자산 파일명 9건만
git grep -n "박지은" -- app/saju/ src/features/ i18n/ public/i18n/  # 손대지 않았으므로 그대로
```

### 보너스 — 개명을 서사로 회수할 수 있다

「서한비(徐寒霏)」는 이 캐릭터의 수식(水息)·검은 비 이미지와 직결된다. EP.36 초반 통성명 자리에 1비트만 붙이면 **무성의 이름이 조롱(無姓=성이 없는 자)이었던 것과 대비**되어 "이 여자는 이름을 다 갖고도 텅 비었다"가 성립한다.

## 4. 작업 2 — 인성의 도서관 아크(EP.19~23) 재구성  ✅ **완료 (PR #1175)**

> 아래는 **착수 전 설계와 진단**이고, 실제로 무엇이 설계에서 벗어났는지는 §1 세션 3 항에 있다. 현재값은 §2-A 다.

### 🔴 사용자가 말한 "사서의 아들"은 존재하지 않는다

백문의 **딸 「소망」**이고, **그 정체는 연이 본인**(전생·시간 순환)이다. 사용자가 성별까지 오기억했다는 것 자체가 "인상에 안 남았다"는 방증이다. **처방은 삭제가 아니라 전달 방식의 재구성.**

EP.41 `#134~140` 회수 대사:
> `[baek]` *"! 그 점. 연이 학생, 자네 손목의 그 점. 그리고 자네가 품에 지녀 온 그 종이 물고기. 소망이가 백 년을 놓지 않았던, '안 도망가는 물고기.' 이 늙은 여우가 접어 준. 아아. **소망아. 소망이가, 자네였어.**"*

미래에서 흑막에게 부서진 연이가 시간의 강을 거슬러 백 년 전으로 떠밀려 와 도서관 반납구로 흘러든 갓난아기가 소망이고, 그 아이가 강을 따라 더 흘러 현실에서 연이로 다시 태어났다. **소망은 EP.23 이후 320회 언급되는 메인 서브플롯이라 삭제하면 결말이 무너진다.**

### 진단 (실측)

| 화 | 비트 | 한글 | 지문(n) 비율 | bg 지정 | bgm | fx | hold% |
|---|---|---|---|---|---|---|---|
| 19 | 200 | 10,204 | 25% | 3 | 2 | 1 | 95% |
| 20 | 200 | 11,104 | 21% | 6 | 4 | 10 | 91% |
| **21** | 200 | 11,638 | **40%** | **12** | 5 | 14 | **84%** |
| 22 | 200 | 11,343 | 28% | 2 | 5 | 12 | 98% |
| **23** | 200 | 12,226 | **18%** | **3** | 4 | 8 | **98%** |

EP.23 블록 구조: `#1–50` 백문이 앉아서 회상을 **말한다**(액션 0) → `#51–62` 연이가 되쓰기(유일한 행동) → `#63–75` 단서 심기(말로 하는 체크리스트) → `#76–130` 정보 브리핑 → `#131–200` 밤·작별·배웅. **정적 대화 79비트, 그 안에 배경 전환 0.**

앞 EP.22 는 4부 클라이맥스(bgm `main`), 뒤 EP.24 는 5부 액션 개시(bgm `crisis2`). 그 사이에 액션 0 인 200비트가 끼어 진행이 멈춘다.

`hold%` 는 `content/novel/scene-matrix.generated.json` 의 `visualCues[].transition` 으로 잰다(`hold`=직전 화면 유지 / `crossfade`=전환). **개선 목표: hold 85% 이하 · 배경 4종 이상.**

### 처방의 템플릿은 이미 이 작품 안에 있다 — EP.21 「기억의 열람실」

**신규 자산 0개로** hold 84% / 배경 10종을 달성한 유일한 화. *"페이지가 넘어갔다"* 서사 장치로 기존 배경을 재활용한다:

```
#0   bg=library2   bgm=stillLake    열람실 진입
#21  bg=bigeopIn1  bgm=riverEnter2  "첫 페이지. 낯익은 물빛 동굴이 나타났다."
#61  bg=siksang    bgm=novaFlex     "무대 조명으로 바뀌었다"
#86  bg=jae                         "다시 페이지가 넘어갔다" — 저울 소리
#88  bg=jaeSlave                    담보로 잡힌 언니
#187 bg=library    bgm=stillLake    본관 복귀
```

### 설계 — 4개 실행 원칙

- **A. 순서 역전** — 연이가 되쓰기로 과거를 **연다**. 백문은 확인만 한다.
- **B. 몽타주화** — EP.21 문법 그대로 재사용.
- **C. 소속 이전** — 상처의 배후에 **그 여자**를 놓는다. 🔴 **이름은 절대 쓰지 않는다.**
- **D. 무손실 분산** — EP.19~22 는 **비트 수를 바꾸지 않고 본문만 교체**(총 17비트). EP.23 은 내부에서만 200→200 재배치.

**C 의 근거는 이미 정본에 깔려 있다 — 새 설정이 아니다:**

| 근거 | 위치 |
|---|---|
| 청토끼는 백 년 전에도 회중시계를 **빌려** 차고 있었다 | EP.23 #6 |
| 신미 함 명패 = 「회장님 관심 자산」, 시들어 가는 창백한 소녀 환영 | EP.24 #143–151 |
| 청토끼: 「그날 밤 나한테 손 내민 건 그 여자뿐이었어」 | EP.26 #135 |
| 소망은 담보 시장이 아니라 **별들의 궁 꼭대기**, 창백한 별과 겹쳐 갇혀 있다 | EP.29 #19, #177–179 |
| 「부서진 너, 그리고 완성된 너. 나란히.」 | EP.36 #155 |

즉 **소망을 사 간 것은 청토끼가 아니라 그 여자였다.** 청토끼는 배달부였다. EP.23 이 이걸 (이름 없이) 보이게 하면 백문의 상처가 **최종보스가 연이의 조각을 처음 훔친 사건**이 되어 사이드 퀘스트가 메인 라인이 된다. EP.24 「회장님 관심 자산」이 1화 만에 회수되고, EP.27 스팅어(「청토끼? 내 도구들 중 제일 하찮은 말이었지」)와 충돌하지 않고 오히려 강화된다.

### EP.19~22 무손실 교체 (17비트, 비트 수 ±0)

| 화 | 비트 | 바꾸는 것 |
|---|---|---|
| 19 | #62 | 탄 자리가 **한 번 접혔다 펴진 자국**을 남기고 있다 (종이 물고기와 각운) |
| 19 | #66 | 종이에서 **아주 옅은 물 냄새** (반납구 장면의 후각 훅) |
| 19 | #71 | 꼬리 추가 — 「나조차 그 아이 이름을 소리 내어 못 부른 지 백 년일세」 |
| 19 | #75 | **계약으로 격상** — 「제가 여덟 글자를 다 채우는 날, 제가 먼저 손 내밀게요」 |
| 19 | #92 | 의식 추가 — 유난히 얇은 책 한 권을 **늘 맨 마지막에 닦고 한 번도 펼치지 않는다** |
| 19 | #94 | 앞 비트와 호응하는 반 문장 |
| **20** | **#110** | 🔴 **최대 레버리지** — 「그 아이는 늘 **종이 물고기** 한 마리를 손에 쥐고 있었네」. 이 사물이 EP.23→EP.29·30→EP.41 까지 5번 회귀한다. 3화 앞에서 심어야 소품이 아니라 모티프가 된다 |
| 20 | #112 | 수수께끼로 심기 — 「데려간 게 토끼인 건 아네. 헌데 토끼에게 **값을 치른 자가 따로 있어**」 |
| 20 | #151·#154 | 「밑이 없는 우물」이 사람이라는 걸 반 걸음 명시 |
| **21** | **#124** | 🔴 그림자 여인이 돌아서는 순간 **그 팔에 이미 다른 것이 안겨 있다** — 작은 보퉁이, 틈으로 삐져나온 **접힌 종이 귀퉁이** |
| 21 | #127 | 연이의 읽기 — 그 종이가 뭔지는 모르나 **어디서 들은 것 같다**(EP.20 #110) |
| 21 | #132 | 「저 여자, 그날 밤 나만 가져간 게 아니야.」 |
| 22 | #160 | 약속을 뒤집는다 — 「이야기로는 안 하겠네. **내일은 내가 말하는 게 아니라, 자네가 읽는 걸세**」 |
| 22 | #163 | 본필 인계에 추가 — 「이 붓으로 내가 마지막에 쓴 글이, 백 년째 내가 못 읽는 그 반쪽일세」 |
| 22 | #192 | 「내일은 이야기 대신, 자네 붓을 빌리겠네」 |

**EP.21 #124 심기의 안전성**: 이 심기는 표면 독해("그 여자가 둘 다 훔쳤다")만 성립시킨다. 진짜 독해(소망=연이·시간 순환)는 전혀 노출되지 않는다.

### EP.23 신 구조 (200비트) — 제목 변경 권장: 「사서의 오래된 상처」 → **「안 도망가는 물고기」**

현 제목은 "조연 백스토리 화"라고 스스로 광고한다. 새 제목은 이미지이자 단서이며 우회가 아니라 약속으로 읽힌다. (변경 시 편집 지점 4곳: EP.22 #200 예고 카드 · EP.23 `tag` · `title` · EP.23 #200 종료 카드)

| 블록 | 비트 | 내용 | BG | BGM | FX |
|---|---|---|---|---|---|
| 1 콜드 오픈 | #1–18 | 연이가 먼저 찾아간다. 백문 1회 거절. 연이가 `뿌리내림`으로 탄 반쪽에 뿌리를 내리자 **문장이 연이를 끌어당긴다** | library2 | gloom2 | root, flash |
| **2 회상 몽타주** | #19–78 | 아래 표 | 7회 | 4회 | 12 |
| 3 현재 복귀·재분류 | #79–98 | 연이가 떨며 나온다. 네오가 붙든다. 백문은 짧게 확인만. **두 번째 그림자 = 열람실의 그 여자** 연결 | library2 | gloom2 | flash |
| 4 단서 확정 | #99–112 | 몽타주가 보여 준 손목 점·종이 물고기를 **이름 붙여** 확정. 「아비는 너를 한 번도 후회한 적 없다」 보존 | — | — | — |
| 5 정당한 계약 | #113–128 | 현 #92–103 유지. 앞발 도장 | — | — | flash |
| 6 금고 브리핑 | #129–152 | 현 #104–130 에서 3비트 압축. **F8·「그 여자는 악이 아니라 상일세」 원문 보존** | — | — | — |
| 7 마지막 밤 | #153–160 | | — | stillLake | stars |
| 8 작별 아침 | #161–180 | 28→20 압축. **등불 물고기 종이 포장 필수 보존**. 검식 개그 6→3(EP.20 #188–193 과 중복) | library | stillLake | flash |
| 9 배웅·축문·상승 | #181–200 | 36→20 압축. **축문 원문 보존**. 상승 통로 대화 16→8(EP.24 도입과 중복) | library | riverCross | burst, flash, vortex |

**블록 2 회상 몽타주 상세 (60비트)**

| 장면 | 비트 | 내용 | BG | BGM | FX |
|---|---|---|---|---|---|
| 2-1 반납구의 밤 | #19–28 | 문서 폭포가 만든 얕은 물 위 첫 빛. 등불 물고기가 빛을 한 자리로 밀어 모은다. 그 한가운데 **반쯤 젖고 반쯤 부서진** 갓난아기 | library | **`firstLight`** ★ | ink, water |
| 2-2 종이 물고기 | #29–36 | 백문이 종이를 접는다. 아이가 머리 위로 들 때 **소맷자락이 흘러내리고 왼쪽 손목의 점이 보인다** | library2 | (유지) | veil, water |
| 2-3 반년의 손님 | #37–46 | **청토끼(`rab`) 실제 화자로 등장.** 「제 고객님께서 이 도서관에 관심이 아주 많으십니다」 | library | stillLake | veil |
| 2-4 대출증 | #47–56 | 「도서관 운영 효율화 협약」. 서명란에 지팡이가 닿자 문장이 뒤집힌다 | library2 | gloom2 | veil, script, ink, shake |
| 2-5 문지방 | #57–66 | 「사서장님, 계약서를 안 읽으신 건 사서장님이십니다」 원문 보존. 아이 손엔 종이 물고기. **청토끼 뒤로 그림자가 한 겹 더 접힌다** | jaeEnter → doorSuck | crisis2 | hands, suck, thread |
| 2-6 사흘 밤낮 | #67–72 | 날아가던 문장이 공중에서 「무효」로 덧쓰인다 | library2 | (유지) | veil, burst, net |
| 2-7 백 년 | #73–78 | 검은 책을 한 권씩 닦는 타임랩스. 맨 마지막 얇은 책은 늘 펼치지 않는다(EP.19 #92 회수) | library | stillLake | veil, ink |

★ **`firstLight`("First Light on Water")는 정본 전체 미사용 데드 엔트리**다. 「반납구의 물 위에 뜬 첫 빛」과 곡명이 문자 그대로 일치한다. `RIVER_FALLBACK` 에 이미 등록돼 있어 **코드 변경 불필요**.
> `bigeopIn5` 도 정본 전체 미사용이다. 8번째 장면이 필요하면 예비로 쓸 수 있다(bigeopIn1~6 은 "회상용" 지정 자산이고 EP.21 이 1·2·3·4·6 을 이미 회상 배경으로 쓴 선례가 있다).

**연출 예산 전/후**: BG 3→12 · BGM 4→9 · fx 8→22 · 지문 비율 18%→32%. **신규 이미지·음원 0건.**

### 🔴 EP.23 재구성 후 반드시 남아 있어야 할 사실 14개

| # | 사실 | 회수처 |
|---|---|---|
| 1 | 백문의 딸, 이름 **「소망」** | EP.24 #101, EP.41 #139 |
| 2 | **반납구**의 빛·등불 물고기 자리에서 발견, **이야기의 강 상류**에서 흘러옴 | EP.41 #138–139 |
| 3 | **「반쯤 부서진 채」** | EP.41 #139 원문 |
| 4 | **왼쪽 손목의 등불 물고기 모양 점** | EP.41 #136 |
| 5 | **종이 물고기 「안 도망가는 물고기」**, 백문이 접어 줌 | EP.41 #137, EP.29 #174, EP.30 #29 |
| 6 | 끌려가던 날에도 그 물고기를 쥐고 있었다 | EP.41 #137 |
| 7 | 백문이 **전병 포장을 등불 물고기 모양으로 접어 연이에게 준다** | EP.29·30·41 **(압축 금지)** |
| 8 | 슬퍼도 남 앞에선 웃으려 애쓰는 아이 | EP.34 #138 |
| 9 | 청토끼가 소망을 담보 시장에 넘겼고 원본이 금고에 있다 | EP.24 #101–108 |
| 10 | 사서인 회수, 문지방 밖 못 나감 | EP.20 #106, EP.25 #175 |
| 11 | 「아비는 너를 한 번도 후회한 적 없다」 | EP.41 **(원문 보존)** |
| 12 | F8 — 「어디서 본 숨」/「색인에 없는 책」 | EP.37 **(원문 보존)** |
| 13 | 「그 여자는 악(惡)이 아니라 상(傷)일세. 미워하지 말고 읽게」 | EP.41 방법론 **(원문 보존)** |
| 14 | 축문 「이 아이가 읽는 모든 것이 진실을 드러내고…」 | EP.42 제목 **(원문 보존)** |

확인 명령:
```bash
node -e "
const d=require('./content/novel/episodes.source.json');
const e=d.episodes[23];const has=s=>e.beats.filter(b=>(b.t||'').includes(s)).length;
['소망','왼쪽 손목','등불 물고기','안 도망가는 물고기','종이 물고기','후회한 적 없다','반납구','색인에 없','악(惡)이 아니라','사랑을 지키게'].forEach(k=>console.log(k,has(k)));
console.log('서한비(0이어야 함)',has('서한비'),'| pje화자(0이어야 함)',e.beats.filter(b=>b.s==='pje').length);"
```
현재 기준선: 소망 47회 → 재구성 후 **25회 이상 유지 권장**.

### 신규 위험 3건과 판정

1. **손목 점을 이미지로 보여주면 연이가 자기 손목을 확인하지 않는가?** → **안전.** EP.23 시점 연이는 꽃돼지다(#100 「앞발 도장으로」). 인간화는 EP.26. 인간 손목이 없다. 또 EP.41 #135 는 창백한 별의 청록빛이 스며든 **그 순간에 점이 떠오른다**고 명시한다. 원문이 이미 알리바이를 제공한다. 단 "연이가 자기 앞발을 내려다본다" 같은 힌트는 **넣지 말 것**(반전 조기 노출).
2. **두 번째 그림자가 EP.27 스팅어를 죽이는가?** → **아니다.** 스팅어의 반전은 "그 여자가 있다"가 아니라 "**청토끼가 하찮았다**"는 위협 축척 낙차다. EP.23 이 청토끼를 배달부로 보여주면 낙차가 오히려 준비된다.
3. **이름 노출로 인한 공개 순서 위반** → 「서한비」는 EP.27 부터만 등장한다. EP.23 에는 절대 쓰지 않는다. 「그 여자」/「토끼에게 값을 치른 자」/「고객님」/「그 그림자」만 사용. 화자 `pje` 도 쓰지 않는다(스프라이트가 붙어 얼굴이 공개된다). **부수 효과: EP.23 은 개명 작업 대상에서 완전히 빠진다.**

### 검토했으나 손대지 않기로 한 것

**EP.33 「그릇」 #68 의 「저는 누구의 아들입니까」는 결함이 아니다.** 조사 중 "폐기된 모자(母子) 설정의 잔재"라는 의견이 나왔으나 **본문을 열어 확인한 결과 아니다.** 12장면 회상 안에서 10번째("제 이름 무성이 사실은 '성(姓)이 없는 자'라는 말장난"), 12번째("이름을 가진 아이들")와 한 줄기로 이어지는 **이름·소속에 대한 갈망**이고, 여자의 답이 모자 관계를 명시적으로 부정한다. 사용자가 지목한 위치는 "인성의 도서관"이므로 대상이 아니다.

## 5. 작업 3 — 후반 클라이맥스(EP.31~43) 연출·BGM 매핑

### 진단 — 연출 예산이 거꾸로 배분돼 있다  ✅ **해소됨 (PR #1169·#1172). 아래는 착수 전 실측이고 현재값은 §2-A 다**

> 후(2026-08-27): EP.31~43 의 BGM 전환 **33 → 91**(큐 1개당 80비트 → 27비트) · BG 전환 **25 → 54** · `ziwei2` 0회 → 3회 · EP.40 배경 1종 → 7종 · `im` 화당 24 → 12~15.
> 🔴 **남은 예외 하나**: 아래 「`im` 이 EP.17~42 전 화에서 24개」는 **EP.31~42 만 고쳐졌다.** EP.17~22 는 여전히 24개다(§2-A 말미 참조).

| 구간 | 비트 | BGM 큐 | 큐 1개당 비트 |
|---|---|---|---|
| EP.29 (일상 회차) | 200 | 17 | **12** |
| EP.31~43 (클라이맥스) | 2,654 | **33** | **80** |
| EP.32/33/35 (반전 3화) | 602 | **3** | **201** |

- **EP.35 「붉은 실의 세계」가 신규 무대에 진입하며 `riverEnter2`(사주의 강 진입)를 쓰고 200비트 내내 유지한다.** `riverEnter2` 는 EP.21·EP.30 에서 "사주의 강 쪽 세계로 넘어감"의 의미로 쓰인다. 단순 미스매치가 아니라 **"또 강으로 돌아왔다"는 지리 오독**을 유발한다.
- 후반 전용 배경 실사용: `ziwei2` **0회**, `memoryVault`·`clearMoonWater`·`cherryMoonPortal`·`fateTarot`·`neoBound2/3` 각 1회. EP.32 는 201비트를 `ziwei3` 한 장으로, EP.40 은 200비트를 `fateTarot` 한 장으로 버틴다.
- `im:true` 가 EP.17~42 거의 전 화에서 **정확히 24개**로 기계 균등 배분 → 임팩트가 임팩트로 기능하지 않는다.

hold% 실측: EP.31 99% · EP.32 99% · EP.33 98% · EP.35 99% · EP.38 99% · EP.41 98%, 배경 종류 1~2종.

### 5-1. 먼저 트랙 문법을 고정한다 (남발 방지의 유일한 방법)  ✅ **적용됨 — 아래 표가 현행 트랙 문법이다**

| 트랙 | 후반 아크 전용 의미 |
|---|---|
| `templeGate` | 성역의 문 / 인간보다 큰 존재 앞 |
| `memWater` | 회상 · 남의 기억을 읽는 순간 |
| `starsName` | **이름을 되찾거나 새기는 순간에만** |
| `zeroPoint` | 존재의 영점 — 소멸·텅 빈 곳간·정해진 결말 |
| `firstLight` | 물이 씻기고 빛이 드는 회복의 첫 순간 |
| `stillLake` | 정적 · 전투음을 끊는 용도 |
| `gloom2` | 회한 · 지친 슬픔 (분노 아님) |
| `crisis` / `crisis2` | 대치·협박 / 실제 교전 |
| `warTheme` | **되돌릴 수 없는 지점에만. 아크 전체 3진입 상한** |
| `novaFlex` | 모카 등장 + 오행 상생 고리 |

### 5-2. `warTheme` 3진입 상한 — 이게 핵심 결정  ✅ **적용됨 (PR #1169). 재구성이 깨뜨릴 수 있으니 §2-A 를 볼 것**

| # | 위치 | 근거 |
|---|---|---|
| 1 | EP.38 `#167` | 「베틀째로 전부 끊어 주지. 이 세계째로!」 — 세계 자폭 선언 |
| 2 | EP.39 `#81` | 네오가 백 년 아낀 금빛을 통째로 푼다 |
| 3 | EP.41 `#24` | 연이가 빈손으로 폭풍 한복판에 뛰어든다 |

(EP.39 `#0` 은 #167 에서 이어지는 연속 유지이므로 진입으로 세지 않는다.)

🔴 **현재 EP.38 `#89`(모카가 "앙코르 무대 왔습니다"로 등장)의 `warTheme` 을 `novaFlex` 로 교체**한다. 축제 같은 등장 신에 최종전 테마를 쓰면 진짜 최종전에서 쓸 카드가 없다. 게다가 `#113~132` 가 정확히 **오행 상생 고리**가 도는 구간이라 캐릭터 테마와 액션 문법이 동시에 회수된다.

### 5-3. `firstLight` 는 2회로 고정  ✅ **적용됨 (PR #1169). `RIVER_FALLBACK` 1줄 권고도 반영됐다**

**EP.38 `#43`**("끊긴 실이 스스로 다시 이어지기 시작했다" + 루나가 물길을 타고 올라옴) + **EP.41 `#116`**("검은 물이 실에서 씻겨 나갔다… 세계가 노을처럼 환하게 밝아졌다"). 둘 다 동일 정서라 두 번째 등장에서 관객이 학습한다. 3회 이상은 정체성이 흐려지고 1회면 학습이 안 된다.

> 부수 권고(코드 1줄): `RIVER_FALLBACK` 의 `firstLight:"daily"` → `"stillLake"`. 폴백 정서가 어긋난다.

### 5-4. 화별 BGM 배치 (전/후)  ✅ **완료 (PR #1169) — 표에서 벗어난 6곳은 §1 세션 1 항 참조**

| EP | 제목 | 비트 | 현재 | 후 | 신규 투입 |
|---|---|---|---|---|---|
| 31 | 별들의 궁 | 201 | 2 | 7 | templeGate(#0), neo2(#37), gloom2(#61), crisis(#112), crisis2(#187) |
| **32** | 검은 깃털의 주인 | 201 | **1** | 7 | crisis2(#0), **stillLake(#80 — 방패를 여는 최대 반전을 음악으로 표시)**, memWater(#87), crisis2(#104), **starsName(#146)**, gloom2(#163), stillLake(#176) |
| **33** | 그릇 | 200 | **1** | 7 | stillLake(#16), **memWater(#33 — 열두 장면 회상)**, zeroPoint(#68), gloom2(#96), stillLake(#107), crisis(#154), crisis2(#163) |
| 34 | 이름을 돌려주다 | 200 | 2 | 7 | stillLake(#40), memWater(#43), **starsName(#58 — "연, 이. 연이! 나는 무성이야!")**, crisis(#139), crisis2(#190) |
| **35** | 붉은 실의 세계 | 200 | **1** | 7 | **templeGate(#0 ← riverEnter2 제거)**, memWater(#12), riverCross(#24), gloom2(#53), crisis(#93), **neo2(#139 — 붉은 실이 시각화되는 비트)**, crisis2(#178) |
| 36 | 서한비 | 200 | 3 | 8 | **zeroPoint(#19 — 곳간)**, crisis(#44), crisis2(#54), gloom2(#72), **memWater(#89)**, templeGate(#115) **제거**, neo2(#192) |
| 37 | 박병하 | 200 | 4 | 8 | **zeroPoint(#52)**, **riverCross(#62 — 벚꽃 강가 약속)**, gloom2(#88), crisis(#104), memWater(#130), crisis2(#182) |
| 38 | 끊을 수 없는 실 | 202 | 2 | 7 | **firstLight(#43)★**, **novaFlex(#89 ← warTheme)**, crisis2(#145), **warTheme(#167)** |
| 39 | 붙잡힌 파수꾼 | 200 | 2 | 6 | neo2(#57), **warTheme(#81)**, gloom2(#100), **zeroPoint(#119)**, #164 큐 **제거**, templeGate(#184) |
| 40 | 운명의 신 | 200 | 3 | 8 | templeGate(#6), gloom2(#15), stillLake(#75), templeGate(#167), riverCross(#191) |
| 41 | 사랑의 힘 | 204 | 3 | 8 | stillLake(#50), **memWater(#56 — 「오늘 그 사람이 나한테 웃어 주었다」)**, **firstLight(#116)★**, starsName(#129), gloom2(#170) |
| 42 | 읽는 이름, 쓰는 운명 | 200 | 6 | 10 | starsName(#13), riverCross(#24), neo2(#46), neo2(#105), riverCross(#178) |
| 43 | 벚꽃 강가의 약속 | 46 | 3 | 6 | teaMoonlight(#24), main(#33 입맞춤), riverCross(#41), teaHouse(#44) |
| **계** | | 2,654 | **33** | **96** | 큐 1개당 비트 **80 → 28** |

### 5-5. 배경 전환 (22개 삽입)  ✅ **완료 (PR #1169) — 컷어웨이 복귀 7개가 추가로 들어갔다**

| 배경 | 현재 | 삽입 위치 |
|---|---|---|
| **`ziwei2`** | **0회** | EP.34 #106, EP.40 #76, EP.42 #13 |
| `doorSuck` | 1 | **EP.38 #183** ("세계 전체가 회오리처럼 감겨 들어갔다") |
| `memoryVault` | 1 | EP.33 #68(곳간 **선행 노출** → EP.36 #19 가 회수로 격상), EP.40 #64 |
| `clearMoonWater` | 1 | EP.38 #43, EP.41 #116 (firstLight 와 같은 지점 — 음악과 배경이 함께 바뀐다) |
| `cherryMoonPortal` | 1 | **EP.40 #88** (EP.43 결혼식 배경의 선행 노출 → 마지막 화가 EP.40 의 상상을 실현하는 구조) |
| `neoBound3` | 1 | EP.40 #58 |
| `handsRush` | 3 | EP.35 #178, EP.36 #171, EP.37 #182, EP.40 #83 |
| `tarotDoor` | 후반 0 | **EP.42 #69** (프롤로그 수미상관) |
| `library` | 후반 0 | **EP.41 #56** — 서한비의 첫 기억이 **"도서관 앞에서"**다. "그 여자도 우리와 같은 곳에서 시작했다"가 그림으로 온다 |
| `river` | 후반 0 | EP.37 #62, EP.43 #34 |
| `hands` | 2 | EP.37 #52 ("네 손이 점점 투명해졌다") |
| `room` | | **EP.40 #44** (카드 속 자취방) |
| `ziwei1/3/4` | 5 | EP.32 #57, EP.33 #107, EP.34 #139 — 6부 4화가 배경 4장을 실제로 순환하게 |

**EP.40 의 배경 전환 6개가 체감 개선 최대**다. 200비트 대화극이 "카드를 넘길 때마다 화면이 통째로 바뀌는" 구조가 된다.

### 5-6. 🔴 `金克木` 이 최종전에서 회수되지 않는다 — 이누야샤 구조 보강 1순위  ⬜ **세션 6**

`docs/webnovel_review/webnovel_story_guideline.md:48` 이 **"최대 드라마 축"**으로 지정한 것("네오의 금은 본디 연이의 목을 베지만, 그 칼을 연이를 위협하는 것에만 쓰고 연이는 절대 베지 않는다")이 **EP.14 에서 한 번 제시된 뒤 후반에 한 번도 돌아오지 않는다.** 정본 전수 스캔: `金克木` 은 EP.14 에만, EP.31 에 `水生木` 만.

실측:
- EP.32 `#13~19` — 단금이 `金生水` 로 역효과 (`金克木` 아님)
- EP.32 `#43~47` — 칼 봉인 (상성 때문이지 연이 때문이 아님)
- EP.37 `#69` — **말로만** 설명
- EP.38 / EP.41 — **없음**
- EP.39 `#57` — 등으로 창을 막음 (몸을 던진 것이지 칼을 거둔 것이 아님)

**처방 3단 (총 7비트)**

- **A. EP.38 `#183` 뒤 4비트** — 검은 실이 **연이의 넝쿨을 관통해** 있다. 실을 끊으려면 넝쿨을 함께 베야 한다. 네오는 벨 수 없다. **칼을 버리고 맨손으로** 검은 실을 붙든다. 손이 갈라진다 → EP.39 `#81` 에서 금빛을 통째로 푸는 선택의 직접 원인이 된다(현재 그 선택은 동기가 헐겁다).
- **B. EP.39 `#63` 뒤 2비트** — 연이의 "네 등 뒤 말고 옆에 서고 싶어"에 네오가 **반 걸음의 진짜 이유**를 답한다. 이게 EP.42 `#141~143`("거기 반 걸음. 이제 그거 안 해도 돼")를 **`金克木` 극복의 최종 회수**로 격상시킨다. 현재는 "고양이 시절 버릇"으로만 읽힌다.
- **C. EP.42 `#143` 뒤 1비트** — 「백 년을 벼렸더니 마침내 베지 않는 법을 배웠다」

합치면 **EP.14(사고) → EP.32(봉인) → EP.37(고백) → EP.38(칼을 버림) → EP.39(반 걸음의 이유) → EP.42(반 걸음이 사라짐)** 6단 회수 라인.

원고 예시(A 의 3번째 비트, 톤 견본):
> `{"s":"n","fx":"hands","im":true,"t":"네오가 백금을 놓았다. 그리고 맨손으로 검은 실을 붙들었다. 손바닥이 갈라지고 은빛이 배어 나왔지만, 연이의 덩굴은 한 올도 다치지 않았다. 상극을 이기는 법은, 더 잘 베는 것이 아니라 베지 않는 것이었다."}`

### 5-7. skill 카드 정본 위반 7건  ✅ **완료 (PR #1172) — 신설 2종은 가이드라인 배정표에 들어갔다**

`webnovel_story_guideline.md:54-60` 배정표 대비:

| 위치 | 현재 | 문제 | 수정안 |
|---|---|---|---|
| EP.32 #21 | 이의 멸 **해일** | 정본 = **수몰** | `name:"수몰",han:"水沒"`, "해일"은 지문으로 |
| EP.38 #18 | 이의 멸 **수벽** | 같은 단위에 이름 2개 | skill 제거, `fx:water` 만 |
| EP.38 #98 | 이의 박 **연화** | 정본 = **연소** | `name:"연소",han:"燃燒"` |
| EP.38 #131 | 삼의 박 **화룡** | 정본 = **화염 스텝** | `name:"화염 스텝",han:"炎步"` |
| EP.38 #141 | 삼의 형 **금강** | #15 에서 이미 **숙살** | skill 제거 또는 이의 형 금선으로 강등 |
| EP.41 #26 | 루나 **삼의 멸** 청류 | **멸(滅)은 흑막 전용 단위** | 루나 전용 **류(流)** 신설 — 일의 류 물길·이의 류 씻김·삼의 류 청류. 가이드라인 표에 루나 행 추가 |
| EP.38 #178 | 연이 삼의 결 **세계수** | 삼의 결 = 뿌리내림(이미 3회) | **사의 결 세계수(世界樹)** 신설 — 최종전 한정 개방. 가이드라인에 1행 추가 |

**미사용 기술 회수**: 정본 표의 `일의 결 움(萌)`이 후반에 0회. **EP.41 `#2`**("얼어붙었던 연이의 손끝에서, 여린 새싹이 다시 돋았다")에 배치하면 **가장 큰 기술로 진 사람이 가장 작은 기술로 다시 시작한다**가 성립한다.

대조 명령:
```bash
node -e "
const d=require('./content/novel/episodes.source.json');
const canon={'metal|일의 형':'단금','metal|이의 형':'금선','metal|삼의 형':'숙살',
'wood|일의 결':'움','wood|이의 결':'덩굴 결박','wood|삼의 결':'뿌리내림',
'fire|일의 박':'불씨','fire|이의 박':'연소','fire|삼의 박':'화염 스텝',
'water|일의 멸':'침수','water|이의 멸':'수몰'};
for(const e of d.episodes)e.beats.forEach((b,i)=>{if(!b.skill)return;
 const k=b.skill.el+'|'+b.skill.unit;
 if(canon[k]!==b.skill.name)console.log(e.no,'#'+i,k,'=>',b.skill.name,canon[k]?('정본:'+canon[k]):'정본에 없는 단위');});"
```

### 5-8. 페이싱 — 4대 결함  ✅ **A(세션 4) · B·C(세션 5) · D(PR #1172) 전부 완료. 아래는 전부 착수 전 진단이다 — 실제로 뭐가 들어갔는지는 §1 을 볼 것**

**A. EP.40 이 긴장 곡선 최대 함몰점 ★★★**  ✅ **완료 (세션 4) — 아래는 착수 전 진단이고, 실제로 뭐가 들어갔는지와 산술이 왜 안 맞았는지는 §1 세션 4 항을 볼 것** — 네오가 끌려가는 최강 클리프행어(EP.39 #143) 직후에 화자 2인만 나오는 200비트 철학 문답. 배경 1장·전투 0. **같은 논지가 5회 반복**된다(`#35~55`, `#80~93`, `#119~130`, `#139~151`, `#152~159`).
→ 반복 5회→3회 압축(제거 후보 `#151~159`, `#144~149`)으로 ~25비트 확보 → §5-5 배경 6개 + "정지된 세계" 컷어웨이 3비트(`#40`/`#100`/`#160`) + **말미 `#197~199` 에 서한비 쪽 컷 2비트**(현재는 훈훈하게 끝나 위협이 사라진 채 EP.41 로 넘어간다).

**B. EP.35 는 적이 92비트 늦게 온다 ★★**  ✅ **완료 (세션 5). 🔴 「`#93` 검은 탑을 `#55` 로 이동」은 의도적으로 안 했다 — 이유는 §1 세션 5 항** — `#1~92` 가 전부 세계 소개, 서한비 첫 목소리는 `#178`(89% 지점).
→ `#178` 을 축약 티저 2비트로 `#40` 부근에 선행, `#93` 검은 탑 발견을 `#55` 로 이동, `#93~130` 실 관광 구간에서 6~8비트를 덜어 뒤로 재배분.

**C. "설득"이 20회 이상 중복 ★★★**  ✅ **완료 (세션 5). 🔴 「6회→2회」는 EP.39 `#164~183` 블록에만 문자 그대로 적용했다 — 이유는 §1 세션 5 항** — EP.36 4회 · EP.37 5회 · EP.38 4회 · EP.39 6회 · EP.41 3회.
→ **5단 계단으로 재설계, 각 단 한 번만**: EP.36 진단("개수만 세고 이름은 모른다") → EP.37 원인("완벽해야만 사랑받는 집에서 자랐다") → EP.38 **광경으로 증명**(말이 아니라 여섯이 서로 딛고 선 것) → EP.39 **실패** → EP.41 회수(포옹 + 첫 문장 낭독, 현행 유지 — 이미 최고).

🔴 **EP.39 가 이 아크 최대 페이싱 결함이다.** 무력해야 할 화인데 **연이가 아크에서 가장 말을 많이 한다**(설득 대사 30회 이상, 무릎 꿇은 뒤 `#164` 에도 계속). 설득이 실패해 동료를 잃은 화인데 실패의 무게가 대사량에 눌려 사라진다.
→ 설득 6회→2회, 확보한 ~15비트를 **침묵·무력함·신체감각**으로. `#164~183` 은 **연이가 거의 말하지 않는 20비트**로 다시 쓴다. 그래야 `#184` 의 시간 정지가 "구조"로 느껴진다. 제거 후보: `#37`/`#39`, `#41`/`#43`, `#140`/`#142`, `#170`/`#172`/`#174`(각 쌍이 거의 동일).

**D. `im:true` 화당 24개 → 12~15개로 감축 ★** — 남길 진짜 정점: EP.32 #141(거목)·#161(후드)·#196 / EP.34 #43·#58·#59 / EP.37 #16(진명) / EP.39 #57(등)·#184 / EP.41 #88(포옹)·#105·#134 / EP.43 #30·#33.

**클리프행어 강도 곡선** — EP.35 와 EP.40 두 곳이 함몰. A·B 처방이 정확히 이 두 지점을 겨냥한다.

### 5-9. 비트 수 보존 — 삽입분의 조달처  ⬜ **세션 6 — 🔴 도너를 고르기 전에 §2-A 의 큐 인덱스를 확인할 것**

🔴 **세션 5 실측: 아래 EP.39 행(`#37`/`#39`, `#41`/`#43`)은 이미 소진됐다.** 그리고 애초에 「덜어서 조달한다」보다 **같은 자리 교체**가 안전하다 — 조달 자체가 필요 없어진다.

| 화 | 삽입 | 압축 도너 (실측 중복) |
|---|---|---|
| EP.38 | +4 | `#8`/`#12`, `#29`/`#30`, `#172`/`#173` |
| EP.39 | +2 | `#37`/`#39`, `#41`/`#43` (설득 감축과 겸함) |
| EP.42 | +1 | `#119`/`#120` |
| EP.40 | — | 반복 5→3 압축으로 ~25비트 여유 |

## 6. 이누야샤 구조 — 이미 있는 것과 빠진 것  ⬜ **세션 6**

사용자 요청 4번은 **새로 갈아엎는 게 아니라 이미 채택된 골격의 후반 발현 점검**이다. `webnovel_story_guideline.md` 가 "고유명사·설정 차용 없이 구조(아키타입)만" 참조하도록 대응표까지 확정해 놓았다.

| 아키타입 | 상태 |
|---|---|
| 우물→이세계 전이 | ✅ 프롤로그 타로 문 |
| 인외 동료 동행 | ✅ 네오 |
| 흩어진 구슬 조각 회수 | ⚠️ **EP.26 에서 끝나고 후반에 회수 리듬이 소멸** |
| 나라쿠형 흑막 + 그림자 자아 | ✅ 서한비 – 무성(EP.33) |
| 봉인된 정체의 각성 | ✅ EP.37 |
| 인간 주인공이 이계에 남는 결말 | ⚠️ **VN 은 의도적 분기** — 현실 재회 + EP.43 결혼식 |

**오행 호흡 체계는 살아 있다** — `skill` 카드가 EP.14(5)·18(6)·22(3)·25(5)·26(5)·32(5)·34(2)·**38(9)**·41(2) 에 배치돼 있고 EP.38 최종전에서 삼의 형 「숙살」까지 나온다.

### 약점 4가지와 처방 (전부 아키타입 층위 — 고유명사·대사 차용 없음)

1. **조각 회수 축 소멸 ★★★** — **EP.31 `#17` 뒤 1비트**로 회수 대상을 **글자 → 이름**으로 명시 전환. 그러면 이미 존재하는 사건들이 자동으로 하나의 회수 리스트로 묶인다: 무성의 이름(EP.34) → 병하의 진명(EP.37) → 소망/연이의 이름(EP.41 #135) → 서로의 이름을 사주에 새김(EP.43 #28). **비용 대비 효과 최대.**
   원고 예시: `{"s":"yeon","x":"soft","tone":"dark","t":"재성에선 글자를 주웠어. 흩어진 내 기둥, 내 사주. 근데 여긴 글자가 아니라 이름이 굴러다녀. 아무도 안 불러 준 이름들이. 좋아. 여기서부턴 이름을 줍자. 하나도 안 빼고, 다 주머니에 넣어 갈 거야."}`
2. **그림자 자아가 EP.34 에서 너무 완전히 아군화 ★★★** — 이후 위협 요소 0, 그릇 설정이 논다. 흔들림 비트 3개: EP.35 `#61` 뒤(서한비의 실을 읽다 "이 실, 맛있어 보여"라고 말했다가 제 입을 막음), EP.38 `#114` 뒤(내 물이 잠깐 저 여자 쪽으로 기울었다), EP.39 `#21` **텍스트 교체**(이유를 "실이 아니라 제 안의 서한비가 그를 끌었다"로 — 비트 수 증가 없음).
3. **인외성 급소실 ★★** — EP.37 이후 사자성이 개그로만 남는다. **EP.43 `#24` 앞 1비트**: 예식 직전 병하가 무의식적으로 하객을 **냄새로 세고 있다**.
4. **이세계물의 "저쪽 시간" 축이 EP.31~41 에 0회 ★★** — **EP.36 `#16` 뒤 1비트**: 서한비가 연이의 불운(우산·계단·버스·면접)을 자기 작품이라 밝히는 자리에 연이가 **"지금 저쪽은 몇 시예요?"**를 되묻는다. 흑막의 현실 조작과 현실 시간축이 한 비트에서 동시에 산다.

### 🔴 문서 2개가 VN 정본과 반대다 (신규 집필자가 정본을 깰 위험)

- `webnovel_story_guideline.md:17` / `webnovel_ending_arc_outline.md:14` → "연이가 **운명 세계에 남는 결말**"
- **실제 VN EP.42** → 연이가 현실로 돌아오고 병하가 연이 쪽으로 건너온다 (**현실 재회**)

처방 2단: ①guideline 대응표에 분기 명기 — `[텍스트 소설] 운명 세계 잔류 / [VN] 의도적 반전: 현실 재회. 두 각색의 확정 분기점` ②EP.42 `#63` 부근에 1비트로 **서사 안에서 선언** — 연이는 남는 대신 **문을 열어 뒀다**. 그러면 분기가 사고가 아니라 선택이 되고, EP.40 `#46~55`(안전한 결말 vs 백지)가 이 선언의 복선으로 자동 승격된다.

## 7. 함께 갱신할 문서  ⬜ **세션 6 — 단 `webnovel_story_guideline.md` 의 오행 배정표와 이름 정본 문장은 PR #1172 에서 이미 갱신됐다**

- **`docs/webnovel_review/SAJU_SERIES_BIBLE.md` — 정본과 어긋난 4곳**
  1. **32행 백문 설정이 정반대다.** 바이블은 "속죄가 아니라 설욕 · 잃어버린 아이 없음 · 구속 사유는 전투 패배"인데, 정본은 "딸 소망을 지키려 사서인을 내줬다가 딸까지 잃음". 바이블이 소망 서사 도입(`d02cd6b6b`·`26dfd026a`) 이전에 멈춰 있다.
  2. **범위가 EP.15~40 로 적혀 있으나 정본은 EP.43 까지 44화다.** §1 의 "6막 붉은 실 35~39 / 7막 완결 40"이 실제(35~43)와 다르고 EP.41·42·43 을 모른다.
  3. **§5 결말이 "EP.40 캠퍼스 재회"로 끝난다** — 실제 종착점은 EP.43 벚꽃 강가 결혼식.
  4. §4 떡밥 원장에 행 추가: `EP.23 문지방의 두 번째 그림자 / 「고객님」` → 회수 `EP.24 회장님 관심 자산` · `EP.27 스팅어` · `EP.41 소망 정체`
- `docs/webnovel_review/webnovel_story_guideline.md` — ✅ 개명 반영 · ✅ 오행 표에 루나 행(류 流)·사의 결 추가(PR #1172) / ⬜ **남은 것: §VN 범위 「4부 인성」 갱신 · 결말 분기 명기**
- `docs/webnovel_review/webnovel_ending_arc_outline.md` — ✅ 개명 반영 + 이름 이력(묘아 → 박지은 → 서한비) 기재(PR #1172). 레온→박병하 브릿지 설명은 유지했다 / ⬜ **남은 것: 결말 분기 명기**
- **`docs/context/content-assets.md:13` — 기존 오류 수정**: 「VN = 단일 자립형 `public/codedestiny-novel.html`(EP1~5)」는 낡았다. 실제는 프롤로그+EP.01~43(44화·8,836비트), 정본 = `content/novel/episodes.source.json`, 산출물 2계통. PR #1166 이후 플레이어는 청크를 fetch 한다.

## 8. 검증

```bash
npm run novel:build                  # 정본 → 청크·리더·매트릭스 재생성 (자산 키·화자·fx·260자를 여기서 잡는다)
npm run verify:story-sync            # sourceHash 동기
npm run verify:novel-runtime         # 런타임 훅 + 자산 + 180KB 예산
npm run verify:novel-player-start    # jsdom 실제 구동
npm run verify:vn-override-safety
npm run sitemap:generate             # 🔴 정본을 고쳤으면 필수 — 아래 설명
npm run verify:sitemap-drift         # → OK 확인
npm run lint && npm run typecheck    # app/stories, lib/stories/vn, BeatsEditor 수정 시
npm run verify:doc-freshness         # 문서 수정 시
```

🔴 **`verify:story-sync` 는 정본 편집 직후 반드시 실패한다**(sourceHash 불일치). `novel:build` 를 먼저 돌릴 것.

🔴 **정본을 고치면 sitemap 원장이 어긋나 PR CI 가 죽는다** — `/stories/` + `/stories/ep-01/`~`ep-43/` + `/stories/prologue/` **45개 서명**이 VN 정본에서 계산되기 때문이다. `app/**` 를 한 줄도 안 건드려도 그렇다. 더 나쁜 건 **실패가 `Typecheck and lint` 라는 이름으로 온다는 것** — tsc/eslint 를 뒤지게 되는데 둘 다 멀쩡하다. `gh run view <id> --log-failed` 로 스텝부터 볼 것. 고치는 법은 `npm run sitemap:generate` 를 돌려 **같은 커밋에** 담는 것이고, 범위 확인은 `git diff -U0 -- config/sitemap-lastmod.json | grep -cE '^[+-]\s+"/'` 가 **0**(라우트 추가·삭제 없음)인지 보면 된다. 2026-08-27 에 PR #1169 가 이걸로 한 번 죽었다.

🔴 **`verify:public-mirror-fresh` 는 윈도우에서 헛실패한다** — `.ignore` 의 CRLF 차이 하나뿐이면 `npm run sync:public` 을 돌린 뒤 `git diff --numstat` 가 0줄인지 보고 무시할 것.

🔴 **파일마다 개행이 다르다.** 정본 JSON·`public/codedestiny-novel.html` 은 **LF**, `scripts/**`·`lib/**`·`app/**` 는 **CRLF**. 문자열로 줄을 끼워 넣을 때 `\n` 을 그냥 쓰면 짝 없는 LF 가 생겨 git 정규화가 꺼지고, 몇 줄 수정이 전 파일 diff 로 부푼다.

읽기 전용 사전 검증(파일을 쓰지 않는다):
```bash
node -e "import('./scripts/build-novel-runtime.mjs').then(m=>{const p=m.buildNovelPayload();console.log('OK',p.episodeCount,p.beatCount)})"
```

워크트리에서 작업하면 `node_modules` 가 대개 안 딸려온다. jsdom 이 필요한 검증 전에 정션을 걸 것:
```
cmd /c mklink /J "<워크트리>\node_modules" "<저장소 루트>\node_modules"
```

## 9. 남은 작업 우선순위와 세션 분할

**세션 5·6 은 창작 판단이 무거워 한 세션에 두 개 이상 넣지 말 것.**

| 세션 | 내용 | 추정 | 리스크 | 제약 위협 |
|---|---|---|---|---|
| ~~**1**~~ | ~~**BGM/BG 큐 96+22개 삽입**(§5-4·5-5)~~ **완료 — 브랜치 `feature/novel-scene-direction`, §1 참조** | — | — | — |
| ~~2~~ | ~~개명 367건(§3) + skill 정합 7건(§5-7) + `im` 감축(§5-8D)~~ **완료 — 브랜치 `feature/novel-antagonist-rename`, §1 참조** | — | — | — |
| ~~3~~ | ~~EP.23 재구성(§4)~~ **완료 — 브랜치 `feature/novel-library-arc`, PR #1175, §1 참조** | — | — | — |
| ~~4~~ | ~~EP.40 재구성(§5-8A)~~ **완료 — 브랜치 `feature/novel-ep40-rebuild`, PR #1177, §1 참조** | — | — | — |
| ~~5~~ | ~~EP.39 설득 감축 + EP.35 순서 재배치(§5-8B·C)~~ **완료 — 브랜치 `feature/novel-ep39-ep35-pacing`, PR #1179, §1 참조** | — | — | — |
| 6 | `金克木` 7비트(§5-6) + 이누야샤 보강 6비트(§6) + 문서(§7) | 4h | 중 | 있음(소) |

**세션 4~6 이 세션 1·2 보다 어려운 이유**: 세션 1·2 는 **텍스트를 안 건드리거나 기계적 치환**이라 250자·1,800자·200비트를 원천적으로 위협하지 않았고, 롤백이 `git checkout content/novel/episodes.source.json` 한 줄이었다. **세션 3~6 은 비트를 지우고 새로 쓴다** — 제약 3종을 전부 밟고, §2-A 의 큐를 밟는다. 🔴 **세션 3 실측: 200비트를 유지하려면 블록별 목표치를 먼저 세고 실제 개수를 기계로 세어 대조해야 한다.** 처음 쓴 초안이 204비트였고, 눈으로는 안 보였다.

**PR 순서**: PR #1166·#1169·#1172·#1175·#1177 머지 완료, **#1179(세션 5) 머지 대기**. 🔴 **세션 6 은 #1179 가 머지된 뒤 `origin/main` 에서 분기할 것** — 정본을 고치면 산출물 4계통(`public/data/novel/**` 44청크 + `manifest.json` + `lib/stories/vn/episodes.generated.json` + `content/novel/scene-matrix.generated.json`)이 **전부** 바뀌고, `sourceHash` 때문에 EP.31~43 밖 청크까지 매번 바뀐다. 같은 생성 파일을 건드리는 PR 을 병렬 분기하면 충돌이 44개 파일로 번진다.

### 착수 체크리스트 (세션 6)

```bash
git fetch origin main
git checkout -b feature/<주제> origin/main
ls -ld node_modules                      # 없으면: cmd /c mklink /J "<워크트리>\node_modules" "<저장소 루트>\node_modules"
```

1. §0 → §2 → **§2-A** → 담당 설계 절을 읽는다.
2. 고치기 전에 **대상 화의 현재 비트를 실제로 연다.** §2-A 의 큐 인덱스와 대조해 **지울 비트에 큐가 붙어 있는지** 먼저 본다.
3. 편집 → `npm run novel:build` → §8 의 검증 전량 → `npm run sitemap:generate`.
4. 커밋 전 `git diff --numstat` 로 **정본 외에 바뀐 파일이 산출물 4계통 + sitemap 3종뿐인지** 확인한다.
5. 끝나면 **이 문서에 결과와 벗어난 판단을 적고** 다음 세션 표를 갱신한다. 세션 1·2 항이 그 서식이다.

## 10. 다시 하지 말 것 (실패·오판 기록)

- **비트를 지웠다 넣었다 하기 전에, 「같은 자리 교체」로 되는지 먼저 볼 것**(세션 5 실측). 세션 3·4 는 삭제 N + 삽입 N 으로 200을 맞추느라 「삭제 수 ≠ 삽입 수」·「큐가 붙은 비트를 지움」을 매번 검산해야 했다. 세션 5 는 대상 44비트를 **전부 제자리에서 갈아 끼웠고**, 그래서 비트 수도 큐도 검산이 아니라 구조로 보장됐다(§2-A 의 EP.35·EP.39 큐 인덱스가 전후 완전 동일). 재구성이 「이 대사를 지문으로 바꾼다」 꼴이면 대개 같은 자리 교체로 된다.
- **`expect` 접두사 검사를 「대충 비슷하니 맞겠지」로 넘기지 말 것**(세션 5 실측). EP.35 `#132` 를 `이미 했잖아, 바보야` 로 적었는데 실제 본문은 `이미 했잖아. 너랑 나, 방금 이었어` 였다 — `바보야` 는 **#75 의 「이미 있잖아, 바보야」** 였고, 덤프를 읽다 두 비트를 섞은 것이다. 접두사 검사가 없었으면 엉뚱한 비트를 덮어썼다. **패치 스크립트의 `expect` 는 장식이 아니라 이 사고를 막는 장치다.**
- **대사를 지우면 바로 뒤 상대 대사가 허공을 가리킨다**(세션 5 실측). 이 작품의 대화는 **뒷 대사가 앞 대사의 낱말을 그대로 받는다.** EP.39 `#39`(고무줄 비유)를 지우자 `#40` 의 「고무줄이라. 귀여운 비유네」가 죽었고, `#170`(평생 후회)을 지우자 `#171` 의 「평생 후회. 나는 백 년을 후회했어」가 죽었다. **한 비트를 갈아 끼우면 바로 다음 비트의 첫 문장을 반드시 열어 볼 것.** 세션 5 는 이 때문에 `#40`·`#42`·`#51`·`#171`·`#173`·`#175`·`#178`·`#182` 의 첫 문장을 함께 고쳤다.
- **새 대사를 쓸 때 그 인물이 이미 말한 것과 대조할 것**(세션 5 실측). EP.35 무성에게 「저 목소리, 백 년 만에 듣는데」를 쓸 뻔했는데, `#98` 에서 그는 「봉인 속에서 나는 저 여자의 밤을 다 들었어」라고 이미 말한 상태였다. 인물의 과거 진술은 새 대사의 제약이다.
- **「같은 것이 몇 번 나오나」는 축자 중복부터 세면 도너가 바로 나온다**(세션 5 실측). EP.35 의 조달처는 고민할 필요가 없었다 — `#39`=`#27~28`, `#111`=`#31`, `#125~126`=`#33~34`, `#131~135`=`#78~90` 이 **거의 축자 중복**이었다. 남길 것은 언제나 **먼저 나온 쪽**이다(정보가 처음 등장하는 자리가 그 정보의 자리다).
- **연출 지시서의 「N비트 이동」은 큐가 몇 개 딸려 오는지부터 볼 것**(세션 5 실측). §5-8B 의 「`#93` 을 `#55` 로」는 `bgm`+`fx`+`im` 이 붙은 3비트 덩어리 이동이었고, 목적(적이 늦게 온다)은 티저 2비트로 이미 달성돼 이득이 없었다. **지시의 문구가 아니라 지시의 목적을 실행할 것.**
- **§5 의 「N비트 확보 → …에 투입」 산술을 검산 없이 믿지 말 것**(세션 4 실측). §5-8A 는 ~25비트를 확보해 「배경 6개 + 컷어웨이 3비트 + 말미 2비트」를 넣으라 적혀 있지만, 배경 6개는 PR #1169 에서 **비트 증가 없이** 이미 들어갔다. 그대로 따르면 195비트가 된다. 착수 전에 삭제 수와 삽입 수를 더해 맞춰 볼 것.
- **패치는 손으로 하지 말고 자기 검산하는 node 스크립트로 돌릴 것**(세션 4 실측). 삭제 수 ≠ 삽입 수 · 결과 비트 ≠ 200 · 250자 초과 · **무효 큐**(직전 유효값과 같은 `bg`/`bgm`) · **삭제 대상에 큐가 붙어 있음** — 이 다섯을 전부 throw 로 막으면 한 번에 끝난다. 정본은 LF 이므로 2칸 들여쓴 `JSON.stringify` 결과에 개행 하나를 붙여 다시 쓰면 원문과 정확히 일치한다(라운드트립 확인 완료).
- **컷어웨이를 대사로 닫지 말 것**(세션 4 실측). 문답 한가운데에 컷어웨이를 꽂고 대사로 닫으면, 바로 뒤 신의 대답이 **두 비트 전 질문에 답하는 구조**가 돼 대화가 붕 뜬다. 지문으로 닫고 원래 문답을 그대로 이어 붙일 것.
- **화 말미에 붙이는 스팅어의 `bgm` 은 다음 화 `#0` 과 겹치면 안 된다**(세션 4 실측). EP.41 `#0` 이 `crisis` 라 EP.40 말미를 `crisis` 로 두면 다음 화 첫 큐가 죽는다. 무효 큐 검사를 화 단위로만 돌리면 **화 경계는 안 잡힌다** — 다음 화 `#0` 을 손으로 볼 것.
- **재구성 전에 「같은 것이 몇 번 반복되나」만 보지 말 것**(세션 4 실측). EP.40 의 진짜 결함은 반복 횟수가 아니라 **계약이 두 번 접수되고 배웅이 세 번 나오는 것**이었다. 「접수했다」·「가렴」처럼 **돌이킬 수 없는 선언**을 먼저 전수로 뽑아 중복을 보면 진짜 도너가 나온다.
- **§4·§5 의 `#숫자` 를 그대로 믿고 편집하지 말 것**(세션 3 실측). §4 의 EP.19~22 표는 **1-based**, §2-A 의 큐 인덱스는 **0-based** 다. 한 문서 안에서 기준이 섞여 있으니 **본문을 열어 내용으로 대조한 뒤** 편집한다.
- **재구성 초안의 비트 수를 눈으로 세지 말 것**(세션 3 실측). 블록 주석을 넣고 기계로 세지 않으면 200 이 204 가 돼도 안 보인다. 빌드는 260자만 잡지 비트 수는 안 잡는다.
- **`hold%` 를 배경 큐 개수로 추정하지 말 것**(세션 3 실측). 분모는 200비트가 아니라 `scene` 이 붙은 비트 수(EP.23 은 98)다. `tone`·`fx`·`im` 만 있어도 분모가 늘어 hold% 가 되레 나빠진다. 반드시 `novel:build` 후 `scene-matrix.generated.json` 으로 잰다.
- **새 소품을 심을 때 기존 소품과 겹쳐 읽히는지 볼 것**(세션 3 실측). EP.19 에 심은 「백문이 안 펼치는 얇은 책」이 EP.23 말미에 연이가 되살리는 얇은 책과 같은 권으로 읽혔다. 한 줄로 갈라 줘야 했다.
- **"박 없는 지은"을 겹침 허용 없는 정규식으로 세지 말 것.** `.{25}지은.{20}` 으로 5건이라 판정했으나 실제는 12건이다. 정규식이 겹치는 매치를 건너뛴다.
- **치환 규칙표를 "이름 + 조사"만으로 세우지 말 것**(세션 2 실측). `'박지은'이라는` 처럼 **인용부호가 사이에 끼면** 조사 규칙이 안 걸리고 일반 규칙이 먹어 비문이 된다. 이름 치환을 짤 때는 `이름([구두점]{1,2})([가-힣])` 로 **한 번 훑고 시작**할 것.
- **기술명을 바꿨으면 그 기술을 부르는 지문도 함께 볼 것**(세션 2 실측). EP.38 #131 의 「화룡」을 「화염 스텝」으로 고치자 #132 지문의 「모카의 불룡이」가 없는 기술을 가리키게 됐다. 카드만 고치고 끝내면 조용히 어긋난다.
- **후반 아크의 개행은 파일마다 다르다**(세션 2 실측). 정본 JSON·`public/codedestiny-novel.html` 은 **LF**, `scripts/**`·`lib/**`·`app/**` 는 **CRLF**. 문자열로 줄을 끼워 넣을 때 `\n` 을 그냥 쓰면 짝 없는 LF 가 생겨 git 정규화가 꺼진다.
- **EP.33 #68 「저는 누구의 아들입니까」를 잔재로 오해하지 말 것.** 서브에이전트가 "폐기된 모자 설정의 잔재"라고 보고했으나, 본문을 열어 확인한 결과 결함이 아니다(§4 마지막). **돌아온 보고는 재료지 결론이 아니다.**
- **소망 서사를 삭제하지 말 것.** EP.41 정점이 무너진다. 사용자 표현("사서의 아들")을 문자 그대로 받아 삭제 대상으로 잡으면 안 된다.
- **연애 시뮬의 「박지은」에 전역 치환을 돌리지 말 것.** 12개 로케일 번역까지 오염된다.
- **`verify:novel-runtime` 의 "실브라우저 필요"라는 미배선 사유를 믿지 말 것.** 순수 정적 검사이며, 이 거짓 사유 때문에 회귀가 오래 방치됐다.
- **`public/codedestiny-novel.html` 의 대본을 직접 고치지 말 것.** PR #1166 이후 플레이어는 청크를 fetch 하므로 정본만 고치고 `novel:build` 를 돌린다.
- **근거를 못 찾으면 추측하지 말고 사용자에게 물을 것.**
