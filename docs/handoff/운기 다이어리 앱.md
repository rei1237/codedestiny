---
status: active
updated: 2026-09-06
next: PR-B 를 시작한다 — 운기 엔진 3계층을 `lib/` 로 축자 복사하고 패리티 테스트로 동치를 증명한다.
---

# 운기 다이어리 앱

## 왜

기존 "운기·기일 다이어리"(정적 셸의 바닐라 IIFE 모달)를 **운기 → 계획 → 행동 → 기록 → 회고가 반복되는 다이어리 앱**으로 바꾼다. 사용자 확정: App Router 신규 `/diary` · localStorage 유지 + 확장 키 + 백업 · 기일 = 기존 일운 등급 승격 · 부가기능 전부 보존('더보기') · ko 전용 + en 폴백 · 신규 `verify:*` 0개(테스트로 대체).

🔴 **기존 사용자의 기록을 한 건도 잃지 않는다.** 🔴 **기존 운기 엔진을 중복 구현하지 않는다.** 🔴 **운기가 결과를 결정한다고 쓰지 않는다** — 사용자가 자기 기록과 운기를 스스로 관찰하는 도구다.

## 지금 상태

- 계획 정본: `C:\Users\user\.claude\plans\agile-foraging-castle.md` (13절, PR 0/A~K). 목업 승인 완료: https://claude.ai/code/artifact/77db9c19-a3f8-4ce9-b9d8-456c2c028717
- **PR-A(라우트 골격) = #1670** 열림, 브랜치 `feat/diary-route-skeleton`, 워크트리 `.claude/worktrees/diary-route-skeleton`.
- PR-B~K 미착수.

## 남은 작업

- [ ] **PR-B** 엔진 3계층 + 읽기 전용 스토어. 됐다의 기준: `__tests__/ui/diary-fortune-parity.test.js` 가 원본 함수를 **실제 실행**해 `tone`/`goodness`/`badness`/`scores` 전건 일치(생년 표본 N × 400일). 0건 매칭이면 실패.
- [ ] PR-C 홈 4카드군 · PR-D 캘린더/기일/Day View · PR-E 🔴 쓰기 개시 · PR-F 계획·성취·회고 · PR-G 기록·검색·태그 · PR-H 통계 · PR-I 더보기 6종 + export/import · PR-J 컷오버 · PR-K 죽은 코드 7건.
- [ ] 화면 목업은 **PR 마다 따로 발행해 승인**받는다(사용자 지시). 남은 것: 기록 · 통계 · 더보기.
- [ ] PR-B/D/E 는 하단바 탭을 열 때 `app/diary/_components/DiaryBottomNav.tsx` 의 `ready: false` 를 함께 뒤집는다.

## 정본 예시

- 운기 등급 원본: `js/luck-sync-diary.js:2677` (`_classifyDayFromSaju`)
- 컷오버 지점(PR-J): `js/core/index-inline-runtime.js:2508` (`openFortunePlanner` 본문만)
- 날짜축: `app/diary/_lib/kst-date.ts` — 🔴 PR-B 에서 새 날짜 유틸을 또 만들지 말고 이것을 쓴다

## 함정

- 🔴 `_classifyDayFromSaju` 는 본문 `:2701-2702` 가 모듈 스코프 `_lsdCtx.dEl`/`_lsdCtx.luckyEl` 를 폴백으로 읽는다. **인자로 승격**하고 계산식은 한 글자도 바꾸지 않는다.
- 🔴 동치 3축 고정: 야자시 `NIGHT_ZI_POLICY.KEEP_DAY`(코어 기본값 `SHIFT_DAY` 를 안 넘기면 23시대가 하루 밀린다) · 일진 조회 `hour:12` · 날짜축 `Asia/Seoul`.
- 🔴 `--cd-mnav-offset` 참조 금지(80px 유령 띠). AppChrome 배열은 **2개 모두** 등록. 둘 다 `__tests__/ui/diary-route-chrome.test.js` 가 문다.
- 🔴 `AppChrome.tsx` 를 고치면 `config/sitemap-lastmod.json` 서명 310줄이 함께 돈다(모든 라우트의 모듈 클로저). `npm run sitemap:generate` 를 같은 커밋에. lastmod 날짜는 안 바뀌는 것이 정상.
- 🔴 **루트 `_headers` 를 고치면 `npm run sync:public` 을 같은 커밋에** — `scripts/sync-legacy-static-to-public.mjs:59` 가 `_headers` 를 미러 목록에 둔다. 빠뜨리면 로컬 게이트는 **전부 통과하고** CI `Static guards` 만 떨어진다(PR-A 에서 실제로 물렸다).
- 로컬 상시 실패 3건(이 작업과 무관): `build:worker`/`build:cf` 는 `workers-og` 미설치, `home-yehwa-motifs.static.test.js` 는 CRLF 헛실패, `sync:public` 뒤 `.ignore` 가 줄바꿈만 바뀐 채 dirty(내용 diff 0줄 — 커밋하지 말고 `git checkout -- .ignore`).
- 나머지(결제 게이팅·`sync:public`·워크트리)는 CLAUDE.md 와 메모리가 자동 로드한다.

## 검증

```
npm run lint && npm run typecheck && npm run check:quick -- --skip-build
npm run test:node
```

## 모르는 것

- PR-E 의 왕복 수동 시나리오(`/diary` 저장 → 셸 모달 확인 → 모달 저장 → `/diary` 확인)를 누가 돌릴지 미정. 자동화 경로가 없으므로 사용자에게 맡길지 물어야 한다.
