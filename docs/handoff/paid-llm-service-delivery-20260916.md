---
status: active
updated: 2026-09-19
next: "운명 찻집 숙요점 궁합 상담 27행(fortune-tea-house-sukuyo-compatibility-consultation, 200코인/₩20,000)까지 mock A~F(경계 있음) 완료. 27행 상세는 docs/verification/fortune-tea-house-sukuyo-compatibility-paid-delivery-20260919.md. 26행이 인계한 첫 일 — 숙요 본명숙 계산 근거(sukuyoCompatibility 초안)가 닫힌 채로도 상담문이 완주하는지를 표의 기재를 믿지 말고 실측 — 을 그대로 했고 **완주했다**. 🔴 27행은 사주 계열이 아니라 isSajuFamilyMode(490행) 밖이고, 25·26행이 세운 assertSajuCalculationBasis 는 5433행에서 숙요를 그대로 통과시킨다. 실측 3케이스(본인 생년월일 1991-02-31, 상대 생년월일 공백, 본인 음력 1960-02-30)가 전부 HTTP 200·저장 completed·llmCalls 8 로 완주했고 배포된 sukuyoCompatibility 의 title·summary·strengths·cautions 는 전량 LLM 창작이었다(정상 대조군도 200·8회로 동일해 겉으로 구분 불가). 원인 사슬도 실행으로 확정했다: parseFortuneTeaSukuyoBirthDate(679행)가 달력 기준과 무관하게 그레고리력 왕복(Date.UTC + getUTC 비교)과 1900~2100 범위로만 날짜를 읽고 → 실패하면 prepareFortuneTeaSukuyoAstronomy 가 if (!birth) continue 로 조용히 건너뛰고(727행) → calculateFortuneTeaSukuyoPerson 의 예외를 buildFortuneTeaSukuyoCompatibility 의 catch 가 삼켜 available:false 초안이 되고 → **찻집 숙요의 품질 게이트 블록 전체가 3365행의 if (fallback.sukuyoCompatibility?.available) 에서 통째로 꺼진다**(신원 대조·8,000자 하한·앵커 검사가 전부 사라지고 공통 길이 검사만 남는다). 25행(낱말 대조)·26행(블록 진입 조건)에 이은 게이트 자가 비활성의 세 번째 형태다. 🔴 숙요는 사주·타로와 달리 서버 전용 계산이다 — sukuyoCompatibilityAdapter.ts 의 calculatePersonSukuyo(123~132행)가 달 황경(options.userMoonLongitude)을 받아야 본명숙을 세우는데 buildConsultResult.ts:746 이 그 옵션을 넘기지 않으므로 **정상 입력에서도 화면의 로컬 초안은 항상 available:false** 다. 그래서 26행식 초안 플래그 대조를 화면에 그대로 옮기면 모든 숙요 상담이 막힌다(옮기기 전에 어댑터 실행 경로로 확인했다). 도달 경로도 추정이 아니라 전수 열거다: lib/korean-calendar 로 1940~2010 실재 음력 25,169일을 만들어 서버와 같은 규칙을 적용하니 92일(0.37%)이 거부됐고 전부 음력 2월 29·30일이었다. QuestionInputScene.tsx:759 는 두 사람 birthDate 가 비었는지만 보므로 그 생일은 결제까지 도달한다. 수정은 fail-closed 3겹이다: ① assertSukuyoCalculationBasis(5437행, 호출 5514행 — 26행 가드 바로 다음 줄, runGeneration try 첫 부분)가 숙요 모드에서 초안의 sukuyoCompatibility.available 이 true 일 때만 통과하고 아니면 422/FORTUNE_TEA_HOUSE_SUKUYO_BASIS_MISSING 을 던진다(기존 실패 경로를 타므로 실측에서 applyCalls 0·저장 generation_failed·llmCalls 0), ② mergeFortuneTeaSukuyoCompatibility(2690행)가 available 을 서버 계산값으로 고정해 초안에 available:true 만 얹어 게이트를 건너뛰는 위조 경로를 막는다(이 줄이 없으면 가드도 게이트도 위조값을 믿는다), ③ 화면은 서버 파서와 같은 규칙만 복제한 isServerReadableSukuyoBirthDate(542행)로 localPreviewResult 대입 **앞**(1119행)에서 던져 결제창을 열지 않는다(대입 뒤면 catch 의 로컬 프리뷰 분기가 오류를 삼켜 가짜 성공이 된다 — 25행에서 확인한 자리 그대로). 🔴 가드 설계에서 배운 것: 처음에는 가드에 두 사람의 astronomySukuyo 존재 검사를 함께 두었는데, 변이를 넣어 보니 merge 의 available 고정과 서로를 가려 **어느 쪽 변이도 잡히지 않았다** — 겹치는 조건은 선언만 된 가드가 된다. 겹치는 절을 걷어내고 3절 구성으로 정리한 뒤 변이 3종이 전부 물리는 것을 확인했다(가드 조건을 if (true) return; 으로 고정 → 신규 2건 실패/93 통과, merge 의 available 고정 줄 삭제 → 위조 회귀 1건만 실패, 가드 호출 5514행 삭제 → 2건 실패/93 통과, 매번 국소 편집으로 원복 후 95/95). 🔴 변이 원복에 git checkout <파일> 을 쓰지 않는다(25행 사고). 신규 회귀 2건은 __tests__/worker/fortune-tea-house-honey-drops.test.js 에 이웃 크기대로 넣었고(계산이 닫히면 422·callGeminiText 0회 / 초안에 위조된 available 무시), 반대 방향(정상 요청이 막히지 않음)은 기존 sukuyo completes saved groups… 테스트가 200·LLM 8회로 그대로 맡는다. 나머지 축: A 는 featureKey 를 서버가 expectedFortuneTeaHouseFeatureKey(1072행)에서 도출하고 200코인=₩20,000 이 레지스트리 245·consultPricing 31~35·PAYMENT_POLICY 42 에서 일치(26행과 같은 금액이지만 별도 키임이 PAYMENT_POLICY 52행에 명시), C 는 후불 과금 무분기(apply 는 저장 성공 뒤 1회), D 는 23행 수정을 코드 변경 없이 상속(/pending 4767행이 requestPayload 통째 복원 + 화면 311행이 sukuyo 복원), E 는 교차상품 fixture 19행 등재, F 는 숙요 전용 3그룹(sukuyo-summary 3,000 / strengths 2,000 / cautions 2,000)+공통 5그룹=8그룹이라 POST 당 4그룹(4204행) 기준 2회 POST·LLM 8회다(26행의 15그룹·4회·16회와 다르다). 품질 게이트 전량 모킹 함정은 이번에도 해당 없음(신규 회귀가 실제 handleFortuneTeaHouseRoutes 에 POST 한다). 측정: honey-drops 95/95, 찻집 워커 6스위트 125/125, 찻집 화면 node --test 4파일 31/31, sitemap:generate 1,282 URL(원장 갱신 1건, .tsx 와 같은 커밋), check:fast exit 0(test:jest 281스위트 3,977/3,977). 전부 mock 이며 실호출은 0회다. 커밋은 8deacfaad(가드+위조 차단+화면+회귀 2건+sitemap)와 e87cc3248(문서)다. 🔴 push 한 e87cc3248 의 main CI 는 Critical checks·Build Pages and Worker·Typecheck and lint·gitleaks·Risk tier·AI locale pipeline invariants 가 success 인데 **CI required 는 failure** 다 — Static guards 와 Main drift 가 둘 다 마지막 verify:public-mirror-fresh 한 스텝에서만 멈췄고(미러가 소스보다 낡았다, 재생성 대상 7개: index.html·public/index.html·public/static/index.html·public/{en,ja,zh,zh-tw}/index.html), 이는 내 푸시 이전 tip d6ad24bb8 에서 이미 **동일한 7개 목록으로 failure** 였으며 내 두 커밋과 그 7개의 교집합은 0건이다(유래 후보는 옆 세션의 9812d039f — 자미 동물 패널 수정이 index.html·js/core/index-inline-runtime.js·js/saju-engine.js 와 미러를 함께 건드렸다). 같은 잡의 앞 스텝인 verify:sitemap-drift·숙요/명리 달력 가드·결제 가드는 전부 통과했다(잡이 그 뒤까지 진행한 것이 증거). 공유 체크아웃에 code-destiny-50 의 미커밋 index.html·js/**·public/** 이 살아 있어 여기서 sync:public 을 돌려 커밋하면 그 세션의 진행 중 작업이 섞이므로 재생성을 대신 하지 않고 원인·목록·근거를 그 세션에 전달했다(원칙 14). 🔴 28행 세션은 시작 전에 origin/main 의 Main drift 가 해소됐는지부터 확인하고, 아직 빨간색이면 그 원인이 여전히 index.html 미러인지(= 내 축이 아닌지) 재생성+diff 로만 판정한다. 🔴 실제로 옆 세션이 64f55a98a 로 그 7개를 전부 재생성해 커밋했는데도 341b722e4 의 Static guards·Main drift 가 **같은 7개 목록으로 다시 failure** 였고 CI required 도 failure 로 확정됐다(27행에 닿는 레인 Build Pages and Worker·paid-flow-gates·Typecheck and lint·gitleaks·scope·Risk tier 는 전부 success 이고 Static guards 안에서도 verify-sukuyo-role-direction 까지 OK). 즉 main 은 이 상속된 미러 드리프트 때문에 빨간 상태로 인계되며 원인은 27행이 아니다. 관측 하나를 같이 넘겼다 — 검사기가 지목한 목록에 원본 index.html 자신이 들어 있고 index.html 은 styles/*.css 의 크리티컬 CSS 를 인라인으로 품으므로, 고치는 쪽은 sync:public 결과를 같은 커밋에 담고 출력이 더 바뀌지 않는 고정점까지 확인해야 닫힌다. 🔴 옆 세션이 같은 체크아웃에서 index.html·js/**·public/**·marketing/** 을 쓰고 있어 이번에도 경로 지정 스테이징만 했고 그 파일들은 건드리지 않았다. 범위 밖 관측 3건: 🔴 실재하는 음력 2월 29·30일 생일(0.37%)은 이번 수정으로 창작 대신 422 를 보게 됐을 뿐 여전히 숙요 상담을 받지 못한다 — 파서를 범위 검사로 완화하면 열리지만 resolveSolarMoment(worker/lib/sukuyo-astronomy.js 129~143행)의 RangeError 가 handleConsult 바깥 catch 로 새어 500 이 되는 경로를 함께 다뤄야 해서 별도 변경으로 분리했다, 2692행이 available:false 일 때 LLM 의 relationType·scores 를 막지 않는 것(현재는 체크포인트 그룹 정의가 그 필드를 안 넘겨 우연히 막힌다), 8,000자 하한과 앵커 검사가 available 에 매달린 게이트 구조 자체는 그대로다. **다음은 28행 휴먼 디자인 프리미엄 리포트(human-design-report, 100코인/₩10,000, 레지스트리 269행)이다** — 🔴 23~27행의 찻집 계보가 여기서 끝난다: 정본이 worker/routes/human-design-report.js(약 36KB)로 완전히 다른 라우트라 깨어남 복구·후불 과금·체크포인트 구조를 하나도 상속하지 못하니 A~F 를 처음부터 다시 세운다. 기존 검사는 __tests__/worker/human-design-paid-delivery.test.js 와 human-design-report.start-revive.test.js 두 개이며, 28행의 첫 일은 그 두 스위트가 실제로 무엇을 태우는지 읽고 25~27행에서 세 번 연속 나온 패턴 — 계산 근거가 닫힌 채로도 유료 리포트가 완주하는가, 품질 게이트가 계산 값을 대조하는가 낱말만 보는가 — 를 이 라우트에서 실측하는 것이다. 🔴 단 휴먼 디자인 **계산 엔진 자체는 외부 검증이 끝난 코드라 손대지 않는다**(엔진 정확성은 이 축이 아니다). npx jest 금지, npm run test:jest --(scripts/run-mock-tests.mjs)로만 돌린다. 남은 것은 28행부터 74행(영냥이 융합 6체계)까지 47개 + 후속 3경로다."
previous_next: "운명 찻집 사주 궁합 상담 26행(fortune-tea-house-saju-compatibility-consultation, 200코인/₩20,000)까지 mock A~F(경계 있음) 완료. 26행 상세는 docs/verification/fortune-tea-house-saju-compatibility-paid-delivery-20260919.md. 25행이 인계한 첫 일 — sajuCompatibility 의 fail-open 을 실측으로 닫는 것 — 을 그대로 했다. **초안 모양 질문의 답은 '둘 다' 였다**: QuestionInputScene.buildInput(688~733행)이 궁합 폼의 나 값을 최상위 birthDate·birthTime·gender·calendarType 으로 복사해 보내므로 buildConsultResult 가 궁합을 내부적으로 saju 로 접어(739~740행) 최상위 saju 를 채우고(896행) 별도로 sajuCompatibility(747행)를 붙인다 — 즉 본인 명식은 draft.saju 이자 draft.sajuCompatibility.user.saju 의 두 사본이고, 상대 명식은 draft.sajuCompatibility.partner.saju 하나뿐이다. 추정이 아니라 buildConsultResult.ts 를 esbuild 로 CJS 묶어(--loader:.wasm=empty 필요) 직접 실행한 5케이스 실측이다: 정상 양력/양력은 전부 true, 상대 미입력·sajuCompatibility 블록 부재·상대만 음력 1960-02-30 은 partner.saju 만 false(본인 saju 는 true), 본인만 음력 1960-02-30 이면 draft.saju 와 compat.user.saju 가 false — 어느 경우에도 예외는 던지지 않는다(25행에서 실측한 sajuAdapter 3중 catch 가 흡수). 닫은 fail-open 은 2건이다. ① buildSajuPersonProfile(1887~1890행)이 personSaju.available !== true 면 undefined 를 돌려줘 buildSajuFactInput 의 partnerProfile(1989행)이 프롬프트에서 통째로 빠지고 dataCheck.partnerDayMaster 가 빈 문자열이 된다(2007행) — 상대 명식이 한 글자도 안 실린 채 두 사람의 사주를 각각 읽고 대조하라는 지시만 남으므로 LLM 이 본인 명식 하나로 궁합을 창작하고 200코인이 확정됐다. 도달 경로는 25행과 같다(음력 작은달 30일류 15.2% + QuestionInputScene:754 는 두 사람 birthDate 가 비었는지만 검사 + 상대 쪽은 화면에 명식 미리보기가 없어 사용자가 알 방법도 없음). ② 찻집 전체에서 명식 값을 본문과 대조하는 유일한 게이트 — 상대 이름과 상대 일간 천간이 본문에 실제 등장하는지 보는 3434~3453행 — 의 진입 조건이 fallback.sajuCompatibility?.available 이라, 궁합 블록이 닫히면 검사 자체가 건너뛰어진다. 나머지 게이트는 25행에서 확인한 대로 전부 낱말 대조다(일간/오행/십성 2개 이상, /대운|세운/, 궁합 전용 requiredTerms 궁합·상대 1662행). 수정은 25행과 같은 자리의 fail-closed 2겹이다: assertSajuCalculationBasis(5408~5422행, 호출 5491행)가 궁합 모드에서 draft.saju.available·sajuCompatibility.available·user.saju.available·partner.saju.available 이 모두 true 일 때만 통과하고 아니면 422/FORTUNE_TEA_HOUSE_SAJU_BASIS_MISSING(두 사람의 생년월일로… 메시지), 화면은 localPreviewResult 대입 앞(1096~1104행)에서 같은 조건을 던져 결제창을 열지 않는다. 오차단이 아닌 근거도 실측이다 — 클라 어댑터가 두 명식이 열렸을 때만 available:true 를 세우고(sajuCompatibilityAdapter 168·190행) 아니면 buildUnavailable 로 떨어지므로 정상 화면 초안은 항상 조건을 만족하며, 서버 폴백 buildMinimalDraft(2509행)에는 sajuCompatibility 키 자체가 없어(전수 grep 0건) 초안 없이 API 를 직접 때리는 경로도 undefined 로 fail-closed 다. 게다가 git grep 결과 궁합 모드를 실제로 태우는 워커 테스트가 0건이라 조여도 깨질 기존 계약이 없었다. 변이 3종 전부 물림: 모드 조건을 원래대로 되돌리면 정확히 3건 실패/7건 통과, compatReady 를 true 로 고정하면 상대 명식이 닫혔을 때 1건만 실패, available 절만 true 로 고정하면 궁합 블록만 닫혔을 때 1건만 실패 — 원복 후 11/11, git grep MUTATION- 잔재 0건. 🔴 변이 원복에 git checkout <파일> 을 쓰지 않는다(25행 사고). 신규 회귀 4건은 fortune-tea-house-saju-timing.test.js 에 넣었고(초안 없음/상대 닫힘/본인 닫힘/블록만 닫힘 → 422·ok:false·사주 명식 메시지·callGeminiText 0회, 그리고 두 명식이 열린 초안은 422 아님·LLM 실호출로 반대 방향까지 증명) test.each 한글 라벨을 attemptId 로 쓰면 헤더 ByteString 오류가 나므로 ASCII attemptKey 를 따로 둔다. 나머지 축은 A 200코인=₩20,000 이 레지스트리 244·consultPricing 26·PAYMENT_POLICY 41·integrity 테스트 41 에서 모두 일치하고 featureKey 는 서버가 1073행에서 도출, C 는 후불 과금 무분기(apply 5563행은 저장 성공 뒤 1회, 실패는 5498→5510 cancel — 가드를 runGeneration try 첫 문장에 둔 이유), D 는 /pending 4767행이 requestBody 통째 복원 + 화면 312행이 sajuCompatibility 복원이라 강등 없음, E 는 교차상품 fixture 19행 등재, F 는 isSajuFamilyMode 포함이라 요약1+필수9섹션+공통5=15그룹으로 25행과 동형(4회 POST/LLM 16회, apply 1회). 품질 게이트 전량 모킹 함정은 이번에도 해당 없음. 측정: 찻집 Jest 6스위트 123/123, 화면 node --test 8파일 50/50, sitemap:generate 1,282 URL(갱신 1건, .tsx 수정과 동일 커밋 3a0afc235), check:fast -- --committed-head exit 0(tier critical 자동 승격, test:jest 281스위트 3,974/3,974). 커밋은 3a0afc235(가드+화면+회귀3+sitemap)와 a9dfd1cfc(available 절+회귀1, 앞 커밋 주석의 100코인 오기를 200코인으로 정정)다. 🔴 작업 중 옆 세션(code-destiny-50)이 같은 체크아웃에서 index.html·js/**·public/** 을 쓰고 있어 경로 지정 스테이징만 했고 그 파일들은 건드리지 않았다. 0바이트 index.lock 이 6분간 남아 있었으나 배타 열기로 살아있는 점유자가 없음을 확인한 뒤에만 제거했다. 범위 밖 관측 3건: sajuCompatibilityAdapter 의 buildUnavailable(131~145행)이 본인 프로필을 input?.user 로 만들어 최상위 복사본과 출처가 어긋나 compat.user.birthDate 가 undefined 로 돌아오는 점(실패 경로 표시용 필드라 현재 도달 화면 없음), 값 대조가 여전히 상대 이름·일간 천간 1글자 수준인 점, 찻집 CSS 디자인 훅 지적 5건(이번 변경이 안 건드린 파일). **다음은 27행 운명 찻집 숙요점 궁합 상담(fortune-tea-house-sukuyo-compatibility-consultation)이다** — 같은 라우트·화면이라 D축은 또 상속이지만 🔴 27행은 사주 계열이 아니다: isSajuFamilyMode(490행) 밖이라 이번 명식 가드가 적용되지 않고 체크포인트 그룹도 sukuyo 전용 3개+공통 5개 구조이며(4161~4165행) 품질 게이트도 별도다. 27행의 첫 일은 숙요 본명숙 계산 근거(sukuyoCompatibility 초안)가 닫힌 채로도 상담문이 완주하는지를 25·26행과 같은 방식으로 실측하는 것이다. 표의 기재를 믿지 말고 변이로 가드가 무는지 직접 확인한다. npx jest 금지, npm run test:jest --(scripts/run-mock-tests.mjs)로만 돌린다. 남은 것은 27행부터 74행(영냥이 융합 6체계)까지 48개 + 후속 3경로다."
---

# 유료 LLM 생성·결제 후 전달 인수인계

## 기존 마스터 기록 — 2026-09-16

사용자 요청: “마스터 인연의 서 결제 이후 제대로 생성되는지 확인하고, 나머지 LLM 서비스를 하나씩 제대로 나오게 할 인수인계 문서.”

main 구현 기준 `77007dc4c1c931ecc148ab73069bf437b78473e9`. [전체 main CI](https://github.com/rei1237/codedestiny/actions/runs/35103084907)는 모든 lane·CI required 성공. [마스터 수정/검증 기록](../verification/master-love-codex-delivery-20260916.md)을 재사용하고 재구현하지 않는다.

**2026-09-16 재확인:** 마스터 서버 생성·암호화 구매 bootstrap·크론 복구 47개, 결과 복귀·원래 구매 재개·SDK 요청 계약 18개 통과. 개인/궁합 각 20장·모의 성공 호출20회·재열람0회. 입력 문자 감소9.7%/17.3%; 실제 청구 토큰·문장 의미는 미검증.

**운영 코드 반영 확인:** 시작 조회는 Pages/Worker 모두 `a3d1b471f319036deb416251a2116ef278097567`로 불일치였으나, 최종 읽기 전용 재조회에서 [Pages](https://code-destiny.com/version.json)와 [Worker](https://code-destiny.com/api/version)가 모두 수정본 `77007dc4c1c931ecc148ab73069bf437b78473e9`로 일치했다. [운영 릴리스 35105200682](https://github.com/rei1237/codedestiny/actions/runs/35105200682)의 정확한 SHA 배포·버전 검증도 success이며 staging job은 skipped다. 이번 세션이 배포한 것은 아니다. **코드 운영 반영과 모의 생성은 확인했으나 실 PG·실기기·청구 LLM·실고객 주문 완주 증거는 미검증**이다.

## 다음 작업

**2026-09-17 베다점 전문가 상담 11행 — mock 완료(경계 있음):** [행별 기록](../verification/vedic-ai-paid-delivery-20260917.md). 5~10행과 같은 클래스의 화면 재개 이벤트 배선 누락(`pageshow`·`focus` 미구독)을 시작 화면(`VedicAiClient.tsx` 수정 전 2996~3005행)과 결과 화면(`VedicAiResultClient.tsx` 수정 전 72~77행) 둘 다에서 재현·수정했다(+10/-2행, +9/-2행). 실제 계산 엔진(`calculateVedicAiChart`)을 정상 생년월일시로 직접 실행해 라그나·나크샤트라·다샤·그라하·바바가 에러 없이 산출되고 로컬 WASM 폴백(`swiss-wasm-local`)이 정상 동작함을 실측했다(A 단계). 신규 UI 행동 검사(`vedic-ai-wake-recovery.behavior.test.js`)는 실제 `useEffect` 콜백 소스를 TypeScript AST로 직접 추출해 `vm` 샌드박스에서 실행하는 방식으로 작성했고, 수정 전 코드에서 2/2 정확히 실패(`window:pageshow handler must be registered`) 확인 후 수정 적용해 2/2 통과로 전환하는 변이 검증까지 마쳤다. 백엔드(`worker/routes/vedic-ai.js`)는 5그룹(읽기 섹션 4 + 근거 흐름 1) 병렬 생성·접근 판정·저장 확인·조회 취소 차단을 기존 워커 회귀 116개와 1:1 대조해 신규 결함 없음을 확인했다. 교차 상품 저장·권한 스위트(`paid-completed-result-access-fixtures.mjs`)에서 "vedic" 문자열 부재로 등록 공백을 의심했던 이전 추정은 오진이었음을 직접 열람해 정정했다(11번째 항목에 이미 정상 등록). UI 2/2(신규)·워커 116/116·`sitemap:generate`는 `/vedic-ai/`가 `robots:noindex`가 아니라 사이트맵 대상임을 확인하고 같은 커밋에 재생성을 포함해 갱신 1건(`/vedic-ai/` 서명·lastmod만, 타 상품 영향 없음)·`check:fast` RED 승격 전체 게이트(`test:node` 1,407/1,407·`run-paid-gate-suite` 88/88·`test:jest` 277 suite·3,881/3,881) 전부 통과. 커밋 `108282e34`를 워크트리(`vedic-ai-mock-20260917-20260917-172000`)에서 만든 뒤 `git fetch origin main`으로 동료 세션의 문서 커밋 1개(`96bdd7a11`)를 확인하고 `git merge`로 병합(머지 커밋 `e215e541a`), 병합 후 `sitemap:generate` 재실행으로 드리프트 없음을 재확인한 뒤 push했다. push한 SHA(`e215e541a`)의 [체크런](https://github.com/rei1237/codedestiny/commit/e215e541a/checks)을 `gh api`로 직접 실측한 결과 `Static guards`(`Verify handoff frontmatter contract` 스텝)와 `CI required`가 failure였다. 로그 전문을 읽어 원인을 확정한 결과 **11행 자신의 변경이 아니라** `96bdd7a11`(동료 zh-TW 세션의 커밋, 병합으로 상속)의 `docs/handoff/2026-09-17-zh-tw-priority-followup.md`가 갖고 있던 `status: resolved`(허용값 `active|blocked|done` 외) 선행 결함이었다 — `96bdd7a11` 자체의 CI도 이미 failure였음을 `gh api`로 확인해 병합 이전부터의 결함임을 확정했다(9행 sitemap-drift처럼 추정이 아니라 실측으로 원인 소재를 가렸다). 문서 본문("4개 허브 전부 완료")과 의미가 같은 `done`으로 교체해 커밋 `3bcf432fc`로 수정·push했고, 이 SHA의 [체크런](https://github.com/rei1237/codedestiny/commit/3bcf432fc/checks)에서 `Static guards`·`CI required` 포함 20개 체크런 전부 success 또는 skipped(실패 0)를 확인했다. **D(실제 화면 증거)와 F(전용 전후 diff 스크립트)는 이번 차례에도 만들지 않았다 — 5~10행과 동일한 남은 경계.**

**2026-09-17 점성술 전문가 상담 10행 — mock 완료(경계 있음):** [행별 기록](../verification/astrology-ai-paid-delivery-20260917.md). 5~9행과 같은 클래스의 화면 재개 이벤트 배선 누락(`pageshow`·`focus` 미구독)을 시작 화면(`AstrologyAiClient.tsx` 수정 전 1586~1605행)과 결과 화면(`AstrologyAiResultClient.tsx` 수정 전 476~481행) 둘 다에서 재현·수정했다(+10/-2행, +9/-2행). 신규 UI 행동 검사(`astrology-ai-wake-recovery.behavior.test.js`)는 실제 `useEffect` 콜백 소스를 TypeScript AST로 직접 추출해 `vm` 샌드박스에서 실행하는 방식으로 작성했고, 수정 전 코드에서 2/2 정확히 실패(`window:pageshow handler must be registered`) 확인 후 수정 적용해 2/2 통과로 전환하는 변이 검증까지 마쳤다. 백엔드(`worker/routes/astrology-ai.js` 906~1928행 전체 정독)는 섹션 계약(6섹션×3400~5000자, 병렬 1차 생성+체크포인트+2차 부족분 재시도)·접근 판정·저장 확인·조회 취소 차단을 기존 워커 회귀 26/26 및 교차 상품 스위트와 1:1 대조해 신규 결함 없음을 확인했다(호출부 없는 `generateConsultation`(1259행) 미사용 함수 1건은 범위 밖으로 기록만). UI 6/6(신규2+기존4)·워커 26/26+교차상품 필터매칭 2/2·`sitemap:generate` 갱신 0건(`/astrology-ai/`는 `robots:noindex`라 애초 사이트맵 비대상)·`check:fast` RED 승격 전체 게이트(`test:jest` 277 suite·3,881/3,881) 전부 통과. 커밋 `d12980b05`를 워크트리(`astrology-ai-mock-20260917-160308`)에서 만든 뒤 `ListAgents`로 동시 세션(`code-destiny-a0`, 확인 시점 idle)을 확인하고 그 세션의 미푸시 문서 커밋 2개(`873bdf254`·`03a1c9b53`)를 보존하며 `git merge`로 병합(머지 커밋 `bf4996793`), 병합 후 `sitemap:generate` 재실행으로 드리프트 없음을 재확인한 뒤 push했다. push한 SHA(`bf499679381665d6e1933188b97d62634d8674d9`)의 [체크런](https://github.com/rei1237/codedestiny/commit/bf499679381665d6e1933188b97d62634d8674d9/checks)을 `gh api`로 직접 실측해 28개 중 `CI required`·`Critical checks`·`Build Pages and Worker`·`Typecheck and lint`·`Static guards`·`Main drift`·`gitleaks`·`Risk tier`·`AI locale pipeline invariants` 포함 16개 success·나머지 12개는 스테이징/릴리스/롤백 조건부 매트릭스 잡으로 skipped·실패 0을 확인했다(백그라운드 `Monitor` 폴링이 15분간 이벤트 0건으로 만료해 신뢰하지 않고 직접 조회로 대체). **D(실제 화면 증거)와 F(전용 전후 diff 스크립트)는 이번 차례에도 만들지 않았다 — 5~9행과 동일한 남은 경계.**

**2026-09-17 자미두수 9행 — mock 완료(경계 있음) + sitemap-drift 오진 정정:** [행별 기록](../verification/ziwei-ai-paid-delivery-20260917.md). 시작 전 "9행이 4행(`ziwei-deep-pdf`)과 화면을 공유한다"는 가정을 대조했으나 근거가 없었다 — 정본 파일(`worker/routes/ziwei-ai.js` vs `worker/routes/ziwei-deep-report.js`)·진입 화면(`/ziwei-ai/` vs `/ziwei/chart/`)·가격 레지스트리 항목이 모두 독립이라 두 상품은 애초에 별개였다(우연히 둘 다 300코인/30,000원일 뿐). 5~8행과 같은 종류의 화면 재개 이벤트 배선 누락(`pageshow`·`focus` 미구독)을 `ZiweiAiClient.tsx` 904~908행에서 재현·수정했다(형제 화면과 동일한 4개 이벤트로 확장, +4/-1행). 신규 UI 행동 검사 1건을 변이 검증(수정 되돌리기 → `AssertionError` 실패 확인 → 복원 → 5/5 재통과)까지 완료했고, 백엔드는 `worker/routes/ziwei-ai.js` 결제·생성·조회 경로 전체를 이번 세션에 처음 정독해 기존 워커 회귀 19/19가 실제 코드와 1:1 대응함을 확인했다(새 백엔드 결함 없음). UI 5/5(신규1)·워커 19/19·`test:jest` 277 suite·3,881/3,881, 커밋 `14e197394`.

🔴 **sitemap-drift CI 실패를 최초에 오진했다 — 실측 없이 부정 단언한 사례(코딩 원칙 8 위반).** push 직후 `CI required`가 `Static guards`의 `Verify the tracked sitemap matches its sources` 스텝에서 실패했고, 이를 "9행과 무관한 선행 드리프트"로 판단해 `git log -1 -- config/sitemap-lastmod.json`이 가리킨 `246f34ad0`(numerology 커밋)을 원인으로 지목·보고했다. 사용자가 "사전 결함을 복구해달라"고 요청해 실제로 `npm run sitemap:generate`를 돌려 diff를 뜬 결과 바뀐 항목은 `/ziwei-ai/` 서명 1개뿐이었고, `git show 246f34ad0:config/sitemap-lastmod.json`으로 그 시점 원장을 직접 열람하니 서명이 이미 정상이었다 — **실제 원인은 이번 행 자신의 `14e197394`**(`ZiweiAiClient.tsx` 콘텐츠 변경 후 같은 커밋에서 `sitemap:generate`를 안 돌림)였다. "원장을 마지막으로 건드린 커밋"은 원인 판정 근거가 못 되며, **재생성→diff가 유일하게 신뢰할 수 있는 진단법**이다. `867153e58`(`fix(sitemap): regenerate ziwei-ai lastmod after pageshow/focus edit`)로 5개 파일을 재생성·커밋해 바로잡고 `verify:sitemap-drift` 재실행으로 OK를 확인했으며, 잘못 보고했던 행별 기록 본문도 `8463df7ed`로 정정했다. 이 커밋의 CI는 `Static guards`(원인 스텝을 포함한 잡) success를 확인했고 `CI required` 애그리게이트는 확인 시점 기준 진행 중이었으나 실패한 체크런은 없었다.

**교훈(일반화):** 5~9행 전부가 같은 클래스의 결함(화면 재개 이벤트 배선 누락)이었던 것처럼, **클라이언트 컴포넌트를 고치는 행은 그 커밋에 `npm run sitemap:generate`를 함께 담지 않으면 다음 행에서 CI가 그 드리프트를 대신 잡아낸다.** 10행부터도 화면 파일을 고치면 같은 커밋에 재생성을 포함할 것.

**2026-09-17 인생 총운 8행 — mock 완료(경계 있음):** [행별 기록](../verification/life-fortune-ai-paid-delivery-20260917.md). 7행과 같은 `worker/routes/life-book-ai.js`를 쓰지만 유일한 실제 `handleStart` 실행 테스트(`__tests__/ui/life-book-paid-delivery.behavior.test.js`)가 `isLifeFortuneInput`을 영구 `false`로 고정 스텁해, 총운 전용 사주 완전성 게이트(`hasRequiredLifeFortuneSaju`, 결제·환불이 걸린 RED 분기)가 이 테스트 파일이 존재한 이래 단 한 번도 실행된 적이 없었음을 발견했다 — "공유 컴포넌트라는 이유로 결함이 없다고 가정하지 않는다"는 인수인계 지시가 실제로 잡아낸 공백이다. 실제 `isLifeFortuneInput`·`hasRequiredLifeFortuneSaju` 함수를 로드해 (a) 구조 불완전 사주(`majorLuck.cycles` 빈 배열) 입력 시 422 `SAJU_CALCULATION_FAILED`·환불 1회·LLM 호출 0회·문서 잔존 0, (b) 완전한 사주 입력 시 정상 202 첫 웨이브(4/15장) 진행·추가 환불 없음을 새 테스트로 검증했다(`+38행`, 커밋 `f151ee39dd32c6a16499884cf63afebe7de7beb7`). 게이트 요구 필드를 실제 계산기(`worker/lib/life-book-ai-saju.js`)가 정상 입력에서 채우는지, 유일한 이론적 거짓-음성(성별 미상→`majorLuck.available:false`)을 `normalizeConsultationInput`의 총운 전용 `gender==="unknown"` 거부가 사전에 막는지 소스 대조로 확인했다(설계상 맞물림, 결함 아님). 장애·동시성 메커니즘(`reserveProviderCallOnce`·`runWithConcurrency`·`releaseSectionLock`·`saveLifeBookState`·`finishLifeBookDelivery`)은 grep으로 모드 분기 0건을 확인해 7행 커버리지가 8행에도 그대로 적용됨을 확인했다(재현 불필요). 저장·권한은 `isStoredPaidResultRevoked`가 저장 문서 자신의 featureKey로 판정해 모드 안전함과 교차 상품 `paid-completed-result-access.test.js`(96/96, 재실행 재확인)의 독립 등록을 확인했다. 가격/카드 노출 등 정적 계약은 기존 `verify-life-book-ai-flow.mjs` 재실행으로 재확인했다(신규 아님). UI 행동 검사 16/16(기존15 무회귀+신규1), 교차상품 96/96, `check:fast` RED 승격 전체 게이트 — `test:jest` 277 suite·3,880/3,880 포함 — 통과(종료 코드 0). 문서 커밋 `31ed6264a`까지 워크트리에서 완료한 뒤 `origin/main`이 다른 세션의 무관한 `c25e5bbb5`(결제 웹훅 trailing slash 수정)만큼 앞서 있어 `git merge --no-ff`로 병합했다(머지 커밋 `e68a309c2ca877fbdf884b0f4f3a471a25acf414`, marketing 미커밋 84개 보존 확인 후 push). main·origin 끝점(동일 SHA) [CI](https://github.com/rei1237/codedestiny/actions/runs/35183457403)는 체크런 21개 중 `CI required` 포함 13개 success·7개 skipped(비동기 `Deploy staging` 등 비대상)·실패 0을 확인했다. **D(실제 화면 증거)와 F(총운 전용 전후 호출 diff)는 이번 차례에도 만들지 않았다 — 7행과 동일한 남은 경계.**

**2026-09-17 인생의 책 7행 — mock 완료(경계 있음):** [행별 기록](../verification/life-book-paid-delivery-20260917.md). 마스터·초융합·자미 심층·네오·나크샤트라와 같은 client-side 깨어남 복구 누락(`pageshow`/`focus` 미연결)을 인생의 책 결과 화면(`app/life-book-ai/result/LifeBookAiResultClient.tsx`)에서 재현·수정했다(4행 diff, 커밋 `c7793e21905d46e114d93c4979a5c6d30072864e`). 이 결과 화면 컴포넌트는 8행(`life-fortune-ai-consultation`)과 공유되어 수정 혜택이 8행에도 그대로 적용되지만, 이번 완료 표시는 요청 범위인 7행에 한정했다. 시작 화면(`LifeBookAiClient.tsx`)은 애초에 pageshow/focus/online/visibilitychange 리스너가 없고, 생성 중 백그라운드 진입 시 결과 화면으로 능동 이관하는 설계(`document.hidden` 분기)임을 코드 대조로 확인했다 — 버그가 아니라 새 테스트를 만들지 않았다. 구매 재개/멱등(`findPaidPayment`/`resolveServerAccess`/`billingContractMatches`)과 GET 재확인 대 POST 재개의 비대칭(`isStoredPaidResultRevoked`는 GET `handleResult`에서만 호출, POST `resumeSessionId` 재개 경로는 재확인 안 함)은 기존 `life-book-paid-delivery.behavior.test.js`·교차 상품 `paid-completed-result-access.test.js`로 대조 확인했다(신규 결함 없음, 나크샤트라·네오와 동일 설계). 전용 크론 복구 태스크는 없고 클라이언트 자신의 `resumeSessionId` 재개(예산 캡 `resumeCallsRef.current>=12`)가 유일한 복구 경로임을 확인했다(네오·나크샤트라와 동일 무크론 전례). 신규 UI 행동 검사(`__tests__/ui/life-book-wake-recovery.behavior.test.js`) 1건을 `git stash`로 수정 전 코드를 임시 복원해 재현 1/1 실패(`window:pageshow` 핸들러 미등록) → 수정 후 통과로 전환 측정했고, 관련 UI 스위트 25/25·worker+교차상품 109/109(섹션 생성 13 + 교차 상품 접근 96) 통과(무회귀). `npm run check:fast`는 결제 인접 파일 수정으로 RED 자동 승격돼 전체 게이트로 실행됐고(590초 전경 제한을 넘겨 백그라운드 전환, 종료 코드 0) 저장 로그에 `verify:staging-llm-mock`부터 `test:jest`(277 suite·3,880/3,880)까지 통과가 남아 있다 — 그 앞 구간(lint·typecheck·test:node·paid-gate-suite·sitemap-drift)은 590초 전경 구간에서 실행돼 종료 코드 0으로 이어졌으나 백그라운드 로그엔 남지 않아 개별 수치는 인용하지 않았다. source `c7793e21905d46e114d93c4979a5c6d30072864e`를 직전 커밋 `0cbe50794`(다른 세션의 love-secret-ai 수정) 위에 별도 진행 없이 그대로 fast-forward push했다. main·origin 끝점(동일 SHA) [CI](https://github.com/rei1237/codedestiny/actions/runs/35179949320)는 체크런 21개 중 `CI required`·`Static guards`·`Typecheck and lint`·`gitleaks`·`Build Pages and Worker`·`Critical checks`·`Risk tier`·`Main drift`·`AI locale pipeline invariants` 등 13개 success·7개 skipped·실패 0을 확인했다(비동기 `Deploy staging` 1건은 확인 시점 진행 중이었고 대기 대상 아님). **D(실제 Playwright 화면 렌더 증거)와 F(전용 전후 diff 스크립트)는 이번 차례에도 만들지 않았다 — 나크샤트라·네오와 동일한 남은 경계.** 8행(`life-fortune-ai-consultation`)은 다음 세션이 같은 컴포넌트의 수정을 전제로 8행 고유 계약만 A~F 검사하면 된다.

**2026-09-17 나크샤트라 6행 — mock A~F 완료(경계 있음):** [행별 기록](../verification/nakshatra-ai-paid-delivery-20260917.md). 마스터·초융합·자미 심층·네오와 같은 client-side 깨어남 복구 누락(`pageshow`/`focus` 미연결)을 나크샤트라 결과 화면(`app/nakshatra/ai/NakshatraAiClient.tsx`)에서 재현·수정했다(10행 diff, 커밋 `0ef57a367e66fec2b57849671f132c750468bffe`). 나머지 구매 재개·생성·장애·저장/권한 경로는 기존 `nakshatra-paid-delivery.test.js`(3-wave·6변형 저장 장애·체크포인트·동시 재시도 등)와 교차 상품 공유 검사 `paid-completed-result-access.test.js`로 대조 확인했다(신규 결함 없음). completed 문서의 POST 재생이 취소·환불 재확인을 건너뛰는 것은 버그가 아니라 기존 테스트(`'does not replace historical short completed results'`)가 명시적으로 보증하는 설계임을 확인했다. 신규 UI 행동 검사(`__tests__/ui/nakshatra-wake-recovery.behavior.test.js`) 1건(수정 전 재현 1/1 실패 → 수정 후 통과로 전환 측정), worker+교차상품 118/118, `verify:nakshatra-flow`/`verify:nakshatra-ai-flow`/`verify:nakshatra-premium` 전부 통과, `check:fast -- --committed-head`(base `755949af0`→head `0ef57a367`) 전체 파이프라인 — `run-paid-gate-suite` 88/88·lint 0·`verify:sitemap-drift` OK·typecheck 0·Node 1,400/1,400·Jest 277 suite·3,880/3,880 — 이 한 번에 막힘 없이 통과했다(네오를 막았던 sitemap-drift 이번엔 없음). 작업 중 다른 세션의 `perf(analytics)` 커밋(`aeeb9f714`)이 위에 이어져 두 커밋을 함께 push했다. main·origin 끝점 `aeeb9f714884771b81a82ace8d8a2f2f43740469`의 [CI](https://github.com/rei1237/codedestiny/actions/runs/35176230590)는 `CI required`·`Static guards` 포함 check-run 24개 전부 success 또는 skipped(실패 0)로 확인했다. **D(실제 Playwright 화면 렌더 증거)와 F(전용 전후 diff 스크립트)는 이번 차례에도 만들지 않았다 — 네오와 동일한 남은 경계.**

**2026-09-17 네오 5행 — 우선 5개 시나리오 mock 완료(부분):** [행별 기록](../verification/neo-operation-room-paid-delivery-20260917.md). 인수인계가 지정한 5개 우선 재현(승인 직후 첫 생성 전 종료·1묶음 저장 뒤 문서 종료·pageshow/bfcache/focus 단독 복귀·checkpoint/완료 확인 유실·정상 completed 대 취소 구매 GET/POST 대조) 중 pageshow/focus 복구 리스너 누락 1건을 재현·수정했다(`NeoOperationRoomPage.tsx`/`NeoOperationRoomResultPage.tsx`, 마스터·초융합과 동일 패턴). 나머지 4건은 기존 통과 테스트로 이미 올바름을 대조 확인했다(새 결함 없음). 신규 행동 검사(`__tests__/ui/neo-wake-recovery.behavior.test.js`) 2/2, 기존 neo-paid-resume 5/5(7/7), worker 76/76, typecheck 0, Node 1,399/1,399, Jest 276 suite·3,874/3,874, build:worker dry-run 0, verify:neo·verify:neo-output-safety 회귀 없음. source `8c9f49664`를 안전 워크트리에서 main에 병합(`2dc2ea9cc`)하고 그사이 origin에 먼저 올라온 문서 커밋(`506579901`)도 병합(`2e07bc0bb`)해 push했다. 이 SHA의 PR CI는 무관한 기존 `verify:sitemap-drift`로 실패했으나 이후 다른 세션 커밋에서 자연 해소됐고, 현재 main 최신 커밋(문서 커밋 `9f8865c52`)의 전 레인이 success임을 확인했다([CI 35169520076](https://github.com/rei1237/codedestiny/actions/runs/35169520076)). **D(실제 Playwright 화면 렌더 증거)와 F(전용 전후 diff 스크립트)는 이번 차례에 만들지 않았다 — 남은 경계.** root marketing dirty 84개는 병합 전후 동일하게 보존했다.

**2026-09-17 심화 자미 PDF 4행 — mock A~F·main CI 완료:** [행별 기록](../verification/ziwei-deep-paid-delivery-20260917.md). 실행 완료 본문 누락·취소 구매 POST 재열람·화면 재개·문서 종료 뒤 서버 실행·완료 확인 유실·Storage 예외·공급자 완료 표시 7종을 재현/수정했다. 관련 Node62/Jest125·실제 고객8case·PDF62페이지/15장 전체 본문 추출·렌더 통과. 호출15→15·prompt36,012→36,012자. source `acdf20c386004505e643fd3db7976f640b655fdf`를 main fast-forward/push했고 [동일 SHA main CI 35123293719](https://github.com/rei1237/codedestiny/actions/runs/35123293719)는 모든 lane·CI required·Node1,397/Jest3,874 성공이다. root dirty84개·파일별 SHA256도 반영 전후 같았다. 다른 세션의 layout/fortune-chat과 Threads 작업은 보존했다. Threads 추가 뒤 VM mock 누락을 재현해 Node62와 공식 check:fast critical·Node1,397/Jest3,874를 통과했다. 반영 직전 다른 세션이 같은 보완을 `851cbe758`로 전달해 이를 보존/rebase했다. 그 SHA CI의 새 Threads 문서 frontmatter 누락 1건만 별도로 보완했다. 전달 SHA `1a622b55595daa7ed5cf1323d49c6a5415dd55d0`의 [main CI 35125744683](https://github.com/rei1237/codedestiny/actions/runs/35125744683)는 Static·CI required success이며 문서 tier의 Type/Build/Critical은 skipped다. **4행을 mock 완료로 체크했다.** 네오부터 구매70키+후속3경로는 개별 A~F 미실행이다.

**2026-09-17 초융합 mock A~F 완료:** [행별 검증 기록](../verification/fusion-paid-delivery-20260917.md)을 추가했다. source `c2c4ce56f543fc785ca639b8c1784826478ce59c`와 결제 직후 입력 보관 후속 `c83f719855aa3f9ef316df024e004e5f69e4b4cc`를 main에 fast-forward하고 push했다. 부분 전달 lease·문서 종료 뒤 서버 복구·승인 후 첫 생성 전 bootstrap·출력 잘림·bfcache/화면 재개·Storage 차단·첫 stream 전 입력 유실을 재현/수정했다. 실제 계산 6체계/타로 6장, 실제 고객 화면 390/430/1280px 마지막 본문, 서버/공용 결제 왕복 및 새 문서 재열람을 mock으로 검사했다. 첫 최종 check:fast는 critical·88/88 gate·276 suite/3,873 Jest 통과. 마지막 입력 보관 수정까지 복귀 행동검사 33개와 추가 check:fast(276 suite/3,873 Jest, 186.216초)가 통과했다. [동일 SHA main CI](https://github.com/rei1237/codedestiny/actions/runs/35116997629)는 모든 lane·CI required success다. **초융합 완료 당시(자미 완료 전) 나머지 71구매 키+후속3경로는 개별 A~F 미실행이었다.** root marketing 84개 dirty 항목과 diff hash가 main 반영 전후 같았으며 다른 워크트리는 보존했다.

사용한 안전 작업 디렉터리: `D:\Development\codedestiny-worktrees\paid-af-mock-20260916-20260916-235632` (동시 Claude 편집 때문에 생성). 종료 시 이 워크트리만 배수한다. 로컬 mock 서버 13070/13071은 종료했다. 재개 때 단독/동시 편집 여부를 다시 확인하고 단독이면 main, 동시 편집의 두 번째 세션이면 새 안전 워크트리를 사용한다. 마지막 유료 source는 `acdf20c386004505e643fd3db7976f640b655fdf`, 이후 보존한 mock/inventory 보완은 `851cbe758`, 검증된 문서 전달은 `1a622b55595daa7ed5cf1323d49c6a5415dd55d0`다. 최근 root의 `public/styles/static-policy.css`·`tailwind.config.js` 수정도 다른 세션의 미커밋 작업으로 확인했다.

```text
D:\Development\code-destiny에서 D:\Development\code-destiny\docs\handoff\paid-llm-service-delivery-20260916.md를 읽고, main/origin·현재 SHA의 CI·동시 편집 상태를 확인하라. 검증 기준 1a622b55595daa7ed5cf1323d49c6a5415dd55d0와 CI 35125744683, 유료 source acdf20c386004505e643fd3db7976f640b655fdf의 CI 35123293719 기록을 재사용하고 네오 5행부터 상품별 A~F를 하나씩 실제 코드 mock으로 검사하라. 재현된 오류만 수정하고 기존/타 세션 변경을 보존하라. 실결제·과금 LLM·운영 DB·운영 승격은 실행하지 말고 mock→commit/push→동일 SHA main CI까지 완료하라.
```

1. **마스터 2상품의 실제 결제 전달 확인은 별도 단계.** 운영 코드 기준은 확인된 `77007dc4c`다. 이후 인수인계 문서만 바뀐 main SHA와의 차이로 코드 미반영을 오판하거나 재승격하지 않는다. 실제 PG/과금 LLM/운영 DB/실기기·고객 주문 복구는 각각 승인 범위 안에서만 확인한다. 승인 없는 단계는 미검증으로 남기고 다른 상품의 mock 점검은 계속한다. 입력 없는 과거 구매는 구매 권리 보존·재입력 후 결제 없는 복구를 유지한다.
2. **[서비스별 재검증표](../verification/paid-llm-service-checklist-20260916.md)의 74구매 키+후속3경로를 순서대로.** 마스터2키는 기존 검사 기록을 재사용하며 이번 전체 A~F 완료로 새로 체크하지 않는다. 초융합1키와 심화 자미1키만 이번 mock 완료다. 네오부터 구매70키와 후속3경로는 이번 개별 A~F 미실행이다. 네오→나크샤트라→기타 장문→질문/찻집→타로·기타→손금→Code Destiny 영냥이28키. 후속3경로도 별도 완료한다.
3. **한 상품마다 A~F:** 실제 입력·계산→mock 결제/권한→생성→품질→저장 확인→완료 기록→새 문서 마지막 본문 재열람. 오류·지급 지연·모바일 복귀·문서 종료·저장 유실·동시 락을 주입한다. 재열람 LLM/결제/차감0회. 필요한 장만 재생성, 공유 재시도 상한·백오프·정체 집계. 상세 계약은 표를 따른다.
4. 기존 [매트릭스](paid-llm-delivery-matrix-20260915.md)는 구현/mock 완료 기록이며 현재 실운영 보장이 아니다. 과거 미수정 기록만 보고 다시 구현하지 않는다. 현재 CTA→import→API→LLM을 대조해 신규 상품/변형을 추가하고, 비LLM·무료·관리자·비활성 UI는 구분한다.
5. 결함을 재현한 상품만 최소 수정한다. 같은 결함의 공통 코드만 함께 수정한다. 가격·분량·계산·이용권/월정석/단건 정책을 유지한다. 영냥이는 통합 `worker/yeongnyangi/`의 기존 단건 전용 계약을 유지하며 별도 SoulCat 과거 검사로 완료 처리하지 않는다.

## 검증·전달

관련 표의 실제 코드 mock 검사→`npm run check:fast -- --plan`→관련 `check:fast`→의도한 파일만 commit/push→동일 SHA main CI. 파일·의도·명령/출력·예산/차감·새 문서 증거·남은 확인을 행별 기록한 뒤 다음 상품으로 간다. 정상 구 completed 구매본 보존·전액 취소/환불/계정 분리도 필수다. 부분 본문/짧은 fallback을 완료로 인정하지 않는다.

main 직접 편집, 마케팅 미커밋 변경 보존. 동시 편집의 두 번째 세션이면 [안전 워크트리 규칙](../../CLAUDE.md)을 따른다. 타 세션의 변경을 stage/reset/restore하지 않는다. 운영 승인·개발 경계는 [실행 계약](../../CLAUDE.md)이 정본이다.

## 다음 상품 — 휴먼 디자인 프리미엄 리포트 28행

**2026-09-19 운명 찻집 숙요점 궁합 상담 27행 — mock 완료(경계 있음):** [행별 기록](../verification/fortune-tea-house-sukuyo-compatibility-paid-delivery-20260919.md). 🔴 **27행은 사주 계열이 아니라 25·26행 가드가 닿지 않는다** — `isSajuFamilyMode`(490행) 밖이라 `assertSajuCalculationBasis` 가 5433행에서 숙요를 그대로 통과시킨다. 표의 “기존 회귀 있음” 을 믿지 않고 직접 실행해 **닫힌 27숙 계산으로도 200코인 상담문이 완주**함을 실측했다(본인 `1991-02-31` · 상대 생년월일 공백 · 본인 음력 `1960-02-30` 3케이스 전부 **HTTP 200 · `completed` · LLM 8회**, 배포된 `sukuyoCompatibility` 의 `title`·`summary`·`strengths`·`cautions` 는 전량 LLM 창작이고 정상 대조군과 응답 형태가 같아 겉으로 구분되지 않는다). 원인 사슬: `parseFortuneTeaSukuyoBirthDate`(679행)가 달력 기준과 무관하게 그레고리력 왕복으로 날짜를 읽고 → 실패하면 `prepareFortuneTeaSukuyoAstronomy` 가 `if (!birth) continue`(727행)로 조용히 건너뛰고 → 계산 예외를 `buildFortuneTeaSukuyoCompatibility` 의 catch 가 삼켜 `available:false` 가 되고 → **품질 게이트 블록 전체가 3365행에서 꺼져** 신원 대조·8,000자 하한·앵커 검사가 통째로 사라진다. 25행(낱말 대조)·26행(블록 진입 조건)에 이은 **게이트 자가 비활성의 세 번째 형태**다. 🔴 숙요는 사주·타로와 달리 **서버 전용 계산**이다 — `sukuyoCompatibilityAdapter.ts` 의 `calculatePersonSukuyo`(123~132행)가 달 황경을 요구하는데 [`buildConsultResult.ts:746`](../../src/features/fortune-tea-house/lib/buildConsultResult.ts#L746) 이 그 옵션을 넘기지 않아 **정상 입력에서도 화면 로컬 초안은 항상 `available:false`** 라, 26행식 초안 플래그 대조를 화면에 옮기면 모든 숙요 상담이 막힌다(옮기기 전에 어댑터 실행 경로로 확인했다). 도달 경로는 전수 열거다 — `lib/korean-calendar` 로 1940~2010 실재 음력 **25,169일** 을 만들어 서버와 같은 규칙을 적용하니 **92일(0.37%)** 이 거부됐고 전부 음력 2월 29·30일이었다. `QuestionInputScene.tsx:759` 는 공백만 보므로 그 생일은 결제까지 도달한다. 수정은 fail-closed 3겹이다 — ① `assertSukuyoCalculationBasis`(5437행, 호출 5514행 — 26행 가드 바로 다음 줄)가 초안의 `sukuyoCompatibility.available` 이 true 일 때만 통과하고 아니면 422 `FORTUNE_TEA_HOUSE_SUKUYO_BASIS_MISSING`(실측 `applyCalls` 0 · 저장 `generation_failed` · `llmCalls` 0), ② `mergeFortuneTeaSukuyoCompatibility`(2690행)가 `available` 을 서버 계산값으로 고정해 초안 위조로 가드와 게이트를 함께 속이는 길을 막고, ③ 화면은 서버 파서와 같은 규칙만 복제한 `isServerReadableSukuyoBirthDate`(542행)로 `localPreviewResult` 대입 **앞**(1119행)에서 던져 결제창을 열지 않는다. 🔴 **가드 설계에서 배운 것:** 처음에는 가드에 두 사람의 `astronomySukuyo` 존재 검사를 함께 두었는데 변이를 넣어 보니 ②와 서로를 가려 **어느 쪽 변이도 잡히지 않았다** — 겹치는 조건은 “선언만 된 가드” 가 된다. 겹치는 절을 걷어낸 뒤 변이 3종이 전부 물리는 것을 확인했다(가드 조건 `if (true) return;` → 2건 실패/93 통과, ②의 고정 줄 삭제 → 위조 회귀 1건만 실패, 호출부 삭제 → 2건 실패/93 통과, 매번 국소 편집으로 원복 후 95/95). 신규 회귀 2건은 [`fortune-tea-house-honey-drops.test.js`](../../__tests__/worker/fortune-tea-house-honey-drops.test.js) 에 이웃 크기대로 넣었고, 정상 요청이 막히지 않는다는 반대 방향은 기존 숙요 완주 테스트(200 · LLM 8회)가 그대로 맡는다. F 는 숙요 3그룹 + 공통 5그룹 = 8그룹이라 POST 당 4그룹(4204행) 기준 **2회 POST · LLM 8회**로 26행(15그룹·4회·16회)과 다르다. 측정: honey-drops 95/95 · 찻집 워커 6스위트 125/125 · 화면 `node --test` 4파일 31/31 · `sitemap:generate` 1,282 URL(원장 1건, `.tsx` 와 같은 커밋) · `check:fast` exit 0(`test:jest` 281스위트 3,977/3,977). 전부 mock, 실호출 0회. 커밋 `8deacfaad`(코드)·`e87cc3248`(문서). 🔴 `e87cc3248` 의 main CI 는 `Critical checks`·`Build Pages and Worker`·`Typecheck and lint`·`gitleaks` success 이나 **`CI required` 는 failure** 다 — `Static guards`·`Main drift` 가 둘 다 `verify:public-mirror-fresh` 한 스텝에서만 멈췄고, 내 푸시 이전 tip `d6ad24bb8` 에서 **동일한 7개 목록(`index.html` + 미러 6개)으로 이미 failure** 였으며 내 커밋과의 교집합은 0건이다. 공유 체크아웃에 그 파일들의 미커밋 작업이 살아 있어 재생성을 대신 커밋하지 않고 소유 세션에 전달했다(원칙 14). 🔴 후속: 옆 세션이 `64f55a98a` 로 그 7개를 재생성해 커밋한 뒤에도 `341b722e4` 에서 **같은 목록으로 다시 failure** 이고 `CI required` 도 failure 다 — 미해소 상태로 인계되며, 27행 레인(`Build Pages and Worker`·`paid-flow-gates` 포함)은 전부 success 다. 옆 세션이 같은 체크아웃에서 `index.html`·`js/**`·`public/**`·`marketing/**` 을 쓰고 있어 이번에도 경로 지정 스테이징만 했다. **D 실화면·F 전용 diff 증거 없음(23~26행과 같은 경계).** 참고: 26행은 본문 기록 없이 frontmatter `previous_next` 와 재검증표 26행에만 남아 있다.

**2026-09-19 운명 찻집 사주 상담 25행 — mock 완료(경계 있음):** [행별 기록](../verification/fortune-tea-house-saju-paid-delivery-20260919.md). **24행과 달리 무결함 행이 아니었다 — B축 신규 결함 1건을 재현·수정했다.** 유료 사주 상담이 명식(命式) 없이 완주했다: `draftResult` 가 없거나 `draftResult.saju.available === false` 이면 라우트가 그대로 생성에 들어가 **HTTP 200 · `generationMeta.mode: "gemini"` · degraded 아님**으로 완성본을 돌려주고 100코인을 확정했으며, 그 리포트에는 `pillars`·`dayMaster`·`fiveElements`·대운이 전무하고 `tenGodSnapshot` 이 `{ available:false, reason:"사주 초안이 전달되지 않았습니다" }`(2636행)였다. 원인은 둘이다 — 명식 계산이 **클라이언트에만** 있어 서버 `buildMinimalDraft`(2509행)는 자리표시자만 채우고, 품질 게이트 `assertSajuDeepQuality`(3133행)가 계산 근거를 **낱말로만** 본다(`일간/오행/십성` 중 2개 이상 3163행, `/현재 운|운의 흐름|대운|세운|월운/` 3167행). LLM 은 명식 없이도 그 낱말을 쓰므로 9섹션·10,000자·공통 20,000자 하한을 전부 통과한다. 도달 경로는 추정이 아니라 실측이다 — 생년월일 입력은 그레고리력 왕복만 검증하는데(`lib/birthDateInput.ts`, `parseBirthDate` 220~231행이 월 무관 일 ≤ 31 허용) `buildSajuProfile` 을 직접 격자 호출하니 1960~2005년 전월 28·29·30일 1,656조합 중 **음력 251건(15.2%)** 이 예외(양력 0건)였고 1900~2100 격자 2,010조합에서도 82건 전부 음력이었다. 그 예외를 `sajuAdapter` 3중 catch(523~570·641~647·649~655행)가 조용히 `available:false` 로 흡수해 사용자에겐 오류가 뜨지 않는다. 수정은 fail-closed 2겹(원칙 10) — 서버는 생성 진입 직전 첫 문장에서 `assertSajuCalculationBasis`(5401·5477행)로 422 를 던지고(기존 실패 경로가 실행 기록 정리 4525행과 deferred `cancel` 5496행까지 이어 돌려 **차감이 확정되지 않는다**), 화면은 `localPreviewResult` 대입 **앞**(1090~1094행)에서 던져 결제창 자체를 열지 않는다(대입 뒤였다면 catch 의 로컬 프리뷰 분기가 가짜 성공으로 삼킨다). 가드 강도는 `saju.available === true` 한정이다 — 기존 정상 테스트가 최소 초안을 쓰고 있어 필드 단위 단언은 타이밍 스위트를 깨뜨린다. 변이 검증: 가드 본문에 `if (true) return;` → 신규 2건만 202/`ok:true` 로 실패, 원복 후 전부 녹색. 측정: 찻집 Jest 6스위트 118/118 · 전체 281스위트 3,970건 · 화면 `node --test` 8파일 50/50 · `sitemap:generate` 1,282 URL(.tsx 와 동일 커밋) · `sync:public` 무변경 · `check:fast` exit 0. 수정 커밋 `40a6373d1`·문서 `377b37599`, main 머지 `bee8f8c7e` 의 `CI required` success(머지 후 `sync:public` 재실행 무변경, `sitemap:generate` 갱신 0). 워크트리 `change-20260919-023948` 와 병합 브랜치는 정리 완료.

🔴 **변이 원복에 `git checkout <파일>` 을 쓰지 않는다.** 같은 파일에 들어 있던 실제 수정까지 통째로 날아간다(이번에 한 번 겪고 `assertSajuCalculationBasis` grep 이 빈 출력인 것으로 감지·복구했다). 변이는 넣을 때와 같은 방식의 국소 편집으로 되돌린다.

🔴 **다음 세션(26행)에 주는 지름길 겸 경고:** 26행(`fortune-tea-house-saju-compatibility-consultation`)도 같은 라우트·화면이라 D축은 또 상속이다. 다만 **25행에서 만든 명식 가드를 `sajuCompatibility` 에는 의도적으로 넣지 않았다 — 지금 26행은 같은 결함에 대해 fail-open 이다.** 그 모드의 초안 모양(최상위 `birthDate` 인지 `sajuCompatibility.user.birthDate` 인지)을 실측하지 않아 확장하면 오차단 위험이 있었기 때문이다. **26행의 첫 일은 그 초안 계약을 실측하고 `assertSajuCalculationBasis` 를 그 모드까지 닫는 것이다.** 표의 기재를 믿지 말고 변이로 가드가 무는지 직접 확인한다.

🟡 **범위 밖(미조치·정정만) 2건:** ① 24행 기록과 재검증표의 POST 횟수 기재가 실측과 달라 이번에 정정했다 — 체크포인트 그룹은 카드당 + 공통이라 실측은 **타로 3카드 3회 POST/LLM 9회, 5카드 3회/11회, 사주 4회/16회**(`apply` 1회라 과금 결론은 불변). ② 품질 게이트는 이번 가드 이후에도 명식 **값** 과 서술의 정합은 대조하지 않는다 — 명식이 있는데 LLM 이 그 값과 무관하게 써도 통과한다. 값 대조 게이트는 별도 과제다.

**2026-09-19 운명 찻집 타로 프리미엄 상담(5카드) 24행 — mock 완료(경계 있음):** [행별 기록](../verification/fortune-tea-house-tarot-five-paid-delivery-20260919.md). **코드 변경 0·신규 테스트 0의 무결함 행이다**(20행이 19행 수정을 상속한 것과 같은 처리). 23행과 라우트·화면이 같고 깨어남 복구(`probeFortuneTeaPending` 597행·effect 655~677행)가 스프레드로 분기하지 않으므로 D축을 그대로 상속한다. 23행 지시대로 A~F 를 처음부터 훑지 않고 **3카드와 갈리는 지점만** 독립 실측했다. **A:** featureKey 를 서버가 `tarotSpread` 에서 도출하고(`expectedFortuneTeaHouseFeatureKey` 1071~1078행) 불일치 명시 키는 빈 문자열로 떨어뜨려 fail-closed 거부한다(`resolveFortuneTeaHouseFeatureKey` 1080~1089행). 레지스트리 242행 100코인=₩10,000 ↔ [`consultPricing.ts`](../../src/features/fortune-tea-house/data/consultPricing.ts) 17행 일치, 허용 키 482행 등재. **B:** 체크포인트가 카드당 그룹이라 5카드는 1,500자/장(4168~4169행)·POST 당 4그룹(4201행)으로 **2회 POST 로 완주**한다(3카드는 1회). 20,000자 본문 하한(4236행)은 공통. **C:** 23행과 같은 후불 과금이고 `apply`(5534행)·`cancel`(5477행) 둘 다 서버 도출 `access.featureKey`/`access.pricing` 을 넘겨 100코인 상품이 100코인으로만 확정된다. **F:** 위 2회 POST 는 재과금이 아니다 — 1차는 202 부분 응답이고 `apply` 는 마지막 성공 저장 뒤 1회뿐이다. **D 상속 확인:** `/pending` 의 `requestPayload`(4749~4767행)와 `buildFortuneTeaQuestionInputFromRequestPayload`(295~315행)가 `tarotSpread` 를 복원하므로 깨어난 탭의 다음 제출이 3카드로 강등되지 않는다. **변이 검증:** 표의 “기존 회귀 있음” 기재를 믿지 않고 스프레드 분기를 제거해 항상 3카드 키를 반환시켰더니 cardwise 10건 중 **정확히 2건(303·309행)만** 실패했고(315행은 변이 후에도 통과가 정상 — 도출값이 3카드로 바뀌어도 명시 5카드 키는 여전히 불일치), 원복 후 `git status --porcelain` 빈 출력·10/10 재통과. **측정:** 워커 7스위트 212/212(찻집 6 + 교차상품 `paid-completed-result-access`), 화면 `node --test` 8파일 50/50.

🔴 **워크트리 함정 1건 추가:** 워크트리에서 `npx jest` 를 직접 쓰면 루트 `node_modules` 링크만으로는 ESM 변환 설정을 못 잡아 위 7스위트 중 **5개가 `SyntaxError: Cannot use import statement outside a module` 로 헛실패한다**(93 failed). 반드시 `npm run test:jest --`(= `scripts/run-mock-tests.mjs`)로 돌린다. 같은 명령이 212/212 를 낸다.

🔴 **다음 세션(25행)에 주는 지름길:** 25~27행도 24행과 같은 구조다 — 같은 라우트·같은 화면이라 D축은 또 상속이고, 갈리는 지점은 타로 스프레드가 아니라 **`consultationMode` 분기**(1072~1074행: `sukuyo`·`sajuCompatibility`·`saju` 를 스프레드보다 먼저 확정)다. 25행(`fortune-tea-house-saju-consultation`)은 명식(사주) 입력 보존과 [`fortune-tea-house-saju-timing.test.js`](../../__tests__/worker/fortune-tea-house-saju-timing.test.js), E축 fixture 등재만 보면 된다. **무결함 행일 가능성이 높지만 표의 기재를 믿지 말고 24행처럼 변이로 가드가 실제로 무는지 확인한다**(모드 분기를 제거해 타로 키를 반환시키는 변이가 지목 테스트만 깨뜨리는지).

🟡 **범위 밖(미조치, 보고만) 4건:** ① 품질 게이트가 “5장”을 세지 않는다(3208·3230~3239행은 `!spreadCards.length` 만 검사) — 다만 카드는 클라이언트 초안에서 오고 featureKey 는 `tarotSpread` 로 결정되므로 카드를 줄이는 것은 과금 우회가 아니라 자기 손해다. ② `TAROT_FIVE_CARD_EXTRA_CHARS = 1200`(57행)이 만드는 7,200자 하한(`getTarotMinResultChars` 1721~1725행)은 공통 20,000자 하한(4236행)에 항상 지배당하는 **사문**이다. ③ 5카드 3번 자리 위치 ID 가 화면 `block`([`tarotAdapter.ts`](../../src/features/fortune-tea-house/lib/tarotAdapter.ts) 20행) ↔ 워커 `obstacle`(493~518행)로 드리프트(워커 표는 초안 없는 퇴화 경로 전용이라 현재 영향 0). ④ 저장 문서에 최상위 `tarotSpread` 가 없어 4794행 폴백 `resolveFortuneTeaHouseFeatureKey(doc, doc)` 은 프리미엄 문서에도 구조적으로 3카드 키만 돌려줄 수 있다 — 단 결과 컬렉션 writer 전수 조회(`results.updateOne|insertOne|findOneAndUpdate` 워커 전체 12곳, 전부 `fortune-tea-house.js`) 결과 **본문 writer 는 `saveFortuneTeaHouseResult` 하나뿐이고 항상 `featureKey` 를 쓰므로 현재 코드가 만든 문서에서는 폴백이 도달 불가**다(과거 데이터 한정 위험, 운영 DB 조회는 계약상 금지라 미측정). ⑤ 재검증표 24~27행의 “기존 검사” 링크 오기는 이번에 **시정 완료**(구 `fortune-tea-house-delivery-billing.test.js` → `fortune-tea-house-tarot-cardwise.test.js`).

**2026-09-19 운명 찻집 타로 상담(3카드) 23행 — mock 완료(경계 있음):** [행별 기록](../verification/fortune-tea-house-tarot-paid-delivery-20260919.md). 🔴 **여기서 라우트 계보가 바뀐다** — 정본 [`worker/routes/fortune-tea-house.js`](../../worker/routes/fortune-tea-house.js)(5,629행, 단일 export `handleFortuneTeaHouseRoutes` 5584행)는 5~22행이 공유하던 `fortune.js`·`runPaidNarrativeDelivery`·`deliverFeatureQuestion` 을 **한 줄도 쓰지 않는 독자 구현**이고, 공유 결제 원시함수 중 import 하는 것은 `isStoredPaidResultRevoked`(23행) 하나뿐이라 A~F 를 상속 없이 처음부터 훑었다. 화면은 App Router [`src/features/fortune-tea-house/FortuneTeaHousePage.tsx`](../../src/features/fortune-tea-house/FortuneTeaHousePage.tsx)다. **기존 코드 정상 축:** A(`verifyFortuneTeaHouseConsultAccess` 1452행, 조작 차단 `expectedFortuneTeaHouseFeatureKey` 1071행·불일치 거부 4457~4463행), B(20,000자 하한 + `assertConsultQuality` + `hasRepeatedReportPassage` 4233~4239행이 **실제로 실행**), E(`isStoredPaidResultRevoked` 4795행, fixture 19행에 찻집 5키), F(`/pending` 4749행·`/results/:id` 4801행에 LLM 경로 없음, 같은 `attemptId` 재POST 는 `cached:true` 5432~5445행). **C축은 구조적으로 무결함이다 — 이 상품은 후불 과금이라 `apply` 가 생성·저장 성공 뒤(5534행)에만 돌고, 전량 실패 시 `apply` 자체가 없어 21·22행에서 고친 이용권(월정석) 환불 누락이 원리적으로 성립하지 않는다.** 예약 해제 `cancel`(5476행)도 `markFortuneTeaHouseGenerationFailed`(4525행)의 상태·토큰 일치 확인 뒤에만 돈다. **재현·수정한 결함 1건(D축):** 화면이 `pageshow`·`focus`·`online`·`visibilitychange` 를 **하나도 구독하지 않았고**(기능 디렉터리 전수 grep — 깨어남 리스너는 꿀편지용 `TeaHouseResultSheet.tsx` 625~626행뿐, 나머지 `addEventListener` 는 BGM·오디오), 서버에 남은 진행 중 유료 상담을 되찾는 `GET /api/fortune-tea-house/pending` 조회가 `useEffect(..., [recoveryOwner])` **안에 인라인**이라 마운트·계정 변경 때만 1회 돌았다. 폴링 예산 약 263초(`FORTUNE_TEA_POLL_BACKOFFS_MS` 317행)가 소진되면 화면은 `goToStage("questionInput")`(1331행)로 떨어지는데 **서버는 아직 생성 중일 수 있어** 백그라운드로 내려간 탭이 결제한 상담을 전체 리마운트 전까지 잃었다(5~19행에서 15회 반복된 결함 클래스). 조회를 `probeFortuneTeaPending`(597행, `useCallback` 빈 deps)으로 들어올리고 마운트 effect 를 649행 한 줄로 축약한 뒤, 깨어남 effect(655~677행)가 4신호를 구독하고 대칭 cleanup 을 둔다(+50/-13행, 워커·가격·정책 무수정). 🔴 **이 수정의 핵심 제약은 `consultRunRef` 불가침이다** — 올리면 1130·1205·1221·1239행의 폴링 취소 검사가 참이 되어 깨어난 탭이 **살아 있는 유료 생성을 죽인다**. 기존 `[recoveryOwner]` effect 는 `consultRunRef.current += 1`·`setConsultResult(null)` 로 파괴적 리셋을 하므로 그 effect 를 깨어남에 재사용할 수 없었고, 그래서 조회만 분리했다. **자동 재폴링도 하지 않는다** — 자동 재개는 `submitQuestion` 호출을 뜻하는데 그건 1010~1011행에서 `consultRunRef` 를 올리고, 탭 복귀 한 번에 4신호가 발화하는 핸들러가 **금전 이동(POST /consult)** 을 트리거하게 두지 않는다(서버가 중복을 막더라도 50코인 경로의 근거로는 부족). 되살린 `unusedPaidAttemptRef` 가 `requestPayload`·`billingGate` 를 들고 있어 다음 제출은 `carriedPaid` 분기(1103~1111·1173행)로 결제창 없이 동일 본문을 보낸다. **변이 검증:** 가드 7종(persisted 게이트·`pendingProbeRef`·cleanup·진입부 `submitLockRef`·`wakeBlockedRef`·`consultRunRef` 증가·`!recoveryOwner` early return)을 하나씩 넣어 **각각 지목한 테스트 1건만 깨지는지** 실측했다. 첫 회차에서 "조회 진입부 `submitLockRef` 검사 제거" 변이가 **아무것도 깨뜨리지 못했다** — 깨어남 핸들러가 같은 검사를 중복해 어느 쪽도 단독 하중을 받지 않았기 때문이라, 핸들러의 중복 검사를 지우고 조회를 단일 판정처로 만든 뒤 재실행해 물림을 확인했다. 신규 3건이 수정 전 실패 → node 16/16·19/19, 워커 4스위트+교차상품 E축 exit 0, `check:fast` RED 통과, `sitemap:generate` 동일 커밋(날짜 라우트 일일 롤 포함). "품질 게이트 전량 모킹" 함정 해당 없음(실행 스위트가 실제 `handleFortuneTeaHouseRoutes` 에 POST 하고 모킹은 인프라 경계뿐). **D축 실제 브라우저 렌더 증거 없음은 5~22행과 동일한 경계.**

🔴 **다음 세션(24행)에 주는 지름길:** 24~27행은 **23행과 같은 라우트·같은 화면**을 쓰므로 이번 깨어남 복구 수정 혜택을 이미 받는다. A~F 를 처음부터 훑지 말고 상품별 차이만 확인하면 된다 — feature key·가격(`paid-feature-registry.js` 242~245행), 카드 수 분기(3카드/5카드 분리는 `fortune-tea-house-tarot-cardwise.test.js` 303·309·315행), E축 fixture 등록(이미 5키 전부 등록됨).

🟡 **범위 밖(미조치, 보고만):** ① 재검증표가 23~27행 "기존 검사"로 지목한 `__tests__/worker/fortune-tea-house-delivery-billing.test.js`(52행)는 이름과 달리 `worker/routes/billing.js` 의 `handleDeferredUsageApply`·`completeDeferredUsageRecord` 두 함수만 vm 실행하는 파일로 **찻집 코드가 한 줄도 돌지 않는다** — 23행에서 링크를 `fortune-tea-house-tarot-cardwise.test.js` 로 시정했고 24~27행도 같이 바로잡을 것. ② `apply` 성공 후 `completeFortuneTeaHouseDelivery`(5543행) throw 시 과금 유지·멱등 복구 의도(5525~5526행 주석) 전용 테스트 없음. ③ 폴링 중 429(325행)가 전용 분기 없이 일반 실패로 떨어짐. ④ 꿀편지(75행) 재개 effect 도 `pageshow`·`focus` 미구독 — 이번과 같은 결함 클래스이나 다른 행 소관. ⑤ `handleAstrologyAIPrompt`(19행, `fortune.js:4246`) 이용권 환불 누락 잔존.

**2026-09-18 베다 점성술 AI 질문 프롬프트 22행 — mock 완료(경계 있음):** [행별 기록](../verification/vedic-ai-prompt-paid-delivery-20260918.md). 정본은 19~21행과 같은 공유 라우트 `worker/routes/fortune.js`의 `handleVedicAIPrompt`(4313행~, 라우팅 6786행)이고 전용 lib `worker/lib/vedic-ai-prompt.js`가 feature key(10행)·`VEDIC_AI_PROMPT_PRICE = 100`(11행)을 갖는다. **이 상품의 유일한 UI 는 정적 셸 `vedic-astrology.html` 인라인 스크립트다**(feature key 전수 grep + `payment-inventory.json` `routes: ["/vedic-astrology.html"]` 2면 확인. App Router `app/vedic-ai/` 의 `vedic-ai-consultation` 30,000원은 **다른 상품**이니 혼동 금지). 🔴 **이 페이지는 `js/saju-engine.js` 를 로드하지 않아 19행이 만든 공유 원시함수(`_cdRetryTransientPost`·`_cdMountQuestionRecovery`)를 상속받지 못하는 유일한 형제였다** — 20·21행이 무결함으로 상속했던 D축이 여기서는 전부 공백이었다. **재현·수정한 결함 3건:** (1) 21행이 예고한 이용권 커버리지 환불 누락(동일 5점 배선, `fortune.js` 4368·4404·4411·4414·4458~4470행, +19/-2), (2) 화면이 서버의 202 부분 응답을 완성으로 확정 — `resp.ok` 가 202 에도 참이라 **단 한 번만 POST 하고 3/10 만 적힌 본문을 최종 결과로 굳히면서 결제 증거까지 비워 무료 재시도도 닫았다**. 전달 루프를 `vedicAiDeliverUntilSaved`(3760행)로 분리해 200(completed)까지 `resumeBody` 로 이어받고 한도·파도 상한(40)에서는 `paymentRetainedForRetry` 로 떨어뜨리도록 고쳤다. (3) 깨어남 복구 부재 — `GET /api/fortune/vedic/ai-result` 라우트는 있는데 화면이 한 번도 호출하지 않아 백그라운드로 내려간 탭이 결제한 상담을 영영 잃었다. `vedicAiRecoverSavedConsultation`(4025행)·`vedicAiMountWakeRecovery`(4073행)를 추가하고 **결제창 진입점 IIFE 밖에서 마운트했다**(7829행 — 잠들었다 깨어난 탭은 결제창을 거치지 않으므로 `registerVedicResumeHandlers` 안에 두면 안 된다). 복구는 GET 조회로 시작하고(새 POST 는 재결제·재생성 위험) 202 면 같은 전달 루프로 이어받으며, `pageshow` 는 `persisted===true` 일 때만·hidden·`inFlight`·무토큰·복구 완료 4가드로 막는다. **변이 검증:** 워커는 `['pass','Vedic']` 추가로 수정 전 1/29 실패→29/29; UI 는 신규 `__tests__/ui/vedic-ai-prompt-partial-delivery.behavior.test.js` 5건(실제 함수 본문을 TS AST 로 꺼내 `vm` 실행, 모킹은 화면·네트워크 경계뿐)이 수정 전 실패→node 1,437/1,437, 복구의 202 분기를 `false` 로 바꾸는 변이에서 해당 1건만 실패함까지 실측했다. 가격 100코인=₩10,000(`paid-feature-registry.js:309` + inventory `priceKRW:10000`), 별칭은 평범한 배열 멤버십(`:547`), E축 픽스처는 `paid-completed-result-access-fixtures.mjs:18`에 이미 등록. "품질 게이트 전량 모킹" 함정 해당 없음 재확인. `sitemap:generate` 산출물 무변경(라우트 불변), `sync:public` 으로 `public/vedic-astrology.html` 미러 재생성, `check:fast` RED 승격 281 suite·3,968 test 통과.

🟡 **범위 밖(미조치, 보고만):** 같은 이용권 환불 누락이 `handleAstrologyAIPrompt`(19행, `fortune.js:4246`)에 잔존한다. 완료 처리된 행이라 별도 후속 과제이며, 형제 3종과 동일한 5점 배선 + `['pass','Astrology']` 케이스 추가로 끝난다.

**2026-09-18 숙요점 전문가 상담 21행 — mock 완료(경계 있음):** [행별 기록](../verification/sukuyo-ai-prompt-paid-delivery-20260918.md). 정본은 19·20행과 같은 공유 라우트 `worker/routes/fortune.js`의 `handleSukuyoAIPrompt`(5457행~, 라우팅 6715행)이지만 **19·20행과 달리 전용 lib [`worker/lib/sukuyo-ai-prompt.js`](../../worker/lib/sukuyo-ai-prompt.js)**가 feature key(13행)·가격 상수 `SUKUYO_AI_PROMPT_PRICE = 100`(14행)·프롬프트 빌더를 갖는다. 전달 로직은 형제와 공유하는 `worker/lib/feature-question-delivery.js`이고 클라이언트는 `js/saju-engine-tarot-sukuyo-quantum.js`의 `syRequestSukuyoPromptByQuestion`(16622행)·`_cdBindQuestionRecovery('sukuyo', ...)`(16752행)다. 예상과 달리 **화면 재개가 아닌 C축(결제) 결함 1건을 재현·수정했다** — 숙요 핸들러는 생성 전량 실패 시(`onExhausted`) 코인 환불·카드 단건 취소만 처리하고 **이용권(월정석) 커버리지로 이미 깎인 `monthlySpendCoin` 을 되돌리는 경로가 아예 없었다**(수정 전 `verify` 콜백이 `passRefund: null` 하드코딩). `PASS_LIMITS`(`profile-limits.js:105-110`) 프리미엄 상한이 정확히 100코인이고 숙요 가격도 100코인이라 프리미엄·VVIP·패밀리 이용권 사용자에게 도달 가능한 경로임을 실측했다(STANDARD 50코인은 미커버). 소비 쪽 주석(`fortune.js:1931` 부근)이 "생성이 실패하면 handleZiweiAIPrompt 가 이 값으로 되돌린다"라고 자미만 지목하고 있었던 것이 근본 단서였다. 자미 구현(5298·5341·5347·5350·5393~5408행)과 1:1 동일 구조로 배선했다(+19/-2행). **변이 검증:** 공유 스위트 `__tests__/worker/feature-question-paid-delivery.test.js:79`의 소진 환불 테스트가 `access==='pass'` 를 `kind='Ziwei'` 로 고정해 형제 3종의 이용권 환불을 한 번도 실행하지 않고 있었다 — `test.each` 를 `[access, kind]` 쌍으로 바꿔 `['pass','Sukuyo']` 를 추가했고 수정 전 정확히 이 1건만 실패(`refund` 기대 1회/실제 0회, 27 passed·1 failed) → 수정 후 28/28 통과로 전환함을 실측했다. `readSajuAIPromptPointRefundContext`(1286~1300행)가 PASS/MEMBERSHIP_PASS 를 `isPointSpend`·`isCardSpend` 둘 다 false 로 분류해 새 분기로 정확히 떨어짐도 소스로 확인했다. A축은 클라이언트가 실제 숙요 계산값(`basicResult.mansionIdx`·`compatibilityResult.partnerIdx`)을 그대로 보내고 서버가 `questionFacts` 고정 근거로 굳히며 근거 0건이면 422 로 생성이 막히는 사슬을 확인했고, 요청 키가 `profileId|domain|question|mansionIdx|partnerIdx|epoch` 로 결정적이라 재시도가 재결제로 이어지지 않음도 확인했다(16607~16619행). F축은 `test.each`의 **Sukuyo 조합 3개(pass/monthly/single)가 숙요 자신의 핸들러 직접 실행 증거**로 재열람 시 `provider` 10회 유지·`refund` 미호출을 단언한다(20행이 도입한 "직접 실행 vs 공유 원시함수" 구분 계속 적용). 가격은 100코인=₩10,000(`paid-feature-registry.js:310` + `docs/payments/payment-inventory.json` prepare/grant `matches:true` + 클라이언트 버튼 "10,000원 AI 상담 받기" 3면 교차), 별칭은 19·20행과 동일한 평범한 배열 멤버십(`:548`). "품질 게이트 전량 모킹" 함정(8·14행)은 이번에도 해당 없음을 재확인했다. 21행 고유 특이점으로 `SUKUYO_AI_PROMPT_PRICE <= 0` 무료 분기(5524~5559행)가 현재 상수 100 때문에 실행 불가능한 죽은 분기임을 기록만 했다(가격 정책 불변 계약). 워커 28/28·node 스위트 1,432/1,432·교차상품 E축 96/96 통과.

🟡 **범위 밖(미조치, 보고만 — 22행이 먼저 확인할 것):** 같은 이용권 환불 누락이 형제 두 상품에 그대로 있다 — `handleAstrologyAIPrompt`(4246행)·`handleVedicAIPrompt`(4408행)가 여전히 `passRefund: null` 을 하드코딩하고 각 `refundGeneration` 에 이용권 분기가 없다. **22행(vedic)은 이번 21행 수정과 동일한 5곳 배선 + `['pass','Vedic']` 케이스 추가로 끝난다.** 19행(astrology)은 이미 완료 처리된 행이라 별도 후속 과제로 남는다(같은 금액 손실이 프리미엄 이상 이용권 사용자에게 발생한다).

## (완료) sukuyo_ai_prompt_generator 21행

**2026-09-18 연이 운명 상담 17행 — mock 완료(경계 있음):** [행별 기록](../verification/fortune-chat-paid-delivery-20260918.md). 인수인계가 정본으로 지목한 `worker/routes/fortune.js`는 다수 상품이 섞인 공유 라우트 파일임을 확인했다 — 17행 전용 핸들러는 그 안의 `handleGuardianFortuneGenerateRoute`(POST `/api/fortune/guardian/generate`)와 GET `/api/fortune/guardian/result`(`deliverGuardianPaid({ readOnly: true })` 호출)다. 같은 파일의 별도 SSE 라우트 `handleGuardianFortuneChatRoute`(POST `/api/fortune/guardian/chat`)는 `FortuneChatClient.tsx`의 실제 fetch 호출 4곳을 grep 전수 확인한 결과 이 화면이 부르지 않아 범위 밖으로 제외했다. 대화 이력 저장(`worker/routes/fortune-chat.js`, 111행)은 결제·LLM 코드가 전혀 없는 별도 순수 CRUD 파일임도 확인했다. 5~16행과 같은 클래스의 화면 재개 결함(중단된 무료/유료 턴 복구 `useEffect`가 `pageshow`·`focus` 미구독)을 재현·수정했다(+10/-1행). 신규 UI 행동 검사(`fortune-chat-wake-recovery.behavior.test.js`)는 실제 `useEffect` 콜백을 TypeScript AST로 추출해 `vm` 샌드박스에서 실행하는 변이 검증으로 수정 전 실패(`window:pageshow must be registered`) → 수정 후 통과(1/1)를 확인했다. **"무료 3회" 문서 드리프트를 발견했다** — `docs/PAYMENT_AND_ACCESS.md:27`·`docs/FEATURE_MAP.md:88,99`는 여전히 "무료 3회 이후"라고 적혀 있지만, 실제 코드(`worker/lib/guardian-fortune-usage.js:9-17`, 2026-08-17 정책 변경 주석)는 `GUARDIAN_FORTUNE_GUEST_LIMIT=0`(비로그인 무료 없음)·`GUARDIAN_FORTUNE_ACCOUNT_FREE_LIMIT=1`(로그인 계정 총 1회, `$setOnInsert`로 박제된 기존 회원의 저장값 3도 이 상수가 상한으로 강제해 함께 1회로 내려감)이다 — 코딩 원칙 14에 따라 그 두 문서는 고치지 않고 이 기록과 인수인계로만 보고한다. **F축(재열람 시 추가 과금·생성 없음)을 실제 호출 사슬로 확정했다** — GET 요청은 `deliverGuardianPaid`(`guardian-paid-delivery.js:28`)가 `method:'GET'`인 합성 `Request`로 `runPaidNarrativeDelivery`를 부르고, `paid-narrative-delivery.js:59`가 `request.method==="GET"`이면 무조건 저장된 문서로 즉시 응답해 LLM `produce` 콜백(`generator(...)` 호출부)이 구조적으로 실행될 수 없음을 확인했다(별도 전후 diff 스크립트보다 강한 근거로 판단해 생략). "품질 게이트 전량 모킹" 함정(8·14행 반복)은 `__tests__/worker/guardian-paid-delivery.test.js`를 재정독해 이번에도 해당 없음을 재확인했다(`deliverGuardianPaid` 자신은 모킹 없이 실제 실행). 가격(`paid-feature-registry.js:346`, 50코인/5,000원)과 별칭 테이블 부재(16행과 달리 평범한 배열 멤버십)도 코드로 재확인했다. UI 27/27(신규1+기존26)·워커 jest 56/56·`check:fast` RED 승격 전체 게이트(`test:jest` 281 suite·3,959/3,959) 전부 통과, `verify:sitemap-drift`가 이미 OK(재생성 결과와 일치, URL 1,281개)를 반환해 `npm run sitemap:generate` 별도 재실행은 불필요했다(결과로 확정, 추정 아님). 커밋 `9d74b3ee0`을 워크트리(`change-20260918-144328`)에서 만든 뒤 `git fetch origin main`으로 origin이 여전히 이 워크트리의 base(`17dda1b37c`)와 같음을 확인해(다른 세션의 SEO P9 커밋 2개는 로컬 메인 체크아웃에만 있고 미push 상태였음) 병합 없이 fast-forward push했다(`17dda1b37c..9d74b3ee0`). [체크런](https://github.com/rei1237/codedestiny/commit/9d74b3ee0/checks)을 `gh api`로 전수 조회해 비동기 `Deploy staging`(대기 대상 아님) 1건을 제외한 20개 중 `CI required`·`Static guards` 포함 13개 success·7개 skipped·실패0을 확인했다. 🔴 **이 커밋에 `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>` 트레일러가 빠졌다** — 이미 origin/main에 push되고 그 SHA로 CI가 통과한 뒤 발견해, main 직접 개발 규칙(강제 push·히스토리 재작성 금지)에 따라 `commit --amend`로 고치지 않고 사실만 기록한다. **D(실제 화면 증거)와 전용 F 전후 diff 스크립트는 이번 차례에도 만들지 않았다 — 5~16행과 동일한 남은 경계(F는 호출 사슬 확정으로 대체).**

**2026-09-18 AI 작명 보고서 16행 — mock 완료(경계 있음):** [행별 기록](../verification/naming-prompt-paid-delivery-20260918.md). 5~15행과 같은 클래스의 화면 재개 결함이었다 — 결과 화면 `NamingAiResultClient.tsx`의 재개 effect가 `visibilitychange`·`online` 2개만 구독하고 `pageshow`·`focus`는 등록하지 않아, bfcache 복귀나 창 포커스만 복귀했을 때 진행 중 생성 상태가 갱신되지 않았다. 실제 배포 코드의 `useEffect` 콜백을 TypeScript AST로 추출해 Node `vm` 샌드박스에서 그대로 실행하는 변이 검증(신규 `naming-prompt-wake-recovery.behavior.test.js`)으로 수정 전 실패·수정 후 통과(1/1)를 확인한 뒤 두 이벤트를 추가했다. **시작 화면(`NamingAiClient.tsx`)은 7행(인연의 서)과는 다른 근거로 무결함을 확인했다** — 7행은 "결과 화면으로 능동 이관하는 `document.hidden` 분기 설계"였지만, 작명 시작 화면은 그 분기도 리스너도 전혀 없다. 대신 `/generate`가 요청 안에서 LLM 생성을 동기적으로 끝내는 라우트(코드 주석으로 명시된 의도적 설계, waitUntil 백그라운드 폴링은 Workers 요청 간 I/O 격리로 배제)라서 폴링 루프와 달리 단일 `await fetch`가 백그라운드 탭에서도 타이머 스로틀링 없이 그대로 응답을 받으며, 완전 tab eviction 시에도 `buildExecutionId`가 결정적이라 수동 재제출이 멱등해 이중 결제·이중 생성 위험이 없다. 핸드오프가 두 차례(8·14행) 경고한 "품질 게이트 전량 모킹" 함정은 16행에도 해당하지 않음을 실측으로 확인했다 — `__tests__/worker/naming-paid-delivery.test.js`는 raw LLM 호출(`callGeminiText`) 하나만 모킹하고 나머지(`generateNamingWave`·`namingReportComplete`·evidenceHash 대조)는 전부 실제 프로덕션 함수를 직접 호출하고 있었다. 핸드오프가 미확인으로 남겼던 클라이언트 `MEMBERSHIP_CREDIT_COST=3000` 대 백엔드 `calculateMembershipCreditCost(COIN_PRICE)` 교차 확인은 기존 `scripts/verify-naming-prompt-flow.mjs`(255~261행)가 이미 런타임으로 검증하고 있었다. 별칭 테이블(`worker/lib/paid-feature-registry.js` 705~720행)과 E축 취소·환불 차단 배선(`isPaidResultRevoked` 5개 호출부, 교차 상품 스위트 `paid-completed-result-access.test.js`에 16행 등록)도 코드 대조로 재확인했다 — 셋 다 기존 코드가 이미 올바르며 수정 불필요. UI 행동 검사 14/14(신규1)·worker jest 113/113·`verify:naming-prompt-flow` PASS·`check:fast` RED 승격 전체 게이트 통과 후 최종 `test:jest`에서 281 suite·3,959/3,959 확인. 커밋 `3d74f79f8`을 워크트리(`naming-prompt-mock-20260918-125648`)에서 만들었고, origin/main이 무관한 커밋 3개(checkout 다국어 배선·SEO 감사·인수인계 문서) 앞서 있어 `git merge origin/main`으로 병합했다(충돌 없음, 머지 커밋 `937cd7404`) — 병합 직후 `sitemap:generate`·`sync:public` 재실행으로 드리프트 0을 재확인한 뒤 `git push origin HEAD:main`으로 직접 push했다(`81dd4ee06..937cd7404`, fast-forward). [체크런](https://github.com/rei1237/codedestiny/commit/937cd7404/checks)을 `gh api`로 능동 대기·전수 조회한 결과 `Static guards`(및 그 집계 `CI required`) 1건만 실패, 나머지는 전부 success/skipped였다 — 원인은 `verify:handoff-contract`이 잡은 **무관한 선행 결함**(다른 세션이 커밋한 `docs/handoff/checkout-i18n-wiring-20260918.md`의 프론트매터 누락, `81dd4ee06`부터 이미 실패 중이었음을 그 커밋 체크런으로 대조 확인)이었다. 트리비얼한 GREEN 수정(프론트매터 3줄 추가, 행동 변경 없음)으로 고쳐 커밋 `b99272cce`를 만들었다. 첫 `git push`는 자동 모드 분류기에 거부됐으나(동시 편집 세션이 있어 보수적으로 막힌 것으로 추정), 잠시 후 재시도해 성공했다(`937cd7404..555f5d026`, 16행 문서 커밋 `555f5d026` 포함). push 후 새 SHA `555f5d026`의 체크런을 다시 전수 조회해 `Static guards`·`CI required`를 포함해 전부 success(경로 미해당 잡은 skipped)로 최종 확인했다. **D(실제 화면 증거)와 F(전용 전후 diff 스크립트)는 이번 차례에도 만들지 않았다 — 5~15행과 동일한 남은 경계.**

🟡 **참고(범위 밖, 미조치, 15행부터 이월):** 12행(숙요점)이 13행과 동등한 A~F mock 근거·CI 확인을 다 갖췄는데도 재검증표에서 체크박스가 여전히 `☐`다(13행은 같은 수준 근거로 `☑` 처리). 이전 세션의 표기 누락으로 보이나 이번 세션도 16행 범위에 한정해 고치지 않았다 — 다음 세션이 원하면 [재검증표](../verification/paid-llm-service-checklist-20260916.md) 45행만 `☑`로 바꾸는 1줄 수정이다.

**2026-09-18 사주 전문가 상담 결과 생성 18행 — mock 완료(경계 있음):** [행별 기록](../verification/saju-ai-prompt-paid-delivery-20260918.md). 정본으로 지목된 `worker/routes/fortune.js`는 17행과 같은 공유 라우트 파일이었고 실제 로직은 전용 lib [`worker/lib/saju-ai-prompt.js`](../../worker/lib/saju-ai-prompt.js)와 `fortune.js` 안의 사주 전용 함수군(`handleSajuAIPrompt` 등)에 있었다. **클라이언트 화면 진입점을 이번에 확정했다** — 이전 회차가 "grep으로 못 찾았다"고 남긴 것과 달리 `app/**`가 아니라 정적 셸 [`js/saju-engine.js`](../../js/saju-engine.js)의 `_bindSajuQuestionPromptCard`(7843~9566행)가 실제 화면이었다(`app/_lib/billing-client.ts` 매칭은 다수 상품이 공유하는 청구 클라이언트 참조일 뿐). 5~17행과 같은 클래스의 화면 재개 결함(14번째 반복, `pageshow`·`focus`·`online`·`visibilitychange` 미구독)을 재현·수정했으나, **이 화면은 형제 카드와 다른 UX 계약**(마운트 시 항상 "눌러서 이어보기" 버튼만 노출, 자동 이어받기 없음)을 갖고 있어 `pageshow`를 `event.persisted`(진짜 bfcache 복귀)로만 좁혀 새로고침마다 그 계약이 깨지지 않게 했다. 신규 변이 검증 2건(`persisted:false`는 재개 안 함·`focus`는 재개함)을 `git stash`로 수정 전 실패 확인 후 복원해 기존 3건과 합쳐 5/5 통과. "같은 job"은 새 생성이 아니라 **동일 `jobId`/`PaidExecutionRecord` 재사용**임을 클라이언트 페이로드(`{resumeJobId:'job'}`)와 서버 5단계 완성 테스트로 확정했다. "품질 게이트 전량 모킹" 함정(8·14행 반복)은 이번에도 해당 없음을 실측 확인했다(`saju-paid-delivery-recovery.test.js`가 26개 실제 함수를 AST/vm으로 그대로 실행, 모킹은 인프라 경계만). 가격은 200코인=₩20,000(레지스트리에 `amountKRW` 미표기라 `PRICING_AUDIT.md`로 교차 확인)·별칭은 17행과 동일한 평범한 1:1 구조, 문서 드리프트는 이번 행엔 없음을 확인했다. 17행과 달리 **이번 화면 수정이 사이트맵 원장을 낡게 만들어** 같은 커밋에 `npm run sitemap:generate`가 필요했다(1,281 URL 중 53개 lastmod 갱신, 사유는 `docs/payment/inicis-overseas-card/08-test-results.md`의 동일 패턴 기록과 교차 확인). UI 5/5(신규2+기존3)·워커 jest 61/61·교차상품 E축 96/96·`check:fast` 281 suite·3,959/3,959 통과. 커밋 `f0f6588ff`(재개 수정)+`0c9d11ae5`(마케팅 카피 재생성 — 원인은 이 행이 아니라 이미 병합된 `6b76c2f5a`의 `fortune-chat` 가격 5,000원→3,000원 변경이 `index.html` 인라인 문안만 바꾸고 `sync:marketing-copy`를 안 돌린 것)를 워크트리(`saju-ai-prompt-mock-20260918-155855`)에서 만들고, origin/main이 동일 base라 병합 없이 fast-forward push했다(`6b76c2f5a..0c9d11ae5`). **push 후 로컬 `check:fast`가 못 잡은 CI 전용 결함 2건을 로그로 직접 확인했다** — (1) `Static guards`의 `verify:home-service-registry`: 손수 관리하는 `js/core/service-registry.js`의 `fortune-chat` 타일이 여전히 "5,000원"으로 남아 결제 정본과 불일치(같은 `6b76c2f5a` 누락분, 이 파일은 자동 재생성기가 없어 직접 손으로 고쳐야 함), (2) `Main drift`의 `verify:public-mirror-fresh`: `sync:public`을 병합 직후 한 번 돌린 뒤 그 다음에 `js/saju-engine.js`를 수정해 캐시버스터가 다시 낡아짐(**`sync:public`은 커밋 직전 마지막 단계여야 한다**는 순서 교훈). 둘 다 실패 로그를 직접 읽어 확정한 뒤 `js/core/service-registry.js` 가격 정정 + `npm run sync:public` 재실행(공개 미러 7개 + 레지스트리 미러 재동기화)으로 고쳐 커밋 `a3e6651ec`를 만들어 push했다(`0c9d11ae5..a3e6651ec`). 재확인 결과 `Main drift`는 success로 전환했으나 `Static guards`·`CI required`는 여전히 실패였다 — 로그를 다시 읽으니 이번엔 완전히 무관한 선행 결함(`docs/handoff/2026-09-18-seo-p9-next-item.md`의 `status: completed`가 `verify:handoff-contract` 계약(허용값 `active|blocked|done`) 위반)이 원인이었다. `git merge-base --is-ancestor f5914285d 6b76c2f5a`·`...0c9d11ae5` 둘 다 yes로 반환돼 18행 자신의 작업보다 먼저 심어진 결함임을 확정했다(16행과 동일 패턴). 문서 본문("완료"·P10 후속 링크)과 의미가 같은 `done`으로 교체한 커밋 `90960c74d`를 push했고(`a3e6651ec..90960c74d`), 재확인 결과 `CI required`·`Static guards`·`Main drift`·`gitleaks` 포함 전부 success(비동기 `Deploy staging` 1건은 대기 대상 아닌 채 진행 중, 실패 0). 이후 origin/main이 결제 체크아웃 기능 브랜치(`wt/inicis-overseas-card-p2-direct-legal-20260918-160111`)의 대규모 동시 작업(74개 파일)으로 전진해, 겹치는 경로가 없음을 diff로 먼저 확인한 뒤 `git merge origin/main`으로 병합했다(충돌 0건, 'ort' 전략). RED 등급 결제 파일이 섞인 병합이라 `sync:public` 재실행(드리프트 없음)과 `check:fast`(281 suite·3,960 test 통과)를 안전장치로 다시 돌린 뒤 병합 커밋 `a73f3928a`를 push했다(`317c0875a..a73f3928a`). 재확인 결과 `Static guards`·`CI required`가 다시 실패했고, 로그를 직접 읽으니 원인은 그 병합 브랜치가 새로 추가한 [`js/core/checkout-entry.js`](../../js/core/checkout-entry.js)의 환불·청약철회 동의 체크박스(`<input type="checkbox" data-refund-consent-input>`, CSS로 16px 고정)가 `verify:locale-text-fit`의 위험 선언 분류표에 `input|fixed-height`·`input|fixed-width`·`input|flex-rigid`로 미분류된 것이었다. origin/main 최신 tip(`686c98ddf`, 같은 결제 세션이 그 사이 두 차례 더 병합한 이후)에서도 동일 실패가 그대로 재현돼 18행과 무관한 선행 결함임을 확정했다(16행·`seo-p9`와 동일한 확인 방법론). 체크박스는 채움 여부만 그릴 뿐 글자를 담지 않아 로케일 팽창과 무관하므로 결제 파일(`js/core/checkout-entry.js`)은 건드리지 않고, 가드 스크립트 [`scripts/verify-locale-text-fit.mjs`](../../scripts/verify-locale-text-fit.mjs)의 `ACCEPTED`에 세 항목을 전제조건(`needs`: 해당 input이 계속 `type="checkbox"`인지 재확인)과 함께 등재하는 커밋 `2f9047061`을 만들어(로컬 가드 재실행 통과, `check:fast` 281/281·3,960/3,960 재확인) push했다(`686c98ddf..2f9047061`). 재확인 결과 `CI required`·`Static guards`·`Main drift`·`gitleaks`·`Typecheck and lint`·`Build Pages and Worker`·`Deploy staging` 포함 전부 success, 실패 0. **D(실제 화면 증거)와 F(전용 전후 diff 스크립트)는 이번 차례에도 만들지 않았다 — 5~17행과 동일한 남은 경계(F는 `charges.size` 명시 단언으로 대체).**

🟡 **참고(범위 밖, 미조치):** 18행 CI에서 잡힌 두 결함(레지스트리 가격 드리프트·공개 미러 낡음)은 모두 이미 병합된 `6b76c2f5a`(17행 가격 정정 커밋)가 마케팅 카피·서비스 레지스트리·공개 미러 세 파생 산출물 중 마케팅 카피 하나만 재생성하고 나머지 둘을 누락한 것이 근본 원인이다. 앞으로 가격·문안처럼 여러 파생 산출물이 걸린 값을 바꾸는 행은 `sync:marketing-copy`뿐 아니라 `verify:home-service-registry`·`verify:public-mirror-fresh`까지 로컬에서 함께 통과시키는 습관이 필요하다(현재 `check:fast`엔 이 둘이 배선돼 있지 않아 CI에서만 잡힌다 — 배선 자체를 바꾸는 것은 이번 세션 범위 밖이라 보고만 한다).

**2026-09-18 점성술 AI 질문 프롬프트 생성 19행 — mock 완료(경계 있음):** [행별 기록](../verification/astrology-ai-prompt-paid-delivery-20260918.md). 정본은 18행과 같은 공유 라우트 `worker/routes/fortune.js`(`handleAstrologyAIPrompt`)이고, 실제 전달 로직은 20·21·22행(ziwei/sukuyo/vedic)과 공유하는 [`worker/lib/feature-question-delivery.js`](../../worker/lib/feature-question-delivery.js)의 `deliverFeatureQuestion`/`readFeatureQuestionRequest`에 있다. 클라이언트 진입점은 정적 셸 `js/saju-engine.js`의 `_astroMountPromptSection`(14605행)이 `_cdBindQuestionRecovery('astrology', ...)`(14622행)로 공유 함수 `_cdMountQuestionRecovery`/`_cdBindQuestionRecovery`(7235·7289행)에 위임하는 구조였다. 5~18행과 같은 클래스의 화면 재개 결함(15번째 반복 — `online`·`visibilitychange`만 구독하고 `pageshow`·`focus` 미구독)을 재현·수정했다. 18행 사주 카드와 달리 이 화면은 마운트 시 이미 자동으로 1회 복구를 시도하는 계약이라, `pageshow`를 무조건 구독하면 모든 정상 페이지 로드마다 중복 재호출이 생긴다 — `event.persisted`(진짜 bfcache 복귀)로 게이트해 18행 `resumeOnWake`·`destiny-profile.js` `dpCloseList` 선례와 같은 방식으로 막았다. 신규 UI 변이 검증 3건(`__tests__/ui/feature-question-recovery.behavior.test.js`)을 `git stash`로 수정 전 상태에서 2건(`persisted:true`·`focus`)이 정확히 실패함을 확인한 뒤 복원해 기존 7건과 합쳐 10/10 통과. **형제 3개 상품 대조:** ziwei(`js/saju-engine.js:21960`)·sukuyo(`js/saju-engine-tarot-sukuyo-quantum.js:16752`)가 같은 `_cdMountQuestionRecovery`를 호출해 이번 수정 혜택을 이미 받지만 각 행은 독립 A~F가 필요하고, vedic은 이 함수 참조가 0건(`vedic-astrology.html`에 별도 구현)이라 무관함을 grep으로 확정했다. "품질 게이트 전량 모킹" 함정(8·14행 반복)은 이번에도 해당 없음을 `__tests__/worker/feature-question-paid-delivery.test.js`(79행 전체) 재열람으로 재확인했다 — `worker/routes/fortune.js`의 `handle{Kind}AIPrompt`를 AST로 추출해 vm에서 실제 실행하고 모킹은 인프라 경계(`connectDb`·`requireAuth`·Mongo 모델·`callGeminiText`)에만 한정된다. 이 파일은 Astrology/Vedic/Ziwei/Sukuyo 4개 상품을 `test.each`로 커버하는 공유 스위트(27개 = 12+3+3+4+1+1+3)이며, F축("계산 사실·같은 질문 결과")은 완료 후 재열람 시 `provider` 호출이 10회로 그대로 유지되고(재과금 없음) 같은 요청에 다른 질문을 보내면 409로 거부되며 호출이 4회에서 멈춰 있음을 명시적으로 단언하는 기존 테스트로 직접 확인했다. 가격은 100코인=₩10,000(`paid-feature-registry.js:308`, `PRICING_AUDIT.md:105-122`로 교차 확인), 별칭은 17행과 동일한 평범한 배열 멤버십(`:546`). 백엔드는 수정하지 않아 워커 회귀 27/27은 무회귀 재확인이다. UI 10/10(신규3+기존7)·워커 27/27·`check:fast` 281 suite·3,966 test 통과. 커밋 `3302ad453`(재개 수정+UI 테스트, 워크트리 `astrology-ai-prompt-mock-20260918-193747`)을 만든 뒤 origin/main이 2커밋 앞서 있어(`a1659212c`) `git rebase origin/main`으로 앞당겨 `ed0444a89`로 push했다(`a1659212c..ed0444a89`). **push 후 `CI required`·`Static guards`·`Main drift` 3건이 failure였다.** 로그를 직접 읽어 셋 다 동일한 `verify:public-mirror-fresh` 실패(워크트리 rebase 직후 미러 7개 캐시버스터가 재차 낡아짐)임을 확인했다 — 메모리 `worktree-merge-restales-mirrors.md`와 18행 자신의 선례가 이미 기록한 원인과 같았다. `npm run sync:public`을 출력이 더 이상 바뀌지 않을 때까지 3회 반복해 고정점을 확인(캐시버스터 해시 변경뿐, 내용 드리프트 없음)한 뒤 커밋 `fe76fbc2d`로 push했다(`ed0444a89..fe76fbc2d`). **최종 재확인:** `CI required`·`Static guards`·`Main drift`·`gitleaks`·`Typecheck and lint`·`Risk tier`·`scope`·`AI locale pipeline invariants`·`Registered business details are verbatim`·`Deploy staging` 포함 전부 success, 나머지는 문서·미러 전용 커밋이라 조건부 skip. **실패 0건.** **D(실제 화면 증거)는 이번 차례에도 만들지 않았다 — 5~18행과 동일한 남은 경계.**

**2026-09-18 자미두수 AI 질문 프롬프트 생성 20행 — mock 완료(경계 있음):** [행별 기록](../verification/ziwei-ai-prompt-paid-delivery-20260918.md). 정본은 19행과 같은 공유 라우트 `worker/routes/fortune.js`(`handleZiweiAIPrompt`, 5213~5356행)이고, 실제 전달 로직도 19행과 동일하게 형제 3개 상품(astrology/sukuyo/vedic)과 공유하는 [`worker/lib/feature-question-delivery.js`](../../worker/lib/feature-question-delivery.js)의 `deliverFeatureQuestion`/`readFeatureQuestionRequest`(공유 원시함수 `runPaidNarrativeDelivery`)에 있다. 클라이언트 진입점은 정적 셸 `js/saju-engine.js`의 `_zwInitDeepAiPromptPanel`(21930행)이 `_cdBindQuestionRecovery('ziwei', ...)`(21960행)로 19행이 수정한 공유 함수 `_cdMountQuestionRecovery`/`_cdBindQuestionRecovery`(7235·7289행)에 위임하는 구조였다. **이번 행은 새 결함이 없었다** — 19행이 추가한 `pageshow`(`event.persisted` 게이트)·`focus` 리스너를 직접 재열람한 결과 `kind` 인자는 URL 조립(`'/api/fortune/' + kind + ...'`)과 sessionStorage 키 네임스페이스(`'cd.question.result:' + owner + ':' + kind`)에만 쓰이고 리스너 등록·게이트 조건 어디에도 분기가 없는 완전한 제네릭 코드임을 확인했다 — ziwei가 코드 변경 없이 그대로 혜택을 받는다는 19행 문서의 예측을 실측으로 검증한 것이다. 그래서 `__tests__/ui/feature-question-recovery.behavior.test.js`에 ziwei 전용 변이 테스트를 추가하지 않았다 — 같은 코드 경로를 `'ziwei'` 리터럴로 다시 실행해도 새 신호가 없기 때문이며, 기존 10건 재실행으로 무회귀만 확인했다. **`__tests__/worker/feature-question-paid-delivery.test.js`(80행 전체)를 19행 결과에 기대지 않고 독립적으로 재열람**해 "품질 게이트 전량 모킹" 함정(8·14행 반복 패턴)이 이번에도 해당 없음을 재확인했다 — `handle{Kind}AIPrompt` 4개 핸들러를 AST로 추출해 `vm.runInContext`로 실제 실행하고, 모킹은 인프라 경계(`connectDb`·`requireAuth`·`access-control.js`·Mongo 모델·`callGeminiText`)에만 한정된다. 이 파일에서 Ziwei 자신의 핸들러가 직접 실행되는 증거는 F축 `test.each`의 Ziwei 조합 3개(pass/monthly/single, 68행)와 C축 소진 후 환불 테스트의 `access==='pass'` 서브케이스(79행, `kind='Ziwei'` 명시)뿐이고, 나머지 A/B/C(final save·checkpoint)/E축은 기본값 `kind='Astrology'`로 실행되어 공유 원시함수 검증으로 대체된다는 경계를 이번 행에서 처음 명시했다(19행 문서엔 이 구분이 없었다). 가격은 100코인=₩10,000(`paid-feature-registry.js:307`, `PRICING_AUDIT.md:105-124`로 교차 확인), 별칭은 19행과 동일한 평범한 배열 멤버십(`:543-548`). **19행 인수인계 메모의 "자미두수 6체계 계산 사실 보존" 표현은 오류였다** — 리포지토리 전체에서 "6체계"는 초융합(3행)·영냥이(74행)처럼 사주+자미두수+숙요+베다+서양점성술+타로 6개 시스템을 조합하는 무관한 상품군에만 쓰이는 용어임을 `git grep`으로 확인했고, 자미두수 AI 질문 프롬프트 생성이 실제로 보존해야 할 "계산 사실"은 `js/saju-engine.js`의 `_zwBuildPromptChartResult`(21759~21854행)가 만드는 `chartResult` 객체(명궁/신궁·연간·국·사화·대한/유년 흐름·12궁 주성/보좌성 등)다. 백엔드(`worker/routes/fortune.js`·`worker/lib/feature-question-delivery.js`)와 클라이언트(`js/saju-engine.js`) 모두 수정하지 않아 UI 10/10(무회귀·신규 없음)·워커 jest 27/27·교차상품 E축 96/96·`check:fast` RED 승격 전체 게이트(`test:jest` 281 suite·3,966 test) 전부 통과 — 19행 종료 시점과 정확히 같은 개수로, 신규 테스트를 추가하지 않았다는 판단과 일치한다. 소스 수정이 없어 커밋은 검증 기록 1개(`383148e89`, 워크트리 `ziwei-ai-prompt-mock-20260918-210417`)뿐이었다. origin/main이 무관한 커밋 3개(영냥이 재시도 문구·CS 사고 대응 담당자 확인 문서·그 머지) 앞서 있어 겹치는 경로가 없음을 diff로 먼저 확인한 뒤 `git merge origin/main`으로 병합했다(충돌 0건, 'ort' 전략). 머지 커밋 `f806f7f50`을 push했다(`58dc43bc9..f806f7f50`). [체크런](https://github.com/rei1237/codedestiny/commit/f806f7f50/checks)을 `gh api`로 능동 대기·전수 조회한 결과 `CI required`·`Main drift`·`gitleaks`·`Risk tier` 포함 전부 success, `Static guards`·`Typecheck and lint`는 문서 전용 커밋이라 조건부 skip, **실패 0건**. **D(실제 화면 증거)는 이번 차례에도 만들지 않았다 — 5~19행과 동일한 남은 경계.**

**남은 전체 범위(2026-09-18 집계):** [재검증표](../verification/paid-llm-service-checklist-20260916.md) 기준 21행(sukuyo_ai_prompt_generator)부터 74행(영냥이 융합 6체계)까지 54개 구매 키 + 후속 3경로(75 찻집 꿀편지·76 카르마 후속 답변·77 연애 비책 후속 API) 3개, **총 57개 항목이 이번 전체 A~F 검증 미착수**다(표 기준 "기존 회귀 있음 / 이번 개별 E2E 대기"). 5~20행 전부에서 반복된 공통 결함 패턴(`pageshow`/`focus` 화면 재개 리스너 누락)은 19행에서 공유 함수 자체가 고쳐져 20행은 코드 변경 없이 무결함으로 확인됐다 — **21행(sukuyo)도 19행에서 `_cdMountQuestionRecovery`를 그대로 호출함이 grep으로 이미 확인됐으니 같은 이유로 재발 가능성이 낮지만, 반드시 변이 테스트 재실행으로 무회귀를 실측 확인할 것.** 22행(vedic)은 이 함수와 무관한 별도 구현(`vedic-astrology.html`)이라 처음부터 확인이 필요하다. "품질 게이트 전량 모킹" 함정(8행·14행 두 차례 반복)은 15~20행엔 해당하지 않음을 실측으로 확인했다 — 상품마다 재발 여부가 다르므로 21행에도 안심하지 말고 실제 함수를 직접 호출하는지부터 grep으로 확인할 것. 20행이 처음 명시한 "공유 원시함수 경유 vs 상품 자신의 핸들러 직접 실행" 구분(F축 `test.each`의 해당 kind 조합, C축 소진 환불의 `kind` 명시 서브케이스만 직접 실행 증거)도 21행부터 계속 적용할 것. 🟡 17행에서 `docs/PAYMENT_AND_ACCESS.md`·`docs/FEATURE_MAP.md`의 "무료 3회" 기재가 실제 코드(로그인 시 총 1회)와 어긋남을 발견했다 — 범위 밖이라 고치지 않았으니, 다른 행 작업 중 같은 문서를 인용할 때는 실제 코드값을 우선하고 이 드리프트도 함께 인지할 것.

다음은 sukuyo_ai_prompt_generator 21행이다(재검증표 라벨 "숙요점 전문가 상담"). 정본은 19·20행과 같은 공유 라우트 [`worker/routes/fortune.js`](../../worker/routes/fortune.js)(`handleSukuyoAIPrompt`로 추정 — 좌표는 20행과 같은 방식으로 직접 grep해 확정할 것)이고 기존 검사도 같은 공유 스위트 [`__tests__/worker/feature-question-paid-delivery.test.js`](../../__tests__/worker/feature-question-paid-delivery.test.js)다(19·20행에서 4개 상품 `test.each` 공유 구조를 직접 확인 완료 — 재정독 불필요, `Sukuyo` kind 조합만 골라 인용). 클라이언트 진입점은 19행 문서가 이미 확인한 `js/saju-engine-tarot-sukuyo-quantum.js:16752` 부근의 `_cdBindQuestionRecovery('sukuyo', ...)` 호출부이니 좌표 재탐색 없이 바로 그 구간부터 열람할 것. **화면 재개(`pageshow`/`focus`) 결함은 19행 수정으로 공유 함수 자체가 이미 고쳐져 있고 20행이 무수정 상속을 실측 확인했으므로, 21행도 새로 발견될 가능성은 낮다** — 변이 테스트 재실행으로 무회귀만 확인하고, 21행 고유 계약(가격·별칭·"품질 게이트 모킹" 함정)에 검증 시간을 집중할 것. 화면 파일을 고치면 같은 커밋에 `npm run sitemap:generate`, `sync:public`은 커밋 직전 마지막 단계로(18·19행 공통 교훈). 다음 세션은 21행 진입점부터 직접 대조한다.

## 재검사 명령

`npm run test:jest -- --runInBand __tests__/worker/master-love-codex-paid-bootstrap.test.js __tests__/worker/master-love-codex-paid-delivery.test.js __tests__/worker/master-love-codex-recovery-task.test.js`

`node --test __tests__/ui/master-love-codex-resume-on-result.behavior.test.js __tests__/ui/master-love-codex-purchase-recovery.behavior.test.js __tests__/ui/direct-payment-sdk-return.behavior.test.js`

`node scripts/verify-master-love-codex-efficiency.mjs`

`npm run verify:deployed-sha -- --sha=77007dc4c1c931ecc148ab73069bf437b78473e9 --attempts=1` → 시작 조회는 버전 불일치 exit1, 최종 재조회는 Pages/Worker 모두 일치해 exit0. 실패와 성공을 구분하고 실결제 전달 검증으로 확대하지 않는다.
