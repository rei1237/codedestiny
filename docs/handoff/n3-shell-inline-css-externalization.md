---
status: active
updated: 2026-09-02
next: 실행 여부를 사용자와 재합의 — 착수 전 "남은 작업" 1번(dist 읽는 검증기 전수 판정)부터
---

# N3 조사 — 셸 인라인 CSS 의 dist 단계 외부화

## 왜

앱/웹 최적화 로드맵(2026-09-02)에서 셸 인라인 CSS 재구조화(N3)는 **조사만, 실행 제외**로 결정됐다. 이 문서가 그 조사 산출물이다. 실행은 별도 세션에서 재합의.

## 실측 (2026-09-02, 이 워크트리)

- 소스 `index.html`: HTML 레벨 `<style>` **86블록 · 816.5KB** (측정: node 로 줄 단위 태그 스캔. 정규식 전역 매칭은 JS 문자열 안 `<style>` 을 오인해 1,432KB 로 부풀린다 — 재측정 시 주의)
- dist(미니파이 후) 기준 감사값: 85블록 · 645.8KB (`home-ux-audit-2026-09-01.md`)
- 분포는 평평하다: 최대 블록 61.6KB(`index.html:1391`), 상위 5개가 전체의 **30.5%**(약 249KB), 20KB 이상이 10개 남짓. 한 방에 큰 덩어리가 없어 "상위 N 개만" 전략의 한계 효용이 빨리 꺾인다.

## 채택 가능성 — dist 단계 후처리 (추천안)

`scripts/externalize-dist-inline-scripts.mjs` 와 같은 자리: **소스는 그대로, dist 의 바이트만** `<link rel="stylesheet" href="/css/shell/<해시>.css">` 로 치환. 근거는 그 스크립트 머리말 — index.html 을 문자열로 읽는 verify 가 61개(함수 본문 절단 19개)라 소스 분리는 시도 즉시 가드가 깨진다(실전례 있음). dist 방식이면 이 61개는 전부 무관하고, 미러 십수 벌이 같은 해시 파일을 공유해 중복 저장도 준다. 배선 자리는 `scripts/run-postbuild.mjs` 의 externalize(JS) 단계 옆, `minify-dist-css.mjs` 와의 순서 조정 필요.

## 예상 효과

- 셸은 no-store 라 인라인 CSS ~646KB(미니파이 후)가 **매 방문 재전송**된다. 외부화하면 재방문은 캐시 히트. 첫 방문 총 바이트는 동일.
- 앱(Capacitor)은 dist 를 번들로 들고 다니므로 네트워크 이득은 없고, 미러 중복 제거로 번들 크기만 준다.

## 위험 (실행 시 반드시 선결)

1. **dist 를 읽는 검증기 34개**(`git grep -l dist -- scripts/verify-*.mjs`, 2026-09-02 — 주석 언급 포함 과대집계일 수 있음, 미판정). CSS 를 텍스트로 읽는 것(`verify:payment-choice-parity` 의 89룰 등)이 dist 검사면 즉사. 전수 판정이 착수 조건.
2. **첫 방문 렌더 경로**: `<link>` 는 렌더 블로킹은 유지하나 네트워크 왕복이 추가된다 — LCP 가 늘 수 있다. 상위 블록만 빼는 절충 검토.
3. 본문 중간 블록(`:35621` 등)의 캐스케이드 순서 보존 — 86블록이 마크업과 교차 배치라 순서가 곧 우선순위다.
4. 신규 경로 엣지 404 캐시(메모리 `edge-404-cache-on-new-assets`) + `_headers` 캐시 규칙 추가(`/js/shell/` 선례).
5. CLS: 메모리 `late-css-blocks-are-cls-in-the-shell`.

## 남은 작업 (실행 합의 시)

- [ ] dist 읽는 검증기 34개 → CSS 텍스트 의존 여부 전수 판정 (끝 판정: 의존 0건 또는 대응책 명시)
- [ ] 프로토타입: 상위 5블록만 외부화 → `measure:home-score` 축3 + LCP 밴드 비교 (끝 판정: LCP 밴드 비악화 + 재방문 전송량 감소 실측)
- [ ] `_headers` 캐시 규칙 + 배포 후 첫 요청 404 캐시 확인

## 모르는 것

- Cloudflare 엣지에서 no-store 셸 + 캐시되는 CSS 의 실제 재방문 절감 폭 — 실측 전엔 숫자를 못 박는다.
