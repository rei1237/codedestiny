# 꽃돼지 이용권 신규 정책과 판매 조건

2026-09-21 비용 상한 재설계안. 공식 단가와 코드의 장문 생성·저장형 재시도·저가 반복 조합을 반영했다. 2026-09-22 Gemini `countTokens`와 Workers AI의 보수적 UTF-8 상한으로 공급자 시도당 입력을 각각 50,000토큰 이하로 고정했다. Workers AI 체인 원가를 포함하면 현재 가격은 Play 30% 스트레스에서 40% 기준에 미달하며, 상품별 실원가·Play 신규 SKU 증거도 없으므로 판매 차단을 유지한다. 현재 정책 버전은 `flower-cost-20260921`, 직전 후보 정책은 `flower-20260921`, 버전 없는 구매는 `legacy`다.

| 신규 이용권 | 30일 판매가 | 건당 상한 | 누적 한도 | 프로필 |
| --- | ---: | ---: | ---: | ---: |
| 꽃돼지 스탠다드 | 89,000원 | 3,000원 | 3,000원 | 3개 |
| 꽃돼지 프리미엄 | 269,000원 | 10,000원 | 10,000원 | 7개 |
| 꽃돼지 VVIP | 879,000원 | 30,000원 | 30,000원 | 15개 |

자동갱신 없음. 상담 횟수 제한을 추가하지 않는다. 실제 상품 가격만큼 차감하고, 초과분을 자동 청구하지 않는다. 영냥이는 기존 1,000~30,000원 단건 결제를 유지하며 이용권·월정석에서 제외한다. Family와 기존 4종의 신규 주문은 닫되, 이미 생성된 주문·구매·선물·환불·재열람은 유지한다.

정본은 `lib/payment/pass-policy.js`. 기존 `pass-pricing.js`와 `profile-limits.js`의 4종 값 및 직전 v2 3종은 과거 구매 해석용으로 보존한다. 새 주문 ID는 `*_1m_v3`, Play 상품은 `cd_pass_*_30d_v3`. 주문 metadata, 선물 productSnapshot, 활성 profileSubscription에 정책 버전을 저장한다. 서로 다른 정책의 활성 기간은 합산하지 않는다. 구정책 구매자의 가격·누적 한도·VVIP 건당 20,000원·Family 권리를 신정책으로 바꾸지 않는다.

## 코드 상한 기반 재설계 결과

재현 명령은 `node scripts/report-pass-economics.mjs --write`, 입력은 `config/pass-cost-planning-20260921.json`, 결과는 `docs/verification/pass-economics-scenarios-20260921.json`이다. 이는 판매 증거가 아니라 코드에서 확인한 호출 구조를 공급자 단가에 대입한 계획 시나리오다.

- Gemini 2.5 Flash Standard 공식 단가: 입력 $0.30/100만, 출력과 thinking $2.50/100만. 확인한 장문 경로는 thinkingBudget 0이지만 실제 배포 모델 환경값은 별도 확인이 필요하다.
- 입력 토큰: `lib/gemini-input-token-limit.mjs`가 공급자의 비과금 `countTokens` 응답을 생성 전에 확인해 Gemini 시도 1회당 50,000토큰을 초과하면 모든 공급자 호출을 차단한다. `lib/workers-ai-input-token-limit.mjs`는 모델 공통 count API가 없는 Workers AI content를 UTF-8 바이트 수+채팅 템플릿 여유 512로 보수 계산해 `env.AI.run` 전에 50,000토큰 상한을 강제한다. 기본 GLM/Llama는 공식 컨텍스트 창에서 요청 출력·예약분을 뺀 값이 더 작으면 그 상한을 쓴다. 두 공급자 상한 모두 환경값으로 올릴 수 없다.
- 출력·재시도: 각 경로의 부분 수, 부분별 최대 출력 토큰, 저장형 재시도 상한, 공통 공급자 재시도 상한을 곱했다. 성공 출력은 논리 호출당 한 번만 최대치로 잡고, 공급자 재시도는 입력 비용에 반영했다.
- Workers AI: Gemini 허용 재시도 뒤 GLM 4.7 Flash와 Llama 3.3 70B가 모두 최대 입력·출력 비용을 만들 수 있는 체인을 합산한다. GLM은 입력 $0.0605/100만·출력 $0.40/100만, Llama는 입력 $0.293/100만·출력 $2.253/100만이다. 실제 환경 오버라이드 모델은 별도 확인 대상이다.
- 저장·지원·환불 등 미확정 변동비는 전체 공급자 토큰 계산에 30% 충당을 더했다. 실제 비용 근거가 아니며 부족하면 상향한다.
- 가장 긴 30,000원 상품보다 3,000원 `tarot-love-relationship` 반복이 더 비싼 최대 소진 조합이다. 21부분 × 부분별 3회 × Gemini 공급자 최대 3회 뒤 Workers AI 체인을 포함한 계획 변동원가는 14,700원/건이다. 이 상품을 월 한도 안에서 1/3/10회 반복하는 조합이 각 등급 가격을 결정한다.
- 변동원가 2배와 Play 30% 시나리오에서 40% 공헌이익을 위한 새 산출 최소가는 119,800/359,400/1,197,800원이다. 현재 89,000/269,000/879,000원은 약 30% 공헌이익에 그쳐 이 시나리오에서 확정가가 될 수 없다. 실제 Play 적용 수수료와 판매 가능한 가격 상한을 확인한 뒤 가격 또는 채널 정책을 다시 결정한다.

| 코드 경로 | 논리 호출 최대 | Gemini 시도 최대 | Gemini 입력 토큰 | 최대 성공 출력 토큰 | Gemini 비용 | Workers 체인 비용 | 30% 충당 후 계획 변동원가 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| MindScan | 57 | 171 | 8,550,000 | 541,500 | 6,270원 | 3,911원 | 13,300원 |
| 연애 관계 타로 | 63 | 189 | 9,450,000 | 598,500 | 6,930원 | 4,323원 | 14,700원 |
| 인생책 AI | 45 | 135 | 6,750,000 | 264,000 | 4,296원 | 2,394원 | 8,700원 |
| 인생 총운 AI | 45 | 135 | 6,750,000 | 402,000 | 4,848원 | 2,980원 | 10,200원 |
| 나크샤트라 AI | 27 | 81 | 4,050,000 | 270,000 | 3,024원 | 1,910원 | 6,500원 |
| 자미두수 심층 PDF | 45 | 135 | 6,750,000 | 405,000 | 4,860원 | 2,992원 | 10,300원 |
| 마스터 연애 궁합 | 60 | 60 | 3,000,000 | 480,000 | 3,360원 | 3,735원 | 9,300원 |
| 초융합 상담 | 27 | 81 | 4,050,000 | 442,368 | 3,714원 | 2,642원 | 8,300원 |

계획 입력 토큰은 코드가 강제하는 공급자 시도당 50,000토큰 상한이다. Workers 열은 폴백 두 모델이 모두 비용을 만든다는 상한 계획이며 실측 평균이 아니다. 표의 30% 충당도 저장·지원·환불 실원가를 실측한 값이 아니므로, 위 표만 실제 최대 과금액이나 판매 승인 증거로 쓰지 않는다.

이 가격은 아직 상품 단위 손실 방지용 확정가가 아니다. 월매출 약 50,000원은 어느 등급 판매가에도 못 미치며, 기준 고정비 108,000원 및 5배 스트레스 540,000원을 회수하지 못한다. Play 15% 계획에서는 40% 기준을 넘고 540,000원 회수에 월 15/5/2개가 필요하지만, Play 30%에서는 가격 자체가 40% 기준에 미달한다. 실제 판매량이 없으면 전체 사업 손익분기도 성립하지 않는다.

## 단건 가격

운명 찻집 5종은 5,000원. 기존 30,000원 상품은 모두 유지한다. 나머지 꽃돼지 운세 상품은 3,000→3,000원, 5,000→3,000원, 7,000/10,000→5,000원, 20,000→10,000원이다. 무료·판매 중단 이력 키는 유지하며 프로필 관리 수수료도 운세 상품 인하에 포함하지 않는다. 변경 상품·해금 ID·코스별 대조표는 `docs/verification/flower-price-changes-20260921.json`이다. 결제 완료 주문의 실제 결제액은 과거 주문에서 읽는다.

## 판매 차단과 원가 검증

PortOne의 2026-09 실정산 집계와 Cloudflare 청구·Workers AI 계정 집계를 읽기 전용으로 확인했지만 신규 v3 이용권·상품별 유료 상담 표본이 아니다. 상품별 실원가·Play 신규 SKU·적용 수수료 증거가 없어 **새 이용권 3종 모두 웹·앱 판매 차단**이다. 외부 집계 정본은 `docs/verification/pass-external-cost-evidence-20260922.json`, 상세 관찰과 재현 명령은 `docs/verification/pass-sale-readiness-20260922.md`. `lib/payment/pass-cost-evidence.js`의 빈 객체를 mock·추정 단가·부분 정산으로 채우지 않는다. GET `/api/payments/pass-offers`가 화면과 신규 주문의 공통 판매 상태를 제공한다. Play는 웹과 별도 증거와 `verifiedProductId`가 필요하다. 기존 주문 확정·선물 수령·복원에는 신규 판매 차단을 적용하지 않는다.

`node scripts/audit-pass-profitability.mjs [evidence.json]`은 읽기 전용이다. 차단된 항목이 있으면 종료 코드 2다. 각 등급/채널별 증거 형태:

```text
{ vvip: { web: {
    reviewedAt, sourceRefs,
    priceKRW: 879000, maxCoveredCoin: 300, monthlyLimitCoin: 300,
    netRevenueKRW, paymentFeeKRW,
    products: { canonicalFeatureKey: { priceKRW, maxCostKRW, sourceRefs } }
} } }
```

- `netRevenueKRW`: 세금 제외 매출. PG/Play 수수료는 채널별 `paymentFeeKRW`에 별도 반영한다.
- `maxCostKRW`: 계산·생성·허용된 전체 재시도·저장·지원·환불 충당을 포함한 상품 1건의 보수적 최대 원가. 단순 평균이나 토큰 비용만 넣지 않는다. 요청 실패·중단·재개·수리 호출도 포함한다.
- 코스별 키는 `featureKey::reason`. 일반 카탈로그뿐 아니라 해금 ID·레거시 reason 경로까지 비용 증거를 요구한다. 중단 키가 보수적으로 남아 비용 증거를 요구할 수 있다. 누락을 0원으로 처리하지 않는다.
- 예산 내에서 반복 구매 가능한 가장 비싼 원가 조합을 동적 계획법으로 계산한다. 표면 판매가가 가장 큰 상품만 반복한다고 가정하지 않는다.
- `(세금 제외 매출 - 최대 사용 원가 - 결제 수수료) / 세금 제외 매출 >= 40%`여야 한다. 이는 승인된 내부 목표이며 실측 수익률이나 무손실 보장이 아니다. 고정비와 판매량에 따른 사업 전체 손익은 별도다.

## 토큰 집계와 남은 실측

Gemini 공식 `models.countTokens`는 생성 전 입력 검사용이며 입력·시스템 지시·멀티모달을 계산한다. 공식 billing FAQ상 이 요청은 과금되지 않고 추론 쿼터에도 포함되지 않는다. 코드 검증은 mock 응답으로만 수행하며 실제 공급자 호출은 하지 않는다. 상한 검사가 일시적으로 실패하면 Gemini 생성 요청은 보내지 않는다. Workers AI는 공급자 usage가 있으면 실제 prompt/completion 토큰을 기록하고, 없을 때만 `estimated=true` 문자수 추정을 남긴다. 인증 대시보드 최근 1개월은 1.03k Neuron·입력 10.94k·출력 26.52k, 9월 청구 가능 사용량은 $0.00, 최근 Workers Paid 청구서는 $5.00였다. 별도 GraphQL UTC 30일 재현은 1,025 Neuron·일별 무료분 초과 추정 $0으로 동일한 규모를 보였다. 둘 다 계정 집계이지 상품별 유료 상담 폴백 실원가가 아니므로 판매 승인 evidence로 쓰지 않는다.

`node scripts/report-llm-token-usage.mjs llm.log --prices tariffs.json`으로 provider/model별 단가를 적용한다. 미등록 모델·추정 토큰은 비용 `null`, `complete:false`다. 캐시 입력·thinking 토큰을 별도로 반영한다. 단가 파일 키는 `gemini/실제모델명` 등이며 값은 `reviewedAt`, `sourceRefs`, `inputUsdPerMillion`, `cachedInputUsdPerMillion`, `outputUsdPerMillion`, `thinkingIncludedInOutput`을 포함한다. 실제 모델 청구 방식에 맞게 thinking 중복 여부를 확인한다. 파일에 실제 공급자 단가와 적용일·출처를 기록한다. 확인한 Flash Standard 단가는 `config/llm-tariffs-20260921.json`에 있다. candidatesTokenCount와 thoughtsTokenCount를 별도로 기록하므로 이 파일의 thinkingIncludedInOutput은 false다. 캐시 보관료·검색·그 외 모델과 요금제는 이 파일로 계산하지 않는다.

기존 단일 단가 모드는 추정 비교용이며 판매 증거가 아니다. 공통 LLM 클라이언트를 거치지 않던 MindScan·love-reading에도 토큰 로그를 추가했다. 요청 내용·개인정보는 새 로그에 넣지 않는다. 이력 전체의 원가가 소급 수집된 것은 아니다. 로그 유실, 타임아웃 과금, unlabeled serviceId, 공통 love-reading 경로의 실제 SKU 연결, USD→KRW 환산 근거, 저장·지원·환불 원가는 별도 실측이 필요하다. 관측 합계가 곧 허용 재시도 전체의 상한이라는 가정도 금지한다.

## 운영 적용 조건

1. 상품별 실원가·재시도 상한 증거와 최대 소진 조합이 40% 기준을 충족할 것.
2. Play 신규 SKU의 KRW 가격·국가별 상품 정보·비자동갱신 조건을 확인하고, 이전 SKU의 신규 판매 종료와 기존 영수증 복원을 함께 검증할 것.
3. 구정책 활성 이용권·미수령 선물·결제 후 미지급 주문의 복구를 실환경에서 승인받아 확인할 것.
4. 명시 승인 후 운영 승격. 실 PG·유료 LLM·운영 DB 쓰기는 별도 승인. Pages `/version.json`과 Worker `/api/version`이 승격 SHA와 일치해야 배포 완료다.
5. 승격일 +7일/+30일에 등급별 최대 사용 고객 원가·공헌이익·재구매·환불·전달 실패를 점검한다. 승격일이 아직 없으므로 점검 날짜는 확정하지 않는다.

카카오 채널 소개는 영냥이 중심 문구로 저장했고 기존 경력·예측 이력은 보존했다. 채널 이름 변경은 카카오 UI가 비즈니스 채널 제한으로 거부했다. 프로필 PNG는 준비했으나 Chrome 확장 프로그램의 파일 URL 접근 권한이 없어 업로드를 완료하지 못했다. 메시지 발송·환영 메시지 활성화는 하지 않았다.

## 추가 손익 재검토 (사용자 제공 예산)

- PG 수수료 5% 이하: 사용자 제공. 부가세 포함 여부가 미확인이므로 계산은 결제 총액의 5.5%를 사용한다. 포트원·부가서비스·건당 최소 수수료가 있다면 별도 추가한다.
- Cloudflare 기본 $5, MongoDB M10 월 100,000원 예산: 사용자 제공. 실제 송장이 아니다. 환율 1,600원/USD는 현재 환율 주장이 아닌 스트레스 가정이다. 합계 108,000원/월, 증가 시 216,000원/월도 비교한다. 추가 백업·트래픽·스토리지·이메일·지원 인건비·광고비·환차 비용이 포함됐다고 가정하지 않는다.
- Google Play 15% 프로그램 가입 미확인. 30일 비자동갱신 상품에 자동갱신 구독 15%를 적용하지 않는다. 한국 적용 약관·계정 프로그램·앱 출시일 규정을 재확인한다. 15%/30%를 모두 계산하며 미확인 상태는 30% 시나리오로 검토한다. 계산은 보수적으로 결제 총액에 수수료를 적용한다. 실제 정산의 세금 제외 기준이 확인되면 net 기준으로 계산한다. Play Billing에 웹 PG 수수료를 중복 합산하지 않는다. 한국 대체결제는 Google 수수료 감면분과 PG 비용을 함께 계산해야 하므로 별도 검토한다.
- 세금 제외 매출은 국내 VAT 10%를 가정한 판매가/1.1. 사업자의 실제 세무·정산 조건을 검증해야 한다.

`node scripts/report-pass-economics.mjs`의 재현 가능한 시나리오는 `docs/verification/pass-economics-scenarios-20260921.json`이다. 현재 실제 생성 원가는 null, saleApproval은 false다.

아래 표는 **v2 이전 후보 비교이며 현행 가격표가 아니다.** 당시 후보가 왜 재설계됐는지 보존한다.

| 폐기된 v2 후보 | 웹: 전체 사용의 허용 변동원가 | 앱 30%: 전체 사용의 허용 변동원가 | 40% 공헌이익 확보 시 서버 손익분기 판매량 |
| --- | ---: | ---: | ---: |
| 9,900원 | 4,855원 이하 | 2,430원 이하 | 월 30개 |
| 29,900원 | 14,664원 이하 | 7,339원 이하 | 월 10개 |
| 59,900원 | 29,378원 이하 | 14,702원 이하 | 월 5개 |

허용 변동원가는 이용권 한도를 전부 썼을 때 생성·재시도·변동 서버·저장·지원·환불 충당의 합이다. 위 표는 실제 원가 또는 수익률이 아니다. 고정 서버 예산이 두 배면 손익분기 판매량도 각각 60/20/10개다. 등급별로 단독 판매하는 비교이며 혼합 판매에서는 공헌이익 합계로 고정비를 회수한다. 손익분기는 이익 0원의 경계이므로 흑자를 위해 이를 넘어야 한다. 다른 고정비가 있으면 추가한다.

당시 VVIP의 앱 원가 여유를 30,000원 상담 3회에만 나누면 건당 약 4,900원이었지만, 실제 가격 결정 조합은 저가 상담 반복이었다. v3는 `passEconomics`의 `minimumPriceForTargetKRW`와 이 반복 조합을 사용했다. 기존 구매권과 결과 품질은 줄이지 않았다.

판매 증거에는 financials가 추가로 필요하다: `{ channel, basis: "reviewed", reviewedAt, sourceRefs, vatRate, feeRate, feeBasis: "gross" 또는 "net", fixedMonthlyKRW, minimumMonthlySales }`. 정산액과 수수료 합계가 일치해야 하며, 최소 월 판매량 가정에 고정비를 배분한 후에도 이익이 양수여야 한다. 최소 판매량은 보장된 매출이 아니며 근거와 민감도 검토가 필요하다. 기존 40% 공헌이익 조건과 함께 적용된다. 미검증 예산·평균 토큰만으로 evidence를 채우지 않는다.

공식 단가 확인 (2026-09-21 열람):
- Gemini 2.5 Flash Standard: 텍스트 입력 $0.30/100만 토큰, 출력·thinking $2.50/100만 토큰, 캐시 입력 $0.03. 캐시 보관료/검색 도구는 별도다. 코드 기본 모델은 lib/llm-client.ts의 gemini-2.5-flash이며 환경 설정의 실제 모델과 대조해야 한다. https://ai.google.dev/gemini-api/docs/pricing
- Play 수수료 및 한국 대체결제: https://support.google.com/googleplay/android-developer/answer/112622
- Workers 기본료 및 요청/CPU 초과: https://developers.cloudflare.com/workers/platform/pricing/
- Atlas는 클러스터·지역·스토리지·백업 등에 따라 다르므로 M10이라는 이름만으로 실제 요금을 확정할 수 없다: https://www.mongodb.com/docs/atlas/billing/cluster-configuration-costs/
- PG 정산의 수수료와 수수료 부가세를 따로 확인: https://help.portone.io/category/admin-console/pg-settlement


### 비용 증가에 대한 추가 내부 판매 기준

사용자가 유지비 증가를 재차 강조하여 신규 판매 심사에 **상품별 최대 변동원가 2배, 고정 서버비 5배** 스트레스 조건을 추가했다. 이는 미래 비용의 정확한 예측이나 손실 보장이 아니라 보수적인 내부 여유분이다. 기준 예산 108,000원의 5배는 월 540,000원이며 그 이상 증가가 예상되면 더 높은 값을 사용한다. 상품 최대 원가 2배에는 환율·단가·재시도 증가 여유를 포함하며, 실제 변동 증가가 2배를 넘으면 다시 심사한다. 기존 구매의 권리를 줄이지 않는다.

증거에 `stress: { sourceRefs, productCostMultiplier: 2 이상, fixedMonthlyKRW: 기본 고정비의 5배 이상 }`를 포함한다. 이 스트레스 후에도 공헌이익률40% 이상이고 최소 판매량 가정으로 나눈 고정비를 뺀 이익이 양수여야 판매 가능하다. 위 폐기 표의 VVIP 기본 원가 허용치7,351원은 v2에만 해당하며 v3 판정에는 사용하지 않는다.

보고서는 고정비 2/3/5배를 모두 계산한다. 환율 2,000원/USD 가정은 달러 비용 25% 증가, 기본 서버 예산 110,000원으로 따로 표시한다. 이는 현재 환율이 아니다. 최소 판매량을 임의로 높여 심사를 통과시키지 말고 실제 매출 근거와 부진 시나리오를 함께 검토한다. 이용권 수익만으로 서버비 전체를 회수하는 보수적 계산이므로 다른 단건 매출을 이중 산입하지 않는다.
