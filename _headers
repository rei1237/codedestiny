# Cloudflare Pages cache policy
# Strategy:
#   - HTML: no-cache (조건부 재검증 — 아래 "HTML entry points" 블록 주석 참고)
#   - SW, manifest, and version.json: no-store
#   - Hashed Next.js build assets: immutable long cache
#   - Fixed-name JS/CSS/assets: short edge cache

# Security headers (all responses)
# Permissions-Policy 주석은 반드시 이 위치(0열)에 둔다 — 규칙 블록 안에서 들여쓴 줄은
# 전부 "헤더 이름: 값"으로 파싱되므로, 들여쓴 `#` 은 주석이 아니라 깨진 헤더가 된다.
# camera/geolocation 은 실제로 쓴다(관상 라이브 카메라 PhysiognomyUI.js, 베다 현재위치 vedic-astrology.html)
# — 끄지 말고 self 로 묶어 광고 등 서드파티 iframe 이 요청하지 못하게만 한다.
# 나머지는 코드에 사용처가 없어 차단. payment/fullscreen/gyroscope/accelerometer 는 각각
# PortOne 교차출처 결제 iframe·모션 연출이 쓰므로 일부러 넣지 않았다(브라우저 기본값 유지).
/*
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  Strict-Transport-Security: max-age=31536000; includeSubDomains; preload
  Permissions-Policy: camera=(self), geolocation=(self), microphone=(), usb=(), serial=(), bluetooth=(), midi=()
  Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' 'wasm-unsafe-eval' https://static.cloudflareinsights.com https://www.googletagmanager.com https://pagead2.googlesyndication.com https://cdn.portone.io https://t1.kakaocdn.net https://cdnjs.cloudflare.com https://translate.google.com https://translate.googleapis.com https://translate-pa.googleapis.com https://cdn.jsdelivr.net; script-src-elem 'self' 'unsafe-inline' https://static.cloudflareinsights.com https://www.googletagmanager.com https://pagead2.googlesyndication.com https://cdn.portone.io https://t1.kakaocdn.net https://cdnjs.cloudflare.com https://translate.google.com https://translate.googleapis.com https://translate-pa.googleapis.com https://cdn.jsdelivr.net; connect-src 'self' https://code-destiny.com https://www.code-destiny.com https://code-destiny-web.bulegyung.workers.dev https://cdn.portone.io https://checkout-service.prod.iamport.co https://tx-gateway-service.prod.iamport.co https://t1.kakaocdn.net https://*.kakao.com https://*.kakaocdn.net https://translate.google.com https://translate.googleapis.com https://translate-pa.googleapis.com https://cdn.jsdelivr.net https://www.googletagmanager.com https://www.google-analytics.com https://*.google-analytics.com https://*.analytics.google.com; media-src 'self' https://music.code-destiny.com https://assets.code-destiny.com; style-src 'self' 'unsafe-inline' https://translate.googleapis.com https://www.gstatic.com; style-src-elem 'self' 'unsafe-inline' https://translate.googleapis.com https://www.gstatic.com; img-src 'self' data: blob: https:; font-src 'self' data: https://assets.code-destiny.com; frame-src 'self' https://checkout-service.prod.iamport.co https://tx-gateway-service.prod.iamport.co https://translate.google.com; worker-src 'self' blob:; object-src 'none'; base-uri 'self'; form-action 'self' https://tx-gateway-service.prod.iamport.co https://*.inicis.com; frame-ancestors 'self'; manifest-src 'self'; upgrade-insecure-requests

# Next.js 해시 자산의 불변 캐시는 이 파일 아래쪽 `/*.css` 블록 **뒤**로 옮겼다(2026-08-14).
# 이유는 그 자리에 남긴 주석 참고 — 요약하면 `/*.js` 가 같은 경로에 매칭되는데 뒤에 있어서 이겼다.
# (규칙 100개 상한 안에 있어야 한다는 제약은 그대로다. 옮긴 위치도 100개 안이다.)

# HTML entry points: no-cache (NOT no-store)
#
# 🔴 `no-cache` 는 "캐시하지 마라"가 아니라 "쓰기 전에 반드시 오리진에 재검증하라"다.
# 배포 직후 stale HTML 을 막는다는 원래 의도는 글자 그대로 유지된다 — 브라우저도 엣지도
# 매 요청 오리진에 물어본다. 달라지는 건 응답에 ETag/Last-Modified 가 살아나서
# 조건부 요청이 304(본문 0바이트)를 받을 수 있게 된다는 것뿐이다.
#
# 왜 바꿨나: `no-store` 는 검증자까지 죽여서 조건부 요청 자체를 불가능하게 만든다.
# 실측(2026-08-15) — 사이트맵 329 URL 전량이 cf-cache-status: DYNAMIC, ETag 없음.
# Googlebot 이 매 크롤마다 40.4MB(홈 1.32MB, 로케일 홈 각 1.42MB)를 통째로 재다운로드하고
# 있었다. 등록 5개월 도메인의 빈약한 크롤 예산에서 이건 그대로 손실이다.
#
# 🔴 `Pragma: no-cache` / `Expires: 0` 은 함께 지웠다. HTTP/1.0 잔재인데, 남겨 두면
# 일부 중간 캐시가 조건부 요청 자체를 막아 `no-cache` 로 바꾼 의미가 사라진다.
# 🔴 엣지 TTL(예: CDN-Cache-Control: max-age=300)은 **일부러 넣지 않았다** — 엣지가 본문을
# 들고 있으면 배포 N+1 이 _next/static 매니페스트를 갈아치운 뒤 배포 N 의 HTML 이 죽은
# 청크를 가리켜 흰 화면이 난다. 지금 형태는 본문을 저장하지 않으므로 그 위험이 구조적으로 0이다.
# 🔴 `/version.json` 과 `/service-worker.js` 는 no-store 를 유지한다(배포 신선도 프로브·SW 갱신).
/music*
  Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' 'wasm-unsafe-eval' https://static.cloudflareinsights.com https://www.googletagmanager.com https://pagead2.googlesyndication.com https://cdn.portone.io https://t1.kakaocdn.net https://cdnjs.cloudflare.com https://translate.google.com https://translate.googleapis.com https://translate-pa.googleapis.com https://cdn.jsdelivr.net; script-src-elem 'self' 'unsafe-inline' https://static.cloudflareinsights.com https://www.googletagmanager.com https://pagead2.googlesyndication.com https://cdn.portone.io https://t1.kakaocdn.net https://cdnjs.cloudflare.com https://translate.google.com https://translate.googleapis.com https://translate-pa.googleapis.com https://cdn.jsdelivr.net; connect-src 'self' https://code-destiny.com https://www.code-destiny.com https://code-destiny-web.bulegyung.workers.dev https://cdn.portone.io https://checkout-service.prod.iamport.co https://tx-gateway-service.prod.iamport.co https://t1.kakaocdn.net https://*.kakao.com https://*.kakaocdn.net https://translate.google.com https://translate.googleapis.com https://translate-pa.googleapis.com https://cdn.jsdelivr.net https://www.googletagmanager.com https://www.google-analytics.com https://*.google-analytics.com https://*.analytics.google.com; media-src 'self' https://music.code-destiny.com https://assets.code-destiny.com; style-src 'self' 'unsafe-inline' https://translate.googleapis.com https://www.gstatic.com; style-src-elem 'self' 'unsafe-inline' https://translate.googleapis.com https://www.gstatic.com; img-src 'self' data: blob: https:; font-src 'self' data: https://assets.code-destiny.com; frame-src 'self' https://checkout-service.prod.iamport.co https://tx-gateway-service.prod.iamport.co https://translate.google.com; worker-src 'self' blob:; object-src 'none'; base-uri 'self'; form-action 'self' https://tx-gateway-service.prod.iamport.co https://*.inicis.com; frame-ancestors 'self'; manifest-src 'self'; upgrade-insecure-requests

# AdSense/Search: thin or input-heavy tool screens stay crawlable but out of the index.
/account/delete*
  X-Robots-Tag: noindex, nofollow
/account/password*
  X-Robots-Tag: noindex, nofollow
/animal/physio*
  X-Robots-Tag: noindex, nofollow
# 🔴 `/maya*` 로 합치지 말 것 — 색인 대상인 `/mayan-calendar/guide` 까지 삼킨다.
/maya
  X-Robots-Tag: noindex, nofollow
/maya/*
  X-Robots-Tag: noindex, nofollow
/oracle/royal-tea*
  X-Robots-Tag: noindex, nofollow
/oracle/sikojen-povailu*
  X-Robots-Tag: noindex, nofollow
/palm-reading*
  X-Robots-Tag: noindex, nofollow
/pdf*
  X-Robots-Tag: noindex, nofollow
# `/premium*` 이 `/premium-reports` 계열까지 덮으므로 별도 규칙은 두지 않는다(규칙 100개 예산).
/premium*
  X-Robots-Tag: noindex, nofollow
/saju-fpti*
  X-Robots-Tag: noindex, nofollow
/saju-picture*
  X-Robots-Tag: noindex, nofollow
/sukuyo/calendar*
  X-Robots-Tag: noindex, nofollow
/404
  X-Robots-Tag: noindex, nofollow
/500
  X-Robots-Tag: noindex, nofollow
# /blog 은 2026-08-14 에 은퇴했다(전편 /insights 정본으로 301, public/_redirects 참고).
# 규칙은 지운 게 아니라 남긴다 — verify:adsense-readiness 가 /blog·/blog/* 커버를 요구하고,
# blog-style.css 는 /famous 가 계속 쓴다. nofollow → follow: 301 응답에서 링크 신호를
# 끊으면 통합의 목적(자산을 /insights 로 모으기)을 스스로 막는다(위 셸 사본과 같은 이유).
/blog*
  X-Robots-Tag: noindex, follow
# 🔴 `/famous*` 로 합치지 말 것 — `/famous-saju/**` 를 삼켜 그 301 응답에 nofollow 가 붙고,
#    그러면 `/insights/famous-saju/` 로 가는 회수 경로가 링크 신호를 잃는다.
/famous
  X-Robots-Tag: noindex, nofollow
/famous/*
  X-Robots-Tag: noindex, nofollow
# 🔴 `/fortune` 의 noindex 는 2026-08-17 에 풀었다. 규칙이 붙던 시점의 그 페이지는 렌더 텍스트
# 531자짜리 빈 허브였지만, 지금은 scripts/build-fortune-hub-shell.mjs 가 sign-profiles 에서
# 만드는 2,955자 클러스터 루트다. 특히 `nofollow` 가 문제였다 — 이 페이지가 걸고 있던 기간 허브
# 4개와 24종 링크의 신호를 스스로 끊어, 유일한 진입 경로를 무효로 만들고 있었다.
# 되살리려면 셸이 다시 얇아진 것인지 먼저 확인할 것(verify:adsense-readiness 가 1,800자를 요구한다).
/fortune/sikojen-povailu*
  X-Robots-Tag: noindex, nofollow
# 셸 사본만 존재하는 라우트 — app 페이지가 없어 홈 셸 복사본이 곧 본문이라 색인을 막는다.
# 🔴 2026-08-23: 나머지 9개(/saju/basic·/saju/sibyl·/tarot/mingri·/tarot/love·
# /tarot/reunion·/tarot/self-esteem·/tarot/year·/astrology/cosmic·/oracle/sukuyo)는
# 여기서 뺐다. 그 라우트들은 이제 셸 사본이 아니라 자기 app 랜딩을 서빙하므로 고유 본문이
# 있고(실측 2,815~4,017자), 도구는 랜딩의 CTA(/index.html?action=…)로 연결된다.
# docs/context/seo-and-adsense.md 의 '색인 여부는 다섯 곳이 함께 정한다' 표 참고.
/oracle/juyuk*
  X-Robots-Tag: noindex, follow
# 🔴 `/oracle/hwatu*` 로 합치지 말 것 — 색인 대상인 `/oracle/hwatu-life` 를 삼킨다.
/oracle/hwatu
  X-Robots-Tag: noindex, follow
/oracle/hwatu/*
  X-Robots-Tag: noindex, follow
# 🔴 이 셸들은 `/x.html` 이 아니라 **`/x` 로 서빙된다.** Cloudflare Pages 가 확장자를 떼고,
# `/x.html` 은 `/x` 로 308 을 준다. 리다이렉트 응답의 헤더는 목적지로 이어지지 않으므로
# `.html` 에만 규칙을 걸면 크롤러가 보는 200 응답에는 noindex 가 없다.
# 2026-08-16 실측: `/tadagochi*` 만 와일드카드형이라 유일하게 적용됐고, `.html` 로만 적힌
# 나머지 18개는 전부 색인이 열려 있었다(`/destiny-poker?lang=en` 등이 GSC 「사용자가 선택한
# 표준이 없는 중복 페이지」로 보고됨). 그래서 **확장자 없는 형태까지 덮는 `*` 로 적는다.**
# 규칙 수는 그대로다 — `/x*` 하나가 `/x` 와 `/x.html` 을 함께 덮는다(규칙 100개 예산).
/tadagochi*
  X-Robots-Tag: noindex, nofollow
# `/ifa-oracle*` 이 `/ifa-oracle-about` 까지 덮으므로 별도 규칙은 두지 않는다(규칙 100개 예산).
/ifa-oracle*
  X-Robots-Tag: noindex, nofollow
# 아래 5개는 사이트맵에도 없고 canonical 도 없는 고아 셸인데 색인은 열려 있었다.
# 실측 본문이 130~1,342자라 그대로 두면 "낮은 가치의 콘텐츠" 표본이 된다.
# (destiny-island·codedestiny-novel 은 HTML meta 로만 막혀 있어 헤더로 이중 확정한다.)
/vedic-astrology*
  X-Robots-Tag: noindex, nofollow
/myungwun_final*
  X-Robots-Tag: noindex, nofollow
/secret-house_real*
  X-Robots-Tag: noindex, nofollow
/destiny-island*
  X-Robots-Tag: noindex, nofollow
/codedestiny-novel*
  X-Robots-Tag: noindex, nofollow
/fortune-teller-fish*
  X-Robots-Tag: noindex, nofollow
/royal-tea-oracle*
  X-Robots-Tag: noindex, nofollow
/blood-type-app*
  X-Robots-Tag: noindex, nofollow
/neville-meditation*
  X-Robots-Tag: noindex, nofollow
/geomancy-oracle-v4*
  X-Robots-Tag: noindex, nofollow
# `/static/` 아래에 같은 레거시 셸의 **두 번째 사본**이 있다(public/static/geomancy-oracle-v4.html).
# 위 규칙은 경로 선두에 앵커되므로 그 사본을 못 덮는다 — 루트 사본만 색인이 막히고
# `/static/` 사본은 canonical·robots meta 가 하나도 없이 열려 있었다(2026-08-27 out/ 실측).
# 🔴 `/static*` 블록에 noindex 를 넣어 해결하지 말 것. 그 블록은 홈 셸 사본
#    `/static/index.html` 까지 덮는데, 그 문서는 canonical 로 홈을 가리킨다.
#    noindex + 타 URL canonical 조합은 noindex 가 canonical 목적지(= 홈)로 옮겨붙을 수 있어
#    Google 이 권하지 않는 조합이다. 그래서 사본 하나만 정확히 집는다(규칙 +1).
/static/geomancy-oracle-v4*
  X-Robots-Tag: noindex, nofollow
/yoga-guru*
  X-Robots-Tag: noindex, nofollow
# destiny-poker 는 여기 없다 — 무료 기능이라 2026-08-16 에 색인 대상으로 전환했다.
# 본문을 838자 → 1,918자로 보강하고 canonical·description·사이트맵 등재를 함께 붙였다.
# 되돌리려면 `_headers` 규칙만 넣지 말고 사이트맵 등재도 함께 빼야 한다
# (`verify-adsense-readiness` 의 indexable-route-coverage 가 어긋난 조합을 잡는다).
/celestial-harmony*
  X-Robots-Tag: noindex, nofollow
/tarot-ijik*
  X-Robots-Tag: noindex, nofollow
/emoi_omikuji_v2*
  X-Robots-Tag: noindex, nofollow
/cosmic-soul-meditation*
  X-Robots-Tag: noindex, nofollow
# 2026-08-16 추가 — 위 셸들과 같은 부류인데 선언만 빠져 있었다. 사이트맵 미등재, 내부 링크는
# 홈 타일 1개(`/` 로만 나간다), 가시 텍스트 647자 중 한글 420자로 tadagochi(458)보다도 얇다.
/pet-saju*
  X-Robots-Tag: noindex, nofollow
# 2026-08-17 추가 — AdSense "가치 없는 콘텐츠" 거절 대응 중에 발견한 마지막 2개.
# 사이트맵 미등재 · canonical 없음 · robots meta 없음이라 **색인 신호가 전무**했다.
# 🔴 /ifa_oracle_v2_full 이 샌 이유: 위의 `/ifa-oracle*` 은 하이픈이라 언더스코어 파일명을
#    매칭하지 못한다. 파일명과 규칙의 구분자가 다르면 `*` 도 구해주지 않는다.
/ifa_oracle_v2_full*
  X-Robots-Tag: noindex, nofollow
# prompt-hub-3004 은 App Router `/fortune/prompt-hub` 를 프리렌더한 사본이다(head 에 그 청크가
# 박혀 있다). 정본이 이미 noindex 이므로 사본도 같이 닫는다. `<title>` 이 비어 있는 것은 별건
# (seo-naver-diagnostic-2026-08-16.md §3-D 의 "description 누락 13" 과 같은 묶음).
/prompt-hub-3004*
  X-Robots-Tag: noindex, nofollow

/
  Content-Type: text/html; charset=utf-8
  Cache-Control: no-cache
  CDN-Cache-Control: no-cache
  Cloudflare-CDN-Cache-Control: no-cache

/index.html
  Content-Type: text/html; charset=utf-8
  Cache-Control: no-cache
  CDN-Cache-Control: no-cache
  Cloudflare-CDN-Cache-Control: no-cache

/*.html
  Content-Type: text/html; charset=utf-8
  Cache-Control: no-cache
  CDN-Cache-Control: no-cache
  Cloudflare-CDN-Cache-Control: no-cache

/*/
  Content-Type: text/html; charset=utf-8
  Cache-Control: no-cache
  CDN-Cache-Control: no-cache
  Cloudflare-CDN-Cache-Control: no-cache

# 🔴 `/*/index.html`·`/*/*/`·`/*/*/index.html`·`/*/*/*/`·`/*/*/*/index.html` 5개를 지웠다
# (2026-08-14). Cloudflare 의 splat 은 **슬래시를 건너뛴다** — 이 파일 아래쪽 `_next/static`
# 주석에 실측이 남아 있다: `/*.js` 가 `/_next/static/chunks/2474.hash.js` 에 매칭됐다(슬래시 3개).
# 따라서 위 5개는 전부 바로 위 `/*.html` 과 `/*/` 에 이미 덮이고, 헤더 블록도 바이트 동일했다.
# 지운 이유는 정리가 아니라 **예산**이다. 규칙이 정확히 100개(= Cloudflare 적용 상한)에 닿아
# 있어서 새 규칙을 하나도 못 넣는 상태였다. 위 `/static` 통합과 같은 이유다.
# 회수한 5칸 중 1칸을 `/js/shell/*` immutable 에 쓰고 4칸은 여유로 남긴다.

# `/static`·`/static/`·`/static/index.html` 세 규칙을 하나로 합쳤다 — 규칙 100개 예산을 벌어
# `_next/static` 을 앞으로 옮기면서 다른 규칙이 밀려나지 않게 하기 위함이다. `/static` 으로
# 시작하는 경로는 이 레거시 셸뿐이다(`/_next/static` 은 접두사가 다르다).
/static*
  Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate
  CDN-Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate
  Cloudflare-CDN-Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate

# API: no-store cache
/api/*
  Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate
  CDN-Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate
  Cloudflare-CDN-Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate

# Version/status files: no-store cache
/version.json
  Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate
  CDN-Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate
  Cloudflare-CDN-Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate
  Pragma: no-cache
  Expires: 0

/status.json
  Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate
  CDN-Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate
  Cloudflare-CDN-Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate

# Service Worker: no-store cache
/service-worker.js
  Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate
  CDN-Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate
  Cloudflare-CDN-Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate
  Pragma: no-cache
  Expires: 0

/tadagochi-sw.js
  Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate
  CDN-Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate
  Cloudflare-CDN-Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate

/sw.js
  Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate
  CDN-Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate
  Cloudflare-CDN-Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate

# Manifest: no-store cache
/manifest.json
  Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate
  CDN-Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate
  Cloudflare-CDN-Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate

/manifest-neo.json
  Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate
  CDN-Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate
  Cloudflare-CDN-Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate

/manifest-tadagochi.json
  Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate
  CDN-Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate
  Cloudflare-CDN-Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate

# Fixed-name JS/CSS: short edge cache
/*.js
  Cache-Control: public, max-age=86400, stale-while-revalidate=604800
  CDN-Cache-Control: public, max-age=604800
  Cloudflare-CDN-Cache-Control: public, max-age=604800

/*.css
  Cache-Control: public, max-age=86400, stale-while-revalidate=604800
  CDN-Cache-Control: public, max-age=604800
  Cloudflare-CDN-Cache-Control: public, max-age=604800

# 🔴 Next.js 해시 자산의 불변 캐시. 이 블록은 반드시 (1) 적용되는 규칙 100개 안에 있고
#    (2) 바로 위 `/*.js`·`/*.css` 보다 **뒤**에 있어야 한다. 둘 다 지켜야 하며 하나만으로는 안 된다.
#
# (1) Cloudflare Pages 는 _headers 규칙을 100개까지만 적용하고 나머지는 **조용히 버린다**(실측
# 2026-08-08: #104 /version.json·#121 /ads.txt 모두 미적용). 이 규칙이 파일 맨 끝(133번째)에
# 있던 동안 통째로 무시돼, 오리진이 해시가 박힌 불변 자산에도 `max-age=0, must-revalidate` 를
# 내려보냈다. 그걸 보완하려고 대시보드 Cache Rule 로 확장자 기반 2일 캐시를 걸었는데, 그 룰이
# **404 응답에도** 2일을 찍어 오리진의 `no-store` 를 덮어썼다. 그것이 2026-07-30 `_next/static`
# 404 가 이틀간 죽어 /points·/me·/login 이 무스타일로 뜬 경로다.
#
# (2) 2026-08-14 수정: 이 블록이 파일 앞쪽(#2)에 있던 동안, `/_next/static/chunks/*.js` 는
# 여기와 `/*.js`(#80, max-age=86400) 에 **둘 다** 매칭됐다. 매칭되는 규칙은 전부 적용되고 같은
# 헤더는 뒤에 있는 쪽이 이기므로, 규칙 수가 정확히 100개가 된 시점부터 immutable 이 조용히
# 깨져 있었다(86400 이 이겼다). 위 주석이 예고한 조건이 실제로 발생한 것이라 처방대로 옮겼다.
# 🔴 이 블록을 다시 파일 앞쪽으로 되돌리지 말 것.
/_next/static/*
  Cache-Control: public, max-age=31536000, immutable
  CDN-Cache-Control: public, max-age=31536000, immutable
  Cloudflare-CDN-Cache-Control: public, max-age=31536000, immutable

/js/*.js
  Cache-Control: public, max-age=604800, stale-while-revalidate=2592000
  CDN-Cache-Control: public, max-age=604800
  Cloudflare-CDN-Cache-Control: public, max-age=604800

# 🔴 이 블록은 반드시 위 `/js/*.js` **뒤**에 있어야 한다. 둘 다 매칭되고 같은 헤더는 뒤가 이긴다.
# `/js/shell/s-<sha256 16자>.js` 는 scripts/externalize-dist-inline-scripts.mjs 가 셸의 인라인
# 스크립트 본문을 해시해서 만든 파일이다 — **파일명 자체가 내용 해시**라 내용이 바뀌면 이름이
# 바뀐다. 위 주석이 `/js/*.js` 를 immutable 로 못 올린다고 한 이유(무버전 참조 11곳)는 이 경로엔
# 해당하지 않는다. 이 디렉터리에는 빌드가 만든 해시 파일만 들어간다.
# 프로덕션 Lighthouse 가 이 파일들을 "캐시 수명 7일" 로 지적했고, 그게 `/js/*.js` 적용 결과였다.
/js/shell/*
  Cache-Control: public, max-age=31536000, immutable
  CDN-Cache-Control: public, max-age=31536000, immutable
  Cloudflare-CDN-Cache-Control: public, max-age=31536000, immutable

/lib/*.js
  Cache-Control: public, max-age=604800, stale-while-revalidate=2592000
  CDN-Cache-Control: public, max-age=604800
  Cloudflare-CDN-Cache-Control: public, max-age=604800

# 셸이 참조하는 /styles/*.css 는 전부 `?v=build-<해시>` 를 달고 나간다(2026-08-14 확인: 31/31).
# 그 키는 index.html+js/**+styles/** 내용 해시라 파일이 바뀌면 URL 이 바뀐다 = 불변 자산이다.
# 🔴 무버전 참조를 새로 만들면 이 immutable 이 그 파일을 1년 동안 굳힌다. 참조는 반드시
#    sync:public 이 `?v=` 를 다시 쓰는 경로로만 추가할 것(수기 `?v=` 금지).
#    같은 이유로 /js/*.js 는 immutable 로 올리지 않았다 — 무버전 참조가 11곳 남아 있다.
/styles/*.css
  Cache-Control: public, max-age=31536000, immutable
  CDN-Cache-Control: public, max-age=31536000, immutable
  Cloudflare-CDN-Cache-Control: public, max-age=31536000, immutable

/css/*.css
  Cache-Control: public, max-age=604800, stale-while-revalidate=2592000
  CDN-Cache-Control: public, max-age=604800
  Cloudflare-CDN-Cache-Control: public, max-age=604800

# Fixed-name assets: short edge cache
/icons/*
  Cache-Control: public, max-age=604800, stale-while-revalidate=2592000
  CDN-Cache-Control: public, max-age=604800
  Cloudflare-CDN-Cache-Control: public, max-age=604800

/fuctionassets/*
  Cache-Control: public, max-age=604800, stale-while-revalidate=2592000
  CDN-Cache-Control: public, max-age=604800
  Cloudflare-CDN-Cache-Control: public, max-age=604800

/tarot-cards/*
  Cache-Control: public, max-age=604800, stale-while-revalidate=2592000
  CDN-Cache-Control: public, max-age=604800
  Cloudflare-CDN-Cache-Control: public, max-age=604800

# AdSense and crawler config files
/ads.txt
  Content-Type: text/plain; charset=utf-8
  Cache-Control: public, max-age=86400
  CDN-Cache-Control: public, max-age=86400
  Cloudflare-CDN-Cache-Control: public, max-age=86400

/robots.txt
  Content-Type: text/plain; charset=utf-8
  Cache-Control: public, max-age=86400
  CDN-Cache-Control: public, max-age=86400
  Cloudflare-CDN-Cache-Control: public, max-age=86400

/fonts/*
  Cache-Control: public, max-age=31536000, immutable
  CDN-Cache-Control: public, max-age=31536000, immutable
  Cloudflare-CDN-Cache-Control: public, max-age=31536000, immutable

# 링크 미리보기 카드: og:image URL 자체가 콘텐츠 해시 쿼리(?v=)로 버전 관리되므로
# 이 TTL 은 이미 캐시버스팅된 URL 에는 사실상 무관하다 — 과거 버전 URL 이나
# 쿼리 없이 직접 접근하는 경로가 오래 굳지 않도록 하는 보조 안전장치일 뿐이다.
/og/*
  Cache-Control: public, max-age=300, must-revalidate
  CDN-Cache-Control: public, max-age=300
  Cloudflare-CDN-Cache-Control: public, max-age=300

/assets/*
  Cache-Control: public, max-age=604800, stale-while-revalidate=2592000
  CDN-Cache-Control: public, max-age=604800
  Cloudflare-CDN-Cache-Control: public, max-age=604800

/images/*
  Cache-Control: public, max-age=604800, stale-while-revalidate=2592000
  CDN-Cache-Control: public, max-age=604800
  Cloudflare-CDN-Cache-Control: public, max-age=604800

/music-covers/*
  Cache-Control: public, max-age=86400, stale-while-revalidate=604800
  CDN-Cache-Control: public, max-age=604800
  Cloudflare-CDN-Cache-Control: public, max-age=604800

/sprites/*
  Cache-Control: public, max-age=604800, stale-while-revalidate=2592000
  CDN-Cache-Control: public, max-age=604800
  Cloudflare-CDN-Cache-Control: public, max-age=604800

/music/*
  Cache-Control: public, max-age=2592000, stale-while-revalidate=604800
  CDN-Cache-Control: public, max-age=604800
  Cloudflare-CDN-Cache-Control: public, max-age=604800

/audio/*
  Cache-Control: public, max-age=2592000, stale-while-revalidate=604800
  CDN-Cache-Control: public, max-age=604800
  Cloudflare-CDN-Cache-Control: public, max-age=604800

/DestinyCafe/*
  Cache-Control: public, max-age=2592000, stale-while-revalidate=604800
  CDN-Cache-Control: public, max-age=604800
  Cloudflare-CDN-Cache-Control: public, max-age=604800

/neo-operation-room/*
  Cache-Control: public, max-age=2592000, stale-while-revalidate=604800
  CDN-Cache-Control: public, max-age=604800
  Cloudflare-CDN-Cache-Control: public, max-age=604800
