name: Deploy Pages Frontend (Deprecated)

on:
  workflow_dispatch:

concurrency:
  group: cloudflare-pages-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  pages-deploy-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "Pages deployment is deprecated for this repository."
          echo "Use Worker deployment only: npm run deploy:cf"
