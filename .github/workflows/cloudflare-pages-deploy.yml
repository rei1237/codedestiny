name: Cloudflare Pages Deploy Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Pages Deploy Disabled Notice
        run: echo "Cloudflare Pages deploy is disabled in GitHub Actions. Use manual deploy only."
