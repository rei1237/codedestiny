# KST 자정(UTC 15:00)에만 일일 운세 JSON 생성 → 커밋·푸시 → main 푸시로 Cloudflare 배포 워크플로가 1회 트리거됨.
# 일반 코드 배포(build:cf)에서는 fortune:generate 를 돌리지 않음(동일 파일 반복 생성 방지).

name: Fortune daily package (once per day)

on:
  schedule:
    # 00:00 KST (한국 표준시)
    - cron: "0 15 * * *"
  workflow_dispatch:

concurrency:
  group: fortune-daily-kst
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  daily-fortune:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Generate daily JSON if missing (KST today)
        run: node scripts/fortune-daily-once.mjs

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          DATE_KST=$(node --input-type=module -e "import { kstYmdToday } from './scripts/lib/fortune-date.mjs'; console.log(kstYmdToday())")
          git add fortune/data/
          if git diff --staged --quiet; then
            echo "No fortune data changes (already generated for ${DATE_KST} or skip)."
            exit 0
          fi
          git commit -m "chore(fortune): daily package ${DATE_KST}"
          git push
