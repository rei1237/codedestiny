<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="false"
        tools:targetApi="31">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="auth"
                    android:scheme="com.codedestiny.app" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- 잠금화면(네이티브 오버레이) — 화면이 켜질 때 키가드 위에 /lock-screen-fortune 을 띄운다. -->
        <activity
            android:name=".LockScreenActivity"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleInstance"
            android:taskAffinity=""
            android:excludeFromRecents="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:exported="false" />

        <!-- 상시 포그라운드 서비스: 화면 켜짐(ACTION_SCREEN_ON)을 런타임 리시버로 받아 잠금화면 Activity 기동. -->
        <service
            android:name=".LockScreenForegroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="lockscreen_overlay" />
        </service>

        <!-- 재부팅/업데이트 후 기능이 켜져 있으면 서비스·알람 복원. -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- 설정 시각 알림(AlarmManager). -->
        <receiver
            android:name=".LockScreenAlarmReceiver"
            android:exported="false" />
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 촉각 피드백. 웹 Vibration API(navigator.vibrate)가 이 권한 없이는 조용히 무시된다.
         호출부는 이미 있다 — js/touch-perf.js 의 safeVibrate(8) 가 버튼·카드 터치에 걸려 있고
         사용자 제스처(userActivation) 안에서만 울린다. 런타임 동의가 필요 없는 normal 권한. -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- 잠금화면(네이티브 오버레이) 기능 권한.
         USE_FULL_SCREEN_INTENT·USE_EXACT_ALARM·WAKE_LOCK 은 일부러 없다 — 앞 둘은 Play 정책상
         통화·알람시계 앱 전용이라 심사 거절 대상이고(알림은 IMPORTANCE_HIGH 헤즈업 + 탭 진입,
         정확 알람은 사용자 취소 가능한 SCHEDULE_EXACT_ALARM + 비정확 폴백으로 충분),
         WAKE_LOCK 은 실사용 코드가 0곳이다(2026-09-01 전수 git grep). 다시 넣지 말 것. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
</manifest>
